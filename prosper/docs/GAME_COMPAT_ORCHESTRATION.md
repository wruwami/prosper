# Game compatibility orchestration

This is the durable playbook and current handoff for concurrent game-compatibility work. It is written for an
orchestrating agent that delegates one bounded investigation to each subagent, integrates real progress, and keeps
the shared repository, GPU, evidence, issues, and pull requests coherent.

Read the repository-root `CLAUDE.md` before this document. `CLAUDE.md` remains authoritative for filesystem,
verification, evidence, review, privacy, and release policy. This document explains how to apply those rules when
several game agents work at once.

The checkpoint below was refreshed **2026-08-10**. The named Lane A–F allocation and Wave 1/2 sections remain
dated historical records; they retain valuable falsifications and apparatus lessons, but their ownership and
"next assignment" text is not live. Update the checkpoint whenever an investigation materially moves, a PR merges,
or ownership changes. Do not let this document replace issue comments: issues are the durable evidence log, while
this document is the map that helps the next orchestrator find and interpret that evidence.

## Current checkpoint

- Repository: `mattias800/prosper` (renamed from `mattias800/ps5ys`).
- Remote branch: `master`.
- Documentation refresh base: `5e7ff0ab` (2026-08-10).
- **Game dumps live at `<DUMP_ROOT>/<TITLE_ID>-app0`, and `<DUMP_ROOT>` is whatever
  `$PROSPER_GAME_ROOT` points at** (gitignored, and deliberately not written down here — the
  repository is public and an absolute path leaks the developer's private directory layout). This
  line used to name `<REPO_ROOT>/testdata/`; the corpus was relocated to a dedicated volume on
  2026-08-21 and that spelling is now wrong.
  **Set `PROSPER_GAME_ROOT` before you build or run anything.** `snapshot.py` reads it directly, and
  `CMakeLists.txt` derives `GAME_DUMP` from it — without it the relative fallback resolves inside
  your worktree, where no dump lives, and you get instrument trap 126: a smaller test suite, silently,
  still reporting `100% tests passed`.
- The 2026-08-04 rung-0 wave below is complete: every listed title produced either a visible milestone
  or a relocated, evidence-backed blocker. Current activity is centered on GTA V's post-lift compute CFG
  frontier (#2412/#2481/#2429) and Dragon Quest VII's Windows progression/mapping frontier (#2448/#2450).
  This sentence is a handoff, not a lock: live `in-progress` labels, claim comments and remote fix branches
  are authoritative for ownership.
- GPU state: sharing is the default; see the scheduling rules below.

### The standing objective, in the user's words

**Rung 0 titles first, then title screens. Breadth beats depth: "progress on many titles is more
important than progress in a few."** The user also asked explicitly *not* to spend heavily on Astro
Bot unless it yields real progress — it is a 149 GB AAA showcase that has produced no shippable title
fix across several sessions. Every new visible checkpoint gets an **unmodified frontend screenshot**
committed under `assets/screenshots/` and referenced from **both** the game tracker issue and
`COMPATIBILITY.md`; the user asks for this specifically.

### Where the library actually stands (2026-08-04)

Read the per-title rung off the `tracker:game` issues, not off this table — it is a snapshot. Of 39
tracked titles:

| Rung | Count | Meaning |
|---|---|---|
| 6 | 13 | snapshot-guarded; "done" |
| 4 | 1 | human-verified |
| 3 | 5 | gameplay with real GPU draws |
| 2 | 9 | title screen |
| 1 | 6 | some real graphics, no title screen |
| 0 | 5 | no graphics at all |

So 28 of 39 are at a title screen or better. The eleven titles at rung 0 and rung 1 are the current
work queue, in that order.

### Doc-staleness warning

The `Lane A` … `Lane F` sections below are the **2026-07-31** allocation and are now partly historical:
Alex Kidd (Lane D) reached rung 6, The Pathless (Lane E) reached rung 2, and Nikoderiko (Lane F)
reached rung 2. Their falsification records remain valuable and binding — read them before reviving
any hypothesis they killed — but do not read their "Public state" or "next assignment" text as
current. Verify against the title's `tracker:game` issue.

**Read the falsification record before planning anything.** The three lanes handed over on `ca0b0258` each had
their assigned premise **falsified** during the 2026-07-31 session, including both items the previous handoff
called its highest-value work. Re-running them wastes a session:

| Previous "highest-value" item | Outcome |
|---|---|
| Astro op221 R11G11B10 storage lowering | **FALSIFIED.** Stored (packed R32ui) and current-raw (native typed float) take provably different backend paths yet produce byte-identical output `28b5ea9a3fbd2ca9`. |
| Dragon Quest binding-36 sampling | **EXONERATED entirely.** LUT contents, tile27 detile, DCC, 10:10:10:2 unpack, 3D coordinate wiring, dimensional opcode, dmask, swizzle, address modes, sampler filtering, and draw90's shader translation are all ruled out. |
| Plucky pc915 barrier-spanning branch | **PROVEN workgroup-uniform and merged** (#1572). |

Two lanes were added because the inherited allocation was skewed to the hardest titles while easier ground sat
idle: **Alex Kidd** (PPSA02664) and **The Pathless** (PPSA01826). Alex Kidd reached the gameplay rung in one
session. Prefer breadth when a hard lane stalls — overall library progress matters more than any single title.

### Tooling hazards that cost real time this session

1. **Build inside the `ps5ys` distrobox.** The host has only the Vulkan *loader*, not the headers. A host
   configure **succeeds while silently omitting** `gpu_replay`, the offscreen graphics harness, and every Vulkan
   **execution** test, logging just `Vulkan not found`. A green host `ctest` is therefore not evidence that
   execution tests ran — confirm the binaries exist and report test names, not an exit code. Wipe the build dir
   when switching; a stale cache keeps Vulkan disabled.
2. **`shader_inspect` was misreporting ~96% of known-good shaders** (109 of 114, across all three stages) as
   `rejected`, because a raw dump carries no descriptors. Fixed in #1575 — it now reports
   `undetermined-no-resource-table` with exit 3. Any shader verdict taken from it before `329153bf` is suspect.
3. **`-DGAME_DUMP` against a non-Messenger title fails 3 dump-parameterized tests** (`module_loads_eboot`,
   `boot_reaches_first_syscall`, `real_shader_render`). This is expected, not a regression — current master is
   **162/162** when configured against `PPSA24651`. Tracked as #1573.

### The instrument-not-the-subject list — read this before believing any surprising measurement

**Every entry below is a phantom defect that came from the measuring apparatus, not the subject.**
Several cost hours; one cost two sessions. This is the single highest-value page in this document.

*Maintenance:* **append, never renumber** — an existing number may already be cited from an issue, a
commit message, another document or a source comment — **118 references on 110 lines in 46 files
do**, naming 63 distinct rows, and **50 of those references are `.cpp`/`.hpp`/`.py` comments**
rather than prose (measured on `origin/master` `8c4c0f09`, 2026-08-17; re-derive rather than
trusting a restated figure). Deliberately
no total is stated here: a restated count goes stale the moment a lane appends, and it already had
(the header read "Fourteen" against a 17-row table). The last row's number is the **high-water
mark**, which is what you want for allocating; it is not the row count, because **gaps are legal**.

*Allocating a number under concurrency:* run
`python3 tools/docs/trap_number.py` before you write the row. It reports the highest number on
`origin/master` **and in every open PR**, names the claimant of each, and prints the next free
number. Reading master alone is what produced the #2574/#2581 collision on 2026-08-17 — both lanes
read 181, both wrote 182 — and it is what #1729 was filed for. The tool is an **advisor, not a
gate**: two lanes running it in the same minute still both see the same free number. It shrinks the
window; only merge order closes it.

*If you collide anyway:* renumber your row to **any** number above the current highest and merge.
Nothing waits on anything. The gapless rule that used to make this a re-derivation against a moving
master was removed on 2026-08-17 (#2089); `check_numbered_table.py --ordered` enforces unique and
ascending, and `--baseline` enforces that no existing row has been deleted or renumbered.

| # | Instrument | How it lied |
|---|---|---|
| 1 | `shader_inspect` | Reported **109 of 114 known-good shaders** as `rejected` — a raw dump has no descriptors, so table-dependent MIMG/MUBUF/MTBUF (and SMEM in graphics) correctly refuse. Fixed #1575. |
| 2 | `PROSPER_FS_TAP` | Exports `(dst, dst+1, dst+2, dst+3)`, so taps with different `dst` compare **different registers**. Manufactured a "~14x amplification". |
| 3 | Snapshot guards | `blue-prince-hall` and `terminator-boot` "FAILED" on **fully-rendered healthy frames** whose routes had drifted to another scene/animation phase. A control run on master reproduced both. |
| 4 | `sgpr:106` | Is `VCC_LO` **scratch**, not a uniform — a static constant-buffer read cannot resolve its value at point of use. |
| 5 | `--through-operation N` | Renders the **last executed draw target**, so adjacent prefixes show *different surfaces*. #1486's founding "corruption boundary" was 1920x1080 vs 3840x2160. Fixed by #1580's `target=`. |
| 6 | `git checkout <file>` | Restores from the **index** — it is not an undo for a temporary edit, and silently wipes uncommitted work. |
| 7 | A stale `in-progress` label | Read as occupancy. #1284's claim had been dead six days (no open PR, no activity); CLAUDE.md expires claims at 24 h. |
| 8 | An orchestrator's instruction | "If it is a missing capability, implement it" — all three opcodes were **already implemented**; following it would have duplicated a working `image_gather4_lz`. |
| 9 | `[compute] skip unsupported program` | **Deduped per `code_addr`**: one line means "failed at least once", NOT "always fails". A program running 763 times and failing once looks identical to a permanently blocked one. |
| 10 | `PROSPER_COMPUTELOG_CODE=<addr>` | Correctly protects a realtime route from ~13k submits of logging, and thereby **suppresses `execute` lines for every other program**. Right for one question, silently wrong for the next asked of the same log. |
| 11 | SHA reachability after a **squash** merge | `git merge-base --is-ancestor` reports "unmerged" for work that *is* on master. Verify by **content**, not by SHA. |
| 12 | `PROSPER_REGWATCH` **joined to** `PROSPER_EXECLOG` by stream adjacency | The two timestamp at **different pipeline stages** — REGWATCH at command-processor *decode*, EXECLOG at *realization* — and the phases interleave per submit. Read by log order, #1606 said **1,049 of 1,050** suppressed draws resolved against a *future* register write. Clean, compelling, and entirely false. Fixed by #1633's `order=`. |
| 13 | A **print-capped** diagnostic read as a frequency | `[agc] WaitRegMem … NOT satisfied` prints the first 40 then every 1024th (`ln < 40 \|\| (ln & 1023) == 0`); `[agc] out-of-range indirect reg write dropped` stops after **4**; `[agc] indexed indirect draw skipped` after 24. #1606 called the `WaitRegMem` volume "the loudest signal, dozens per second"; the true rate is **~1.5/s** (see below). A line count from these is a cap, not a rate. Same trap as #9's dedupe, different mechanism. |
| 14 | A **decoded-draw census read without the render phase** | Draw counts per frame are phase-dependent by two orders of magnitude *within one title*. Blue Prince (rung 3) decodes **7-13 draws/frame on its own menus** and **1,500-3,200 in its 3D scene**. So Nikoderiko's 53/frame on a title/EULA screen looks exactly like #1641's "implausibly few" signature and is simply **normal for a 2D UI screen**. Reporting it would have sent a lane chasing nothing. |
| 15 | "is this dword pair a mapped pointer?" | The user-data window is 32 dwords and holds many live pointers, so **several** seed offsets satisfy "every declared descriptor is readable". On #305 a shifted seed made all declared pointers land cleanly on **9 of 9** stages — and the hardware field that bounds the window (`SPI_SHADER_PGM_RSRC2_GS.USER_SGPR`) proved the stage cannot see there at all. A live A/B then raised rejects 118-141 -> 521. A numeric fit over a pointer-shaped predicate is weak evidence; find the register that bounds the search space. |
| 16 | One diagnostic **label** covering two packet kinds | #305's bind trace emitted `[bind] DRAW` from all five per-item snapshot sites in `GpuState::apply` — and **two of those are compute dispatches**. A dispatch never consumes `SPI_SHADER_PGM_LO_ES`, so a bind/draw agreement statistic computed over those lines mixes in events that cannot agree by construction. Graphics-queue dispatches read the *graphics* register file, so they print plausible `es_lo` and are **indistinguishable from draws in the log** — a re-run, not a re-parse. **Re-measured after relabelling** (a fresh boot — the two figures are not the same run: 434,239 bind packets vs 193,397), the contaminated "871,648 of 876,217 (99.5%)" became **300,404 of 300,404 (100%)**, so the residue had been dispatches. Tag every emitter with the kind it actually observed. |
| 17 | **Asymmetric** exhaustion of per-site caps | A cousin of #13. When one side of a paired trace caps (bind lines, one counter at 4,000) and the other runs on **five independent counters**, the first side dies during the pre-title load while the second keeps emitting; a "does each draw follow a bind?" analysis then reported a 41% mismatch that was pure counter exhaustion. Bound the analysis to the region where **every** stream is still logging. Related: `command_order` is **per-submit and resets**, so ordering events globally by it interleaves submits and manufactures impossible pairs. |
| 18 | `VAR=1 $unquoted_var cmd` | The shell expands `$unquoted_var` **after** it has decided which word is the command, so an expansion like `PROSPER_NO_PLUGIN_AUTOLINK=1` becomes the **command name**, not an assignment. The arm never runs, exits 127, and — with stdout redirected to a per-arm log — leaves an empty file that reads exactly like a legitimate "no output" result. #1609's founding 14,666-vs-18,167 "20% regression" came from an A/B whose OFF arm had never executed. Build env into the command directly, or use `env VAR=1 cmd`. |
| 19 | `$HOME` read as private scratch | Every lane on this box shares one `$HOME` (410 files at the time of writing). Generic scratch names — `pr-body.md`, `ab-result.md`, `app.log`, `apr.log` — **already collide across lanes**. Writing one destroys another agent's work in flight, and reading one silently answers your question with someone else's data. Title-scope every scratch file (`tales-pr-body.md`, `tales-rate-*.log`). The tell that saved it here: the Write tool **refused to overwrite a file it had not read**. A tool declining to clobber is a safety net, not an obstacle — an agent that "fixes" it by deleting the file and retrying destroys the thing the guard was protecting. |
| 20 | An **empty CI rollup** read as "queued" | GitHub does **not run checks on a conflicting branch at all**, so a PR whose base has moved reports `pass=0 fail=0 pending=0` with `mergeStateStatus=DIRTY` — permanently, until it is rebased. That is byte-identical to the "checks have not registered yet" state, and #1656 sat in it while master moved twice. Same shape as #18's empty log: **an absent signal mimicking a pending one**. Read `mergeable`/`mergeStateStatus` before concluding anything from a check count, and never wait on zeros. |
| 21 | The **same draw census, now read without the resident *content*** — trap 14 firing a second time, on the lane that recorded it | #1641 spent a session treating "~23 draws/frame is implausibly few for a 4K UE4 title" as the anomaly. Decoding the title's own asset reads against its pak index showed it loads **exactly one of the 481 maps** in the container — `L_GameloftSplash.umap`, **7,870 bytes** — and never opens `L_Main` or `L_StartMap`. 23 draws is a **complete and correct** deferred frame over a splash world. Worse, the premise was invalid in *both* directions: `L_Main.uexp` is **616 bytes**, so this title's front end is UMG/Slate and a *working* title screen would also decode few 3D draws. Nothing was ever missing. |
| 22 | A **runtime `eboot+0x…` offset** | Several diagnostics subtract the literal `0x400000000` while the eboot maps at `BOOT_EBOOT = 0x410000000`, so the printed offset is **`0x10000000` too high** — wrong but entirely plausible, and every downstream `edis.py`/`xref.py`/`PROSPER_BP` lookup then fails in a way that looks like a code problem. `boot_trace.cpp:97` uses the right constant, so two conventions coexist under one label. Filed as **#1659**. Tell: an `eboot+0x1…` offset **past the module image size** is a labelling artifact, not a wild pointer. |
| 23 | A **call-count table read as evidence of *what* is being waited on** | `PROSPER_PROGRESS_UNIMPL`'s per-NID counts showed **1,300,185,430 `select()` calls in 144 s** on PPSA19244 while every other socket call stayed a frozen singleton — a real, liveness-controlled measurement. It was then read as "the guest is blocked polling a socket", and a shared-substrate design decision (implement a loopback socket backing) was nearly escalated on it. Reading the **arguments** took one `PROSPER_STUBDUMP` lookup plus one gdb breakpoint and showed `select(nfds=0, NULL, NULL, NULL, {3,0})` — the portable **`sleep()`** idiom, no descriptor involved. The thread was trying to sleep 3 s; the 0-returning stub made it a no-op. Not a socket poll, not the stall — a CPU tax. A count answers *"is something spinning?"*; it can **never** answer *"on what?"*, and the function's name is not evidence. |
| 24 | A log whose **emission gate** is narrower than the population you reason over | `[udmap]` (#305) prints a stage's user-data map **only under failure replay**, so every row it emits is a stage that ALREADY FAILED. Its rows therefore sample failures, not stages — and a row showing readable pointers is a stage that failed for a *different* reason, **not a passing stage**. Building a pass/fail table from it silently answers a question it was never sampling. Same category error as #9 and #13 (a count that answers "did this ever happen?" but not "how often?"), one level up: here the **rows** are the biased sample, not the counts. Before treating any log's rows as a population, read the condition it is emitted under — grep the `if` above the `fprintf`, do not infer it from the message text. |
| 25 | `gh pr merge --delete-branch` run **from a worktree** | After the server-side merge succeeds, `gh` tries to check out the base branch locally and dies with `fatal: 'master' is already used by worktree at …` — which is unavoidable from a worktree, since master is checked out in the main tree. **The merge lands; the branch survives; the exit status does not cleanly separate the two.** Two lanes hit this in one session, both on their own merge. Read as "the merge failed" it invites a retry against an already-merged PR; read as "it all worked" it leaves a stale remote branch that later reads as an unfinished lane (and doubles as a false claim-lock, per the claim rules). Verify the two halves **separately**: `gh pr view N --json state,mergeCommit` for the merge, `git ls-remote origin 'refs/heads/<branch>'` for the branch, then `git push origin --delete <branch>` explicitly. |
| 26 | A **commit-SHA-pinned asset URL** in a PR body or issue | Screenshots referenced as `raw.githubusercontent.com/<org>/<repo>/<sha>/…` break silently once that SHA is **orphaned** — which a rebase does immediately and a squash merge does at merge time. The images render perfectly while the PR is open and are dead weeks later, when nobody can reconstruct what they showed. Pin progression evidence to a **branch that survives** (`…/master/assets/screenshots/…`) once the file is on it, and re-point any SHA URL after the final rebase. Cousin of #11: both are the same mistake of treating a SHA as a stable name across a history rewrite. |
| 27 | **A confident document header with no observation behind it** | Three parked `reach-gameplay.pad` drafts (Joe & Mac, Asterix, Summer Sports) carried three *different* title-specific rationales — "Options is a fallback for a start-button title prompt", "the title screen is reached with no input at all", "Cross confirms the default selector entries" — over **byte-identical input sequences** (`md5` of the non-comment lines matched across all three). Three different findings cannot come from one identical button mash, so the prose was generated, not observed. The genuine routes were in the tracking issues the whole time. |
| 28 | A **distinct-colour count quoted across two different render scales** | Triage issue #1593 recorded Joe & Mac gameplay at **444,000-468,000** distinct colours; profiling the same route through `tools/snapshot` measured **at most 79,167**, a 5.6x gap that reads exactly like a severe colour regression. Both are correct. Triage captures run the `screenshot` frontend at native 1920x1080; every snapshot guard runs at `scale: 4`, so the frame is **480x270 = 129,600 pixels** and cannot exceed that many colours by construction. A 3840x2160 title (*Summer Sports Games*) yields 960x540 at the same scale, so even `dims` differs between titles. **Never lift a `min_colors` threshold from a triage issue**, and read `dims` off the profile instead of assuming the 1080p value. |
| 29 | **Ranking candidate guard windows by metric stability** | Choosing a content-guard window by "tightest colour spread / highest self-SSIM" is the obvious automation, and on *Summer Sports Games* it confidently selects the settled span from 150 s to the end of the route: 48,201-48,489 colours, 1.0000 coverage, **SSIM 1.000 against itself** — the best-scoring window in the entire run by every available metric. It is the static `JAVELIN THROW` **standings overlay**, not gameplay. A guard adopted there would have passed forever while the athletics scene collapsed. Stability is a property of *held* frames, and menus, results screens and pause overlays are the most held frames a title has, so the metric is actively biased toward them. Rank windows to shortlist; **decide by opening the images**. Same family as the earlier note that a diagnostic clear can out-score real content on distinct colours. |
| 30 | **A wall-clock guard window placed in a route that keeps moving** | A content guard samples by wall-clock seconds, but in a route that drives the player *forward* the scene at second N is a function of ~N seconds of accumulated progress, so ordinary run-to-run variance moves the **scene**, not just the timing. Joe & Mac's movement route passed `verify` twice and a `check` twice, then the third run drifted onto a level transition plus two death fades and failed 19 structural matches against 24 required — no build change, same route. Replacing it with the **stationary** route inverted the problem: the player is killed every ~22 s but respawns to the *same* opening screen, so a 73-frame window spanning three lives is one visual state and tolerates the deaths (69 matches against 55 required, worst-frame SSIM 0.97 instead of 0.81). The more impressive route made the worse guard. Prefer a route whose guarded span is **self-restoring** — an idle screen, a respawn loop, a held camera — over one whose span is a trajectory, and treat two green runs as far too small a sample to call a moving window stable. |
| 31 | The **first `grep` hit for a register**, when it has more than one consumer | Tracing why Astro's draws resolve `cwm=0`, `CB_COLOR_CONTROL.MODE` is read at `render_state.cpp:143` — where it feeds **only** the gated `PROSPER_MSAA_LOG` diagnostic. Reasoning from that site says MODE does not participate in mask resolution, which **inverts the conclusion**. The site that matters is ~650 lines later, where `MODE == DISABLE` explicitly zeroes `color_write_mask`, `color1_write_mask` and every `color_targets[].write_mask`. Same symbol, two consumers, opposite implications — and nothing at the first site announces that it is not the one. **Find the consumer on the path you are reasoning about**: for a render-state question that is where the pipeline state is built, not where a diagnostic prints. |
| 32 | **`std::mismatch` read as a failure count** | The R11G11B10 exactness sweep (#1681) printed its first difference with `std::mismatch`, which returns only the **first** differing element, while the assertion compared the **whole vector**. The printout is byte-identical whether 1 texel differs or 92 — so "the same single texel fails on both machines" was a number the instrument never produced, and the supporting argument built on it ("a genuinely wrong encoding would fail broadly rather than at a single value") had no evidence behind it. The real count was **92**. Cousin of #9/#13/#24: an instrument that answers *"did this happen?"* being read as *"how often?"* — here at the level of a single printed element rather than a rate. **When an assertion compares a collection, the diagnostic must summarise the collection**: count the differences, classify them, and report the classification, because the breadth of a failure is usually the thing that selects between explanations. |
| 33 | **A skipped draw's clear colour read as a rendered pixel** | Six assertions in `test_recompiled_fragment` and one in `test_recompiled_shaders` "failed a rendered-pixel comparison" on lavapipe (#1681). No comparison happened: the shaders require an exact 64-lane fragment subgroup, lavapipe is fixed at 8, and prosper **correctly refused the draw** (`[render] skip draw: fragment shader requires subgroup size 64 (device range 8..8)`, 15 times). The target simply kept its BLUE clear, and a clear colour is a perfectly plausible-looking pixel value — nothing about `center=(0,0,255,255)` announces that no draw ran. The neighbouring assertions in the *same files* already gated on `supports_fragment_wave64_vote`; these did not. **A pixel assertion is only evidence if the draw executed** — assert the capability, or assert the clear, but never read back a target without knowing which of the two you are looking at. Note the failure direction: a fail-visible skip is designed to be loud, and it was — on **stderr**, while the assertions printed to stdout, so the two never appeared together in the ctest summary. |
| 34 | **A check of the ANSWER read as a check of the RULE** — and a shape shared by several entries here | Deriving an NV12 frame size (#1687), the rule "`w*h*3/2` and `wh + (wh+1)/2` agree" took three attempts: "identical for every reachable input", then "agree whenever `w*h` is even", then the correct one. **Two** were wrong, each in a smaller way; the third was right. The truth is **exact iff BOTH dimensions are even** — independently re-derived three times, most widely at 200x200 (40,000 pairs), never over-reporting — an even *product* is not sufficient and is actively a trap, since `1920x1081` has an even product and the derivation is **960 bytes short**. The error survived three rounds of competent review because every size anyone checked (`1920x1088`, `1920x1080`, `640x480`) is **both-even**, so each spot check confirmed the true rule *and* the wrong one equally. **The cases in front of you are not a sample; they are a coincidence until you show they discriminate.** An exhaustive grid settled it in one command and could have been run first. The general form: verifying the *answer* on the examples you already have tells you nothing about the *rule* — you must ask what input would distinguish your rule from its near-miss, and check that one. Named instances rather than a fraction: an A/B whose arms agree when they must differ (the tell under #10), a pre-registered decision rule that cannot separate its two hypotheses, a vacuous assertion that holds whether or not the event occurred, #33's pixel assertion on a draw that never ran, and this one. **Every one of those PASSED**, and that is the mechanism of concealment, not a coincidence: a green result is **self-authenticating**, so a check that *could not have failed* is indistinguishable from a check that genuinely passed, and both get filed as confirmation. A failing check gets investigated; a vacuous one gets believed. **So the question to ask of your own test before you write down what it showed is not "did it pass?" but "what result would have falsified it?"** — if there is no answer, the test established nothing, however green. Note where it bit: this is normally reached for around *instruments*, and here it was plain arithmetic — nobody thinks to apply the discipline to a formula. |
| 35 | A **`.bmp` and a same-named `.prgbundle` assumed to be one F9 grab** | The frontend writes a grab's bundle through a `.tmp` and its screenshot separately, so **killing the app mid-grab leaves a `.bmp` whose same-named `.prgbundle` is from a previous grab entirely**. A Dragon Quest VII investigation was handed `dq7_grab_001.*` and `dq7_grab_002.*` as "two frames of the same scene in two states" — the highest-value comparison available. `stat` showed 001's pair 0.2 s apart, but `dq7_grab_002.prgbundle` **51 minutes older than `dq7_grab_002.bmp`, from a different boot**; the screenshot's real partner was the abandoned `frame_grab_002.prgbundle.tmp` written 0.7 s before it (and it loads fine). The mismatched pair was a 1920x1080/1519-draw **scene** submit against a 3840x2160/97-draw **composite** submit, which would have manufactured a large, entirely artefactual structural difference in exactly the comparison the investigation was told to trust. Names and adjacent ordinals are not provenance: **`stat` the artifacts and confirm sub-second pairing before treating a screenshot as evidence about a bundle** — and prefer a `.tmp` with the right mtime over a completed file with the wrong one. **Fixed at the source (#1693 / #1694), and the replacement rule is exact:** a grab claims BOTH names when it is armed, from one timestamp and at one collision suffix, so **the two artifacts of one grab always carry the identical full stem, suffix included** — the suffix is a property of the capture, not of a file. So pair on the **whole stem**, never on the timestamp: `…-210000-123.bmp` and `…-210000-123-2.prgbundle` are two *different* grabs despite an identical title and millisecond, and reading them as a pair re-creates this exact trap with more confidence than before. And note the superseded `stat` check does not merely go *quiet* on such a pair — it **endorses** it: capture A's screenshot and capture B's bundle are written tens of milliseconds apart, so "confirm sub-second pairing" returns a confident **false positive**. A check that actively affirms the wrong answer has to be superseded in writing rather than left to fall into disuse — which is why this row states where it still applies rather than simply deleting it. The `stat` habit remains correct for artifacts captured before that landed — `captures/frame_grab_001.*` in the main checkout is exactly that shape — and for anything else named by ordinal. |
| 36 | **`snapshot.py verify` passing, on a scene the title regenerates randomly** | `verify` builds its structural references from **both** of its own two runs and then asks whether those runs match that combined set — so a scene that is *different every run* still passes, because each run matches the half of the reference set it contributed. Worms Armageddon's Quickstart generates its terrain per match: two fresh-save runs produced a pirate/treasure map and a fairytale/castle map, each scoring 70/70 against the combined references and looking like a healthy `CONTENT-STABLE` baseline. Measured **across** runs instead — run B's frames against run A's references **only** — the same window scores at most **0.6567**, with **0 of 70** clearing the 0.85 floor. Such a guard would be adopted green and fail on the very first `check`, whose third random map matches neither. The tell is available before any capture: the 16x9 SSIM is highly layout-sensitive on this title (the *same* map mirrored scores 0.4595), so any terrain variation is fatal. **Before adopting a window, score one run against the other run's references alone** — verify's own output cannot answer this, and 'it passed verify twice' is not evidence of stability (cousin of #30, where two green runs were also too small a sample). The fix here was a route change, not a threshold change: Training levels are authored fixed maps and cross-run scoring on them gives 0.9612-1.0000, 47/47. |
| 37 | **A key injected into `prosper-app` that the app never receives — silently** | Validating the F9 frame grab (#1693) on this box, `xdotool key F9` reached a correctly named, correctly focused XWayland window and produced **nothing**. Two readings were available and both were wrong: "F9 is broken" and "my change broke F9". The controls settle it: injected **Esc quits the app** and injected **Pause pauses** it, while **F10 and F11 — which this change does not touch — are lost exactly like F9**, and `Pause` and `F10` are literally the *same command* in the same handler. So XTEST on this host does not deliver **function keys** to the window; nothing about the app is implicated. `ydotool` (uinput, `ydotool key 67:1 67:0` for F9) does deliver them, and the whole F9 path then worked first time. **Two rules:** a key-injection experiment must carry a positive control through the *same* path — end the run with an injected `Esc` and require the app to exit, or the negative result is void rather than negative — and `2>/dev/null` on the injector hides the commonest failure of all (the first attempt ran `xdotool` **inside** the distrobox, where it is not installed, and the redirect swallowed `command not found`). |
| 38 | **A driver-version split, read as a driver bug** | Two 3D storage-volume assertions in `test_game_compute` failed on Mesa 25.2.8 lavapipe and passed on Mesa 26.1.4 lavapipe **and** on RADV (#1690). Both driver *classes* agreed with each other and disagreed with one version, which is about as clean a version signal as this list ever gets — so the working hypothesis was an upstream lavapipe defect, and the proposed resolution was a version-conditioned skip. It was **prosper's own fixture**: `image_copy_3d` prepared a third address register and called itself 3D while leaving MIMG.DIM encoded as 2D, so the recompiler faithfully emitted `OpTypeImage … 2D` with two coordinates while the backend bound the `VK_IMAGE_VIEW_TYPE_3D` view its 3D *resource* descriptor calls for. A 3D view on a descriptor whose module declares Dim 2D is **undefined** in Vulkan, so all three results are conformant and the "version axis" was three drivers each resolving one piece of UB their own way. Encoding DIM=3D passes everywhere. **The rule this teaches is about what a cross-implementation split licenses you to conclude: it localises the disagreement, it does not assign the fault.** Two implementations agreeing does not make them right — UB is agreed on far more often than it is diagnosed — so before attributing a split to the odd one out, ask what *you* are asking all of them to do, and whether it is defined. The cheap discriminator was available the whole time and settles it in one run: a **standalone reproduction of the same operation** with none of prosper's code in the path (own instance, device, images, hand-written kernel). It passed on 25.2.8, which exonerates the driver and points the search back inside — the inverse of the usual instrument trap, since here the *subject* was fine and prosper's own input was malformed. Two corroborations were also sitting in the tree, unread: `fill_3d`, in the same test file, already encoded `0xF0200F10` as `dim:3D` and passed on 25.2.8, and `test_rdna2_decode.cpp:309` pins the same DIM field — bits [5:3] = 2 — against an `llvm-mc gfx1010` capture, though on a different literal (`0xf0000f12`: another opcode, plus an NSA bit), so compare the field rather than the constant when following that pointer. Cousin of #34 — a green result on two of three configurations is self-authenticating, and "it works on the good drivers" is exactly what correct code and undefined behaviour look like alike. Note the cost of the wrong reading: the proposed skip would have removed **484 executed assertions** from CI permanently, annotated as a driver limitation. |
| 39 | **A diagnostic that falls silent in the very cases it was cited to distinguish** | Issue #1692 described `PROSPER_AUDIOLOG=1` as "a three-way instrument whose negative result is informative in each branch" for Dragon Quest VII's missing audio, listing *no lines* -> the guest never submits, *`1s-peak=0.0000`* -> silent PCM, *non-zero peak* -> our sink. Reading the code inverts that: the `[audio] port N` line is only reached after `have_pcm && open_ok`, so it never prints when no MAIN port yields frames, and its companion `PROSPER_AUDIO2_PROBE` deliberately prints only ports whose `\|max\| > 0`. **Both fall silent in branches 1 AND 2**, so the documented "the guest never submits" signature is also exactly what a fully-submitting title produces. A sharper form sat one level down: a port rejected for an unsupported layout (DQ7's 12-channel `fmt=0xc00`, discarded 188x/s) is **never read**, so "no audio there" was an assumption no instrument had tested. **Two rules from that half.** A negative result is only evidence if the instrument would have printed something in the other branches — check where its print sits relative to the early-outs, not what its docstring claims. And a suppression added for log hygiene silently narrows what the instrument can *prove*: `\|max\| > 0` was a reasonable anti-spam filter for object-heavy titles, and it is precisely what made silence unreportable. **Then the replacement instrument caught its own author with the same class of error, one level up — and this is the more useful half.** Its counters reset every second, and the first reading took the LAST line as the run's answer: port1 `nonzero=0/580608` -> "the guest submits only silence", case 2, defect upstream in the title. Wrong, and inverted: that port's run total is **1,782,048 non-zero of 18,456,576** samples at peak **0.38976**, while the 8-channel port prosper actually forwards is the exactly-zero one. The truth is case 3 — **prosper's own mix loop discarding 2.31 MB/s of real audio** because the port declares 12 channels and the loop rejects >8 (#1700). Music has gaps; any single interval can land in one. A second defect compounded it: the per-port table was global while the report iterated it per context, so each context printed **and reset** the other's counters, splitting every port's per-second totals across unrelated lines. **A per-interval counter answers "what happened in the last second", which is not the question "does this port carry audio"** — so the fix is a never-reset lifetime total on the same line, making one line sufficient. Generalising past audio: whenever a diagnostic resets on a timer, one sample of it is a *rate*, never a *verdict*; if you intend to conclude something about the run, aggregate the run or print a running total. Cousin of #34 — ask what result would have falsified the reading. Note where both errors sat: on the **believing** side. The instrument printed enough to catch each one, and the reading did not use it. |
| 40 | A **pipeline's exit status**, read as the exit status of the command you cared about | A shell pipeline reports the status of its **last** stage, so `cmd \| tail`, `cmd \| head` and `cmd \| grep` all discard `cmd`'s failure. Four people hit this in one session, in four disguises: `ctest … \| tail -3` inside an `&&` chain sailed **past a red ctest** and committed (the test source had been restored but not rebuilt, so it was also running a stale binary); `… \| head` truncated a run whose failure was below the cut; `printf "$?"` after a pipeline printed the wrong stage's code; and `grep -c` **exits 1 on zero matches**, so a legitimate "none found" result kills the rest of an `&&` chain. The two failure directions are opposite and both silent: a red that reads green, and a green that aborts the chain. **Never join a command whose status you need to a pipe inside `&&`.** Capture it (`cmd > log; rc=$?`), use `set -o pipefail`, or check `${PIPESTATUS[0]}` — and separate `grep -c` with `;` rather than `&&`. Cousin of #25, where a merge's two halves shared one exit code. |
| 41 | A **wholesale `git checkout <branch> -- <file>`**, read as "I brought my edit across" | Rebuilding a branch onto a moved master, taking `tools/AGENTS.md` whole from the pre-rebuild branch **silently deleted the ten lines #1701 had just added** documenting `check_numbered_table.py`. No conflict, no failing job, and the diff read as the clean one-bullet edit it was supposed to be — the file simply reverted to its older state wherever the other lane had touched it. It surfaced only because `--stat` said `12 +-----` for a file believed touched once. **The new `Docs` CI gate cannot catch this**, by design: it validates table structure and numbering, never prose, and `AGENTS.md` has no numbered table — so the numbering half of the two-lane collision problem is now mechanically guarded and the content half is not. After any wholesale checkout of a file another lane may have touched, **diff it against `master` and read the `-` lines, not the `+` ones**. Same family as the clean-merge-semantically-broken hazard: the operation succeeds, the diff looks right, and the loss is invisible without asking what was removed. **A SECOND DELIVERY MECHANISM, and it is worse: STALENESS, with no wrong command at all (#2082).** A branch rebased earlier that then sits while master moves will, on merge, delete other lanes' work from shared documents — no `git checkout`, `mergeable: CLEAN`, every required check green, and the deletions reading as the author's own diff. Two near-misses within an hour: the R-Type Delta lane (#2061) would have deleted the entire Crisis Core rung-2 section from `COMPATIBILITY.md` (landed by #2060) and reverted `CLAUDE.md`'s `[compute] skip` correction (landed by #2074) — after rebasing again, `CLAUDE.md` went to +1/-0; the Crisis Core lane (#2060) had the same shape caught by review, because #2055 had appended to the very `ARCRUNNER_STATUS.md` Ruled-out table it was editing. **What makes this variant worse than the original: trap 41 proper requires someone to run a specific wrong command, so it is avoidable by not running it. This one requires NOTHING — only time between a rebase and a merge, which every PR has.** With several lanes live and master moving every few minutes it is the default state, not the exception. The defence is the same one, made per-file and run against CURRENT master immediately before pushing — a `+N/-0` on a file you only added to is the shape you want, and any `-` line in a file another lane owns is the alarm: `for f in COMPATIBILITY.md CLAUDE.md prosper/docs/GAME_COMPAT_ORCHESTRATION.md; do echo "$f +$(git diff origin/master HEAD -- $f \| grep -c '^+[^+]') -$(git diff origin/master HEAD -- $f \| grep -c '^-[^-]')"; done` |
| 42 | **A validation layer that is enabled but reporting nothing** | Switching `VK_LAYER_KHRONOS_validation` on for the first time (#1704) and seeing a test print no findings is **not** evidence the test is clean, and there are two independent reasons for the silence that look identical. The loader may never have inserted the layer at all — `VK_LOADER_LAYERS_ENABLE` names a layer that need not be installed, and nothing complains. Or it loaded and its `report_flags` default to **`error` only**, so a probe asking for best-practices warnings sees nothing; that first probe here also used the wrong setting key (`validate_best_practices` instead of `enables = VK_VALIDATION_FEATURE_ENABLE_BEST_PRACTICES_EXT`), a silent no-op that reads exactly like success. **Prove the reporting path before trusting a null result**: `VK_LOADER_DEBUG=layer` must print `Insert instance layer "VK_LAYER_KHRONOS_validation"`, and a deliberate violation must come back named. `tools/vkval` now refuses to report clean until it has watched the loader say so. **The second half is about what a green run of this guard does and does not license.** Its finding set is driver- and layer-version-dependent **in both directions**: the same suite yields **9** message ids on RADV with layers 1.4.341 and **7** on lavapipe with layers 1.3.275 (the CI runner) — **8 and 6 after #1726 fixed `pCode-08737`**; `tools/vkval/allowlist.txt` carries the current baseline, and this row is the historical measurement, and the two absent ones are absent for reasons unrelated to correctness — `VUID-vkCmdDispatch-maintenance4-08602` is a newer check than 1.3.275, and `VUID-VkMappedMemoryRange-size-01389` cannot fire on lavapipe, whose `nonCoherentAtomSize` is 1, so every range is trivially aligned. A defect that only a stricter layer or a stricter device reports is invisible to a green CI run, so run the guard on hardware too. Cousin of #34 and #39 — ask what result would have falsified the reading, and where the instrument's early-outs sit. |
| 43 | **A declared format read as a description of the content** | Dragon Quest VII's MAIN AudioOut2 port declares `data_format=0xc00` -> **12 channels**, and #1700 was specified from that number: "12 channels is a plausible real layout (7.1.4 - a 7.1 bed plus four height channels)", with the fold-down's difficulty framed as recovering the height tier's order. Measured with `PROSPER_AUDIO_LAYOUT=1` over **19,962,368 frames**, the bed is a **stereo mix in a 12-channel container**: ch0/ch1 carry everything (peak 0.407, mutual correlation +0.46), ch2 and ch4..ch7 hold a ~1e-9 residue, and **ch3 and ch8..ch11 are exactly zero for the entire run** - five channels never written at all, and ten of the twelve carrying nothing audible. The design problem the issue posed did not exist. Two mechanisms, and the second is the one to carry away. First, a **declared** format describes the container the guest allocated, not what it wrote into it; a title may open the widest bed the platform offers and fill two channels of it, exactly as a 4-channel texture may be written with one. Second, and worse, the corroboration looked independent and was not: the `2,310,144 B/s` in the issue is `frames x channels x 4` with `channels` taken from the same decode, so the byte rate could not have disagreed with the channel count **whatever the buffer really contained** - it restates its own input. Same shape as #34: a check that could not have failed, read as confirmation — and a close neighbour of **#42**, which is the same trap approached from the reporting side: there a positive control passed in the development environment while observing 0 of 187 messages in CI, so the instrument was silent for a reason unrelated to the subject. Both entries reduce to the same question — *what, exactly, produced this number, and could it have produced a different one?* **A statistic derived from a decode cannot cross-check that decode**; you need a measurement whose value comes from somewhere else. Two were available here and only one delivered: the per-channel content settled it, while the guest's grain-pointer stride - the other independent measure, and the more direct one - returned nothing, because this title republishes a single buffer and never exposes a second address to difference. Build both anyway; you do not know in advance which the guest will let you have. The practical rule for any width/stride/format field: before designing for the declared shape, measure the payload, and note that this cuts **both** ways - the same evidence that showed ten channels empty is what confirms the two that are not, and reading `rms=0.000000` alone would have merged "exactly zero" with "1e-9 residue", which are different findings (`nonzero%` separates them). **Postscript, one hour after this entry was written: its author nearly filed a second finding from the same investigation — a `mov edx,0x800` inside the title's UE mixer, read as the port `data_format` it had spent the day looking for. It is a `memset` size; the call target is an import stub, so the three registers are `memset(dst, 0, 0x800)`.** Caught only by disassembling the callee instead of trusting the constant. Knowing the rule does not make you immune to it — a value that matches the shape of your answer is read as your answer, and the defence is mechanical (check what consumes the constant) rather than attentional. |
| 44 | A **documented gate whose external tool was never installed**, and a corpus that never grew | `tools/spv_validate` is what CLAUDE.md, both READMEs, `ROADMAP.md` and `RECOMPILER_REMAINING.md` mean when they say prosper's shaders are strictly `spirv-val`-gated. It shells out to `spirv-val` — and when the validator was absent it printed **`== PASS (recompiled; spirv-val not found) ==` and exited 0**. `spirv-val` is on no GitHub runner image and `ci.yml` never installed `spirv-tools`, so **the gate had never validated a single module in CI**; it degraded the same way on any dev host without the package, and unconditionally on Windows, where the availability probe redirected to `/dev/null` that `cmd.exe` cannot open. What it cost is #1711: `build_compute_compare_uvec4()` walked struct -> runtime array -> `uvec4` and claimed a `uint` pointer for the result, and that module is created at runtime by the live frontend (`live_compute.cpp`'s compare pipeline), not only by a test. **Drivers are not required to validate SPIR-V and neither lavapipe nor RADV does**, so it was accepted everywhere and reported by nothing until the first validation-layer pass (#1704). The second half is coverage: the corpus only ever walked `recompile_*`, so `spirv_builder.cpp`'s hand-assembled modules were **not in it and nothing said so** — an emitter is invisible to a gate that does not know it exists. **Two rules. When a check delegates to an external tool, the tool's absence is a FAILURE, not a pass — and 'the gate exists' is not 'the gate runs', so check that its dependency is installed where it runs, which for a `system()` call means reading the CI dependency list, not the test's source. And a fixed corpus silently stops covering the code as the code grows: derive the list of things that must be checked from the source (here, every emitter declaration under `src/gpu/`) so an omission fails instead of going unmentioned — and derive it under **every spelling the code actually uses**: the first version of this scan keyed on the literal `std::vector<uint32_t>` return type and therefore could not see the live draw path's `SharedShaderWords` entry points at all, reinstating the same blind spot one level up while the docs asserted it was closed.** Closed in both directions plus mutation-tested: **eleven** deliberate re-introductions each make it fail — invalid access chain; an uncovered emitter declared in an existing header, in a NEW header, in a new SUBDIRECTORY, in the frontend root, and under the other return-type spelling the live path uses; missing validator; unwritable output directory; stale gap entry; gap naming no emitter; and an emitter that is called but whose module is never validated. The last two mutations are the ones worth copying: the first drafts of this guard read the tool's own source text for a call-shaped string, so a mention in a comment satisfied it and a call whose result was discarded satisfied it. Coverage is now a **runtime** fact recorded where each module is emitted, which is the property meant all along — **if your check is a proxy for the thing you care about, mutate the gap between them, not the thing** — and mutate it along every axis the proxy could be wrong on, because the first three revisions of this guard were each closed on one axis (text, then filename, then return-type spelling) while remaining open on the next. Note the trap caught its own fix, which is the sharpest part: the first draft recorded `recompile_interpolation_geometry` as a known-gap 'because #1715 says spirv-val rejects it'. **Measured, spirv-val accepts it** under both the universal and vulkan1.1 environments — #1715's `VkPipelineShaderStageCreateInfo-stage-00715` is a *pipeline-creation* VUID that spirv-val does not check — so the emitter is fully covered and the gap list is empty. An issue title is a claim about where a defect lives, not a measurement; running the tool took one command. Cousin of #34: a green `== PASS ==` is self-authenticating whether it validated 49 modules or none. |
| 45 | A **shell string passed to `gh` as a comment body**, read as literal text | Posting a measurement with `gh pr comment --body "…"` ran it through the shell first. A backtick-quoted identifier inside it — `` `pCode-08737` ``, part of a quoted diff excerpt — was taken as **command substitution**, and the comment posted with that token **silently deleted**: `insert at master[1334] -> ' — **8 and 6 after #1726 fixed **; …'`. `gh` exited 0, the URL printed, and the rendered comment looked entirely well formed. **The damage lands in the durable record rather than in your terminal**, which is what separates this from the ordinary quoting mistake — you are not debugging it later, you are not looking at it at all. Note which case this was: the backticks held a NON-command, so bash printed `pCode-08737: command not found` and there was at least a warning to notice. Wrap something that IS a command — `` `date` ``, `` `ls` ``, `` `git status` `` — and substitution succeeds **silently** and splices its output into the permanent artifact; `$(…)` in a pasted log excerpt executes it. So the visible failure is the lucky branch, and reasoning "I would have seen it" from a case where you did is exactly backwards. **The defence is mechanical, not attentional: a quoted heredoc (`<<'EOF'`) or `--body-file`, never an interpolating string** — and this is the same trap as the pipeline in #40, one layer out: the shell silently rewrites what you thought was data. Escaping backticks by hand inside `--body "…"` does work, and is luck rather than discipline the moment a body is pasted rather than typed; two agents' bodies survived this session only that way. Applies equally to `gh issue create/comment`, `gh pr create --body`, and `git commit -m` with a pasted message. Repair with `gh api -X PATCH repos/OWNER/REPO/issues/comments/<id> -F body=@file` and say what was eaten, since a reader cannot tell a mangled measurement from a wrong one. |
| 46 | **Three samples of a monotonic series, read as the value of the series** | Tracing Astro Bot's dominant compute program (#1732), the first three `PROSPER_COMPUTELOG_CODE` lines all read `groups=1x1x1` — one 256-invocation workgroup. That is a clean, repeated, self-consistent reading, and it licensed the conclusion that the launch geometry was fine and the kernel's 128,005-word SPIR-V module must be why one dispatch took 982 ms. Both halves were wrong. The **whole** series grows: `groups_x` is 1 for the first 22 dispatches, then increments by exactly 3,345 every dispatch and never decreases, reaching **752,646 workgroups (192.7 M invocations) by dispatch 247** and still climbing. The geometry was the entire defect, and the sample that hid it was drawn from the only region where the series is flat — its first frames, which is precisely where a `head -3` lands. Cousin of #34: the cases in front of you are a coincidence until you show they discriminate. **The rule is specific and cheap: when a value might drift, never quote an element of it — print the series, or at minimum its first, middle, last and the deltas.** `sort \| uniq -c` on the traced field would also have exposed it in one command, since a constant collapses to one row and a drifting value does not. Note which direction the error ran: the wrong reading was *more* interesting (a recompiler defect in a huge module) and would have sent a lane into the SPIR-V for something a two-column table settles. |
| 47 | **A phase timer that reports a NEGATIVE interval, and relocates the bottleneck while doing it** | `[compute-phase]` initialises every phase mark to `phase_start` and only advances `phase_dispatch`/`phase_writeback` on the success path, so a dispatch that fails early prints the phase spanning the break computed **backwards** — a real Astro Bot record reads `dispatch_ms=-61.52` — while `cleanup_ms` silently absorbs the entire record (#1744). Which field goes negative depends on where the break landed, so a consumer must check every phase rather than grep for one field name. On a title where **82 % of the dispatches that reach `execute_item` fail** (63 % of all guest dispatches — the two figures are true of different populations, and this sentence is about the density of broken records *in the file*, #1743) this is not an edge case, it is most of the data: aggregating the log naively yields `dispatch (GPU) = -20,532 ms` and moves 46 % of the run into `cleanup`, which reads as a perfectly plausible "cleanup is the bottleneck". **The instrument does not fall silent on failure — it confidently names a different bottleneck**, which is strictly worse than #39's silence, because there is nothing missing to notice. Two rules. **A decomposition must be split by success before it is summed**, since failed and succeeded work are different populations with different meanings for the same field names. And **an aggregator should assert its own arithmetic is possible** — elapsed time is non-negative, shares lie in [0, 100], children do not exceed their parent — because those checks cost one line and are the only thing standing between a broken emitter and a confident wrong table. `tools/perf/compute_phase_report.py` now excludes `ok=0` records, reports them separately, and warns on any negative total — on stdout as a banner inside the table as well as on stderr, because `> table.txt` discards stderr and keeps a table that still looks authoritative. |
| 48 | **A mutation that turns the SUITE red, read as proof that the check you wrote for it works** | The escalation of #34 and the natural next error after "mutate the mechanism and watch it fail". Verifying #1747, every fix was mutated away and the suite confirmed red, and the commit message said so. **One check had nevertheless survived its own mutation, masked by three red siblings.** `"0.00" not in token` was standing in for "is zero", and it was wrong in both directions: changing an unrelated fixture constant from 10 to 13 turned it red (it scanned the whole output and caught the phase table's row, passing only because `"10.000"` contains `"0.00"`), while deleting the alias exclusion it guarded left it **green**, because the buggy value is `0.001` and `"0.00" in "0.001"`. Suite granularity cannot see this: a survivor hidden behind other failures is indistinguishable from coverage. **Record the check name per mutation, not the suite's colour** — assert that the mutation is killed by the check written for it, and treat "killed by something else" as a failure. Making that mechanical (`tools/perf/mutate_compute_phase_report.sh`) immediately found two more defects in the very suite that had just been declared fully mutation-checked: a fixture whose succeeded and failed records **cancelled**, so the negative-time assertion proved nothing, and a mutation aimed at the *reporting* filter when the *exclusion* filter was the mechanism — killed by an unrelated check, which at suite granularity reads as success. Note the compounding shape: this is the **fourth** member of the family inside one PR whose subject was documenting rows 46 and 47 of this very table — and row 46 is itself *about* quoting one element of a growing series, which is the same error one level down. Knowing a rule demonstrably confers no immunity to it, which is the argument for mechanising the defence rather than resolving to be careful. And the specific trap in the substring: **a formatted number contains other numbers**, as a prefix (`"0.00"` in `"0.001"`) *and* as an infix (`"0.00"` in `"10.000"`) — both of this row's own examples — so `in`/`not in` against a rendered number is never a numeric comparison. Parse the field and compare it. **Finally, the residual, which this entry of all entries must not overstate: mechanising per-check attribution does not close the ladder.** The committed harness shipped with a *fifth* instance in it — a mutation broader than the defect it stood for (returning on ANY alias where the original returned only on ALL), which let an unrelated check claim the kill and print `killed by:`. So there is a level five: **the mutation must reproduce the defect's shape**, or the per-check assertion is satisfied by a check that could not have caught the original. Expect a level six. |
| 49 | **A falsified hypothesis, still believed after the boot it was measured on stopped existing** | `COMPATIBILITY.md` recorded *"Input gating — falsified. A 200 s route mashing cross/circle/triangle/options left `draws_last=28` across all 99,427 submits"* for Tales of Graces f (`PPSA19991`), and every later pass — including two that re-measured the title — reasoned from it and looked at the opening movie instead. The measurement was sound when taken. Then #1656 and #1666 landed, the guest started reaching a **modal confirmation dialog** it had previously never got to, and an identical mashing route now takes `draws_last` from 8 to 132 and renders the title screen, the EULA, the main menu and the new-game Options screen (#1609). **Input gating was the answer the whole time after those two fixes.** The trap is not the original test; it is that a `## Ruled out` entry is written as a timeless fact ("input gating is not it") when what was measured is a fact about one build ("input gating was not it *at that boot depth*"). A negative result about **guest behaviour** decays whenever a fix changes how far the guest gets, and progression fixes do that by construction — so the entries most likely to go stale are exactly the ones on titles that are actively being advanced. Two practical rules. **Record the boot state a negative result was measured on**, not just the result: the commit, and cheaply, the depth (`draws_last`, the last phase reached, the last visible frame). And **re-run the cheap negatives after any fix that moves a title forward** — an input route costs one run and is the cheapest hypothesis in the catalogue, so it should be re-tested rather than inherited. Cousin of #34 in reverse: there a green check could not have failed; here a red check could not have *stayed* red, and nobody asked it again. |
| 50 | **A diagnostic that reports what came OUT of a transform, read as a description of what went IN** | Earthion's (`PPSA28061`) rejected 320x224 composite samples two textures and its resource table held one, so #1590 concluded the AGC front half had *recovered* only one of the two direct T# slots and re-scoped the blocker to descriptor-decoding in `agc_shader_layout.cpp`. Measured with a new instrument, the front half declares and reaches **both** (`ro[0] offset_dw=1`, `ro[1] offset_dw=9`, `samp[0] offset_dw=17` — exactly the shader's own decode); the second is removed by the degenerate-T# guard because the guest's user-data registers hold **stack residue** at those dwords. **The follow-on reading of that residue — "a `0x7f…` value is host stack, therefore an HLE returned success without writing an out-parameter" — is itself falsified, and by the same output-vs-input mistake one layer down:** prosper runs *guest* threads on host pthread stacks, so the numeric range names no owner, and the classifier added in #1590 puts a guest-module **executable** address (`eboot+0x12255`) inside the same 32 bytes. It is the guest's own uninitialised local — the guest asks a render-target group for index 1 when it created the group with one target, ignores the resulting `0x8A6C0010`, and binds the untouched stack slot — with no HLE anywhere in the chain. Read the residue as an address before attributing it to a writer. Every diagnostic in that function (`[t#]`, the `[mimg-unresolved]` table dump) described its **output**, and `build_shader_resources` has around ten `continue`s between input and output — so "never declared" and "declared, then dropped" produced byte-identical evidence while pointing at different files. This is the general shape, not a graphics detail: **when a stage both selects and transforms, an output-side instrument cannot separate a missing input from a rejected one**, and the reader will supply whichever explanation their current hypothesis prefers. The fix is mechanical and cheap — print the declared input, and make every rejection path name itself (`PROSPER_SHARPLOG=1` now does both, deduped per stage). Distinct from #39, where the instrument fell silent: this one was loud and accurate, and measured the wrong side of the transform. Same family as #43 — *what, exactly, produced this number, and could it have produced a different one?* |
| 51 | **A tripwire whose CAP was quoted as its count, and a bookkeeping counter read as a fact about the guest** | Two errors from one hour on ArcRunner (#1226), plus a third caught in review. `PROSPER_FORGE_TRIP` printed exactly `if (n++ < 64)` lines, so "the tripwire fires **64 times**, every hit identical" — restated across sessions as a measurement — was the **limit**, indistinguishable from 64,000 hits; the neighbouring `PROSPER_MB3_FREELIST_GUARD` figure it was compared against ("64 suppressed live-protocol writes per run") may or may not be the same population, and neither log could say which: that one prints sparsely past 64 but carries no ordinal, so its 64 lines bound its population only at at most 4,096 (the 65th line lands on the 4,097th event). With an ordinal added the real population exceeds **1,024**. **A capped diagnostic must carry its ordinal and keep printing sparsely past the cap**, or its ceiling becomes the finding — and the cap must be **per value/per key**, or a noisy control starves the log of the thing under investigation. The second error: the guard beside it suppressed *none* of those writes, and the reason was `live_pair` = `dma_exec_n > rel_exec_n`, read as "the guest still owns this label, so the write is correct, so the tripwire is a false positive". Those counters are **prosper's own record of which of its writes it has performed**. They are *necessary* for the live-fence story and nowhere near *sufficient* — `waf_guard()`'s comment already documented the case that produces them on an **already-freed** block — and the conclusion was one edit away from a doc row declaring the lead dead. What settled it was making the two hypotheses predict **different observables** instead of arguing about the same ones: the forged value is a pointer with its low bit set, so print what a misaligned dereference of it would return. It returned `0x30016000` on every *reported* hit — byte-for-byte the constant the terminal fault dereferences — and the disassembled pop showed one pop both returning that node (the guest's own `unrecognized block 2000000001` fatal) and storing the misread as the next head (the SIGSEGV). That collapsed two "separate legs" tracked since #1249 into one chain. **Third error, caught only in review: the replacement instrument written to document all this shipped with a bare 256-line cap, no ordinal, a budget shared across armed values, and a doc paragraph claiming the total was visible — and its author then wrote "256 hits" in the same comment.** Knowing the rule is not protection; the defence is mechanical (make the cap per key, print the ordinal, and re-read your own numbers as if someone else had produced them). Cousin of #46 and #34. |
| 52 | **A counter-arm that REMOVES a feature instead of BREAKING it, so the tests SKIP rather than fail** | Verifying #1757, the author's mutation reverted the **registration** of the new HLE function and reported the suite turning red as proof the new tests were live. It proved nothing. The tests begin `auto waitvbl = Hle::lookup(...); CHECK(waitvbl != nullptr, ...); if (waitvbl) { ...three behavioural checks... }` — with the registration gone the lookup returns null, **the guarded block never executes**, and the single red line is the registration check reporting on itself. The three assertions that matter were never run in either arm. Reviewed independently, the real arms are: **A** registration kept, body reverted to `return 0` -> 3 named failures; **B** registration kept, body kept, the shared epoch replaced by a private one shifted half a period -> **exactly 1 failure**, which is the discriminating run because it shows *which* assertion carries the claim. **The rule: revert the BEHAVIOUR, never the registration, the wiring, the build entry or the test's own precondition** — otherwise you are testing whether the test runs, not whether it works. This is the escalation of #48 ("a mutation that turns the suite red, read as proof that the check you wrote for it works") and its mechanism is the one #34 names: a **skipped** check and a **passing** check are byte-identical in a green suite, so the guard pattern that makes a test suite robust — `if (looked_up) { … }`, `if (file_exists) { … }`, an `EXPECT_*` inside a conditional — is exactly what makes this mutation silent. The tell is cheap and mechanical: **count the assertions that RAN in each arm, not the ones that failed.** If the arm you expect to fail executes fewer checks than the baseline, the mutation removed the test rather than breaking the code. The same review found a live survivor in the same file by the same discipline — an assertion named "shares GetVblankStatus's timebase" that only ever tested the *period* and passed a half-period phase shift, i.e. it could not fail for the reason its name claimed. |
| 53 | **A scripted edit that CONFIRMS the new text is present, without checking it replaced the old** | Correcting two withdrawn claims in `COMPATIBILITY.md` (#1767), the edit script computed each old row as a slice `s[s.index(A):s.index(B)]` and called `s.replace(old, new, 1)`. One slice came out **empty**, and `str.replace("", new, 1)` does not fail — it **prepends**. So the corrected row landed as line 1 of a public document, *above the H1*, while the row carrying the withdrawn claim stayed exactly where it was, inside `### Ruled out`. The author verified with `grep -c` for the new text and got `1`. **That check passed, and it was incapable of failing**: it asked *is the new text in the file* when the question was *did the new text replace the old*, and both a correct substitution and a prepend-plus-original answer `1`. Cousin of #34, specialised to editing: an insertion and a substitution differ only in what is ABSENT afterwards, so the verification must assert the absence. Three rules, all cheap. **Assert the old text is gone**, not just that the new text is there. **Assert the document's invariants** — the file still starts with its H1, every table row still has the same cell count as its header (that one alone catches a dropped `Ref` cell, which this same edit also did, twice) — because a structural check does not care how the damage happened. Count cells on **unescaped** pipes and nothing else — GFM splits a row into cells *before* it parses inline content, so a pipe inside a code span genuinely IS a cell boundary and has to be written `\|`. **This row used to prescribe the opposite** — exclude code-span pipes from the count, on the belief that `ptr\|member`-style spans false-positive on legitimate rows — and that is backwards: it suppresses the real defect instead of finding it. **No legitimate row of that shape existed to false-positive on, under either reading of "this very file"**: `COMPATIBILITY.md`, which this row is about, had none when the advice was written (`7593dabb`) and has none now; `GAME_COMPAT_ORCHESTRATION.md` had four, and all four were the defect rather than legitimate rows. Meanwhile six rows of the trap table below were rendering truncated on GitHub, trap 40 among them — three lost issue citations into the discarded region and one lost a dedicated citation cell outright. `prosper/tools/docs/check_numbered_table.py` now enforces cell arity in CI (#2108). And **make the script assert its own preconditions before writing**: `assert old in s`, or pin indices and assert their geometry, so an anchor that silently stops matching aborts instead of degrading into a no-op or a prepend. The failure is invisible to review-by-diff-summary too: the diff stat read `+4 −2` on the right file, which is what a correct edit looks like. Note where this one sat — not in an instrument, not in a measurement, but in the *edit that records the finding*, which is the last place anyone looks and the one that outlives the session. |
| 54 | **A substitution that restores a missing INPUT, read as making the consumer RUN** | On #1590 the rejected Earthion composite was missing one of its two texture descriptors, so a gated switch supplied it — purely to answer "if this dropped draw executed, would anything appear?" by eye. The A/B ran through the headless `screenshot` frontend and produced two visually identical frames, one edit from being recorded as *"executing this draw changes nothing on screen"*. **The draw never executed in either arm.** The substitution did exactly what it claimed — the resource table went from `(1 res)` to `(2 res)` — but the *consumer* failed for a second, independent reason: the shader copies its T# out of user data with eight `s_mov_b32`, so the SRSRC range reads as written, the direct `by_sgpr_base` lookup is skipped, and no `s_load` SRT tag exists to key on. Both arms emitted the same 14 `[exec-recompile-reject]` lines climbing to `occurrence=8192`. The tell was one grep of a log already on disk, and it is the rule: **count the events the experiment was supposed to eliminate, in BOTH arms, before looking at the output** — an A/B whose arms still fail for a shared reason renders identically *for that reason*, and a screenshot cannot say so. Note the direction relative to #52: there a counter-arm removed a feature so the checks skipped; here the experimental arm removed the symptom's first cause while a second cause held the symptom in place. Same failure of attribution, opposite end of the pipeline. The fabricating switch was never landed — output produced by forced guest state is evidence about a pass, never about correctness. |
| 55 | **A buffer-full event with space still left, read as "prosper asked for more than the guest budgeted"** | The #1756 probe's own first reading rule, falsified by the corpus it was built to survey. `AgcDcb::available_dw()` subtracts the guest's `reserved_dw`, so any title holding a reserve reaches *space left, but not enough* at the end of **every** buffer, for whatever packet arrives next. Corpus-wide every repeated such triple carries `reserved=18`, with the leftover tracking each packet's own size (`need=3 avail=2` … `need=69 avail=25`) — which read naively says prosper is oversized on seven different builders by seven different amounts. It cost a reported candidate: `SetShRegsIndirect need=4 avail=3` in three titles is the exact arithmetic of #1748's off-by-one and is an artefact. The signature needs `reserved=0` **and** a repeating triple, and it is *sufficient, not necessary* — an overrun under a live reserve is still invisible to it. |
| 56 | **A command-buffer window, read as a reservation** | Same probe, same hour. `cursor_down - bottom` is the current segment of a segmented Dcb, not a size the guest budgeted: The Pathless produces 58 buffer-full events on windows of 1–7 dwords, every one with the cursor exactly at the limit. A window is only a reservation when a *tight* one holds a known packet sequence — which is what made Babylon's 16-dword epilogue evidence (#1748) and makes these 58 noise. |
| 57 | **The interval between two late wakes, read as the current wake's lateness** | #1771 tried to make `videoout_queries` self-invalidating under Rosetta load by admitting a phase sample only when one `WaitVblank` call took less than one period plus 4 ms. Its premise was that a wait takes `period + current_lateness`. A wait starts *after the previous wake* and targets the next boundary, so consecutive duration is approximately `period - previous_lateness + current_lateness`: if the scheduler is consistently 8 ms late, the latenesses cancel, duration still reads one period, and the >4 ms phase sample is admitted and fails. Both attempts of unrelated PR #1789 reproduced exactly that on macOS x86_64/Rosetta (#1793). **An elapsed interval cannot isolate one endpoint's error when both endpoints move.** Nor can another host-timing threshold solve the identifiability problem: scheduler delay and a shifted grid produce the same late return. The platform-stable discriminator is a deterministic clock/sleeper seam underneath the real HLE calls: positive-control that the fake clock reached `GetVblankStatus`, let `WaitVblank` advance it, then assert its returned boundary belongs to the status grid. A half-period private epoch now fails exactly the named phase check while the real five-wait blocking guard remains on the host clock. |
| 58 | **A handler-entry count, read as completion of asynchronous delivery** | `win_exception_delivery`'s 100-delivery stress waited for the guest handler to increment `delivered`, then immediately raised again. On the forced Windows path that increment happens on the dedicated exception stack **before** `RtlRestoreContext`; the delivery slot intentionally remains active until a later raiser suspends the target and proves its RSP has left that stack. The next raise could therefore observe the handler count and still legitimately return EBUSY. The same exact head failed once and passed on rerun, while its R12-preservation and target-resume checks stayed green (#1798). A later two-delivery CPU-fallback check made the same inference and carried the same latent race. A sleep or EBUSY retry would hide either race without identifying completion. The causal oracle now comes from the other side of the boundary: the target's restored normal-context loop copies each observed `delivered` generation to an acknowledgment while R12 remains live, and both raisers wait for that acknowledgment before proceeding. Removing only the acknowledgment store makes both causal delivery checks fail — including the named 100-delivery check — while R12 preservation and target resumption still pass. **Entry is not completion; for asynchronous work, acknowledge from the resumed consumer context after the boundary whose completion you claim.** |
| 59 | **A timeline's address-copy census, read as a census of every `DMA_DATA` packet** | Astro Bot's retained frame-17 timelines reported `dmas=0`, which was one edit away from becoming evidence that no GDS counter reset preceded the runaway dispatch (#1732). Through timeline v9, `dmas` came exclusively from `GpuState::dma_copies`: the ordered-execution vector for **address-backed copies**. Immediate fills, GDS-offset destinations, and packets later rejected by execution never entered that vector, so a submit containing a decoded GDS reset and a submit containing no `DMA_DATA` at all produced the same zero. The old captures therefore cannot answer reset-versus-dispatch ordering; their raw journal is **unavailable**, not empty. Timeline v10 makes the populations explicit: `dma-copies` remains the address-backed execution record, while `dmas` is the raw journal's original 64-bit count, taken before execution classification. The retained raw records are bounded at 8,192 and carry exact operands, selectors, PM4 order and packet address; an explicit truncation flag distinguishes `count > retained` from a complete journal, and the format rejects inconsistent count/truncation combinations. Collection is enabled only when the run starts with `PROSPER_GPU_TIMELINE`; without it the decode path performs no raw-journal count, allocation, or record writes, and the missing records cannot be recovered later. Thus a non-truncated v10 zero **from an enabled journal** proves only that no packet was decoded — a raw entry is an observation, never proof that execution succeeded. **Before reading a diagnostic count, identify its producer, population, enablement and format version; an unavailable population must never be converted to zero.** |
| 60 | **A title boot without `PROSPER_RENDER`, read as GPU-free** | Asterix's semantic-timeline run deliberately omitted live graphics and was scheduled as a CPU-only experiment. The premise was false: `boot_trace` still registered the live compute backend, and the first supported dispatch initialized the Vulkan device and executed the compute seed-skip verification. `PROSPER_RENDER` gates graphics rendering; it does not turn a title boot into a decoder-only run. This matters operationally on the shared workstation: an apparently headless timeline can contend with another lane's performance measurement while its own log still looks like a cheap semantic index. **Treat every title boot as GPU work unless the exact runner configuration independently proves that no graphics or compute backend can initialize.** Offline `gpu_timeline`, `gpu_replay --inspect-only`/`--graph`, bundle extraction, and standalone `shader_inspect` remain CPU-only; other `gpu_replay` dump options require the control-flow classification in #63. Rendering a replay is GPU work. |
| 61 | **A shader-draw dependency model, applied to a fixed-function resolve** | Asterix's retained 14-operation submit first produced `edges=0 external-leaves=15`; specifically, the graph called pixel-shader binding 34 at `0x2011800000` an external leaf consumed by operations 8/11. Operations 5/6/9/10 were `CB_COLOR_CONTROL.MODE=RESOLVE`, for which the live backend executes no shader: it copies raw color0 to color1 despite color1's zero shader write mask. The graph instead invented color0 writes, omitted color1 writes, and counted resources from shaders that never ran. Resolve-aware graphing produces the exact chain 4→5, 4→6, 6→8, 4→9, 4→10, 10→11 and no external image leaf for that composite source. A source format is also part of the dependency: an external FP16 resolve source must remain FP16 or an exact temporal seed cannot close it safely. **Classify an operation by the backend mechanism that actually executes before interpreting its shader resources or export masks; fixed-function work needs explicit read/write semantics.** |
| 62 | **A capture with complete payload bytes, read as a self-contained record of the live resource** | R-Type's retained movie frame became detailed after its texel-coordinate fix but stayed green/purple, apparently localising a second live renderer defect. The payload looked conclusive: complete R8 Y and RG8 UV planes, exact T# swizzle `(X,Y,0,1)`, and a coherent BT.709 shader. It was the apparatus. F9 retained the plane bytes but recorded both resources with `linear_row_pitch_bytes=0`; replay also lacks the live process's AvPlayer pitch registry. Without either provenance arm, `avplayer_chroma_layout` intentionally falls back to the historical narrow-coverage path and broadcasts the first byte, turning `(U,V)` into `(U,U)`. The discriminator is unusually strong: 494,043/552,960 captured U/V pairs differ, yet an independent BT.709 `(U,U)` model matches replay at 42.17 dB / 0.9816 SSIM while correct `(U,V)` does not. The screenshot therefore proves coordinate/detail recovery and says nothing about live chroma. This is distinct from #59's unavailable population converted to zero: here **the payload is present and correct, but missing interpretation provenance selects a different transform**. A capture is self-contained only if it retains every side table, registry fact, layout field, feature/policy bit and temporal dependency that can change how those bytes are consumed—or explicitly marks the missing fact unavailable. Before reading replay output as live evidence, enumerate the live branch predicates and prove replay took the same branch; otherwise validate the apparatus first, not the title. |
| 63 | **A `gpu_replay` dump option, read as extraction-only** | During Asterix's retained-capture follow-up, `--dump-realized-shader` and `--dump-resource` were scheduled as CPU inspection because both write a requested artifact. On current master each did write its artifact, then **fell through to renderer registration and a full Vulkan replay**. The two unintended runs were visible only because the log continued with the Vulkan device and final output hash; both completed before the mistake was noticed. In the same observed path, `--dump-shader` returned immediately and did not initialize Vulkan. The rule is narrower and mechanical: **an option that dumps a CPU-readable artifact is not thereby CPU-only; classify it by the control-flow return before renderer registration, and treat it as GPU work until that return is proven.** This is the command-line twin of #60: the option name describes the requested output, not every backend the command will exercise while producing it. |
| 64 | **A successfully armed per-thread breakpoint, read as coverage of every guest-execution boundary** | `PROSPER_HWBP_ALLTHREADS=1` armed `run_entry` and workers created through the HLE pthread trampoline, but `boot_program` executes dependent-module init functions **before** `run_entry`. Those functions are guest code and can themselves create guest workers; the primary perf event did not yet exist while they ran. Frontends add a second ownership wrinkle: prosper-app and screenshot initialize on their caller, then enter the main guest on a distinct `std::thread`, so treating one global fd as “the primary thread” loses the real topology. A process-thread census does not close either gap: a late host thread named after the executable is not proof that it executes guest code, while an unlisted module-init boundary can execute guest code on an already-known TID. The fix attaches arming to one explicit guest-execution boundary used by module init, `run_entry`, and HLE worker trampolines; each TID retains its own fd/step state and the first valid fd remains only an always-live signal-handler anchor. A CPU-only mutation check keeps init and frontend threads simultaneously live and proves both cross the boundary before their guest markers; deleting only the init registration leaves its guest function running and fails exactly that named check. **Instrument coverage must be stated in terms of the semantic boundary that makes an event observable, not thread creation, names, or the set of arm messages that happened to print.** #1832. |
| 65 | **A static-destructor summary, read as available from a booted `prosper-app`** | #1839 made a compute-timing run self-validating through a final `seen`/`matched` selector verdict, initially emitted only by a function-local static destructor. A booted app cannot reach that destructor: while its guest thread remains alive, the established safe teardown is `std::_Exit`, which skips all C++ static destruction. The accepted banner and first match could therefore appear while the promised zero/nonzero-match summary was structurally impossible. #1840 makes the report idempotent, calls it explicitly immediately before the app's deliberate `_Exit`, and retains the destructor as the ordinary-frontend fallback. A pure policy mutation proves the two paths cannot duplicate the verdict. **Any diagnostic whose completeness is deferred to destruction must be checked against every frontend's actual exit primitive; `_Exit` requires an explicit flush/report path.** |
| 66 | **A process-wide file-size limit, read as a cap on one redirected log — and `$!` read as the process group it launched** | An Astro exact-hash timing arm set `ulimit -f 8192` intending to keep only `run.log` below 8 MiB. `RLIMIT_FSIZE` is inherited by the subject and applies to every file it writes, not to the shell redirection: an ordinary non-log app write raised SIGXFSZ before guest boot while the log was only 806 bytes. The selector's accepted banner survived, but there was no first match or terminal summary, so the arm was **VOID**, not a title result. The corrected external watcher exposed a second ownership error: under this launcher `setsid command &` forked, so `$!` named the short-lived `setsid` parent rather than the long-lived timeout/app session; the wrapper exited before attaching and left the subject alive. A missing `ATTACHED` witness and a process census caught it, and the watcher was attached to the resolved exact PID/PGID before measurement. **Cap an artifact outside the subject by polling its exact path, and prove the watcher owns the real long-lived process group: exact command, PID, PGID/session, expected child count, `ATTACHED` before measurement, and terminal `DONE`/`FIRED` plus final size. `$!` identifies what the shell launched, not necessarily the descendant that owns the workload. Absence of a cap event says nothing if the watcher died first.** #1732. |
| 67 | **A per-binding provenance flag, read after the binding was folded into an exact alias** | Syberia's first compute-authority census reported binding 4 of one image range as `ProvenGpuImage` and binding 5 of the same range as `GuestImage`, apparently proving that deferred authority had to return to guest memory. Binding 5 was an exact sampled-descriptor alias of binding 4. Alias folding copies the owner's concrete Vulkan image, view and sampler, then returns before repeating cache/transfer setup; the alias shell therefore retains false local borrow/persistent flags even though its descriptor binds the owner's GPU-proven source. The census read those shell-local defaults and manufactured an architectural boundary. Retained gate evidence makes the discriminator exact: binding 4 records the sole transfer hit, while bindings 5–7 have reflection records but no independent sampled-gate evaluation. **After aliasing, classify provenance from the concrete bound owner and prove image/view identity; per-binding setup flags describe how that shell was constructed, not necessarily the source it consumes.** #1854. |
| 68 | **A page-protection writer trace, read as process-wide after its first single-step window** | `mprotect(PROT_READ)` covers every thread and every known alias until the first write fault, so that first fault really is the first CPU writer to the protected page. To retain post-store bytes, however, the diagnostic must restore RW, set TF, execute one instruction, then re-protect. Another thread can write during that RW interval without faulting; even if no queued sibling fault is observed, absence is not proof that the window was clean. The dmem writer trace therefore marks event 1 `coverage-valid-before=yes`, increments `coverage-gaps` after every step, and never turns a later absence into a process-wide negative. A same-page write outside the selected byte range is especially important: it consumes the only initially complete interval even though it is not the selected writer. **The first protected-page fault has process-wide provenance; after opening the page, later events are useful history but completeness is unprovable unless a stronger stop-the-world mechanism independently closes the window.** #1902. |
| 69 | **A caller-chain plus allocation-size family, read as one unique allocation** | Asterix's four-field dmem writer arm matched a real chain-1 16 MiB allocation, armed it, and only then encountered a second exact family member. The exact GPU positive control later proved the desired resource belonged to occurrence 2, while the run contained 23 such allocations. Treating the first match as the subject would have watched a plausible but wrong address. Four-field selection therefore stays strict and invalidates on occurrence 2; the optional five-field form requests one run-relative occurrence explicitly and reports requested, observed, and selected counts. **A dynamic family identity is not an object identity. Either prove uniqueness, or select a bounded occurrence and independently prove that selected mapping equals the later subject address; never repair ambiguity with a prior run's VA.** #1902. |
| 70 | **Two page-protection owners represented by one owner's boolean** | The production dirty watch and the dmem provenance trace can cover the same physical page. Releasing a production registration used to restore RW unconditionally, leaving the trace reporting `Armed`, zero gaps, and a page that `/proc/self/maps` proved writable; production re-arm during the trace's one-instruction RW step made the inverse error and restored RO before the pending store ran. **Logical ownership and actual protection are different state. Compute protection from the union of every owner, with an explicit RW override for a pending single-step, and refuse a new/re-armed owner that cannot truthfully establish its contract during that override.** #1902 / PR #1910. |
| 71 | **Invalidating a diagnostic read as permission to abandon its pending TF trap** | A sibling mapping/host/GPU/physical notification can invalidate the trace after the SIGSEGV handler publishes `SingleStep` but before that thread receives `TRAP_TRACE`. Clearing the pending TID at invalidation does not clear the thread's hardware TF bit; completion then returns `NotHandled`, falls into unrelated breakpoint machinery, and may terminate without the promised invalid verdict. **Invalidity ends evidentiary trust, not signal ownership. Retain the exact pending TID until it consumes the trap, clear TF there, return an explicit invalid completion, preserve the first invalid reason, and never re-arm stale topology.** #1902 / PR #1910. |
| 72 | **CR2/`si_addr` read as the complete span of a memory write — then a process-wide post delta read as attribution** | A qword store beginning four bytes before the selected interval faults at that earlier address while modifying four selected bytes; start-address classification alone recorded `selected=no`. The bounded signal-safe decoder now proves contiguous spans for common MOV stores and tests interval overlap. The first correction still promoted an unrecognized off-range instruction when selected bytes changed after its step. That is also unsound: every alias is process-wide RW during the window, so a sibling can produce the delta. A deterministic real-fault canary pauses an unknown off-range store, changes a selected byte from a sibling, and requires `selected=unknown changed-during-window=yes`. **A fault address is one byte, not an operand extent, and a process-wide delta does not identify its writer. Attribute only a proved overlapping span (or a fault byte already inside the selection); otherwise retain the delta and say unknown.** #1902 / PR #1910. |
| 73 | **A retained historical event, read as a verdict after its selector identity invalidates** | Strict-unique dmem selection can arm occurrence 1 and observe a selected write before occurrence 2 proves that caller-chain/size family ambiguous. The event is real history about occurrence 1, but the original terminal rule checked `selected_faults` before `status=invalid reason=ambiguous-allocation` and still printed `result=writer-observed` for a subject it never identified. A real-fault canary now produces exactly that order and requires the terminal decision to become `undetermined` while retaining the event. **Evidence can survive invalidation without its attribution surviving. Derive the verdict from both the observation and a still-valid identity; never let an earlier event override a later self-invalidating selector.** #1902 / PR #1910. |
| 74 | **A normal-context reset seam, read as permission to replace signal-owned state** | `guest_dmem_write_trace_configure()` used to clear the pending TID and overwrite the trace even while a faulting thread owned TF between SIGSEGV and `TRAP_TRACE`. Completion then returned `NotHandled`, abandoning the same live hardware state trap 71 protects against notification-driven invalidation. The seam now refuses reconfiguration without mutation while a step is pending; a paused real-fault canary proves the original TID still completes and re-arms. **A setup/reset API is not quiescent merely because callers normally use it before a run. Enforce the ownership precondition in state, especially when reset would erase a signal obligation.** #1902 / PR #1910. |
| 75 | **A value-initialized post buffer, printed as observed zero bytes after the read was skipped** | `CompleteInvalid` correctly avoided dereferencing topology a sibling had removed, leaving `event.after` at C++'s zero initialization. The formatter nevertheless printed `post=0000…`, manufacturing exactly the data readers use to identify a writer. Events now carry explicit post-image availability and render the skipped read as `post=unavailable`; the real invalidation canary asserts that exact representation. **Storage being initialized says nothing about whether an observation happened. Carry availability beside retained data and make the formatter consume it, or safe defaults become fabricated evidence.** #1902 / PR #1910. |
| 76 | **A page-fault overlay, read as owner of TF that was already set** | HWBP, BP, and STEPWIN disable/rewrite state, set TF, and rely on the next instruction's `TRAP_TRACE` to restore it. If that instruction writes a dmem-traced page, the overlay used to claim the SIGSEGV, then run first on the completion trap, clear TF, and return—stranding the original owner with its breakpoint disabled or byte unrestored. The overlay now detects pre-owned TF in the fault context, invalidates and opens its page without claiming a step, and resumes with TF untouched. A real `int3` → protected store sequence proves the original owner receives exactly one completion. **Trap ownership is compositional state, not an implication of which handler ran last. Never set or clear TF without proving it was previously free or belongs to you.** #1902 / PR #1910. |
| 77 | **A compatibility wrapper's `handled` boolean, read as permission to create signal state its caller cannot service** | The retained `guest_write_watch_handle_fault(addr)` API can only tell a caller to resume; it carries neither RIP/TID nor a `SingleStep` action. Delegating it to the provenance fault path nevertheless opened every trace page RW, stored `stepping_tid=0`, and published 0—the no-owner sentinel—while nobody set TF or routed a completion. The store completed and the diagnostic remained permanently `Stepping`, RW, and non-reconfigurable. A real SIGSEGV handler using only this bool API now proves the overlapping production watch becomes Dirty and the store completes, while the diagnostic fail-visibly becomes `single-step-unavailable` with no pending event. **A legacy return type is a capability boundary. Do not create ownership obligations that its caller has no vocabulary to receive or discharge.** #1902 / PR #1910. |
| 78 | **A decoded operand width, added to CR2 as though the fault byte were always the operand start** | For a qword beginning at `P-4` with writable page `P-0x1000` and protected page P, CR2 reports P—the first inaccessible byte. If the selection begins at `P+4`, the real store ends at `P+3` and is disjoint, but inventing `[P,P+8)` falsely reports a selected writer. A real split-page canary now executes exactly that store and requires `selected=unknown`, unchanged selected bytes, and no writer verdict. Same-page decoded crossings remain exact; a selected CR2 byte is also exact. **Instruction width proves size, not effective address. At a page-boundary fault, either derive EA from saved operands or conservatively withhold attribution.** #1902 / PR #1910. |
| 79 | **A precomputed alias list, read as proof that every operation over it is allocation-free** | Three dmem SIGSEGV exits walked already-built trace pages but invalidated through the normal-context protection helper. That helper created a rollback `std::vector` and `push_back`ed once per alias, so bool-capability, pre-owned-TF, and target-copy-failure faults could enter the allocator asynchronously despite the signal path's no-allocation contract. Signal invalidation now walks the immutable page/alias vectors in place, continues opening siblings after a protection failure, preserves the first invalid reason, records partial opening as an extra coverage gap, and resumes only if either trace or production handling restored the faulting alias. A hook on the allocating helper stays at zero through both real signal branches; routing the common invalidator through that helper makes exactly those named checks fail. **Preallocation of inputs does not make an algorithm allocation-free. Audit every helper reachable from the signal path, including rollback/error handling, and test the forbidden edge rather than inferring it from successful faults.** #1902 / PR #1910. |
| 80 | A `gpu_replay` **bundle extraction option, read as an inspect-only operation** | `gpu_replay --bundle-final-capsule` sounds like file extraction, but it initializes Vulkan and executes the captured bundle before exporting the final seeded capsule. During #1903 that assumption caused an accidental roughly one-second GPU overlap with another lane; the process exited normally and the overlap was disclosed, but the other lane's concurrent sample had to be repeated cleanly. **Treat every bundle mode as GPU work unless its documented path has been proved inspect-only. Acquire the shared GPU lease before `--bundle-final-capsule`, replay, compact/extract variants of unknown execution behavior, or any other bundle operation that may construct final state; only a documented `--inspect-only` path is exempt.** #1903. |
| 81 | A rebuilt **static library or sibling frontend, read as proof that the executable being measured contains the change** | #1903 rebuilt `prosper_core`, `test_eop_write`, and `prosper-app`, then ran the separate statically linked `screenshot` frontend. CMake had no reason to relink that sibling target, so the supposedly fixed arm reproduced the old DMA rejection exactly: zero positive-control lines and 24 old-path failures. The run was void, not negative. Rebuilding the exact `screenshot` target changed its mtime/hash and `strings` then proved the gated positive-control text was present before the replacement run. **Before a live A/B, build the exact executable you will launch and prove that binary contains the lever—target name, mtime/hash, build revision, or a unique diagnostic string. Rebuilding a shared archive or another frontend does not update an already-linked static executable.** #1903. |
| 82 | A **first post-realization draw-resource census, read as proof that no later draw consumes a retained result** | Syberia's first draw after its save-warning atlas reported 700 exact resource ranges across 140 epochs, all unrelated, but that probe deliberately closed after draw one. A full-submit probe retained the same atlas identity; all 140 valid epochs had at least one later overlap and the summary counted 700 realized overlaps. Its 21 bounded detail samples consistently showed a full-size texture range at draw ordinal 34 and bindings among 35/38/39/40/41, but do not prove that population-wide distribution or the earliest overlap. Dropping the retained range after draw one makes the exact later-draw canary fail. **A zero-overlap first draw proves only that draw is unrelated. Retain identity to the real lifetime boundary, keep draw ordinals, distinguish aggregate counters from sparse details, and make early retirement a defect-shaped mutation before authorizing full-submit deferral.** #1737. |
| 83 | `prosper-app --help` or `--version`, assumed to be a **metadata-only frontend mode** | `prosper-app` does not currently consume either option as metadata. Both invocations entered the no-game UI and initialized Vulkan; two agents independently had to notice and terminate the unintended GPU process. The `--help` arm presented zero frames in about one second, while the later `--version` arm remained alive for roughly 21 seconds. Both were disclosed and followed by an exact clean process census. **Do not execute a frontend to prove its revision unless that exact option is documented as non-rendering. Inspect the binary non-executingly (`strings`, `readelf`, hash), or use source/build metadata. Treat unknown frontend options as a possible GPU run requiring a lease.** #1737. |
| 84 | A simultaneous **`PROSPER_HWBP` + `PROSPER_BP` run, read as two independent breakpoint records** | Oregon's owner arm produced one genuine HWBP at `0x901edd`, then the int3 at `0x88ae5a` arrived while an HWBP fd was live. The Linux handler's HWBP branch accepted **any** `SIGTRAP` before the BP branch could test ownership, printed the int3 event as a plausible second `[hwbp]` at `0x88ae5b`, skipped BP's restore/back-up/single-step sequence, and the subject immediately aborted with allocator corruption. The first genuine HWBP record remains usable because it preceded the int3 and independently carried `caller_rbp=eboot+0x88ae5a`; the fabricated second HWBP record and every later outcome are void. **Two instruments sharing a signal are not independent merely because both armed. Route by explicit owner state (`g_bp_stepping` / BP RIP, HWBP perf fd / step state) before mutating either state machine, and require a coexistence test that proves both original instructions execute.** **Fixed** — the Linux handler now arbitrates every `SIGTRAP` through `src/host/fault/trap_arbitration.hpp` and yields to `PROSPER_BP` whenever BP's own state proves ownership (`RIP == g_bp_addr + 1`, or `g_bp_stepping` with TF set and this TID not mid-HWBP-step), so the two instruments may be combined in one run again; `tests/host/fault/test_trap_arbitration.cpp` arms both live and reproduces the misroute when the pre-fix condition is restored. The two records above stay void — they predate the fix. The same steal of the `PROSPER_WATCH_LABEL` / `PROSPER_WATCH_COMPANION` step completions is still open. #1932, #2559. |
| 85 | An **asset load trace, read as proof that the guest executed the code that uses those assets** | Oregon Trail's `tools/re/pak_index.py --distinct` trace showed `WBP_Splash_Gameloft`, `BP_EULA_Controller`, `WBP_System_Disclaimer_Popup` and the splash logo atlas loading, which reads exactly like the shipped Blueprint's `Create` + `AddToViewport` step having run. It had not: a **pre-fix control arm loaded the identical set at the identical trace ordinals over a run 12x longer**, while demonstrably still stuck at the account gate. UE4 pulls a level's referenced classes through the map package's import table, before any Blueprint bytecode executes, so the load trace answers *what is resident*, never *what ran*. The discriminator that did work was second-order and much narrower: `.ufont` + `icudt64l/brkitr/line.brk`, which Slate touches only when it lays text out, appeared in the fixed arm alone. **Classify each asset as `referenced-by-a-loaded-package` or `loaded-only-on-demand` before reading it as progression, and always run a control that is at least as long as the arm.** #1606 / #1945. |
| 86 | A **generic-named shell redirect into the shared `~/` scratch**, read as writing a private file — trap 19's other half, on the *write* side | Trap 19 covers *reading* a peer's data, and its saving grace was the Write tool **refusing to clobber a file it had not read**. A shell redirect has no such guard: `> ~/babylon-work/build.log` truncates whatever was there, silently, exit 0. The Asterix lane found `~/babylon-work/` **already populated by an earlier lane on the same title** (a dozen `pr*.md` / `issue-*.md` from two days before) and had by then written `cmake-configure.log`, `build.log` and `build2.log` into it — three names any lane on any title would pick. Whether those overwrote that lane's files is **unknowable after the fact**, and that is the point: the loss is undetectable from both sides, and the victim meets it as a log that is missing or wrong during a re-run they cannot explain. Only the lane's `babylon-*`-prefixed artifacts were safe, and they were safe by construction rather than by care. **Two rules. Prefix EVERY file a lane writes under `~/` with its lane or title — including configure and build logs, which feel like throwaways and are exactly the names that collide. And a scratch directory you did not create is a peer's workspace: `ls -la` it before writing, because finding it populated is the only warning you will get.** Same family as #19, with the asymmetry that matters: the tool that asks permission is the protected one, so the plain `>` that feels lighter is the dangerous one. |
| 87 | A **diagnostic that prints only on its failure branch**, read as a verdict on the whole run | `[render] … Vulkan render FAILED` (`live_renderer.cpp:5936`) has **no success counterpart** unless frame dumps are enabled, so a log containing 946 failures and nothing else is byte-identical to "946 failures, then 1,100 silent successes". Sonic Frontiers publishes nothing for exactly its first 946 present callbacks and then publishes continuously, deterministically, in both independent arms — the earlier intake sampled entirely inside that silent prefix (all six samples by t=30 s; the first composited frame lands at t=50 s) and recorded rung 0 for a title that renders a full 3D opening sequence. Before quoting a failure census, check whether success is even printable. Cousin of trap 39. #1891, #1968. |
| 88 | **`pgrep -x <frontend> \| head -1` used to find "my" process**, then attached to with gdb — it returns the OLDEST match on the box, which on a shared machine is a **peer lane's run** | The rule above ("count with `pgrep -x`") is about *counting*; this is the *selection* mistake behind it, and it fails silently in the worst possible way. The Little Nightmares III lane took an all-thread `guest_bt` dump this way and got a coherent, plausible 64-thread UE4 backtrace — thread names, sensible unwinds, a `pthread_rwlock_wrlock` block that looked exactly like a root cause. It was **another title's process**. The tell was not in the dump but outside it: reading the LN3 PID's own `/proc/<pid>/task/*/comm` gave a completely different thread-name set (`TaskGraphThread`x39 / `RenderThread 1`, not `CRI MPV Worker` / `RenderThread 0`). Two arms and the "root cause" they produced were void, and three gdb attaches froze a peer's live run for a few seconds each. Nothing in the dump can reveal this — every title under prosper maps its eboot at the same base, so `guest_bt`'s own module map resolves and unwinds happily against the wrong binary. **Pin the PID to your own child (`pgrep -P "$WRAPPER" -x <frontend>`) AND assert `/proc/<pid>/cmdline` contains your title id before attaching; refuse to attach otherwise, so a mis-target fails loudly instead of producing evidence.** A debugger attach is also a write, not a read: it stops a peer's process. #1962. |
| 89 | **A global "is a present surface registered?" (`present_width() != 0`) used as the predicate for the per-submit question "is this frame going to the publish gate?"** | #1986 needed the live renderer to refuse a rendered pass whose extent the publisher cannot accept — but only on the present path, because `render_submit_items` deliberately returns the last pass **at its own declared extent** and its consumers (gpu_replay's ordered-prefix inspection, #1330; the renderer's own tests, #526) infer the extent from the returned byte count afterwards. `present_width()`/`present_height()` look like exactly the right discriminator: they are non-zero only when a scanout exists, and `hle_agc.cpp:1703` derives the present `w`/`h` from them. They are wrong, and they are wrong **in the tests specifically**: `tests/misc/test_gpu_capture_render.cpp` registers a **real 128x128 VideoOut present surface** at line 87 to simulate a reduced-resolution render, so the predicate is true inside the very test asserting *"per-target chain returns the last pass at its declared 16x16 extent"* — a gate keyed on it changes what that chain returns. **A global proxy for a scoped fact is true in exactly the places a test has set the global up, which is to say in exactly the places you were relying on it to be false.** "Is a present surface registered?" is global and durable; "is this submit going to the publish gate?" is per-submit and scoped, and no amount of care about the first answers the second. The discriminator that works is the reusable part: a **counted thread-local scope opened by the caller that actually presents** (`PresentSubmitScope`, opened by `execute_ordered_and_present`/`execute_and_present`), because it is true only on the path it names and a test can enter it explicitly to assert both arms. Unlike most of this list, this one **failed loudly** — a named check went red and named the wrong predicate before it could ship — and it is worth recording *why*, because it is buildable on purpose: that test asserts a **declared extent** rather than merely "some pixels came back", so it can detect a gate that silently changes what the chain returns. Contrast the defect the same PR fixes, where a wrong-extent frame was dropped for 31 consecutive submits with nothing logging it at all. Assert the specific property, not liveness. #1986 / #1990. |
| 90 | **`frame_seq` read as a liveness oracle** — it was one only as an accident of the bug #1990 fixed, and that PR removes the property while leaving the habit | Master's frozen publish counter was a genuinely useful health signal for Sonic Frontiers: `frame_seq` stuck at 2,081 meant the screen was stuck, because a frame that could not be published was not published at all. #1990 gives the renderer an extent contract and a retained-frame fallback, so the same title now publishes **~4,000 frames post-wall with an unchanging image** — `frame_seq` climbs to 5,499 while every sample carries `distinct_rgb_colors == 1` and the identical `pixel_crc32`. "The counter is climbing" no longer means "the image is alive", and the orchestrator had already drawn exactly that inference once before the numbers were in. **The general rule is about the aftermath of any fix, not about this counter: when a fix removes a failure mode, ask which signals you were reading as health were only side effects of that failure — a fix can silently retire an oracle, and nothing warns you, because the oracle keeps returning values.** Pair a counter with a **content** metric before calling a title alive: `distinct_rgb_colors`, `nonblack_rgb_pixels`, and above all a **differing `pixel_crc32` per sample**. The Bendy control in the same PR is the shape to copy — 15 samples, a *different* hash on each, which is liveness; the Frontiers arm has a climbing counter and one repeated hash, which is not. And when the distinction itself is the question, instrument the branches rather than inferring: #1990's `fresh=`/`retained=` totals showed fresh growing by 1 while retained grew by 2,048, which no counter reading could have separated. Cousin of trap 87 (a signal that cannot express the state you care about) and of #1990's own defect. #1990. |
| 91 | **A pixel-inspection metric that hard-codes one pixel format** — `PROSPER_PASS_LOG`'s `px_nonblack` counted 4 bytes per pixel and tested bytes `p`, `p+1`, `p+2` for *every* render target | This does not degrade on another format; it **inverts**. On Sonic Origins' 3840x2160 `R16G16B16A16_SFLOAT` scene target (8 bytes per texel) the loop walked half-texels, reading `{R_lo, R_hi, G_lo}` and then `{B_lo, B_hi, A_lo}`, and reported **8,294,400 non-black pixels — exactly `w*h`** for a frame with no colour in it at all. Maximum brightness for black: the failure is indistinguishable from the best possible result, so nothing about the number invites a check. It briefly became this lane's root cause ("content is rendered and the present path drops it") and was killed only by a second instrument, the `PROSPER_DUMP_PERSISTENT` census, which converts properly and reported black. **The mechanism is worth reading closely, because the obvious explanation is also wrong**: it is *not* "alpha 1.0 is non-zero". An alpha of exactly 1.0 (`0x3c00`) would have counted **zero**, since its only non-zero byte is the `p+3` the loop skipped. The measured target holds RGB bit-zero with alpha `0x3c05` (1.00488), whose low mantissa byte `0x05` sits at texel byte 6 — the second group's `p+2`. So the count tracked *where a half-float happened to put its bytes*, not the image; the same buffer with a rounder alpha reads 0, and an independent review caught the plausible story before it reached the record. Same family as the seed-miss gradient scoring more distinct colours than real content, and as a draw census without a positive control. **The fix, and it generalises:** Convert through the format's own inspection path before counting anything (#1994 routes `px_nonblack` through `inspection_rgba8`, the converter the persistent dump already used). Where no conversion exists, report **`-1`**, not a number: an honest refusal is a signal a reader can act on and a plausible number is not — and print that case *more* readily than a zero, because it is the one where the instrument cannot answer. Whenever a metric is fixed this way, name the past conclusions that rested on it; a silently corrected instrument leaves live wrong conclusions behind it. |
| 92 | **A startup-timing number taken without recording the host page-cache state** — the same binary, on the same host, is 2-4x apart depending only on whether the guest's assets are already resident | A guest `read`/`pread` is served from the **host page cache** on every launch after the first, so a title's asset load runs at memory speed rather than storage speed and *nothing in the run says which happened*. On PPSA26414 that is not a rounding error, it decides the outcome: the INPUT-thread-to-`DLLInit` interval is **289-362 ms warm and 779-1158 ms with the dump evicted**, and the title faults at `eboot+0x24055` in five of five warm arms and boots in five of five cold ones. It invalidated a published figure: #2009's *"~17 ms of file I/O inside a 244 ms load"* (`strace -f -c`) is true of a **warm cache**, not a property of prosper — the same load spends 552-719 ms when the reads reach storage. Note what makes the confound nasty here: CPU contention is the *other* lever on the same interval, so a cold-cache pass reads exactly like a contention pass. The discriminator is that the **least-loaded** arms land on opposite sides — warm at load 13.4 faulted, cold at load 14.7 booted. **Record the cache state with every startup/boot timing, and make it an input rather than a hidden variable**: `tools/dropcache.py` evicts a named tree with `posix_fadvise(POSIX_FADV_DONTNEED)` and prints `mincore(2)` residency before and after, so a "cold arm" can prove it was cold instead of asserting it. And treat that tool as **shared state, not a local one**: the page cache is global, so evicting a dump evicts it for every process on the box — on 2026-08-05 six evictions of the R-Type dump silently slowed a concurrent lane running the same title, which could have been read as a regression. Announce an eviction of a shared dump before you do it. #1746 / #2019. |
| 93 | **A `gh`/`distrobox` invocation that hangs to timeout, read as "no results"** — on this box `gh` is a **distrobox wrapper script** (`exec distrobox-enter -n my-distrobox -- /usr/bin/gh`), so every invocation is a container exec | Under podman contention from concurrent lanes the lookup fails with `Error: beginning transaction: database is locked`; distrobox then concludes the container does not exist and falls back to an **interactive `Create it now? [Y/n]` prompt** that a non-interactive run cannot answer. The call hangs until its `timeout`, exits 124/143, and leaves an **empty output file** — which reads exactly like "the command produced no output", i.e. like a *negative result*. It cost several lanes time on 2026-08-05, and the same failure was first misdiagnosed as a shell tilde-expansion bug because it is intermittent and appears cwd-dependent. The unauthenticated REST API is the reliable substitute for status queries (`curl -s https://api.github.com/repos/OWNER/REPO/commits/<sha>/check-runs`) — **but it has its own instance of the same rule**: at 60 requests/hour it answers **HTTP 403** with a body that has no `check_runs` key, so a poller doing `d.get('check_runs', [])` computes "0 failures" from an error and reports **ALL SETTLED, failures: NONE** for a PR whose builds were still running. **A timeout, an empty file, or a missing field is never a negative result.** Any gate over such output must demand *positive* evidence — a non-empty response, the expected key present, and a **non-zero** count of checks that equals the number expected — rather than scanning for badness and concluding its absence is health. Check `x-ratelimit-remaining` before believing an unauthenticated answer. #2019. |
| 94 | **An existence check standing in for a capability check** — a guard that asks "is the tool installed?" when the question is "can the tool do the thing I need?" | `tools/re/nid_gate_scan.py` disassembles a raw byte window with GNU `objdump -b binary`. Its ctest skipped when `objdump --version` failed — which is never, because **macOS ships LLVM's objdump as plain `objdump`**: it answers `--version` perfectly and rejects `-b` outright (`error: unknown argument '-b'`). The guard therefore passed on the one platform where every decode raises, and the macOS CI job went red at 156/157. **Probe the capability you actually need, not the artifact that usually provides it** — here, run a real one-byte disassembly and require both exit 0 *and* the expected `nop` in the output, then accept `$OBJDUMP` / `objdump` / `gobjdump` so the tool works with Homebrew binutils rather than merely failing more politely. Two traps sit behind this one. First, it is the same family as *a test that passes without its mechanism ever running*: an existence check is a proxy that correlates with the property right up until a second implementation of the same command name appears. Second — and this is the part that nearly shipped — **a skip guard can itself become the thing that passes without running**: the first fix skipped the *whole* test file, yet 12 of its 26 assertions (`canon`, `arg0_before`) need no `objdump` at all, so macOS would have reported `re_nid_gate_classifier` green while testing nothing. Split the guard so the capability-free assertions always run, and express classifier cases as decoded tuples so the logic under test is exercised even where the decoder is unavailable. An independent review caught the second half; CI caught the first. #2021. |
| 95 | **A queued container job leaves the previous run's artifact in place**, so a stale result is indistinguishable from a fresh one | The mutation-arm table for #2021 was re-run after its test grew two checks. `distrobox enter` sat in the podman lock queue (trap 93) for minutes without starting, so the script's `: > "$OUT"` had not executed and **the previous run's file was still on disk, complete, well-formed, and two hours old**. It listed six arms instead of seven and the *old* nine check names — a result that looks exactly like a finished run, because it is one, just not this one. It was caught by `ls --time-style` on a hunch, not by anything in the output. **Check the mtime of any artifact you are about to read as a result, and compare it against when you started the run**; better, write each run to a fresh per-run path (or `mv` the old one aside before launching) so a stale file cannot occupy the name a new result is expected at. The general form: **an artifact's existence says nothing about which run produced it**, and truncate-on-start gives no protection when the start itself is what is delayed. Same family as trap 93 — absence and staleness both present as ordinary data — and as trap 20, where an absent signal mimicked a pending one. Note the asymmetry that makes this expensive: the stale file is *more* believable than a missing one, so it invites no check at all. #2021. |
| 96 | **A run that never reached the code under test emits zero diagnostic lines** — which is indistinguishable from the diagnostic looking and finding nothing | `PROSPER_AVPCHROMA_LOG` was used to A/B whether #2032's `AvPlayerVideoEx.width` change moved R-Type Delta's NV12 plane extents. Three of four "after" arms printed **no `[avpchroma]` lines at all** — which reads exactly like the honest negative you are hoping for ("the changed clause is never reached, so nothing moved"). It was not: the guest died at frame 34 on the #1746 startup race, hundreds of frames before the movie, so the instrument was never given anything to report. **The adverse condition is the one that looks ideal.** #1746 is decided by host CPU speed, and the `tools/dropcache.py` route (#2019) is called deterministic because it *wins* that race — but it wins it by making the asset load slow. On a **quiet** box prosper finishes the load well inside the guest's 400 ms window, the shell runs first and dereferences an empty user vector, so the route is **less** reliable at load ~2 than at load ~20. Every instinct that says "measure on an idle machine" is wrong here. **The tell is a content signal, not the absence of output**: `source-distinct=1` in the screenshot manifest, and a frame counter stuck at 34 against 2,248 in the arm that worked. That is the reusable part — a run that stalls still produces a well-formed manifest, a `status=ok`, ten files and an exit code of 0. General rule: **when a diagnostic's output volume depends on the guest reaching a state, its silence is only evidence once an independent, POSITIVE liveness witness from the same run says the state was reached.** Quote that witness beside the zero, or the zero is void rather than negative. Same family as trap 93 — absence presenting as ordinary data — with the extra sting that here the void arm is produced by the *better* machine conditions. **Half of this is now instrumented away, and only half:** since #2007, a run whose **primary guest thread dies** reports `guest=faulted status=GUEST-FAULT` and exits non-zero, so the specific "well-formed manifest, `status=ok`, exit 0" phrasing above no longer describes a crashed guest. A guest that *stalls* — alive, not advancing — still produces exactly that, so the content witness (`source-distinct`, the frame counter) remains the check. #2032, #2019, #1746, #2007. |
| 97 | **gdb records a longjmp landing value as a function's return value** — so a capture is not merely incomplete, it is confidently wrong, and the completeness counter says it is fine | `tools/hle_calls --values` arms a `gdb.FinishBreakpoint` per call to record what each HLE handler returned. On a handler that longjmps back into its own caller, that breakpoint sits at an address **the landing pad reuses**, so gdb captures the value present at the landing and reports it as the return. Measured on gdb 17.2 with a fixture longjmping on one call in three: the row printed `ret 0x80960007 x13, 0x0 x7` with a **complete-looking 20/20 capture**. What a reader would otherwise conclude is the expensive part — the row says prosper answered the guest `0x0` seven times, i.e. it names a specific wrong HLE answer that never happened, and that is exactly the class of finding these censuses are run to discover. It would have been chased as a mismodelled Sony return. **The per-row `(captured N/M)` field, which exists precisely to flag lost values, reads 20/20 and confirms the lie**: the values were not lost, they were *substituted*. General rule: **a completeness counter measures presence, never provenance** — it cannot distinguish "the value I recorded belongs to this call" from "I recorded a value". Any capture keyed on a code address is open to this wherever control flow can re-enter that address by a path other than a normal return: longjmp, signal return, exception unwinding, a reused tail-call landing pad. The only check that caught it was the **source-derived positive control** — `s_user_getevent` returns `0` exactly once per process and `0x80960007` forever after, known from `hle_service.cpp` before any run — which flagged `positive-control=VIOLATED(login-x7)` while every other field in the output looked healthy. Pair every value capture with a control whose answer you derived from the source beforehand; nothing computed from the capture itself can detect substitution. #2035, #1997, #2054. |
| 98 | **A mutation arm implemented as "stash the file and re-run"** — void the moment the change under test is committed, and it reports every check *passing* | `git stash push -- <path>` removes only *uncommitted* changes to that path. Once the fix is committed the stash removes nothing, the rebuild is byte-identical, and the arm reports 3-of-3 green. Nothing in the output distinguishes that from a genuinely non-discriminating test — the procedure, the build and the exit code are all unchanged. The failure direction is what makes it expensive: it fails toward *"my test doesn't discriminate"*, which an author is **pleased to disbelieve** and which a reviewer cannot see from the diff at all. Caught on #2061 only because the same arm had failed **2-of-3 an hour earlier** and then passed **3-of-3 after the test was made stricter** — a discriminator that gets better and starts passing is the signature. **Mutate the source in place** (neutralise the exact lines) rather than relying on VCS state, and **require the arm to show at least one failure**: an arm that cannot show its lever moved is *no result*, not a negative one. Note the defence here was the author disbelieving an implausible improvement in their own result, not review — review cannot see it. #2061. |
| 99 | **A fault reporter that is not reentrant** — the fatal worker-fault dump kept the interrupted context in *process globals* (`g_fault_addr`, `g_fault_rip`, `g_rax`…`g_r15`), so a second thread faulting mid-dump rewrote them underneath the report already printing | The guest's faults are correlated: one heap corruption takes several worker threads within milliseconds. The first thread's report then continued with the *second* fault's registers and still read as one complete fault — right thread name on every line (`pthread_getname_np(pthread_self())` is per-thread), wrong numbers beside it. On Crisis Core one report printed `rbp` as `0x7fe46d761280` in its header and `0x7fe4603ff3a0` sixty bytes later, `rsp` likewise, and `fs(rax)=0x0` against a ucontext `rax` of `0x30016000`; another printed two `guest fp-chain` blocks that were **both** `PoolThread 6`'s, one of them under the RHIThread's header. The tell was a self-contradiction nobody had cashed: `insn bytes @rip` decoded to `mov eax,[rbx]` with `rbx = 0x20`, which cannot fault at the `0x30016000` in its own header — the bytes were fetched at another thread's rip. It reached further than the prose: #1992 had widened `[faultobj]`'s probe-page list *specifically* so #1945's corrupted pointer would not be missed, and that list was built from the same globals, so under a concurrent fault it scanned pages derived from another thread's registers and still printed `probe-pages=N/M` — making a zero-hit ring or clock-fence result **void rather than negative**, the exact failure #1992 existed to remove. **The general rule is the expensive part: a diagnostic that is not itself reentrant misreports most severely exactly when the bug is worst.** A single-fault program never exposes it, every unit test passes, and the one situation that breaks it — a racing multi-thread fault — is the one where you most need the dump to be trustworthy, so the instrument degrades precisely as the subject gets harder. Ask of any handler-resident diagnostic: what happens if this runs twice at once? A report is also cheaply self-checking — **the same register printed twice in one dump must agree**, and any that does not invalidates everything between the two prints. Same family as trap 12, where two interleaved streams read as one ordered sequence. Fixed by #2030 (a stack-local per-fault snapshot plus a single-owner gate on the report); #2018. |
| 100 | **`pidof <frontend>` returns EVERY concurrent lane's process**, and an attach-by-PID instrument then produces a complete, well-formed histogram **of the wrong game** | Trap 88 is the same family for `pgrep -x … \| head -1` + `guest_bt`; this is the `pidof` spelling, and it fails through a *tool* rather than a hand-written attach, which changes what the tell looks like. The CrossWorlds lane ran `tools/hle_calls --pid $(pidof screenshot \| head -1)` during a phase it wanted to characterise and got 47 distinct handlers, 15,950 calls, a sensible AGC/audio/VideoOut mix and a plausible positive control — a result with **no internal defect of any kind**, because it was a perfectly good measurement of *Sonic Origins* running in another worktree. Three lanes were on `screenshot` at that moment; `pidof` returns them all, in an order nobody should rely on. Note the shape, because it is the worst one a false positive can take: an *empty* histogram would have been questioned immediately, and a *malformed* one would have been debugged. A complete one is simply believed, and every number in it is then reasoned about as if it described your title. The tell was outside the data, in the tool's own header line naming the binary it enumerated (`hle_calls: 763 handler symbols in …/worktrees/origins3/build/screenshot`) — which is exactly why `hle_calls` prints it, and exactly the line a reader skips on the way to the numbers. **The discriminator is one command and it is title-independent: `readlink /proc/<pid>/exe` must equal your own build's binary path.** Assert it, do not eyeball it — with several worktrees the paths differ by one directory component. Trap 88's stronger form (`pgrep -P "$WRAPPER"` to pin the PID to your own child, plus a `/proc/<pid>/cmdline` title-id check) is better still where you control the spawn; the `exe` check is the one that works when you do not. Cousin of the counting rule under *The environment is part of the system under test* — `ps \| grep` and `pgrep` mislead about **how many**, this misleads about **which one**, and only the second silently answers a question about someone else's title. #1895 / #2013. |
| 101 | **A one-frame headless F9 grab reporting `window had no submits` is a fact about the WINDOW, not about the title** | The `PROSPER_CAPTURE_BUNDLE_TRIGGER_FILE` grab arms on the next present and closes on the one after, so its window is exactly **one present interval**. On CrossWorlds it fired at present 470 and reported `[grab] frame-bundle: window had no submits; press F9 again`. Read as a statement about the title — "it has stopped submitting GPU work, that is why the composite is empty" — it is a diagnosis, and it is wrong. The counter-evidence is direct and was already available: `PROSPER_SUBMITLOG_DIM=3840x2160` over the *same phase* counts **53 submits to the 4K present extent and 89 draw items per 10 s**. The title presents faster than it submits, so a randomly-placed one-present window legitimately catches zero, and the grab's message is an accurate report of an empty window rather than a defect. **Any title whose submit rate is below its present rate makes the default single-frame grab a coin flip, and nothing in the message says which kind of zero you got.** Fix: `PROSPER_CAPTURE_FRAMES=N` (1..240) widens the window to N presents; pick N from a measured submit-per-present ratio rather than a guess, and treat a still-empty wide window as the interesting result. The general form is worth more than the mechanism: a diagnostic whose scope is one sampling unit reports the *unit's* emptiness, and turning that into a claim about the subject requires a second measurement at the subject's own timescale. Cousin of trap 90 — there a counter climbed while nothing changed; here nothing was counted while plenty changed. #2013. |
| 102 | **A single sample from an arm that carried an invasive instrument is not a measurement of the subject** — and the invasive part need not be running when the sample is taken | The CrossWorlds lane published "the uniform post-logo frame is a phase, not a terminal state" — in the status doc, the game tracker *and* a PR body — on the strength of **one** sample: the final t=270 s frame of a 270 s arm read pure black instead of the uniform colour it had held since t=90 s. It does not reproduce. A clean 900 s arm is uniform at **all 30** samples from t=90 s to t=900 s, and a second 270 s arm at the same cadence is uniform at t=270 s. The arm that produced the outlier had `gdb` attached twice, at t≈120 s and t≈170 s, for `hle_calls` and a backtrace — both finished long before the sample, which is precisely why the confound was not felt: **an attach stops every thread and resumes them in an order the guest never chose, and a UE5 frame loop that survives that can still have taken a different branch.** "The instrument was not running when I measured" is not the same as "the instrument did not affect the run". What makes this worth a row rather than a note is the reflex it needs, which is cheap and general: **before publishing an outlier, re-run the arm with the instrument removed.** One clean arm settles it, costs one run, and is the only thing that distinguishes a phase transition from an apparatus artifact. Two further notes from the cleanup. First, the surprising direction is the dangerous one — the black sample was *more* interesting than the truth (progression rather than a wall) and that is what made it publishable; see trap 46 for the same asymmetry. Second, **a retraction has to reach every place the claim reached.** This one was in three documents, and retracting it in one would have left two live copies that the next agent has no way to know are dead — the status doc keeps a `## Ruled out` row saying so explicitly rather than deleting the original line. #1895 / #2013. |
| 103 | **A diagnostic gated on a hard-coded address window goes silently inert when the addresses move, and its silence is indistinguishable from "the thing did not happen"** | `PROSPER_MB3WATCH`'s arm hook filtered on `[0x20..0x21)` while **every current title's cache base is `0x30xxxxxxxx`**, so it armed nothing, on any title. Every null it produced before #2060 is **void, not negative** — including nulls that were quoted as evidence. This is the third inert-instrument discovery in two days: #1998 (`PROSPER_WATCH_LABEL`/`WATCH_HOT` gated on a DOLL-era window excluding all four titles it was written for) and the `{rdx, rax}`-only fault probe fixed in #1992 are the others, which makes it a class rather than an accident. The cheap defence is that **an arm hook must report whether it armed** — `armed=0` and `fired=0` are different facts and only one of them is a measurement. Before quoting any zero from a windowed diagnostic, confirm the window still contains the subject. #2060 / #2080. |
| 104 | **A read-only probe whose *duration* decides whether the subject lives** — "it only observes" is exactly the wrong reassurance for a timing-decided failure | `PROSPER_MB3_POISON` mutates nothing in the guest and is still a confound on `PPSA07809`, because #1945 there is decided by **how long the guest's own submit call takes**: 12 arms, 4 doses, one binary differing only in a `nanosleep` — **0/3 survive at none, 0/3 at 500 µs, 3/3 at 1500 µs, 3/3 at 3000 µs** (Fisher two-tailed p ≈ 0.002). So a diagnostic that changes nothing observable can still change the outcome, purely by costing time. Read-only is not the same as non-invasive. Before trusting a result taken under a read-only probe on a title whose failure is timing-sensitive, **measure the probe's own cost and re-run without it**. Note this does NOT revive "the guest outruns prosper" — that reading is separately falsified on ArcRunner, where `PROSPER_RENDER=0` faults at the same site with the same value, sooner. The per-title dose-response for ArcRunner is #2084. #2060 / #2080. |
| 105 | **A per-handler call census that silently omits a whole class of handler** — its zero for the omitted class reads as a measured "the guest never calls this" | `tools/hle_calls` enumerates handlers out of the binary by their six-`unsigned long` HLE signature; on Sonic Origins (#1905) it reported `armed=153` and a clean, complete-looking histogram. **`s_np_check_cb_entry` and `s_netctl_check_cb_entry` are not among those 153** — they are `PROSPER_ASM_TRAMPOLINE` entries rather than C++ handlers, so the signature walk cannot see them and they are never armed. Their zero is not "these were not called", it is "these were not looked at", and nothing in the output distinguishes the two. What that invites is specific, plausible and actionable: the title registers an NP state callback and a NetCtl callback during boot, so a census showing neither `CheckCallback` ever ran says **the guest never pumps its callbacks, and prosper's SIGNED_OUT / DISCONNECTED events are therefore never delivered** — a coherent boot-stall mechanism for a title that renders an empty scene forever, and one this lane wrote down before falsifying it. It is false: the same run's `PROSPER_SVCLOG` carries `[svc] Np state callback DELIVERED (userId=1, state=SIGNED_OUT, guest_fs=1)` and `[svc] NetCtl state callback DELIVERED (eventType=DISCONNECTED, guest_fs=1)`. General rule: **a census reports absence for whatever its enumeration step excluded, and the exclusion never appears in the result** — `armed=N` counts what was armed, never what should have been, so it reads as a completeness figure while being silent on coverage. Before believing a zero from any census, confirm the thing you are asking about is in the enumerated set (here: `nm build/boot_trace \| grep s_np_check_cb` against the tool's own armed list), and settle the question with a second instrument that reaches it by a different route — the `[svc]` log was that instrument, and it needed no cooperation from the census. Same family as trap 102, where `pidof` selected another lane's process: both are censuses that are complete-looking and wrong, and in both the output's own summary field is what makes them believable. #1905, #2076. |
| 106 | **A misspelled environment variable is indistinguishable from a feature that ran and reported nothing** | `avp_log()` reads `PROSPER_AVPLOG` (`src/hle/service/hle_service.cpp:978`). Two ArcRunner arms passed `PROSPER_AVP_LOG=1` — one underscore — and reported `video-ex calls: 0`, which was read as "the movie never started" and nearly became a finding about the guest. Re-run with the correct spelling, the same arms report **31**. `getenv` returning NULL is silent by construction, so a typo produces a confident zero with no diagnostic anywhere. Generalises past this variable: **an env-gated diagnostic that finds nothing must be made to say so** (`ARMED` on the way in, or a population counter on the way out), or its silence is unattributable. Before quoting any `PROSPER_*` zero, `grep` the exact string in `src/` and confirm something reads it. |
| 107 | **A documented diagnostic that is inert in the configuration the document prescribes** | `docs/ARCRUNNER_STATUS.md`'s reproduction block told the next agent to run `PROSPER_PRESENT_NZLOG=1` under `boot_trace`. Alone it emits **zero** `[render-nz]` lines over ~100 presented frames: the line is gated on `!px.empty()` in `frontends/shared/live/live_renderer.cpp`, `px` comes from `selected_pixels`, and the registrar announces `dump=0` because the readback that fills it is opt-in and this switch does not request it. The variable exists and is read — so trap 106's grep passes — it simply cannot fire without a companion. A recipe is not evidence that a recipe works. **Re-run a prescribed command and check it produces output before trusting a result taken with it**, and when correcting one, fix the recipe itself and not only the prose warning 300 lines away. |
| 108 | **A rate limiter working exactly as designed makes a RARE class invisible when it shares an ordinal with a common one** | The sharpest of this group, because nothing is broken. `PROSPER_LIVEPTR_TRIP` printed on the standard schedule — first 64, then powers of two — off one global counter. ArcRunner produces hundreds of free-list-residue hits for every one module-image vptr hit, so the vptr landed at ordinal ~300: past the first-64 window, not a power of two, **counted in the totals and never printed**. The run that mattered was thrown away and the arm re-run. The limiter did its job; the sampling was simply not stratified. **Key a diagnostic's print schedule per CLASS, not globally, whenever one class is rare and another is common** — the first hit of every new class must always print. The same failure returns at the bucket-table ceiling, so a nonzero "other" bucket means *raise the table and re-run*, never *no further classes*. |
| 109 | **`snprintf`'s return value passed to a raw `write(2)` — a real out-of-bounds read, not merely a wrong count** | Not an instrument that lied so much as one that corrupted its own evidence, and it belongs here because it degrades on exactly the richest lines. A new `[labelhist]` fault dumper formatted into `char b[512]` embedding a `char hist[512]`, then called `raw_write(2, b, (size_t)n)` with `snprintf`'s return — the **would-be** length, not the written one. A full 16-event label ring formats to ~450-500 characters, so the kernel copied up to ~35 bytes past a stack buffer, appending stack residue to evidence lines people paste into issues, or losing the line entirely to `EFAULT`. Caught in review, never shipped. **`snprintf` returns what it WANTED to write; use `strlen(buf)` for the byte count**, and treat any `off += snprintf(...)` accumulation as an out-of-bounds index until proven otherwise — the same file had a second, latent instance in a `buckets[320]` formatter. **Recording the class was not the same as closing it**: the sweep it should have triggered (#2050) found **88** live sites in `exec_image_linux.cpp` alone, so the accompanying claim that every other `raw_write` there was already length-bounded was wrong. They now go through `prosper::raw_write_fmt(fd, buf, sizeof buf, n)` (`src/host/platform/raw_syscall.hpp`), which clamps and announces the loss — **use it rather than re-deriving a clamp**. The predicted `EFAULT` was measured: against a `PROT_NONE` guard page the unclamped write delivers **0 bytes**, so the failure is a lost line, not a dirty one. The `off += snprintf(...)` half is still open as #2161, and it is the worse half — that one overflows a *write*. |
| 110 | **A review whose `file:line` citations come from the shared main checkout instead of the PR head** — the trees differ and nothing announces it | The #2091 reviewer read `command_processor.cpp` from the shared working tree at `0bf229b0` rather than `origin/master` `c3b362f6`. The file differs by **442 lines** between them, so every line number in its first review was wrong — published as verified fact in a review otherwise careful enough to hand-recompute a Fisher exact and open `defer_enabled()` at source. The substance survived re-verification, which is what makes it a trap: nothing failed, and the wrong citations would have been inherited by anyone acting on them. It also *caused* a second wrong finding — `grep -rn 'nonheap' prosper/src/` returned nothing from the stale tree where master has four hits, and that silent disagreement read as "the symbol does not exist" rather than "this is not the tree under review". **A grep that returns nothing where you expect hits is a checkout-identity signal.** CLAUDE.md already requires an own worktree for authors; it binds reviewers just as hard, and a reviewer has no build step that would otherwise expose a stale tree. Same family as traps about a cited claim reading as a verified one, but a distinct mechanism: not misreading a file, but reading the wrong copy of the right one. #2091 |
| 111 | **A wrong-but-plausible palette passes visual review, because the frame is correct in every other respect** | ArcRunner's intro cinematic was one edit away from being published as progression evidence with a green nebula and magenta thrusters. Geometry, composition, frame pacing and decode throughput were all correct (1,901 of 1,908 frames at 4K), and a green nebula in a cyberpunk-styled game reads as art direction. A reference decode of the same movie with prosper out of the path gives **orange** and **cyan**; the transform was then named as `V := U` and confirmed globally by `corr(Cb,Cr)` = 0.994/0.9997 in prosper's frames against **-0.649** in the reference — a global statistic, not a fitted patch. The generalisation is the point: **prosper's acceptance evidence is overwhelmingly visual, and hue is the property a human reviewer is least able to falsify by looking.** A colour claim needs a reference decode, not an opinion. Note this is the converse of the diagnostic-gradient trap — there a metric was fooled and the fix was to open the image; here opening the image is exactly what fails. #2094 |
| 112 | **A rendering pipeline that DISCARDS what it cannot fit, so the page looks authoritative while the evidence column is simply gone** | GitHub-Flavoured Markdown splits a table row into cells on `\|` **before** it parses inline content, so a pipe inside an inline `code span` is a cell boundary and not text. A row that ends up with more cells than the header has the excess **silently discarded** — the spec's word. Six rows of this very table rendered truncated on GitHub for months, losing their trailing text and, where they had one, their issue citation; the raw file is correct, so an editor shows nothing wrong and the diff that introduced each one looked perfect. The sharpest instance is **trap 40 — the rule about `cmd \| tail` — which was itself broken by writing that pipe unescaped**, rendering it as an 8-column row in a 3-column table and dropping most of its text. (Writing this very row unescaped is how the new check earned its keep: it failed on it immediately.) Note what made it durable: `check_numbered_table.py` reported the file `unbroken` throughout, because it validated numbering contiguity and never cell arity, so there was a green gate pointed at the right file that could not see this class at all. **A gate's silence is only evidence about the class it inspects — before citing one as coverage, ask which failures it is constitutionally unable to report.** Cousin of trap 105 (a census is silent about whatever its enumeration excluded), and of trap 53, whose own advice on this — exclude code-span pipes when counting cells — was inverted and would have suppressed the defect; it is corrected above. The gate now checks arity on every tracked `.md`. #2108. |
| 113 | **A breakpoint on a `mov reg,[mem]` reports the register's STALE value — gdb stops before the instruction runs** | Nearly produced a phantom guest bug in the Sonic Origins boot trace. `eboot+0x58751b` is `mov r14,[r12+rbx]`, the success arm of a component search; a breakpoint there reading `$r14` printed `0x0` twice, because r14 still held the `xor r14d,r14d` from the loop preamble. The next instructions store a coroutine continuation through r14, so the reading was "the search failed and the guest is about to write through a null pointer" — a plausible, self-consistent, entirely fabricated defect. The search had in fact succeeded, which the *branch* counters said all along. **Read a register at the instruction AFTER the one that writes it, or read the memory operand the instruction is about to load** — and prefer counting which branch was taken over sampling a register that a nearby instruction owns. Generalises to every `$reg` read in a gdb breakpoint script: the breakpoint address names the instruction that has NOT yet executed. #1905 |
| 114 | **An rbp-chain guest backtrace silently returns the caller's caller when the import is reached through a local wrapper** | The companion to trap 113, from the same PR, and the more expensive one: it put four wrong rows into a *measured* cross-title evidence table that was heading into a durable doc. Breaking on prosper's HLE handler and walking `[fp+8]` recovers a return address, but when the guest calls the import from a thin wrapper the frame belongs to the wrapper's caller, so the reported address sits after a `call` to an ordinary intra-module function. Nothing looks wrong — it is a genuine return address with a plausible instruction after it, which is exactly why 4 of 5 rows were misclassified and one title was recorded as "discards the return" when it has the subject title's identical `test eax,eax; jle` (a second title whose save path was silently failing, #2125). The walk is still the right tool for "who called this" across 23 dumps; it is not sufficient for "what does the call site do". **Confirm the five bytes ending at the reported address are an `e8` whose target is the `ff 25` PLT thunk for that NID**, and read the *thunk's* direct callers instead — resolve the import's GOT slot out of `JMPREL`, find the `ff 25` pointing at it, then byte-scan `PT_LOAD` for `e8` displacements that reach it. #2121, #2125 |
| 115 | **A short census window aimed by a renderer-internal ordinal, with the ordinal estimated from a *different* subsystem's counter** | `PROSPER_DUMP_PERSISTENT` and `PROSPER_PASS_LOG` open a **three-callback** window at a renderer-callback ordinal; `[gpucap]` prints a **submit** number. On one 360 s Sonic Frontiers route those are **6,560** and **26,209** — a 4x gap, and in the direction that makes an estimate overshoot. Overshooting produces **no census at all**, and that silence is byte-identical to "the census ran and every target was empty" — which, on a title being investigated *for black frames*, is the answer you already expect, so nothing about it invites a check. It cost a full 360 s arm before the ordinal was calibrated (`PROSPER_PASS_LOG`'s own out-of-window lines happen to print the counter, which is how it was finally read). Two rules. **An internal counter is neither a clock nor another subsystem's counter — calibrate it in the same configuration before aiming anything at it.** And prefer a window aimed by something externally observable: `ms:<millis>` was added to both switches for exactly this (#2110), with the same kind of origin `PROSPER_GPU_CAPTURE_AFTER_MS` uses. Failing that, make a **missed** window say it was missed, because a diagnostic that is silent when it never ran cannot be told from one that ran and found nothing. Cousin of trap 87. #1968 / #2110 |
| 116 | **A multi-kind census in which one kind is silently unarmed — and whose in-run positive control structurally cannot reveal it** | `PROSPER_PROVENANCE_ADDR=<addr>[:size]` documents itself as reporting *every recorded write overlapping one guest range, naming the writer kind*, and `GuestWriterKind` has four values. Three of them — `ComputeBuffer`, `DmaData`, `WriteData` — gate only on `writer_provenance_enabled()`, which the address watch itself makes true. The fourth, `ColorTarget`, i.e. **every draw**, is recorded only inside `diagnose_resource_provenance()`, which returns early unless the *separate* `PROSPER_PROVENANCE_DIM` is set **and parses as `WxH` with both dimensions non-zero**. An `ADDR`-only run therefore reports zero `color` lines however many draws hit the watched range, while `compute-buffer` lines stream past demonstrating that the watch is armed and printing. **That positive control is real and answers the wrong question**: "this instrument can print" is not "this instrument can print the kind you are asking about". On Sonic Frontiers the zero was **true** for the scanout addresses and **false** for another address in the same run (1,166 pass records show draws targeting it) — a correct conclusion validating a broken instrument, which is the worst case available, because being right is precisely what stops anyone re-checking. Filed as #2111. **The same class reaches you by a second mechanism — a capture's scope rather than a recorder's arming — and it is worth carrying in the same entry.** A single-submit capture **cannot establish a consumer set**: any temporally adapted quantity is written in one submit and read in another, so "X's consumer is Y" is a claim the instrument cannot make, however clean the capture. That one reached a `GRAPHICS.md` row two rows below the warning above — a luminance reduction's 2x1 output was said to be consumed by the tonemap, true of the captured frame and unknowable in general — and was caught only in review (#2118). **So before quoting an absence, or any completeness claim, write down what the instrument structurally cannot observe — arming, scope, or rate — and check that your claim is not in that set.** Then state the scope in the sentence ("in the captured frame") and make the load-bearing half of the argument the half that does not need it. #2110 / #2111 / #2118 |
| 117 | **`nz=0` on a `gpu_replay --inspect-only` resource marked `temporal-RTT-seed`, read as "this target is empty"** | That count is over the resource's **guest-memory** bytes, and a renderer-owned target is legitimately all-zero there on the persistent-GPU-target path — prosper renders into a Vulkan image, not into guest memory. The content is on the `rtt-seed` line for the same address, and on Sonic Frontiers the two disagree outright: the 4K `RGBA16F` post-chain buffer reports `nz=0` while its seed hash is **not** the all-zero hash, and a `PROSPER_DUMP_PERSISTENT` census then showed the target is **opaque black with alpha exactly 1.0** — a different finding from empty, and the one that says the guest programmed it. Read the seed, not the `nz`. **The sub-trap is in the verification you would reach for next, and it is the more dangerous half.** Those hashes come from `gpu_capture_hash`, which uses the FNV prime with a basis of **`0x14650fb0739d0383`** — the FNV-1a 64 offset basis *with a digit dropped*. A stock FNV-1a 64 therefore produces a value that can never match for **any** input (xor and odd-multiplication are both bijections mod 2^64), and the mismatch reads as "this buffer is not zero" — the exact false conclusion the hash was being used to rule out. The constant is deliberately frozen, because changing it would invalidate every recorded capture hash, bundle manifest and seed hash in the project's evidence trail, and it now says so at its definition. To decode one of these offline, reimplement `gpu_capture_hash` including its basis: the hash of *N* zero bytes, or of any repeating texel pattern, is then computable and comparable — which is how "opaque black" and "alpha 1.0" were identified byte-exactly above. Same family as trap 91. #1968 / #2110 / #2118 |
| 118 | **A sampling window measured in the guest's own sleep ticks, compared across two phases of one run** | `hle_calls --ticks N` counts ticks of `prosper::k_usleep`, **not wall time and not frames**, so the size of the window it opens depends entirely on how often the guest happens to sleep. On one Sonic Frontiers route the same `--ticks 300` recorded **1** call during the intro and **164** in the black phase — a >100x difference in call density per tick, because the movie-playback path and the post-chain path sleep at wildly different rates. The intro window was therefore **shorter than a single game frame** and could not have observed a draw of any kind; read as a control against the black-phase arm it would have "shown" that the guest stopped issuing indexed draws, when in fact it had barely been sampled. Note what makes this one hard to see: **the instrument was working exactly as designed and reported its own completeness honestly** (`entries=300/300 window=complete`) — it is the *comparison* that was invalid, and no field in the output is wrong. **So size an `hle_calls` window by something the subject defines, not by ticks: check `agc_dcb_set_flip` (or the title's flip handler) in the result to see how many frames you actually captured, and treat any window with a flip count of 0 as void rather than negative.** Then state the frame count beside every figure, so two windows are only ever compared per-frame. The same caution applies to any `--ticks`-style budget on a clock the subject controls. Cousin of trap 92, and the mirror of it: there the window's hidden variable belonged to the **host** (page-cache residency), here it belongs to the **subject** — which is worse, because a subject that changes behaviour between phases changes your window size in exactly the phases you are trying to compare. #2023 |
| 119 | **A build killed mid-`ar` leaves a TRUNCATED static library, and it resurfaces as a link error in a different target — reading exactly like a code defect** | Killing a contended `cmake --build` (load average 17, six of my own compiler processes, another lane's PR merging under me) is ordinary housekeeping, and it is safe at almost every point except one: partway through writing `libprosper_core.a`. `ar` leaves a valid-looking file that is short, nothing checks it, and the next build happily reuses it. The failure then appears **later, in an unrelated target**, as `ld.bfd: libprosper_core.a: error adding symbols: no more archived files` while linking `test_rdna2_to_spirv` — a *linker* error naming a *test* whose own sources compiled cleanly, so the natural first hypothesis is that the change under test broke something. It did not; the archive was corrupt before the edit. The tell is that the message names the **archive**, not a symbol: a genuine link failure says `undefined reference to …`, while `no more archived files` means the container itself is malformed. **Delete the archive and rebuild — do not debug the target it surfaced in**, and prefer letting a contended build finish over killing it, since the window where a kill corrupts state is precisely the archive step every target downstream depends on. Same family as the traps where the apparatus rather than the subject produced the defect, with the added twist that here the apparatus damage was self-inflicted and silent. **A kill is not the only trigger, and the archive is not the only casualty: TWO `cmake --build` runs on one build dir truncate each other's `.o` files the same way.** The easy way to get there without meaning to is the harness itself — a build backgrounded on timeout is still running, so the "retry" you launch in the foreground is a second concurrent build over the same objects. It presents as `file format not recognized` while linking, names a **different target each rebuild** (the giveaway: a real defect is deterministic), and survives deleting the one object the error names, because the next run corrupts another. Confirm with `pgrep -f 'cmake --build'` before retrying, wait for the background job, then rebuild once — sequentially. Rediscovered at full cost during #2190. #2123 |
| 120 | **A diagnostic whose pattern appears in the diagnostic itself — it reports its own existence as the subject's** | Two instruments, one shape; the recognition cue is identical, so they are one row rather than two pieces of lore competing for the same slot. **`git diff origin/master HEAD` manufactures the very defect trap 41 screens for.** Several lanes share one `.git`, so another lane's fetch advances `origin/master` under you and the diff then reports *their* additions as *your* deletions, in files you never opened — it fired twice in one session and cost a second look each time. *Tell:* `merge-base == HEAD`. *Fix:* rebase first so `merge-base == origin/master`, then `git merge-tree --write-tree origin/master HEAD`, and run the deletion check only after that. **`pgrep -f '<pattern>'` manufactures the process it hunts for**, because the pattern sits in the hunting command's own command line: `pgrep -f 'gh pr checks 2135'` matches the shell running it, and a loop grepping `/proc/*/cmdline` for a marker matches its own `grep` argument — the classic `ps \| grep` self-match, which the process-counting note already records in its over-counting direction. *Tell:* the count never reaches zero no matter what you kill, and every scan returns a **different, always-fresh** pid. *Fix:* the self-excluding bracket form (`chec[k]s`), or `pgrep -x` on the executable name — **and anchor the pattern**, because excluding yourself is only half of it. The bracket form above, run as `pgrep -f 'gh pr chec[k]s 213'`, correctly excluded itself and was **still wrong**: it matched another lane's watcher on PR **2131**, because `213` is a prefix of it. The detector was honest about itself and still wrong about the subject — a PR number is a prefix of ten others. What it nearly cost is why the row is worth reading. A lane told to "kill your remaining waiter loops" found six candidate processes and **none of them were its own** — the orchestrator's heartbeat, the orchestrator's persistent PR monitor, and four other lanes' shells — while the "surviving parent" that kept respawning the match was the lane's **own `claude --resume` session**. `TaskStop` had worked on the first call; there was never anything to kill. The three plausible targets were therefore the orchestrator's heartbeat, the orchestrator's monitor, and the lane itself, and that monitor is specifically the mechanism that catches an **approve/reject split on one SHA** — the exact failure that created the task — so **tidying up would have removed the safeguard against a repeat**. Net terminated: zero, only because ownership was verified before acting rather than the instruction followed as given. Note the instruction was the orchestrator's and it was wrong: a kill order issued without a way to tell one lane's processes from another's, on a machine running eight lanes. **The defence is to make the detector unable to describe itself** — exclude self and ancestry, match on a field the query cannot contain, or ask what the command would report if the subject did not exist. Inverse of trap 110, where a grep returning **nothing** where you expect hits is a checkout-identity signal; here a grep returning **something** is the same signal wearing the opposite sign, and the false positive is the more dangerous of the two because it names a remedy. #2135 |
| 121 | **A recorded trap did not stop its own re-derivation one day later — when the remedy is a discipline the reader must remember, assume it will be skipped and mechanise it instead** | This row is deliberately NOT a second description of trap 116's mechanism; 116 owns that, and #2111 owns the defect. It records what happened *after* both existed. Chasing #2132 I reached for the guest-write history, set `PROSPER_WRITER_PROVENANCE=1` — the switch documented as the explicit "retain everything" request — and got **183 negatives it could not have falsified**, because the `ColorTarget` recorder was behind the unrelated `PROSPER_PROVENANCE_DIM`. That is trap 116 exactly, already written down, already filed, in a document I had read. I re-derived it from scratch and spent arms doing so, then compounded it by inferring "the history was empty" from a zero that a *dim-gated consumer* had produced by never running — it held **42** events, not 0. **The general form is worse than the instance: a diagnostic split across a producer and a consumer can be half-armed, and the half that prints is the half that looks healthy.** A watch that streams `compute-buffer` lines while an entire writer class is unreachable reads as a working instrument, and the in-run positive control confirms the wrong proposition — 116's point. What 121 adds is the conclusion drawn from 116 failing to protect the next reader: **116's remedy is "before quoting an absence, write down what the instrument structurally cannot observe", and that is a discipline, so it decays.** The durable fix is to make the negative carry its own falsifiability — the diagnostic now prints `(history=8589 recorded: color=39 compute-buffer=4744 dma-data=8127 write-data=1111)` beside every "no recorded GPU writer", and the address-watch banner names the armed recorders, so `color=0` is visible in the line itself rather than reconstructable from a second run and a code read. **Two limits a count still cannot express, and both had to be written down explicitly**: a SKIPPED dispatch records nothing however the switches are set, and a kind can read non-zero while having DISCARDED the write that matters (`DmaData`/`WriteData` drop writes below 256 bytes / 64 dwords unless full retention is on) — an armed-but-not-recording case no summary reveals, and worst over exactly the few-dword windows these queries are used for. **So: when you find a trap, ask whether a future reader could hit it while doing everything right; if yes, the row is necessary but not sufficient, and the instrument owes them a self-report.** Also check the tracker for the instrument you are about to trust, not only for the code you are about to change — #2111 had the gate, the file:line and the fix written down before I started. Cousin of 116 (same mechanism, opposite lesson: 116 is how to read the instrument, 121 is that reading it correctly cannot be the only defence). #2111 / #2132 / #2143 |
| 122 | **A fuzz whose generator could not construct the case it reported absent — and a positive control that passed, certifying the model was LIVE rather than that it could express the case** | Reviewing #2150 I had to decide whether completing a truncated tracker snapshot widened `win_protect`'s accept set anywhere beyond the single case the PR body disclosed. Rather than argue it from the diff, I modelled the whole validation walk from source — both slicers, the reservation check, and the region walk including its `next_slice_base` scan — and fuzzed old-against-new over **225,502** random configurations of host regions, tracker entries and span placement, classifying every accept/refuse difference by the branch that produced it. It reported exactly two deltas and **confirmed the PR body precisely.** Suspecting nothing, I ran a positive control anyway — and **it passed**: the population constructed the hole-then-committed-slice precondition shape **41,206** times and produced **17,659** narrowings, exactly the evidence I would cite to prove the discriminator works, against **0** widenings. All of it was void. I had placed the model's module aperture flush against the **top** of its address space, so `region_end - 1` was always inside the aperture and the second widening class — the one in the `MEM_COMMIT` arm, where a completed vector shrinks `next_slice_base` and therefore shrinks the range put to the aperture test — was **structurally inexpressible**. Moving the aperture into the interior and re-running (330,179 configurations) surfaced **1,277** instances of the missing class immediately. **What separates this from traps 64, 116 and 121 is that the lever moved.** Those rows are about instruments that are unarmed, half-armed or scoped too narrowly, and in every one of them the discriminator is *silent*; mine was loud, and the noise is what made the zero look validated rather than void. **A live lever certifies that the model runs, and nothing whatever about whether its domain contains the case under test.** The control failed to catch it for a reason worth stating on its own: **a positive control drawn from the same generator as the null it validates tests the discriminator, never the domain** — it can only ever confirm that the machinery fires on cases the generator can build, which is the assumption in question. *Tell:* a categorical zero for one class sitting beside a large non-zero for another **in the same run**; real behaviour is rarely that tidy, so read a clean zero first as a bound on the generator and only then as a property of the subject. *Concrete, reusable form:* **a synthetic model of an address-space walk is an instrument, and where you put its apertures, page boundaries and region edges decides which deltas it can express at all** — flush-against-an-edge geometry silently deletes every case that has to straddle that edge, and edges are exactly what these walks are about. *Fix:* **before believing a fuzz's clean zero, construct one positive instance of that class BY HAND, outside the generator, and confirm the model can represent it at all.** A generator that cannot produce the defect reports its absence in the same voice it would use to report a real one. Two general points, in decreasing obviousness: a null from a model is a statement about the model until the model is shown to span the case; and **"my instrument agrees with the claim I am checking" is the reading most in need of an adversarial pass, not least** — this null would have confirmed the PR body exactly, which is the whole reason it deserved a second look. #2150 / #2144 |
| 123 | **A test assertion with no lever in the code under test — it passed on whether glibc happened to grow a block in place, and what decided that was the HLE registry's own size** | `test_libc_alloc` asserted that `realloc` preserves a `memalign` block's **256-byte** alignment. **Nothing in prosper can make that pass or fail:** on POSIX `guest_realloc_portable` is `return realloc(p, size)` (`hle_libc.cpp:183`), and `h_realloc` is handed a pointer and a size and **never an alignment**, so the arm measured glibc's placement rather than any behaviour of ours. Instrumenting both arms pins the mechanism exactly. Unperturbed, `memalign` returns an address and `realloc` returns **the same address** — the block grew **in place**, which keeps the original alignment for free. Adding **four inert, fabricated, never-imported NID registrations** to `register_service_hle()` on otherwise unmodified master puts the block somewhere the growth cannot be satisfied in place, `realloc` **relocates** it, and the new address is 16- but not 256-aligned. The contents half passes in both arms; only the alignment half moves. So **any PR that registers a NID could turn this file red without touching it**, with a message naming the allocator — and it sits directly under the 904-NID census, where every fix registers NIDs. *Recognition cue:* **an assertion whose subject function was never told the property being asserted.** *Second trap inside the first, which is why this row is long:* three synthetic in-test perturbations built to demonstrate the sensitivity — 65 pre-arm allocation counts, 9 blocker sizes placed between the `memalign` and the `realloc`, and 41 counts injected before `register_builtin_hle()` — **all returned a clean zero**: 0 relocations and 0 failures across 115 configurations. Every one is **void, not negative.** None of them moves the `memalign` block at all (it sits at page offset +256 in all 115), because the registry's real effect is an `unordered_map` **bucket-array rehash** — one large reallocation at an unpredictable point — which uniform small `malloc`s never reproduce. Read on their own they would have "proved" the test sound. This is trap 122's shape at a different injection point, and what saved it is that rule's applicable half: **the hand-constructed positive instance existed before the model**, so the model's zero could be recognised as void instead of believed. Do not hand-tune such a model until it agrees — that fits the instrument to the answer. *Fix:* assert what the contract actually promises. C17 7.22.3p1 gives contents plus `alignof(max_align_t)`, and 6.2.8p3 makes 256 an **extended** alignment that nothing promises to carry across a resize; the platform agrees rather than merely permitting it, `libSceLibcInternal` exporting **`reallocalign`** (`OGybVuPAhAY`) as a function separate from `realloc` (`Y7aJ1uydPMo`) precisely because a two-argument resize cannot know an alignment. #2165, #2185 |
| 124 | **An env var that names the right concept and controls a different store — so a "per-lane isolated" run silently inherited a peer's save data, and the guest took a different ROUTE** | `PROSPER_SAVEDATA_DIR` sets the save-data-**memory** store (`hle_service.cpp`, `savemem_path()`; the line numbers this row used to cite were stale by the time anyone followed them, so it names the function instead). The mounted `/savedata0` area — what a title actually reads and writes — is `PROSPER_SAVE0` (`hle_file.cpp`, `save0_base()`), which at the time was **`/tmp/prosper-savedata0` by default, one directory shared by every lane and every title on the box.** Four Sonic Frontiers investigation arms set `PROSPER_SAVEDATA_DIR` to a private path and were read as isolated; every one of them mounted the shared default. That is not merely untidy — Frontiers writes `option/`, `arcade/` and `challenge/` there on first boot and **probes for a PS4 save to import only after reading them back**, so whether the boot reaches the wall's real cause depends on what some earlier run left on disk. A `PROSPER_PROGRESS_UNIMPL` census run on the empty-save route reported "3 distinct unimplemented NIDs, 5 calls, none polled" and was quoted on the issue as evidence that no missing NID was involved; the populated-save route reports a **fourth called 1,319 times**, and it was the entire blocker (#2023). *Recognition cue:* **a diagnostic env var whose name matches your intent better than it matches its own `getenv`** — grep the variable, not the concept, and check the default it falls back to. *Half of this is now fixed and half is not:* since #2734 both roots are namespaced by title id and both variables are ROOTS rather than final directories (`docs/SAVE_DATA_LAYOUT.md`), so a run can no longer inherit **another title's** saves — but the two variables are still different stores, and a run still inherits **the same title's** earlier saves from a shared root, so the trap's actual lesson is untouched. *Corollary the save case makes concrete:* on a shared box, persistent guest state is an **input** to the route. Record which save area an arm used the way you record the binary's revision, and give a first-boot arm its own directory. #2023 |
| 125 | **A straight-line interpreter reporting register values from a path the wave cannot be on — every field in the line honest, the conclusion a phantom that survived four investigations** | prosper's scalar const-fold walks a shader's decoded instruction stream in order and models no control flow. On *Sonic Racing: CrossWorlds*' NGG vertex stage that produced `[dyntrace] SMEM op=0x0 load sdst=s20 sbase=s18 base=0x0` followed by `addr 0x0 unreadable` — read, correctly, as *"the guest's constant buffer holds a null bindless-table pointer"*. It does not. The dword is `1.0f`, ordinary uniform data; the fold had executed `s_buffer_load_dwordx16 s[16:31], s[8:11], 0x60` at `pc=45` and `pc=113`, which sit inside the block that the `s_cbranch_vccz` at `pc=21` **skips** whenever the block reading `s[18:19]` at `pc=173` runs, and those loads overwrote the stage's two seeded direct pointers. **Four lanes hunted the writer** — a GPU-side writer (8,589 recorded writes, none overlapping), a skipped compute dispatch (8/8 programs, 138 ranges, zero coverage), a stale/wrong buffer (#305's comparison came back equal), and finally "the null is legitimate and we should fold that arm" — and each negative was *correct*, because the question was wrong: nothing was ever supposed to write it. *What makes this one hard:* the trace has no field that is false, no rate limit, no unarmed recorder, and no missing positive control — the instrument reported exactly what the interpreter did, and the interpreter is what was wrong. It is trap 122's shape one level down: there a generator could not *express* the case; here an interpreter could not *represent* the control flow, so the value it printed was outside the domain the reader assumed. *Tell:* a chain whose inputs are self-consistent and whose conclusion requires the guest to have done something senseless — a shader dereferencing a float constant as an address is a statement about the model, not the guest. *Fix, and it generalises:* **when a fold prints a register value, ask which basic block it came from before asking who wrote it.** Disassemble the stage and check whether the writing instruction dominates the reading one — the branch encodings settle it in minutes and no amount of memory-watching can. Validate a hand disassembly by requiring it to land a known instruction on the pc the emulator itself reports (here the MUBUF at dword 211); a linear `llvm-mc` decode of the same bytes drifted 26 dwords and would have put every branch target in the wrong place. **And the sting in the tail, from this row's own fix being rejected:** the CFG proof written to repair it ran over the fold's *compacted* instruction stream — which drops ordinary VALU/EXP while preserving PCs — so "the previous instruction is an unconditional branch" was really "the previous *retained* instruction is", and a dropped block between them was invisible. **A CFG proof is only as good as the stream it walks; if that stream was filtered for another purpose, adjacency is not adjacency.** Check physical adjacency (`prev.pc + prev.len_dwords == target`) rather than list position, and count predecessor edges in *both* directions — a tally that skips backward branches is not a tally. Two dead conditions in the first version (testing for `s_endpgm` on a stream that stops at `s_endpgm`) were the tell that the author had the wrong stream in mind. #2132 / #2202 |
| 126 | **A verification build whose `GAME_DUMP` does not resolve silently runs a SMALLER SUITE, and the only tell is the test COUNT** | `CMakeLists.txt:148` sets `HAVE_GAME_DUMP` from `EXISTS ${GAME_DUMP}/eboot.bin`, and `GAME_DUMP` defaults to `${CMAKE_SOURCE_DIR}/../PPSA24651-app0` — which resolves **inside the worktree**, where no dump lives. A worktree-local build therefore skips every dump-backed test, and `ctest` reports **212 passed** where a configured build reports **217**. Both are `exit 0`; both print `100% tests passed`, because it is a percentage of what ran. Measured 2026-08-06: an orchestrator quoted "212/212 green" as the merge gate for a whole session, including for a change to shared descriptor resolution — the absent tests being `boot_reaches_first_syscall`, `game_compute_exec`, `game_compute_exec_no_adaptive_storage_result` and `real_shader_render`, i.e. precisely the ones that execute real recompiled shaders. *Why it survives:* CMake **does** announce it — `Game dump not found at … skipping dump-backed tests` — but once, at configure time, in a log nobody re-reads, and never again on the fifty builds that follow. *Recognition cue:* **a peer's test count HIGHER than yours is evidence about your build, not about their staleness.** That same session twice diagnosed a lane's larger number as a stale base: correct once (a branch genuinely behind) and wrong once, where the lane's 217 was right and the orchestrator's 212 was the defect. *Fix:* set `PROSPER_GAME_ROOT` (CMake now derives `GAME_DUMP` from it, and the configure step WARNs rather than whispers when the dump does not resolve — #2893), or pass `-DGAME_DUMP=<DUMP_ROOT>/PPSA24651-app0` explicitly in any worktree build, and quote the COUNT, never the exit code alone — the count is the only falsifiable half, which is why #2188 required it. #2202 |
| 127 | **Two frontends returned OPPOSITE verdicts on whether a change fixes a title — and the obvious explanation was excluded by the runs' own arithmetic** | A candidate fix for #1226 (`PROSPER_POST_SUBMIT_VISIBILITY=1`) was scored on ArcRunner's established `boot_trace` route: **0 of 3 faulted**, 788-838 delivered video frames against a default arm's 29-32, and a 260 s run rendered the title screen. The same head, same dump, same env, under `tools/screenshot`: **2 of 2 faulted**, at 35 and 42 delivered video frames. Had either frontend been the only one used, the session's whole conclusion would have flipped. *The tempting explanation is wrong, and checking it is what makes this a trap rather than a note:* `screenshot` reads back a 4K frame per sample, so "the readback perturbs a timing-sensitive title" is instrument trap 104 exactly and fits the shape — except both runs died **before their first sample interval elapsed** (`--seconds 8` and `--seconds 20`, deaths at ~9 s), so no readback had happened. Whatever differs is in the frontends' setup, not their sampling. *Recognition cue:* **a frontend is an apparatus, and a progression claim inherits it.** `tools/screenshot` is what the project uses for evidence and `boot_trace` is what lanes iterate on, so the two are routinely treated as interchangeable views of one run; they are not, and no row above covers a case where the *frontend* is the variable. *Fix:* name the frontend in every survival or progression claim, and when a lever's verdict matters, run it under both before writing it down — it is one extra arm and it is the difference between a fix and a phantom. Filed as #2217; the ArcRunner evidence in `ARCRUNNER_STATUS.md` § 2026-08-07 states its frontend for exactly this reason. #2217 / #2219 |
| 128 | **One diagnostic tag over two roles — a fast path's ROUTING DECLINE logged identically to a terminal failure, so counting the lines renamed a whole frontier** | *Sonic Racing: CrossWorlds*' eight skipped compute programs were recorded on #2013 and in the title's status doc as failing on **control flow**: "21 `[cfg-recompile-reject]` and 26 `[compute-struct-reject]` lines per boot, on `s_cbranch_vccz`/`vccnz` and a forward `s_cbranch_execz`, one of them a `backward else` shape (#590)". Not one of them fails on a branch. **Seven of the eight stop on a single arithmetic, SDWA or MIMG instruction** (`v_mov_b32_sdwa … sext(…)` ×3, `v_rndne_f16_sdwa`, `v_ceil_f16_sdwa`, `image_atomic_add dim:2D_ARRAY`, and an `s_flbit_i32_b64` whose opcode is implemented and whose *scalar source is untracked*); the eighth is 12,916 dwords of zero and is not a shader. Settled by A/B rather than by re-reading the log: implementing two SDWA families and nothing else restored **four of the seven**, leaving their branch shapes untouched. *The mechanism, stated precisely because a loose version of it is also wrong:* `[compute-struct-reject]` is emitted from **21 sites in two different roles** — **14** inside `detect_forward_ifs`, the narrow structurizer, where it is a **routing decline** (the recompiler then runs the CFG dispatcher, which on these programs accepted the same shapes and carried on to the arithmetic op), and **7** inside the structured emitter, where it is terminal and *is* the cause. So "read it never as a reject" would itself mislead. Worse, 12 of the 26 lines are `structured emission stopped next-pc=N next-if=…`, which is **not a control-flow complaint at all**: `next-pc` is the blocking instruction — matching the `[cfg-recompile-reject]` pc exactly — and `next-if=4294967295` states in the diagnostic's own fields that no `if` is pending. The answer was in the output the whole time, in the field nobody read. *And the routing reading has a precondition:* a `detect_forward_ifs` decline is only a routing decision when the dispatcher is actually selected (`cfg_dispatch_safe` and either `exact_compute_wave_cfg` or `cfg_branches > 2`); otherwise `cf_rejected` clears the loop list and the branch genuinely does block. *Recognition cue:* **two reject diagnostics from one program describing different causes — ask which emitter ran LAST, not which line explains more.** The convincing one here was the wrong one for three compounding reasons: more numerous (26 vs 21), more specific (branch pcs, targets, merge points), and it matched an issue that already existed (#590), so it read as corroboration rather than noise. *Generalisation:* wherever a translator has a narrow fast path and a general fallback, the fast path's refusal is a routing decision logged at the same severity as a real failure — and if the two share a tag, no amount of grepping separates them. Attribute a reject to a **program** before attributing it to a **cause**: `PROSPER_SHADER_DUMP` plus a `pc → (fmt, op, words)` listing gives a 1:1 mapping, and `tools/re/disasm_words.py` names the instruction from its bytes. *What it cost:* the lane was briefed that the 8 skipped compute programs fail on control flow and that #590 was the frontier; the actual fix was two SDWA decode admissions, and it halved the population (8 → 4) in one session. #2013 |
| 129 | **A test suite's own arm count was a stale snapshot, so "my two new arms did not run" was read off an instrument that structurally could not have counted them — and the wrong conclusion sent the investigation back to the start** | `test_check_build_revision` prints `N arms, M skipped`, and that count exists precisely so the suite's coverage is falsifiable from a log. `coverage` was a **local assigned partway down `main()`**, so every arm added below it went uncounted. Chasing the red Windows MinGW job I added two arms just above the summary print — which is exactly where a new arm gets added, and exactly where the assignment sat — and CI reported the **identical** `29 arms, 5 skipped` before and after. Read as *"the change did not reach the runner"*, which is the natural reading and was false: the arms had run and **passed**. It took an arithmetic check to see it — 29 reported + 2 unreported + 5 skipped is the 36 the file defines, and locally the same head printed `33 arms, 3 skipped`, also 36. *Why this belongs here rather than in the PR that caused it:* the failing suite and the counting suite were the same file, so the instrument under suspicion was reporting on itself, and the number that would have exposed the staleness is the number that was stale. *The compounding half is the reusable part:* it then happened a **second time, in the commit fixing the first**, because the remedy was "move the assignment down" — a position, and positions rot the moment anyone appends. Trap 121's lesson at a smaller scale: **when a stale reading comes from WHERE a value is computed, moving it is not a fix; make it uncomputable-in-advance.** `coverage` is a function now, so it reads the counters at the moment the summary asks, which is the only moment the number means anything. *Recognition cue:* **an unchanged count after adding arms is evidence about the counter first and about the runner second** — and more generally, a metric that a code change should have moved and did not is a claim about the metric until the metric is shown to be live. *What it cost:* a correct fix (the `--show-cdup` change) was believed ineffective and a whole hypothesis was retracted before the real cause — MSYS2 brace-expanding `HEAD^{commit}` in the command line before git parsed it — was found, and it was found by making the failure print what the tool and git **actually did** rather than by reasoning harder. #2232 |
| 130 | **A diagnostic added INSIDE an unpartitioned region bills its own cost to the subject — and the more expensive the instrument, the more significant the phantom finding looks** | `res_buffer_ms` had no exhaustive partition: `acquire` and `copy` were added as **two candidate mechanisms**, and `render_runner.h` said so in as many words. A `PROSPER_BUFFER_CHURN` diagnostic was then placed just before the upload `memcpy` but **outside** `phase_copy`'s scope, to FNV-1a each payload and answer whether the bytes were content-unchanged. It hashed ~450 GiB per run, and every millisecond of that landed in the one part of the buffer branch nothing measured. The resulting stderr window read `buffer=332.08 (acquire=1.50 copy=25.82)` on Blue Prince gameplay — **304.76 ms/submit, 60.9% of a 500 ms frame, attributed to the renderer** — and was written up and posted as the dominant term, with the recommendation to stop optimising the copy path. Single-variable A/B on one binary: `PROSPER_BUFFER_CHURN` set → `buffer=360.15 other=+324.72`; unset → `buffer=40.41 other=+3.23`, with `copy` 27.82 vs 31.33 and `acquire`, `create` and `hash` unmoved. The whole 321 ms delta is the instrument. *Why it survived scrutiny:* the enclosing line **self-checks to +0.00** — `texture + buffer + descriptor + other == resources` exactly — so the partition looked audited at the level anyone would audit it, and the defect was one level below, where no partition existed to fail. This is the scale-invariance finding from #2245 with a number on it: an identity constrains the level it is written at and says nothing about the level beneath. *Recognition cue:* **an unmeasured region does not read as unmeasured — it reads as somebody else's cost.** So a large remainder inside a bucket you just instrumented is a claim about your instrument until an arm without it says otherwise, and that arm costs one environment variable. *Fix, and it is structural rather than a rule to remember:* partition the region exhaustively and print the remainder **signed**, so `other=+324.72` is visibly anomalous against a healthy `other=+3.23` on the very first run. The generalisation is trap 116's, inverted: there a silent instrument under-reported, here a loud one over-reported, and both were invisible because the quantity that would have exposed them was the quantity nobody had partitioned. *What it cost:* one confident cross-lane finding published and retracted the same day, plus a nominated prime suspect (`create_transient_storage_buffer_upload`) that the partition then showed **never fires at all** — `create=0.00` in every window of two runs. *The limit, worth stating with it:* an exhaustive partition protects **only the region it partitions**. `pass_control`, `readback` and `prelude` are still single numbers with nothing below them, so a diagnostic placed inside one of those gets exactly the free ride this one got. **An added measurement belongs inside a named bucket, or it becomes the residual.** #2246 |
| 131 | **A claim conditional on a platform NO REVIEWER RUNS gets single-author scrutiny however many agents read it — and the count of agents is what makes it feel checked** | Two agents review each other's PRs here, one on Linux/AMD and one on Windows. `h_reallocf` (#2203) shipped a comment saying `guest_realloc_portable(p, 0)` *"already frees p and returns NULL on **both platforms**"*. The tree builds for **three**, and macOS is the one that differs: `realloc(p, 0)` there returns a valid minimum-size block and frees nothing, which C17 7.22.3.5 permits — all three conform. **Three passes could not have caught it.** The author wrote the comment; the reviewer read it and *restated it in a review as though it were a check*, with the borrowed authority of two adjacent claims that reviewer had genuinely verified from source (the NID against `../PS5-3.20_Libs/libSceLibcInternal.c:17893`, and all five sibling rwlock handlers); the regression test then encoded the same two-platform assumption as an assertion. Every one of the three was reasoning from the same two platforms, so agreement between them carried no independent information. macOS CI falsified it within the hour. *What makes this a trap rather than an oversight:* **the shipped code was never wrong.** `if (!r && p && a1)` keys on the RESULT rather than on an assumption about the platform, so it is correct under all three without a branch — established by diffing the implementation separately from the comments and finding the function body byte-identical across both heads. The defect lived entirely in a comment and an assertion, and the assertion is the artefact the next reader would have trusted. A wrong comment beside right code is not inert; it propagates into tests. *Recognition cue, and it is cheap:* **a platform count stated in prose — "both platforms", "on either OS" — in a repository that builds for more than that number.** One `grep` over the CI matrix settles it, and neither agent ran it. *The structural point:* cross-agent review adds real independence only where the reviewers' environments differ. Here it covers Linux and Windows genuinely well and macOS **not at all**, so the effective review depth for a macOS-conditional claim is one, while the process reports two. Know which platform-conditional claims your reviewer pool cannot see, and treat those as unreviewed regardless of how many approvals they carry. #2203 / #2247 |
| 132 | **A share of a bucket is a function of the RENDER PHASE, and the misleading run is the one whose residual looks clean** | `build_R`'s descriptor-reflection leaf was measured twice on *Blue Prince* (`PPSA25009`), same binary, same instrument, same host, minutes apart. A default launch reaching the **title screen** at **10.2 draws/submit** reported `build_resources 4.69 [build_R=4.64 (texture=4.42 buffer=0.04 reflect=0.14 other=+0.03)]` — reflection **3%** of `build_R`, remainder `+0.03`, a bucket that reads as fully explained and not worth touching. The fresh-save route to **Day One gameplay** at **2,103 draws/submit** reported `build_resources 66.46 [build_R=57.16 (texture=10.92 buffer=11.19 reflect=30.37 other=+4.68)]` — the same leaf at **53%** of `build_R` and 9.3% of the frame. A **200x** difference in the answer, and #2256 would have been closed as not-worth-fixing on the first. *What makes it a trap rather than an obvious sampling error:* the misleading run is the more **convincing-looking** one. Its residual is near zero, so every audit a reader can perform on it passes, and a partition that self-checks to `+0.03` is exactly what trap 130 taught people to look for. Cleanliness of a partition says nothing about representativeness of the workload it was measured on. *Why the phase moves it so far:* per-draw fixed costs scale with the draw count while per-target and per-frame costs do not, so every leaf's SHARE re-ranks between phases — the two runs above are not the same measurement with more samples, they are different workloads. *Recognition cue:* **a percentage quoted for a renderer bucket with no draw count beside it is unusable.** This is the charter's decoded-draw-census rule applied to timing rather than to draws, and the same day produced a second instance across hosts: a lifetime total at `draws=351.1` (8.48 ms) placed beside a window sample at `draws=2106.8` (48.95 ms) as though the two were comparable — shares transfer between aggregations, totals do not. *Fix:* print the draw count on the same line as the partition, name the phase and the route in any quoted share, and reach real gameplay before believing a leaf is small. A title screen is not a cheap approximation of gameplay; for this bucket it is a different subject. #2256 / #2259 |
| 133 | **A pixel guard and a unit invariant are each structurally blind where the other sees — and both were satisfied while something was wrong** | Routing index buffers through the shared arena (#2253/#2258) was checked two ways, and each instrument passed over the other's defect. **Direction one: pixels right, invariant violated.** `blue-prince-hall` returned OK — 145,178 colours, 96 frames — while `test_multidraw_render`'s `pool.misses == before + 1` was red. That assertion is named *"two logical uploads populate one persistent host-buffer arena"*, but the equality also required the arena to be **empty on entry**, which is a property of the FIXTURE and not of the renderer; indexed draws now leave a partially-used arena, so the two uploads fit inside it and allocated nothing. Property satisfied, proxy violated — and had the author shipped on the guard alone, as the snapshot-is-a-release-gate rule invites, nothing would have said so. **Direction two, and it is the sharper one: invariant satisfied where it cannot discriminate.** The suite's indexed-draw arms (`px_three_indexed == px_three` and every draw in `test_indexed_render`) each render a *single* draw per pass, so each gets a fresh arena and binds at **offset 0** — the one value that stays correct if the bind offset is dropped from the code entirely. Those arms read as covering the index path and validate it only at the point where the bug they would catch is invisible. Confirmed by mutation: hardcoding `0` at both `vkCmdBindIndexBuffer` sites left the whole suite green until an arm with **two** indexed draws in one pass was added, whose second slice is non-zero by construction. *Recognition cue:* **ask which VALUE of the parameter under test the existing arms actually take.** A test that exercises a feature at its identity element — offset 0, scale 1, empty list, single element — is testing that the feature exists, not that it is computed. *And the payload half:* the second draw's indices must DIFFER from the first's, or both slices render the same picture and the arm is void whatever offset it binds. *Fix:* pair them deliberately — a content guard for what pixels must look like, a unit invariant for what the machinery must account for, and for each, name the value that would make it blind. Neither subsumes the other, and a green pair is not a green product. #2258 |
| 134 | **`ld: cannot open dependency file …/link.d` means the build TREE is gone, not that the link failed** | Mid-build, every remaining link began failing with `/usr/bin/ld.bfd: cannot open dependency file CMakeFiles/test_cond_clock.dir/link.d: No such file or directory`, repeated across a dozen unrelated targets. That reads as a link defect in the change under test, and the natural first move is to debug the code — especially when the change touches `prosper_core` and therefore relinks every test binary, so *many targets broke at once* looks like a real consequence of it. It was neither: a batch `git worktree remove --force` sweep had deleted the worktree **while a build was running inside it**. The compiler processes survived on open file descriptors, so only newly-created paths failed — the build kept *progressing* and died only at each link step, which is why it looked like a link problem rather than a missing tree. *Tells, cheapest first:* the message names **a path the linker wanted to create**, not a symbol — a real link failure says `undefined reference to …` and a truncated archive says `no more archived files` (trap 119); "cannot open X: No such file or directory" for a file **the build itself owns** is an environment failure. It hits **every** target, including ones whose sources were untouched. And `ls -d <worktree>/build` settles it in one command. *Rule:* before debugging a link error, confirm the build directory still exists, then check `df -h` — an exhausted filesystem produces the same class of message. *The other half is for whoever is sweeping:* a worktree with a running build looks idle from outside, so check for processes rooted in the tree before removing it. Nothing was lost here only because the work was already committed and pushed — that is the lane's discipline covering the sweep, not the sweep being safe. #2179 |
| 135 | **An `until ! pgrep -f '<pattern>'` wait loop MATCHES ITS OWN SHELL and waits forever** | The obvious way to serialise against another lane's build — `until ! pgrep -f 'cmake --build'; do sleep 30; done` — never terminates, because `pgrep -f` matches **full command lines** and the waiting shell's own command line contains the literal string it is searching for. Confirmed by reading the matches: `pgrep -af 'cmake --build'` returned three of the author's own waiters, each keeping the others alive. *Why it survives:* the failure is silent and **looks exactly like a slow build**, which on a contended box with another lane running `-j10` is entirely plausible, so it outlives several rounds of "just wait a bit longer". It also **stacks** — each new waiter makes the condition permanently harder to satisfy, so the more you retry the worse it gets. *Rules:* never wait on a pattern that appears in the waiting command itself — match the real process (`pgrep -x cmake`), a PID captured when the build started, or a sentinel file the build writes on exit; and if a wait loop has outlived any plausible build, run `pgrep -af <pattern>` and **read the matches** before concluding the build is slow. This is the wait-loop form of the well-known `ps \| grep` self-match, and it is less recognised precisely because the loop hides the evidence. *Simplest fix of all:* do not serialise — separate worktrees have separate build directories and contend only for CPU. #2179 |
| 136 | **A `--draw-steps` ladder from an EXTRACTED single submit is not evidence about where a frame loses content** | `gpu_replay --bundle-extract-submit N` then `--draw-steps` on the collapsed CrossWorlds frame produced a tidy, plausible ladder: a downsample pyramid to 60x34, back up to `1920x1080` carrying `visible-px=14003`, then a final `3840x2160` operation at `visible-px=0`. That reads as a located defect — "the 4K composite drops the 1080p content" — specific enough to file against an operation. The **same procedure on the KNOWN-GOOD bundle** (the one replaying to a correct SEGA logo, busiest submit, 29 operations) reports `nonzero-px=0 visible-px=0` for **every** operation. So by this metric the broken frame outscores the working one, which is impossible: an extracted submit replayed in isolation does not carry the frame's accumulated state (bound targets, prior contents, resources produced by earlier submits), so its ladder describes a fragment out of context. Whether a submit shows activity tracks how self-contained it happens to be, not whether the title renders. **The danger is that it fails in the FLATTERING direction** — the output does not look empty or broken, it looks *informative*, complete with a villain operation, so nothing prompts a second look. Cousin of the same-source-control warning in `CLAUDE.md`, but inverted: there the control was corrupt, here the *experimental* arm was seductive and only a hand-built known-good control exposed it. Rule: a `--draw-steps` claim about lost content REQUIRES the same ladder from a frame known to render, printed beside it. #2013. |
| 137 | **A positive control at 236 calls validates nothing about detecting 9 — match the control's RATE to the rate of the thing you are looking for** | A CrossWorlds `hle_calls` census over 400-tick windows reported **no `libSceVideodec2` handler at all**, and it carried a positive control — `g_vo_flipstatus`, present in both windows — so the null looked earned. It became a `## Ruled out` row: *"the post-logo wall is not a stalled CRI Mana movie"*. **The wall was a movie.** `PROSPER_SVCLOG=1` over the same phase shows `sceVideodec2CreateDecoder` plus eight `sceVideodec2Decode` calls, and implementing VP9 decode carries the title through it (#2281). The control was firing at **236 calls**; the traffic being looked for is **nine** one-shot calls at the moment the movie starts. A sampling histogram over 400 ticks exists to show steady-state work, and nine scattered events are exactly what it discards — so the control established that the instrument fires on a high-rate handler and could not, even in principle, establish that it would catch a nine-call event. This is the CLAUDE.md same-source-control warning specialised to **rate**: the control and the target must be comparable in *how often they occur*, not merely drawn from a different source. A cheap discriminator exists — name a handler known to fire a handful of times and confirm the census retains it. **The cost is what makes this worth a row**: the false row sat directly over the answer for two days, and a wrongly-closed hypothesis stops people looking while wearing evidence. #2013, #2281. |
| 138 | **`PROSPER_DUMP_ATLAS` filters its own input three ways, so an empty dump directory is not evidence of anything** | It writes a texture only when `tw <= 2048 && th <= 1024` **and** the resource decodes to RGBA8; it writes **24-bit BMPs, dropping alpha entirely**; and enabling it **switches the renderer onto the CPU RTT-copy path**, so the run it observes is not the run being debugged. Each filter is individually defensible and none is visible in the output. The general rule: **a diagnostic that silently narrows its own input set converts "I looked and saw nothing" into "I did not look"**, and the two are indistinguishable from the artifacts. An empty output directory from this tool means nothing at all, and any alpha-dependent conclusion drawn from its BMPs is void by construction. **The cost is what earns the row**: this was hit during #1946, whose defect *was* an alpha-blending one — the atlas turned out crisp and correct, and the real cause was the whole title rendering with blending disabled because prosper offered the RT0 blend key only for SDK >= 13 while the title asks for 12. The instrument most likely to be reached for was the one least able to answer. #1946, #2031, #2059. |
| 139 | **`import` and `xdotool` cannot see or drive a Vulkan window under Xwayland, and a black `import` reads exactly like a black frame** | `import` returns **pure black**; `xdotool key --window` uses `XSendEvent`, which **never reaches SDL**. So a scripted route that appears to deliver no input and a title that appears to render nothing are *the same observation* through these tools. On this project that is the single most consequential false negative available, because a large share of open investigations are about black frames — the failure mode and the measurement artifact are the same image. Use the frontend's own paths: the `screenshot` tool or F9 in the windowed frontend for capture, and the frontend's own input path for routing — never an X11 screen-grabber, never `XSendEvent`. Found while diagnosing #1946, where it made routed verification unreliable for as long as it went unnoticed. #1946, #2031, #2059. |
| 140 | **A gdb hardware watchpoint on guest memory can report ZERO writes while the watched byte demonstrably changes — no error, no failed arm** | Tracing Sonic Origins (#1905, #2093), `watch *(unsigned char *)(mgr + 0x11C)` armed successfully and the script ran to completion recording **zero writes**. In the *same run*, software breakpoints on the two candidate stores (`eboot+0x55b428`, `eboot+0x55b480`) caught three writes to that exact address, and independent reads showed the byte going `0 -> 1 -> 0 -> 1 -> 2 -> 0`. Whether it failed to arm in hardware, armed on the wrong thread, or something else was never determined — **and that is the point: nothing in the output distinguishes those from "no writes occurred"**, which is what the zero reads as. A lane with only the watchpoint would have concluded the byte was never written and gone hunting a stale-read or aliasing explanation that does not exist. **Any gdb `watch` zero on guest memory is VOID, not negative, unless the same run shows the watchpoint firing on a write you caused or independently observed.** Distinct from the `PROSPER_HWWATCH` / `perf_event_paranoid` dispute — different mechanism, and that one reports an errno. Adjacent to trap 113 (a breakpoint on `mov reg,[mem]` reports the STALE register, because gdb stops before the instruction runs): same debugger, same investigation, opposite direction — 113 gives a confident wrong VALUE, this gives a confident wrong ABSENCE. #1905, #2093, #2100. |
| 141 | **`gpu_replay`'s `operations=A/B` is a replay SETTING over a DECODE count — it can never report a skipped operation, and it sits one line away from the number that can** | Every submit prints `[gpureplay] bundle-submit=N operations=A/B output_bytes=… hash=…`, and on a *Sonic Racing: CrossWorlds* frame every one of the 34 submits read `26/26`, `27/27`, `7/7`. That is not a realized-over-attempted ratio. `gpu_replay.cpp:1701` prints **`operation_limit` and `replay.operations.size()`** — how many operations the replay was told to run, over how many were decoded — so `A < B` happens only when the *user* passed `--through-operation`, and a dispatch that the backend declines leaves both numbers untouched. **There is no run in which this field reports a failure.** Read as a success ratio it says every operation realized, which was false on that exact frame: the capture records **four** declined dispatches per frame (three `shader-recompile`, one `descriptor-contract`), and the full-screen one among them is #2265. *What makes it worse than an ordinary misread:* the replay's log **also** stays silent — the `[compute] skip …` lines are emitted by the live executor and are not re-emitted on replay, and the one skip line that did appear (`layered image deferred to #657`, trap-free and real) works as a false positive control: it proves the compute path runs and can log, which is exactly the assumption a reader needs, and it is unrelated to the declines in question. Trap 122's shape — a *loud* control laundering a null. *Fix, and it is one flag:* realization failures are recorded **in the capture at capture time**, not derived at replay, so extract the submit and ask for them — `--bundle-extract-submit N out.prgcap` then `--inspect-only out.prgcap`, which prints `failure[i] … reason=<descriptor-contract\|shader-recompile\|…>` or the explicit `failure-diagnostics: none`. That line is falsifiable in both directions; `operations=A/B` is falsifiable in neither. *Recognition cue:* **an `A/B` pair in which you have never seen `A < B` is not a ratio you have confirmed, it is a ratio you have never tested** — find the `fprintf` and read both arguments before quoting it. #2265 |
| 142 | **A title WAITING FOR INPUT presents a perfectly healthy renderer, so every graphics diagnostic agrees the subject is fine while nothing progresses — and none of them can say why** | *Sonic Racing: CrossWorlds* sat at rung 1 for months as *"the SEGA logo renders; the composite then goes uniform"*. In one day this lane ruled out, with evidence, the compute inventory, a polled unimplemented NID, PlayGo content availability, a stale cached SPIR-V, and the layered image atomic (#2265 — implemented, merged, and restoring it moved **not one pixel**). Every one of those negatives was correct and every one was beside the point. **The title was waiting for a button.** With a pad route it walks SEGA -> Unreal Engine -> CRIWARE -> licensor -> auto-save notice -> **title screen** -> player-profile menu, all correct at 3840x2160 (#2358, #2360). *What makes this a trap rather than an oversight:* the evidence of health is real and complete. 0 dropped pipelines, 0 unresolved descriptors, 0 `[exec-recompile-reject]`, a clean unimplemented-NID census (12 distinct, top one 127 benign `pthread_setschedparam` calls), and a presenting loop at ~10.8 flips/s with `draws_cum` climbing ~425 every 5 s. Nothing is broken, nothing is stalled, and **a renderer drawing one correct static screen forever is indistinguishable from a renderer that cannot draw anything else.** Every instrument in the graphics toolbox measures the pipeline; none of them has an opinion about whether the guest's state machine is advancing. *Two mechanisms nobody would guess, both of which cost an arm here:* **(a) a held button is not a press** — holding Cross from frame 0 advanced nothing over 170 s *with delivery confirmed from the guest's own read* (`pad_read_state call#512 connected=1 buttons=0x4000`), because the guest needs a neutral->pressed transition; and **(b) the route's own shape can be the wall** — the first committed route ended in a long hold and stopped at the licensor screen, a limit the route created rather than one the title had. *Cheap discriminator, and it is title-independent:* `PROSPER_PADLOG=1` answers **does the guest poll the pad at all**, which is one run and settles whether input is even in play. If it polls, drive it with `PROSPER_PAD_SCRIPT` **pulses** before spending another session on the renderer. *Related but not the same:* trap 101 is a diagnostic whose *window* was empty; this is a subject that is genuinely, correctly idle. #2013 / #1895 / #2358 / #2360 |
| 143 | **`PROSPER_RENDER_TIMING`'s per-submit line mixes WALL-CLOCK totals with THREAD-SUMMED component times, unmarked — so `component / total` from that line is not a share, and three of its components are individually larger than the total** | One *Blue Prince* window, verbatim: `total=464.05 realize_draws=230.55 tables=918.47 shader_lookup=532.45 ... table_parts: metadata=516.13 fold=243.83 ... parallel: threads=8.0 wall=228.63`. `tables`, `shader_lookup` and `metadata` each **exceed** `total`, because draw realization runs on 8 threads and those components are summed across them while `total` is wall. Nothing in the line says which is which; `wall=228.63` is the only field labelled. *Why it is a trap and not an obvious inconsistency:* the impossible values sit at the END of a long line, and the ones a reader reaches first (`realize_draws`, `backend`) genuinely ARE wall and genuinely DO compose to `total` — so the first arithmetic you try works, and confirms the habit. *Cost, measured:* this produced **six** wrong shares from one lane in a single day — `buffer copy` at 2.4%, `gpu-device` at 43% 'of the frame', `draw_setup` at 61% 'of the frame' (it is 61% of the backend, ~21% of the frame), a frontend share derived by subtracting two accountings that do not compose, and the headline **30-39 fps** that was a menu present-rate — and it is the same shape as the other lane's retracted *'54% of the thread is copying a hash map'*. Two agents, same unmarked line. *Recognition cue, and it is free:* **before dividing, check that the components you are dividing sum to no more than the total** — here they sum to over 2,000 against 464, which is visible in one glance and settles it. *What IS safe from that line:* `realize_draws` (~230.55) and `backend` (~233.45) are both wall and do compose; `submits per presented frame` measured 1.04, so per-submit figures are per-frame on this title. Everything else needs its denominator established before it is quoted. #2215 / #2276 |
| 144 | **`PROSPER_PAD_SCRIPT` is recorded as METADATA in two files that never inject anything, and the real consumer is SILENT by default** | The natural check after a scripted run -- `grep pad <log>` -- returns **nothing on a route that is firing correctly**. `grep -rn PROSPER_PAD_SCRIPT prosper/src` shows it in `gpu_capture.cpp:4829` and `gpu_timeline.cpp:205,1081`, where it is stored into capture metadata and drives no input at all; the actual consumer is `src/input/pad.cpp`, which logs only under **`PROSPER_PAD_SCRIPT_LOG=1`**. So the two greps most likely to be reached for both point at the wrong thing. **The failure is that it looks like a located defect rather than a dead instrument**: a GTA V Story-Mode run reached ~2880 frames and crashed with zero `[pad]` lines, which reads exactly as "the route never fired, so this crash is at the menu" -- a specific, plausible, wrong conclusion that would have sent the crash investigation to the wrong screen entirely. With the flag on: 31 events, Cross at frames 600/800/1000, route confirmed, crash genuinely downstream. **Rule: set `PROSPER_PAD_SCRIPT_LOG=1` on every scripted-input run as a matter of course**, and never infer route failure from absent `[pad]` output. Same family as trap 138 -- an instrument whose silence is indistinguishable from a negative result -- and a cousin of the vkval build-directory trap, where the tool answers truthfully about something other than what you asked. #2396. |
| 145 | **A clean zero needs THREE checks, not one: was the instrument armed, is it measuring the object you think, and can the space it searches express the case** | The general form behind trap 144 and several others, written after both lanes hit it from different directions in one day. **(1) ARMED.** Most `PROSPER_*` diagnostics are `getenv`-gated, so an un-set variable makes "not instrumented" and "not happening" print identically -- zero. Measured instances: `PROSPER_MEMLOG` gating a placeholder-repair log made a working fix read as *"the repair never fired on master"*; `PROSPER_DBG` gating the reject census made a Windows run report `smem-reject: 0, mubuf-raw-unresolved: 0`, which reads as *"this platform does not have the descriptor-resolution defect"* -- with the gate open it is 98 and 488, and the false version would have sent the other lane hunting a second cause that does not exist. **The cheapest check is a CONTROL LINE you know should be present** (`grep -c memhle` over the whole run); a grep filter that omits the pattern you later count is the same failure one layer out. **(2) MEASURING THE RIGHT OBJECT.** An instrument can be fully armed, fully truthful, and answering about something else: `vk_validation_scan.py` reads the **build directory**, not the checkout, so a branch switch without a rebuild reports the other branch's binaries. No control line catches this -- the fix is checking what the tool READS. **The nastiest measured instance is a `git grep` PATH FILTER that matches nothing, because it returns ZERO rather than an error and there is no artefact to distrust** (2026-08-10): run from `<worktree>/prosper`, `git grep -c '<string>' -- prosper/src` resolves the filter to `prosper/prosper/src`, matches no files, and reports 0 for every pattern. It produced `recompile-reject 0`, `unresolved-operand 0`, `mubuf-unknown 0` and three more -- one step from reporting to the other lane that diagnostics the charter itself documents *do not exist*. Unlike the vkval case there is no stale build, nothing to rebuild, and the command is well-formed; the only surviving defence is **grep for something you KNOW is present, in the same command** (`nullpage` and `pad-script`, whose `fprintf` had been read an hour earlier, also returned 0 -- impossible, so the method was broken rather than the codebase). **The same day produced the INVERSE failure, and it is why the control must be chosen carefully rather than merely present:** a control grepping `VkDescriptorSetLayout` under `prosper/src` returned 0 and read as another broken grep, but the path was fine and the *control* was wrong -- those symbols live in `prosper/tests/fixtures/render_runner.h` and `prosper/frontends/shared/live/live_compute.cpp`, and `prosper/src` genuinely does not contain them. A control must be a string you know is present **in the path you are searching**; otherwise it cries wolf, and a real search and a broken one both come back 0. #2412. **(3) ABLE TO EXPRESS THE CASE.** The trap-122 shape: a positive control drawn from the same generator as the null passes while the class under test is structurally inexpressible in it, so the zero is real and meaningless. The three are independent and fail in that order of subtlety. Confirm all three before a zero becomes a `## Ruled out` row -- a wrong falsification is the most expensive artefact in this repository, because it stops people looking while wearing evidence. #2396, #2412, #2424.  |
| 146 | **A coverage/PEAK figure taken from an ANIMATED sequence measures WHEN the capture landed, not whether the frame is correct — and the instrument is armed, aimed correctly, and still lying** | Verifying master after ten merges, a rung-6 control (*The Messenger*, PPSA24651) scored **coverage 20.39%** against a **9.28%** reference from the same route, same trigger delay, same build family. That reads as a doubling — a regression or an improvement, either of which would have been reported. **Opening the image settled it in one look: two different frames of the same animated intro cutscene**, the wide blue-lit ruins shot versus the darker ninja scene. Sprites, flame and text all correct in both. *Why this is a distinct trap rather than an instance of 145:* trap 145's three checks all pass here. The instrument was **armed** (a real 6.2 MB frame was written), it measured the **right object** (that title's actual scanout), and the space could **express** the case. The number was accurate; what was wrong was the assumption that a scalar over a moving picture is comparable between runs. **Cost so far: five instances in one session across both lanes** — three on this side (this control, a CB_DISABLE A/B where 2.473% vs 0.180% turned out to be gameplay versus a prologue title card, and a phase-mismatched GTA V pair), and two on the Windows side, which published 84.2% / 47.5% / 56.8% as a trend before recognising them as run-to-run video position. *Rule:* **for any title phase that moves — cutscene, FMV, attract loop, camera pan — a coverage/PEAK figure is only comparable against a frame you have looked at.** Either open both images, or anchor the comparison to a state that does not animate (a paused menu, a HUD element, a title card held on screen). A single scalar per run is a valid *content* signal only where the content is static. *Recognition cue:* if you cannot say which frame of the sequence each number came from, you are comparing timestamps. #2412 |
| 147 | **A diagnostic or repair path can be STRUCTURALLY STARVED — something upstream consumes the trigger it waits for, so it never fires on the very path it documents** | Distinct from 145's first check (was the instrument armed?) because these are armed, correct, and still silent: the event they key on is eaten before it arrives. **Two instances in one day, from opposite directions.** *(a) A diagnostic that cannot fire on the path it describes.* The compute materialization log in `gpu_executor.cpp` was gated on `g_dyntrace_force`, which is set **only** around the `PROSPER_DYNTRACE_FAIL` replay — and that replay calls `resolve_dynamic_fetch` directly, never re-running the materialization loop the line lives in. So the diagnostic printed what the fold *recovered* and never what the materializer *did with it*, for the one stage where those can differ; the graphics equivalent prints during the real build and had no gap, which is exactly why a recovered-vs-materialized join was free on that side and impossible on this one. Un-gating it produced 1676/1676 `-> materialize` and redirected the whole GTA V investigation off textures. *(b) A repair path whose fault was already answered.* Windows resets the user `%fs` base on kernel transitions and prosper re-applies it **only when a `%fs` access faults** — but `PROSPER_NULL_PAGE`'s low-read emulator answered that exact fault first (`decode_low_read_dest` treated `0x64`/`0x65` as ignorable prefixes), zeroing `rax` and stepping over it. `guest_fs_reapply()` waited for a fault that never reached it; *Dragon Quest VII* died on the next instruction with `addr=0xfffffffffffffff0`, pointing at the guest rather than at us. **Recognition cue:** when a diagnostic or handler is silent, do not only ask *was it enabled* — ask **can this code path reach it at all, and does anything upstream consume the event first**. The tell in both cases was a *count*: exactly one null-page emulation in a whole boot whose rip was the instruction after the one it swallowed; and a log line that appears for one stage and never for its sibling. **The two were found by DIFFERENT routes, and both belong in the cue.** (a) came from comparing a stage against its own sibling — the graphics fail-replay prints during the real build and the compute one does not — which is cheap wherever a sibling exists. (b) came from reading the **ORDER of handlers**: `try_emulate_null_read` (`exec_image_win.cpp:1001`) runs before the `%fs` re-apply (`:1027`), so the question is not *is my handler correct* but **what else could answer this fault first**. *Do not reach for the sibling comparison on (b)'s shape:* a Linux/Windows sibling for the null page does exist (Linux maps a real zero page, Windows emulates the read), and conflating those two is exactly what produced #2112's WRONG original diagnosis — the sibling here points at the thing that misled. #2412 / #2112 / #2447 |
| 148 | **Reasoning about the WRONG DEFINITION of a symbol that exists twice behind a platform guard — the code compiles, links, tests green, and is DEAD on the platform you are debugging** | Distinct from a stale comment (text that no longer matches code) and from trap 147 (a live path starved of its trigger): here the code you read is real, correct, and *not the code that runs*. Measured 2026-08-10 on #2450. `hle_kernel_mem.cpp` splits at `#if defined(__linux__) \|\| defined(__APPLE__) … #else … #endif` and defines **`k_batch_map` twice** (`:2281` POSIX, `:5448` Windows). Reading the first hit and reasoning carefully about its control flow gave *"ENOMEM is reachable from exactly two ops, MAP_DIRECT and MAP_FLEXIBLE; everything else is excluded"* — **true of the POSIX definition and false of the Windows one**, where `UNMAP` (`ok = !start \|\| win_unmap(...)`) and `PROTECT` (`ok = win_protect(...)`) can also fail. The conclusion was published on a Windows-only bug, excluding the two ops that only fail on Windows. **More care would not have helped; only a different question would** — the reasoning was sound and aimed at the wrong object. The same session added a diagnostic to that first definition: it compiled, linked and passed **179/180** while being dead code on the platform under test. *Detection is mechanical and takes one command* — after building, ask whether your string is in the object file, **with a positive control** so a broken grep cannot masquerade as absent code: `strings <build>/…/<tu>.cpp.obj \| grep -c '<your string>'` returned **0** while the control returned 2. An object that recompiles cleanly and does not contain your string is code the preprocessor removed. *Recognition cue:* before trusting any control-flow conclusion on a platform-specific bug, count the definitions — more than one means you must establish which you read. Helpers named `win_*`/`posix_*` far from your reading position are the ambient tell. #2450, #2454. |
| 149 | **Traps document the failure; REVIEW supplies the vantage — and this table's own length is an argument that reading it is enough, which the data contradicts** | Measured 2026-08-10: of **six** defects found in one day between the two lanes, **five required a second reader, the sixth was caught by an instrument that could not hide its own failure, and ZERO were caught by an author re-reading their own code.** Each author had personally documented the failure class **hours earlier, in this file**. Linux: a capability whose member comment asserted an inheritance that **nothing implemented and no struct field could carry**, so the flag stayed false on the shared path while reading as handled (#2458); and a starved control **inside the probe written to detect starvation** (trap 147) -- the periodic report sat after a first-sight early return, so it could fire only on a repeat observation. Windows: a `git grep` pathspec that matched no files and returned zero for six diagnostics that demonstrably exist (trap 145 check 2); control-flow reasoning about `k_batch_map` aimed at the **wrong one of two definitions** behind a platform guard (trap 148); and the same starvation defect again, in the census built to fix an attribution error -- a 5,000-observation threshold below which it printed nothing, its silence indistinguishable from never having run. **The sixth is the instructive exception**: a summary emitted from a **destructor**, in a probe whose own comment explained that a summary must not depend on an exit path. Its author caught it **unreviewed** -- because the run printed per-slot lines and **no denominator**, so the OUTPUT asked *"can this line be reached?"* on the author&#39;s behalf. That is the second route to the vantage and the only one available to a lone author: **not re-reading, which never worked once here, but observing an instrument built so its own failure is visible in what it prints.** **The mechanism is not that writing a trap down fails to inoculate — priming was never the missing ingredient.** The destructor bug was written by someone who had *just* reasoned about exit paths, i.e. maximally primed. **The author cannot occupy the position the check requires:** the author asks *"does this report the totals?"* while the reader asks *"can this line be reached?"*; and on the wrong-definition case more care would have produced a more confident wrong answer, not a right one. Only a different **question** helps, and a second reader is the only reliable source of one. *Rule:* **review is not a gate proportional to diff size.** Keep it mandatory on work of this class even when the diff is three lines, the author is confident, and the author has just written the trap row for the exact defect they are about to commit. *Corollary for this table:* a growing trap list quietly implies that reading it is sufficient protection. It is not, and never was — every row here records a failure a checklist did not prevent. #2447, #2454, #2456, #2458, #2465. |
| 150 | **An OBSERVER that perturbs a TIMING-DEPENDENT subject — and whose own health check reports success while the conclusion inverts** | Distinct from 145 (the instrument is armed, truthful and aimed correctly) and from 146 (the number is accurate; here the *subject* changed). Measured 2026-08-10 by the Linux lane on *Dragon Quest VII*: the documented routed capture holds its title screen (mean luminance 173-220 across frames 33-44), and **the identical command with `PROSPER_GFXLOG=1` added goes pure black** (mean 0.00, frames 33-39) on a clean master. `PROSPER_GFXLOG` emits ~160,000 lines over that route (159,619 `[render] item` counted) and slows it enough to shift a **time-dependent pad script** out of alignment with the sampled frames. **The lethal part is that the route's own health check passes:** `[pad-script] elapsed=30.049 frame=479 buttons=cross` confirms every press was delivered on schedule, so `PROSPER_PAD_SCRIPT_LOG` — the instrument that exists to catch a route failure — reports success while the game is in a different state by the time each screenshot fires. Both halves healthy, composite wrong, and the black frames are indistinguishable from a real regression. It nearly produced a phantom title-screen regression report; the only thing that stopped it was noticing `GFXLOG` was in the command and not in the documented recipe. The same effect is visible unrouted as a magnitude rather than a phase error: the Windows lane measured a **50% stall rate** under `GFXLOG`+`DBG` against **1 in 11** without, and could only label it an apparatus figure until this measurement named the mechanism. *Rule:* **every routed capture in this project is timing-dependent, so adding a logging variable to a scripted route invalidates the baseline — re-establish it under the same variable, or compare only runs whose variable sets match exactly.** *Recognition cue:* a capture that disagrees with a known-good baseline while every delivery and liveness check passes. Ask what you added to the command before asking what changed in the code. #2448, #2459, #2465, #2468. |
| 151 | **A defect class that NO check in the suite can see — invisible to the assertion on one platform and to the entire job on the other** | Measured 2026-08-10 across both lanes on the descriptor-array lift. A cached `VkPipeline` built under an arity-1 layout was replayed with an arity-3 layout bound (`VUID-vkCmdDrawIndexed-None-08600`, *"descriptorCount 3 doesn't match 1"*). **Two independent blind spots hid it.** (1) *The assertion cannot see it:* the stale pipeline still draws a correct green quad, so every pixel arm passed — confirmed by mutation, where reverting the fix returns the VUID byte-identical **while the test still passes**. Author verification, author review and independent review all went green on a spec-invalid draw. (2) *The job does not exist:* `test_descriptor_array_render` is triple-gated at `CMakeLists.txt` — `if(TARGET prosper_core)` `:208` -> `if(UNIX)` `:1283` -> `if(Vulkan_FOUND)` `:1416` — so the whole Vulkan-execution render suite (`multidraw_render`, `indexed_render`, `texture_sample_render` too) is **never configured on Windows**, and this class is invisible to Windows CI rather than merely unrun locally. `UNIX` is the operative gate: Vulkan is found in every Windows build cache, so **installing the SDK on Windows changes nothing** — the obvious fix attempt fails silently. **So: `ctest` green is not evidence about spec validity, on any platform. Run `tools/vkval/vk_validation_scan.py` deliberately for any change touching descriptor counts, layouts or pipeline keys** — it is the only check in the repo that sees this class, and the only reason this defect was ever found. Third leg, recorded because it is the reflex: a second **driver** could not have discriminated it either, since `08600` is a **usage** error raised by the validation layer rather than driver behaviour, so it is driver-independent close to by construction. *Recognition cue:* a green suite on a subsystem whose correctness is defined by a spec rather than by output — ask which job would fail if the code were wrong, and whether that job runs on your platform at all. #2471, #2476. |
| 152 | **A `file:line` citation invalidated by someone else's merge between writing and landing — and the stale number lands on a PLAUSIBLE neighbour** | Measured 2026-08-10, twice in one session, both times in a file **neither the author nor the reviewer touched**. A doc cited `render_runner.h:4469`-`:4478`, `:4484` and `:4558`; an unrelated PR then merged 155 lines into that file, 18 of them above the region, and every number was silently off by +18. Nobody was careless — the author cited correctly, the reviewer verified correctly, and a **third** change invalidated both between review and merge. **The `Docs` CI gate structurally cannot catch this**: it validates table structure and numbering, never whether a `file:line` resolves to what the prose claims. **What makes it dangerous is where the stale numbers land.** They do not point at nothing — `:4484` landed on `available_… \|= kFragmentSubgroupShuffle`, subgroup-shaped and adjacent, so a reader spot-checking it sees something plausible and moves on **having verified nothing**. *A citation that lands somewhere obviously wrong is safer than one that lands somewhere convincing.* This is trap 151 one level up: there the gate could not see the defect class, here the gate cannot see whether its own evidence still resolves. **So: re-resolve every `file:line` against the head you are MERGING INTO, not the head you wrote against** — find the construct by name (`grep -n '<the exact code>'`) and read the line back, never add the offset, because an offset is right only if the insertion was contiguous and above every citation. *Recognition cue:* any PR citing a line in a file it does not modify, merging more than an hour after the citation was written, into a branch that has moved. #2459, #2471. |
| 153 | **A mutation arm that perturbs a DIFFERENT SITE than the fix tests the assertions, not the code -- and the giveaway is that it FAILS, loudly and correctly, on the wrong object** | The stronger form of the positive-control rule in `CLAUDE.md`: there, a control drawn from the same source as its null tests the discriminator rather than the domain; here the control is aimed at the wrong *object*, and **it responds**, which is what launders it. An inert arm invites suspicion; an arm that goes red on demand closes the question. Measured 2026-08-10 on #2487 (descriptor-array arity). The fix changed a **layout call site**, `lb[i].descriptorCount = r.written_descriptor_count()`; the mutation arm perturbed the **accessor** `written_descriptor_count()`, which the arm's own assertions call directly. Both mutations were run, and they diverge:</br></br>`accessor  -> descriptor_arity()   FAIL x2   -- detected, arm goes red`</br>`call site -> descriptor_arity()   PASS      -- NOT detected, 232/232 green`</br></br>**So the arm proved it could fail, while the fix's own site was never tested at all** -- and the accessor mutation *responding* is precisely what concealed that. *The correct mutation was measured only after review pushed back, and it inverted the author's published reasoning:* with the arm corrected, reverting **only the call site** gave `exit 139`, the suite at 0%, and vkval reporting **pool exhaustion plus `dstSet` NULL twice**. **The mechanism is the part neither lane predicted, and it is why the wrong arm looked adequate to both:** the pool reserves 1 sampler, the mutated layout declares 3, `vkAllocateDescriptorSets` asks for 3 from a pool of 1 and returns no set, `vkUpdateDescriptorSets` is handed `VK_NULL_HANDLE`, and the process segfaults. **The class is visible through the POOL, not through pixels** -- so the claim that ctest could not see it and only the validation layer could was itself wrong, and was retracted on measurement. Reconciles with #2471 rather than contradicting it: there the pool arm genuinely was unverifiable, because RADV over-allocates and absorbs the mismatch; a pool can be over-allocated into, but it cannot invent capacity for a descriptor **type** it was never sized for. *Rule:* **state the mutation as a diff against the FIX HUNK and revert exactly that -- if you cannot point at the line your arm perturbs and show it is a line the PR changed, the arm is testing something else.** *Recognition cue:* the arm mutates a helper the assertions call rather than the site the diff touches. **A red arm is not evidence the arm is aimed correctly**, and that is the whole row: the reassuring outcome and the diagnostic outcome are the same colour. #2487, #2471, #2476. |
| 154 | **Trap 148 catches REVIEWERS harder than authors, because the wrong definition APPEARS TO CONFIRM the claim being checked** | Trap 148 records reasoning about the wrong one of two definitions behind a platform guard. This is its reviewer-side form, and it is worse: an author reading the wrong copy writes something false, but a reviewer reading the wrong copy **manufactures corroboration** -- and a verified claim is exactly the artefact this project treats as settled. False negatives get re-checked; false confirmations do not. Measured 2026-08-10 on #2486, reviewing a Windows-only diagnostic in `hle_kernel_mem.cpp`. The reviewer's first look landed on the **POSIX** copy's `[batchmap-fail]` -- pre-PR `:2373`, against the Windows copy at pre-PR `:5582` (post-PR `:5611`) -- carrying its own `if (!ret) ret = 0x8002000Cull;` three lines below. **Verified against the pre-PR tree rather than asserted, and reproducibly** (`5e7ff0ab`, the parent of the #2486 merge): the two eleven-line logging blocks at `:2368-2378` and `:5577-5587` are **byte-identical**, not merely equivalent: `cmp` reports no difference, which reproduces under any framing, and for a fixed constant `sha256[0:16]=79df2ea630d95434` each **over the 11 lines with no trailing newline** (the obvious `sed -n '2368,2378p' > f` appends one and yields `99a4b5e4e6f6d18c` instead -- stated because a verification constant needing an unstated convention is one step from being a stale figure, and the reader's conclusion would be "the hash is wrong" rather than "my framing differs") -- and the single difference anywhere near them was a trailing comment (`// ENOMEM on a failed map`) on the POSIX side's following line. So checking a Windows claim against the POSIX half would have produced a confident `APPROVED` citing a real line containing the real construct, and would have survived any spot-check that did not ask which side of `#else @2704` the line sat on. Only that question caught it. **Compounding tell:** trap 148 itself cites `:5448` for the Windows copy, which has since drifted to `:5490` -- the row documenting this hazard already carries a stale line number for it (trap 152), and this row's first draft repeated the error by pairing a pre-PR line number with a post-PR one. The review survived that drift because the *claim* was stated as **"`GetLastError`/`SetLastError` appear nowhere before the platform `#else`"** -- a construct-and-boundary claim, checkable by counting (28 lines total, **0** before `:2704`) and immune to every line shifting. *Rule:* **when reviewing a platform-specific change, establish the boundary line first, then locate every copy of the construct relative to it -- and state findings as constructs and counts rather than line numbers.** *Recognition cue:* you found what you were looking for on the first grep hit, in a file with a platform `#if`. First hit and correct hit are different things, and a duplicated file makes the first hit *plausible*, which is the trap. #2486, #2450, #2454. |
| 155 | **A check whose two failure directions are ASYMMETRIC, automated anyway -- and the harmless direction is the one you get to observe** | The general shape, which is not specific to any repository: when a check can fail toward *blocking something valid* or toward *admitting something invalid*, the first announces itself and the second is silent by construction. Observing the safe direction misfire therefore tells you nothing about the rate of the dangerous one, and it *feels* like evidence the check works, because you caught it. Instance, measured 2026-08-10, and the reason `CLAUDE.md` forbids automating this particular read: every agent here operates as the repository owner, GitHub refuses `--approve` on your own PR, so the review **state** is always `COMMENTED` and `reviewDecision` stays empty -- for approvals and rejections alike. The verdict lives only in the body, which invites a substring match. A `jq` filter testing whether a body *contained* `REJECTED` reported an **APPROVED** review as a rejection, because the prose discussed an earlier rejection. **That direction fails loudly and stops a merge.** Its mirror does not: a genuinely `REJECTED` body containing the word `APPROVED` -- *"this is not yet APPROVED"*, *"I will move to APPROVED once..."*, a quoted prior verdict -- reports as an approval. **That is the #2026 shape exactly**, where a daemon counting `reviews > 0` merged a PR with two blocking findings outstanding (revert: #2044). The filter had been running all session and happened never to bite that way. Reading the **leading token** rather than searching the body narrows the window; it does not close it, since a review may open by restating the verdict it supersedes. *Rule:* **a machine may LIST candidates and flag staleness; a reader decides the verdict.** The genuinely mechanical checks are the ones with no prose in them -- is this review attached to the current head (`commit_id` versus head SHA), and if not does `git diff <reviewed-sha> <head>` show a content change. *Recognition cue:* any predicate of the form "body contains `<VERDICT>`" -- ask what it does with a sentence mentioning the other verdict, and which of the two answers you would ever find out about. #2026, #2044, #2465. |
| 156 | **PROXIMITY READ AS IDENTITY: an address near a known structure, or a diagnostic line near a known header, attributed to the program you are investigating.** Three times in one session on one program (GTA V `0x413ce6000`, 2026-08-12). (a) A `user-sgprs=11` grep matched an `s10=0x0e` dispatch of `0x413dd6e00` five lines from a ce6000 header and nearly became evidence about ce6000. (b) A 24-byte constant buffer at ce6000's own `s[0:1]` was read as a bound on its access, when its `pc=` field named `0x413dc6700`. (c) Eighteen read-before-write resources were attributed to ce6000 because they sat at `s0 + 0x114` for three observed ce6000 base pointers -- and ce6000 appears in that dependency graph **zero** times, necessarily, because every one of its dispatches failed and failed operations never become graph nodes. **Two programs sharing an arena is ordinary; an address inside a structure does not name the program that reads it.** (a) and (b) were caught by hand; (c) was caught only by adding `producer-program=`/`writer-program=` to `gpu_replay --graph`, which falsified the claim of the person who wrote the tool. Attribute from a program-tagged field, never from an address or a neighbouring line -- and when the tool has no such field, that is the thing to fix first. | #2481, #2516 |
| 157 | **A rate-limited diagnostic quoted as a census — it records a program's WORST moment, not its state** | GTA V, 2026-08-14. `[compute] skip unsupported program 0x…` named 15 programs, and the frontier was reported as 15. Retrying each offline against its own captured resource table showed **16 of 20 recompile cleanly**: the line is capped at one per program and fires on the *first* dispatch, before caching and descriptor state settle. The same shape had already appeared as `[submitorder]`'s `if (k < 32)` print cap being read as a rate. **Before quoting a diagnostic's population, find its rate limit and its trigger point.** The offline `--retry-failed-stage` verdict is the census; the skip line is a pointer. |
| 158 | **A grep for a verdict that matches a DIFFERENT field containing the same word** | 2026-08-14. `grep -E 'recompiled\|rejected'` over `--retry-failed-stage` output reports "rejected" for shaders that compiled, because `[compute-cfg] … cf_rejected=0` contains the word. Six false failures were published from it. **Match the verdict line (`^\[retry-failed-stage\] N:0 …`), never the word** — and when a census disagrees with a spot check, suspect the pattern before the subject. |
| 159 | **Selecting "the big pass" by draw count selects the SHADOW atlas, and its state reads as a defect** | GTA V, 2026-08-14. A capture filtered on `MIN_DRAWS>=100` in the black window returned 427 draws whose every resource showed `target-mask=0 effective=00` — colour writes fully masked, the exact shape of #1946. It was a **512x512 depth-only shadow pass**, where that is correct. Its main-view submits carry 43-72 draws, and both render into the same 3840x2160 target, so `TARGET_DIM` cannot separate them either. **Read the viewport before reading the render state**; the candidate census now reports it. |
| 160 | **A changing frame CRC plus millions of non-zero pixels is not evidence of rendered content** | GTA V, 2026-08-14. Frames measured 0.00% non-black at a threshold of 8/255, yet their CRC changed every second and 12.7-13.5% of pixels were non-zero at luminance 1-2 — over a million each. That reads exactly like a world rendering at ~1/128 brightness, i.e. a collapsed exposure. Amplified 64x it is **uniform dither with no structure, edges or geometry**. The animated grain is what changes the CRC. **Amplify and look before concluding a dark frame contains a scene.** |
| 161 | **Every render-target seed prosper retains can be a PLACEHOLDER, so an offline replay is not a visual oracle until you check them** | GTA V, 2026-08-14. `--draw-steps` over a 72-draw main-view capsule reported **100% non-black, max=255 at every step**, composing a blue gradient with a horizon-shaped band — apparently the sky rendering correctly offline while the live frame is black. It is the seed-miss gradient: it is already 100% non-black at step 4, before most draws run, and never changes. A second seed dumped 29.4% non-zero and proved to be the **yellow R+G diagnostic fill** from #2014. Extends the existing colour-metric trap to the replay path: **check `RTT-seeds` content before treating any replay image as evidence.** |
| 162 | **A tool flag that FILTERS where you read it as TRANSFORMS, and its silence looks like a broken tool** | 2026-08-14. `gpu_replay --draw-steps-target WxH` dumps only steps whose output is exactly that size; it does not downscale. Passing `480x270` against a 3840x2160 capture skipped every step, wrote zero images, and exited 0 — which was written up as a tooling gap blocking the investigation. **A tool that plans N outputs and writes none is a filter that matched nothing at least as often as it is a bug.** |
| 163 | **A diagnostic that prints only SUCCESSES cannot distinguish "declined" from "never offered", and one without an identifier cannot be attributed at all** | GTA V, 2026-08-14. `[indirect-pointer-relocation]` logged only valid proofs and carried no program address, so several hundred records belonged to no shader and a declined analysis was invisible. `0x413d14100` is byte-for-byte the StaticFootprint analyzer's own 386-dword target and still rejected, and nothing said whether the analyzer had declined it or matched and failed later. Adding the address **and the decline** answered it in one run: 229 of 234 succeed, so relocation was never its blocker. **Log the negative case and the subject's identity, or the diagnostic answers a different question than the one asked.** |
| 164 | **A LEVER THAT DID NOT MOVE, reported as a falsification — verify the artefact changed before reading the result** | GTA V, 2026-08-14. `PROSPER_NATIVE_COMPUTE_MULTIWAVE=1` still hung, and that was published as killing the emulation-cost hypothesis. The emitted module is **byte-identical** with and without the switch — `d04fd09b13408f9b4da7287fae34f692` in both arms and in a capsule from hours earlier — so the two runs were the same run and the result was void. What made it credible was `[subgroup] cs=… native=64 … multiwave=1 disabled=0`: that line reports the resolved **config**, and it was read as reporting the emitted **lowering**. Third time in one thread that a diagnostic reporting an input was read as reporting an output. **Hash the artefact your switch is supposed to change, and check the hash BEFORE you read the outcome** — with dump-and-skip (trap 166) it costs nothing. |
| 165 | **Interleaving failure REASONS read as interleaving successes, killing a correct hypothesis** | GTA V, 2026-08-14. Device loss was rejected because "a loss would give a monotone tail" and the capsule's reasons alternated (`declined -> indirect-dependencies -> declined -> shader-recompile -> …`) across 22 transitions. The tail **was** monotone: the last realized compute is source 38 and every source from 39 on is a failure. Only the *reasons* vary, because the *operation kinds* vary — after `device_lost`, `fail_closed_items()` refuses each dispatch while the indirect latch refuses each indirect draw. The hypothesis was correct and was thrown away. **Test the property you actually named: for "monotone failure", grep for the last SUCCESS, not for a run of equal reasons.** |
| 166 | **Dump-and-skip: a diagnostic skip ordered BEFORE the dump blinds every other instrument to the thing under suspicion** | GTA V, 2026-08-14. `PROSPER_COMPUTE_SKIP_PROGRAM` first returned above `trace_compute_item` / `maybe_dump_traced_compute_spirv`, so skipping a program also prevented dumping it — exactly backwards for its purpose, since the program worth skipping is the one that hangs the GPU. Reordered below both (the rule the timing and authority censuses already follow: observe, then return). The combination is what makes a recompiler change checkable against a hanging program at **zero device losses**, and it is what caught trap 164. |
| 167 | **Citing a STACKED/MERGED working tree into a PR whose base is `master`** | GTA V, 2026-08-14. Three separate review findings in one PR stack traced to one habit. I read `gpu_executor.cpp` on a local branch with five open PRs merged in, then pasted its line numbers into PR bodies and commit messages based on `master` — where the same code sits ~270 lines earlier (`:9598/:9609/:9354/:9422` should have been `:9287/:9358/:9118/:9187`). The same habit put `RealizationFailureReason::ComputeExecutionDeclined` into a shipped source comment, a PR body and a merged status doc: that enumerator exists only on the #2536 branch, and `git grep` on the PR's own head returns exactly one hit — the new comment itself. Every number was a line I had actually opened; the tree was wrong, not the reading. **Verify a citation against the PR's own base (`git show origin/master:<file>`), never against your working tree** — and note the failure is invisible to CI, to `diff --check`, and to the author, because the citation is *true somewhere*. |
| 168 | **A hardware watchpoint is PER-THREAD, so its zero says nothing about a multithreaded writer** | GTA V, 2026-08-14. `PROSPER_HWWATCH_ABS=0x20f848a240` armed cleanly (`[hwwatch] armed W-watch … (fd=13)`) and fired **zero times** over a 120 s route that reaches gameplay — which reads as "no CPU writer touches this address". It is not evidence. x86 debug registers are per-thread state, and this arms them on **the main guest thread only** (the source comment says so: *"owner = this = the main guest thread"*). A guest worker thread, and prosper's own renderer thread doing the compute write-back memcpy, are both structurally invisible to it — and the write-back is *known* to write that exact address ~20 times per run, so the instrument missed a writer it was pointed straight at. **Before believing a watchpoint zero, establish that the thread you armed is the thread that could write.** A same-thread positive control does not help here: it confirms the debug register works, which was never the doubt. |
| 169 | **"the program's binding N" is not a stable identity — the SAME program at the SAME fetch PC resolves to DIFFERENT buffers per dispatch** | GTA V, 2026-08-14, and it produced a fully-published wrong root cause. `--dump-compute-resource 37:8` was read as "the hanging dispatch's traversal table". `compute[37]` is source **38**, which *completed*; the hang is source **39**, and its binding 8 at the same `pc=0x5b` resolves to a **different address** (`0x20f848417c` vs `0x20f848a240`). A whole causal chain — 183 cyclic indices, an offline/live divergence, a "183 must become 0" oracle — was built on the buffer belonging to the dispatch that worked. The hanging dispatch's table is acyclic with a longest chain of **11 steps**. Nothing flagged it: both are 8252 bytes, same program, same fetch PC, same binding number, and the census returned a plausible non-zero answer. **Index a capsule by the OPERATION you mean (`failure[N]` vs `compute[N]`) and check the resolved ADDRESS matches, every time** — a program dispatched repeatedly with different arguments makes binding numbers meaningless as identity. |
| 170 | **A device loss names the submission that OBSERVES it, not the one that caused it** | GTA V, 2026-08-14, and it misdirected an entire multi-day investigation. `[compute] fatal Vulkan device loss stage=queue-submit … program=0x413dc6700` reads as "this program hung the GPU". It means `vkQueueSubmit` returned `VK_ERROR_DEVICE_LOST` — the device was **already** dead, so the named program is the first one *after* the reset. The check that settles it is the one nobody ran for days: **count the fence-wait timeouts.** `execute_item` waits 30 s and reports a timeout as `queue-wait`; across every routed run there were **705 waits, 705 completions, 0 timeouts, 28 queue-submit losses**. No compute dispatch ever hung — the hang is in another queue entirely, and compute merely discovers the corpse and disables itself, which is what then drops every indirect draw. Corroborating tells that were visible all along: the named program varied run to run (dispatch 39, dispatch 40, a different program), and one of the accused was a 753-dword module with **zero loops** running 42 threads. **Before attributing a hang to the named submission, prove that submission failed to COMPLETE — a loss at submit time is evidence about ordering, not about the culprit.** |
| 171 | **A zero TIMEOUT count is not a zero LATENCY count — measure the duration, not the threshold** | GTA V, 2026-08-14, and it reversed a conclusion I had already published. Having established that a device loss names the submission that OBSERVES it (trap 170), I counted compute fence-wait timeouts to find the real culprit: **705 waits, 705 completions, 0 timeouts**, and concluded no compute dispatch hangs and the fault must be in the graphics path. The count was right; the inference was wrong. The timeout is **30 seconds** and the pathological event is **2 seconds** — a dispatch can be three orders of magnitude slower than every other one and never trip it. Logging the wait DURATION, and reporting any wait over 100 ms *even when it succeeds*, produced exactly one line in a whole route: `SLOW fence wait 2045.2 ms result=0 program=0x413dc6700 dispatch=39`, immediately before the loss. **A threshold instrument answers only "did anything cross my threshold"; when the question is "is anything anomalous", record the distribution.** |
| 172 | **A field's LABEL is part of the instrument — and a VARIABLE'S NAME is not evidence of what it stores** | GTA V, 2026-08-14. The `PROSPER_CFG_TRIP_BOUND` witness stores four dwords and the host printed the third as `last-block`. A hit reporting `14` was read as "basic block 14", mapped by hand onto the block holding the guest's loop body at pc88..97, and published as *the guest's loop is spinning*. **Then the correction was wrong too, in the opposite direction.** The emitter's variable is called `pc_var`, so it was renamed `next-guest-pc` and the loop-body claim withdrawn as if the number were a program counter — on the strength of the identifier's NAME, without opening the store sites. Every write to `pc_var` stores `dispatch_for_block[...]`: a dispatcher switch-case **ordinal**, neither a block index nor a pc. So the first reading made an ordinal a block, the second made it a pc, and both were published. The number never changed and was never wrong. **Read the STORE SITES — all of them — before naming a diagnostic field, and treat a variable's name as a hypothesis exactly like a printf's label.** Fixed by reporting `next-dispatch` and having each phase print its ordinal -> guest-pc map, so the mapping step that produced both errors no longer has to be done by hand. |
| 173 | **A racy record publishes a NARROWER result than the truth, and narrow reads as strong** | GTA V, 2026-08-14. A device-side witness recorded the span of dispatcher ordinals a runaway loop visited. Every invocation that hit the cap wrote the same slots with plain `OpStore`, so the host read one invocation's local extremes: **`dispatch-range=6..9`**. Four adjacent ordinals over 4,096 iterations is a vivid picture of a tight cycle, and it was published as one. Republishing the same fields through device-scope `OpAtomicUMin`/`UMax` — a true reduction over every invocation — gives **`0..9`**, a ten-ordinal span that shows nothing of the kind, because ordinal 0 is the program entry every invocation passes through. **The conclusion survived and the derivation did not:** what actually establishes the loop never exits is the CEILING (max ordinal 9, so no lane ever reached the code after the loop), which the racy record also happened to report correctly. Note the direction of the error — last-writer publication produced a TIGHTER, more striking interval than reality, and a tighter interval is the direction that reads as better evidence, so nobody questioned it. **Reduce per-invocation extremes on the device; a record whose fields come from different invocations is not a record, and its most quotable form is the one least likely to be true.** |
| 174 | **A token that records INTENT is not evidence that anything happened — and shared guest storage is not isolated by proving ONE program does not use it** | GTA V, 2026-08-14, caught in review. A diagnostic wrote a five-dword witness into the internal GDS buffer, and the host gated reading and CLEARING those dwords on a token set from the selector state plus "does the selected program access GDS". Two holes, both invisible from the token's name. First, it was computed **before translation**, so a program every selector accepted but that emitted nothing — a structured loop, or a phase ordinal the program does not have — still read as instrumented, and the host then reported whatever guest data occupied those dwords and destroyed it. Second and worse, the buffer is **one persistent allocation shared by every dispatch**: proving the instrumented program cannot read it says nothing about the program that runs next and does, so the instrument could perturb a different program's state while claiming single-program isolation. Fixed by carrying a real emission result out of compilation, and by save/prepare/report/**restore** so guest memory is byte-identical afterwards. **Gate on what was EMITTED, never on what was SELECTED; and for shared mutable storage, preservation is the property to test, not exclusivity.** |
| 175 | **An execution test can be INVALID and still print "passed" — run the focused tests under the validation layer** | GTA V, 2026-08-14, caught in review. Module-shaped assertions had already proved too weak twice, so the trip-bound test was strengthened to actually EXECUTE a bounded dispatcher and assert the truncated result. That arm dispatched a pipeline whose layout omitted the binding the module statically uses for its witness — `VUID-VkComputePipelineCreateInfo-layout-07988` and `VUID-vkCmdDispatch-None-08114`. The focused test does not make validation messages fatal, so it printed **all passed** while measuring undefined behaviour; the expected value may even have been produced, but nothing was established. **Fixing it introduced a second one:** the added buffer was never destroyed, so every `run_compute` call leaked it past `vkDestroyDevice` (`VUID-vkDestroyDevice-device-05137`, eight instances) — a defect invisible to every assertion in the file. **Run focused GPU tests under `VK_INSTANCE_LAYERS=VK_LAYER_KHRONOS_validation` and require ZERO errors, especially when the test was added to make a weak assertion strong: an execution arm that is itself invalid is the most convincing wrong answer available.** |
| 176 | **An assertion is unverified until a mutation of the LINE IT NAMES makes it fail — feature-level mutation is not enough** | GTA V, 2026-08-14, found by mutating my own fix. A reviewer showed that a predicate deciding whether the host may write guest memory matched a witness slot NUMBER rather than the witness RESOURCE. The fix was correct: resolve the atomic through the variable decorated set 0 / binding 127. The arm written to prove it moved that decoration from 127 to 126 and asserted the module was no longer recognised — which passes, and proves nothing about the base check, because a module with no binding-127 variable is already rejected by an earlier `no such variable` exit. **Deleting the base check left every assertion green.** The case that isolates it is a module that HAS the binding-127 variable and performs the slot atomic through a DIFFERENT one — built by repointing one word of the real emitted module — and that arm fails the moment the check is removed. Two assertions can sit under the same heading ("resource identity") while only one of them is covered. **Mutate the specific line an assertion names, not the feature it belongs to; and be most suspicious of an arm you wrote to satisfy a reviewer, because it is shaped by the finding rather than by the mechanism.** |
| 177 | **Enumerate the conditions in the DIFF, not the ones in the review — a fix is usually broader than the finding that prompted it** | GTA V, 2026-08-14, and it is trap 176 failing on its own author. A review said a predicate must resolve an access chain's BASE to the right resource. The fix did that and, in the same edit, tightened the site to a canonical shape: exact word count, member-zero first index, slot second index — **four conditions where the finding named one.** An arm was written for the base, the commit message stated that per trap 176 each new check had been reverted individually to confirm its arm bites, and that sentence was true of the three checks its author had in mind and false of the three the same commit introduced. A reviewer relaxed the shape back to `len >= 5` and deleted the member-zero check: **the entire focused test stayed green.** The gap is not ignorance of the rule; it is that the rule was applied to the checks being thought about rather than to the checks being shipped, and an arm shaped by a finding stops exactly where the finding stopped. **Before claiming per-condition coverage, list the conditions the DIFF adds and revert each one; the breadth a fix gains beyond its finding is precisely the part nobody has tested.** |
| 178 | **A new instrument on the WRONG code path reports a clean zero — anchor it to a line already proven to run on the route you are measuring** | GTA V, 2026-08-14. `PROSPER_COMPUTE_ADDRESS_WATCH` was added to answer "which other programs touch this buffer", and it printed **nothing for a whole route**. Not because nothing touches the buffer — because it went into `realize_compute_dispatches()` while the live route realizes through `realize_retained_compute()`. Two realization paths; the instrument was on the unused one, so its silence was structural rather than informative, and "no other writers" is exactly the conclusion it would have supported. **The zero was also the answer that agreed with what I had just concluded** — a retraction had moved suspicion away from the known writer, and "no other writers" would have pushed it back, which is precisely why it deserved distrust rather than acceptance (cf. the charter's note on corrections that RAISE a claim getting checked least). What caught it was arithmetic, not suspicion: 352 lines in the log, zero from the watch, on a buffer another instrument was reporting by address in the same file. **Anchor a new watch beside a call site with known output on that exact route, and before believing a null, confirm the instrument produced ANY output at all.** |
| 179 | **`cmake … \| grep \| head` can report RC=0 on a FAILED build — and a false zero survives its own fix** | GTA V, 2026-08-14, and it put a non-compiling commit on a branch. A scripted edit left a stray `}` in `gpu_executor.cpp`. The build was checked with `cmake --build … 2>&1 \| grep -E 'error:' \| head -4; echo RC=${PIPESTATUS[0]}` — `head` closing early SIGPIPEs the pipeline, so the construction printed **RC=0 on a build that failed**, and the commit was pushed. The charter's own rule ("a pipeline's exit status is its LAST stage's") had been violated all session without consequence until the filter hid a real failure. **The compounding part:** the same instrument had ALREADY produced a false zero from being on an unused code path (trap 178), so when it reported zero again after the placement fix, the natural reading was "placement was not the cause, look elsewhere" — a false zero that survives its own repair falsifies the repair. What caught it was neither the exit code nor the failing instrument but `strings <binary> \| grep -c <symbol>`: **present in the source, absent from the binary**, a contradiction no build status can explain away. **Redirect build output to a file and read the status directly; before running an instrument, confirm its symbol is IN the binary.** |
| 180 | **`PROSPER_TARGET_WATCH` parses base-0, so an address without `0x` silently watches a DIFFERENT one — and reports a confident zero about it** | GTA V, 2026-08-16. The switch answers "is this address EVER a colour target, in any of the eight MRT slots", exact and unsampled, which is exactly the question behind the absent scene-colour producer. Invoked as `PROSPER_TARGET_WATCH=2063380000,20431c0000` it prints `watching 2 address(es)` and then `addr=0x7afcae20 draws=0 ... NEVER A COLOUR TARGET` — `strtoull(p, &end, 0)` read the un-prefixed digits as **decimal**, so 2063380000 became 0x7AFCAE20 and the run confidently reported nothing about an address the title never had. The failure announces itself only if you read the echoed address rather than the verdict, and the verdict is the line you went looking for. **Write `0x` on every address, and check the address the instrument echoes back against the one you meant.** *Second half, and the more expensive one:* I only found the pre-existing switch because my own duplicate of it collided — I had searched `getenv("PROSPER_...MAP` and the file I was editing, never the whole tree for an existing colour-target watch, and was ~40 lines into building a worse version (sampled by nothing, but only reachable through the dimension-filtered colour-state trace) when the collision surfaced. The existing one is better: it states in its own comment why a 1-in-32 sampled census cannot answer an "ever" question (a ten-draw writer is missed 73% of the time). **Grep the tree for an existing instrument before writing one; the in-tree version usually carries the reasoning you are about to re-derive.** |
| 181 | **A bloom smear inflates `nonblack_rgb_pixels` and `distinct_rgb_colors` exactly as a gradient does — and can rank the WORSE frame higher** | GTA V, 2026-08-16. Three depth/stencil arms were scored on the presented frame with two controls agreeing to 0.15%, so the numbers were real and the spread was not noise: `PROSPER_NO_DEPTH=1` +43.5%, `PROSPER_NO_STENCIL=1` +7.4%. Recorded as "depth rejection is the dominant loss of presented content". **Opening the images retracted it.** Both arms are a large overexposed white blob plus blue haze with **no world geometry**, and the depth arm additionally **loses the radar** — plainly worse to look at than the control it beat by 43%. The counter rose because bloom flooded the frame. The reverse also holds and is the sharper half: `PROSPER_RTT_ALIAS`, the one arm that genuinely shows the bank interior with correct perspective, scores **19,623 distinct colours against the control's 30,262** — *more world, fewer colours*. So the metric is not merely noisy here, it is **anti-correlated with the thing being optimised**, and a ranking built on it points away from the fix. This is the colour-metric trap already on this list wearing a new costume (there a gradient, here a bloom smear), which is why knowing that entry did not prevent it: the earlier form was about a *placeholder* fooling a metric, and this one is real rendered output. **Never quote a frame counter without opening the frame, and be most suspicious when an arm you expected to help produces a big number.** |
| 182 | **A REDUCED fixture reproducing a known bug is a positive control for the reduction, not for the bug** | #2149 audit, 2026-08-17. Building the split-gate scanner, four fixtures were written from four measured instances, and all four passed on the first run — then the tree scan found **none** of the two that are still live. Each reduction had removed the property under test. `hle_agc.cpp:1774` is `dump_call_log(stderr)`, a call with no format string, and the fixture used an `fprintf`, so a printf-anchored rule matched the fixture and could never match the original. `gpu_executor.cpp:6402`'s real guard is `if (log \|\| g_dyntrace_force)`, where the second alternative is a plain **global flag** set only inside env-gated blocks two files away, and the fixture used `if (log)` — a direct `getenv`. Both fixtures were faithful to the *description* of the bug and unfaithful to its *mechanism*, which is what a reduction does by design. The tell was available and nearly missed: the fixtures passed **first try**, and a discriminator that needs no debugging on a hard case usually is not exercising it. **Reduce the surroundings, never the mechanism: keep the exact call form, the exact guard expression, and the exact indirection the original used — and confirm the rule fires on the ORIGINAL file:line before believing the fixture.** This sits beside trap 122: there a same-source control laundered a null, here a hand-built control laundered one, so "construct the positive instance by hand" is necessary and not sufficient — the hand-built instance still has to be able to express the case. |
| 183 | **`git stash push -- <file>` with nothing to stash creates NO entry, so the paired `git stash pop` pops ANOTHER agent's work** | #2149 lane, 2026-08-17. Re-running a mutation arm that had worked earlier in the session: `git stash push -- src/hle/graphics/hle_agc.cpp` (output suppressed), run the checker, `git stash pop`. The first time this was correct, because the fix was uncommitted. The second time the fix had since been **committed**, so push printed *"No local changes to save"* and created nothing — and pop then took `stash@{0}`, which belonged to a concurrent lane on a different branch. It conflicted, leaving `UU prosper/tools/gpu_replay/gpu_replay.cpp` in my tree. Two things saved it: a conflicting pop **keeps** the entry (`git stash list` still showed it, 41 insertions intact), and the conflict was loud. **A clean apply would have been silent** — their changes would have landed in my worktree, their entry would have been dropped, and the first sign would have been someone else's code in my next `git add -A`. The charter already says the stash stack is shared; what it did not say is that the *push* half can no-op, which is what decouples the pair. **So: never pair a blind push with a blind pop — check `git stash list` before popping and pop by name (`git stash pop stash@{N}`), or avoid the stash entirely.** For reverting a COMMITTED hunk, the stash is the wrong tool anyway: `git show <sha>^:<path> > <path>`, test, then `git checkout HEAD -- <path>` touches only your own file and cannot reach a shared stack. |
| 184 | **`gh pr checks` prints `pending 0` for a job that has been RUNNING for forty minutes, identically to one that never started** | #2149 lane, 2026-08-17. Reporting CI status on a saturated runner pool, I wrote that four jobs were "stuck in the runner queue, all at 0 s elapsed". One of them, `Linux` — the job carrying ctest, i.e. the one being waited on — had been executing for **over half an hour**: `gh api .../actions/jobs/<id>` showed `status=in_progress`, `started_at=08:01:09`, and six of eight steps already `success` with `Test` in flight. `gh pr checks` computes elapsed only for COMPLETED jobs and renders every other state as the single word `pending` with `0`, so "queued, never dispatched" and "running, nearly done" are the same two characters. The failure is the one this lane is about, aimed at me: a well-formed zero from an instrument that was not measuring the thing I read it as. It also produces the wrong ACTION — a queued job means wait or investigate capacity, a running job means wait and read its steps. **Use `gh api repos/OWNER/REPO/actions/jobs/<id> --jq '.status, .started_at'` (or `.../runs/<id>/jobs`) before describing a job as not started, and quote a step name rather than an elapsed time.** |
| 185 | **A premise witness that does not fire is VOID, not negative — and the cause can be a different NID that prosper answers CORRECTLY, short-circuiting the chain upstream** | *Earthion* (`PPSA28061`), 2026-08-17, #2186. #2167 changed `sceNpTrophy2GetGroupInfoArray` from the dispatcher's `0` to an error, and the A/B designed to check the one call site its evidence did not cover was built to verify its own premise: the "before" arm reverts the registration, so the NID must appear in the unimplemented table and must vanish in the "after" arm. **It appeared in neither.** The instrument was armed, on the right route, and correct; the *subject* was unreachable. The title's only call site is step 5 of a six-step trophy-init chain in which every step short-circuits the rest on a negative return, and step 4 has answered `0x80551500` since **#231** — `sceNpTrophy2GetGameInfo`, a **registered** NID roughly 1900 PRs older than the change under test, and therefore invisible to an unimplemented-NID table, which can only report absences. Every explanation a missing witness naturally suggests — too short a run, no input route, wrong frontend, dead code — points at the experiment, and **not one of them could have made the witness fire.** What resolved it was the charter's own rule applied to reachability: unregister the **upstream** gate to construct the reachable case by hand, outside whatever produced the null. That arm called the site exactly once (matching a static read in which the guest pre-zeroes its own out-count), and the real A/B could then be run where the code is actually reached — both arms pixel-identical. **When a premise witness does not fire, map the call chain to the import that gates it BEFORE touching the route** (`tools/re/stub_nid_map.py` names the import behind a stub address, which is what makes this a lookup rather than an afternoon); a correct answer upstream silences a downstream witness just as thoroughly as a broken instrument, and leaves no trace in any absence-based diagnostic. |
| 186 | **A line-based read of a registration table counts BOTH arms of an `#if`, and every duplicate line, as separate Sony entry points** | HLE registration census (#2070), 2026-08-17. The question was "how many Sony NIDs collapse onto one prosper handler?", which a runtime `hle_calls --values` histogram structurally cannot answer because it keys on the handler. A grep over `src/hle/**` reported **41** shared gated rows on *Sonic Origins*, with `s_ok` serving 15 names. Both figures are apparatus. `hle_kernel_mem.cpp` defines `register_kernel_mem_hle()` **twice**, ~3,200 lines apart, in the two arms of one `#if defined(__linux__) \| defined(__APPLE__)` (a two-pipe OR), so a platform-blind read sees every kernel-memory handler registered for two names and promotes it to "shared" — five handlers that answer exactly one Sony function each (`k_dmem_size`, `k_virtual_query`, `k_alloc_dmem`, `k_mtypeprotect`, `k_mprotect`) became collapses that way. Separately, `scePthreadAttrSetaffinity` is registered to `k_attr_noop` at both `hle_kernel.cpp:4539` and `:4588`, so counting *sites* rather than *distinct Sony names* inflates any handler with a duplicate. The true figure is **36**, and platform-blind per-site counting reproduces 41 exactly — the artifact is fully explained, not merely suspected. `s_ok` serves **14** on every platform: its "15th", `sceNpCheckCallback`, is registered to `s_ok` only in the `#else` (Windows) arm at `hle_service.cpp:4596`, and the two arms **swap** a name rather than adding one, so the platform-blind count is wrong on Linux and Windows alike. **The direction is the danger: both errors inflate, and an inflated collapse census reads as diligence rather than as a broken instrument.** `tools/re/hle_handler_map.py` now evaluates the arms per `--platform`, counts distinct names, and is reconciled on every ctest run against the registry the compiled binary actually builds (`tools/re/hle_registry_dump.cpp`) — the compiler is the only authority on which arm won. **When you parse source to count anything, ask what the preprocessor would have deleted, and count the THINGS you mean rather than the LINES that mention them.** |
| 187 | **`gh run rerun` re-executes inside the SAME run, against the ORIGINAL merge ref — after the base branch moves it reports a stale answer wearing a fresh timestamp** | CI verification, 2026-08-17 (#2604). A one-line CI PR's `Linux` job was rerun *after* the PR it depended on had merged, on the assumption GitHub would recompute the merge commit against the new `master`. It does not. Run `32019332338` was **created 10:15:52Z**, the dependency merged **10:58:59Z**, and the rerun executed inside that same run at **~11:00** — so it rebuilt a tree 45 minutes stale and failed with precisely the symptom the merged fix removes (`Cannot access memory at address 0x1149`). **Nothing on the job page says the tree is old**: it shows a new timestamp, a fresh log and a red X, which is why the failure was briefly read as "the fix is incomplete" rather than "the instrument answered about a different tree". The fix is to force a genuinely new merge ref — **rebase onto current `origin/master` and push** — and the reading rule is that any `rerun` after the base moved is **void, not negative**. Cheap check before believing one: compare the run's `createdAt` against the merge time of anything it depends on (`gh run view <id> --json createdAt`). |
| 188 | **A flaky test bisects EXACTLY like a real regression, and the run that would clear it is the one nobody performs** | macOS/Rosetta CI, 2026-08-17, #2427 / #2617. `sce_pthread_encoding` SEGFAULTed on two of three attempts of one identical job, on a branch touching only GPU code, and a CI bisect reported every earlier head green and only the last one red. That shape is the *definition* of a regression, so a full investigation cycle went into a diff that was not at fault. It was a **3.0% flake**: measured across 502 completed attempts of that job (2026-08-08..17), 15 died here — 71% of every failure the job produced — and a 2-of-3 cluster on a 3% base rate is unremarkable. **The trap is that a bisect over a flake is not noisy, it is CONFIDENTLY WRONG.** Each head is a single sample, a green head is weak evidence of nothing, and the experiment that falsifies the result — re-running an EARLIER head until it fails — is precisely the one nobody runs, because those heads have already "passed". `--output-on-failure` was already enabled on this job and its output held the answer in plain sight (every failing log stopped at the same line), but no two failures were ever compared, because each was read as a one-off. **So: before bisecting an intermittent symptom, measure its base rate over recent runs of that job, and re-run a GREEN head enough times to bound it — a bisect is valid only where a single run is a reliable oracle.** The rule has a counterpart for the fix: this one was closed by deleting the row that spawned an unjoined thread, so the race became *impossible* rather than rarer. A clean run count is worth exactly the N you derive from the **measured** rate, and the two rates here differ by an order of magnitude — 0 of 2000 is overwhelming against the 3% CI rate (`0.97^2000` ≈ 3e-27) but only ~1e-3 against the 1-in-300 rate the local reproduction actually had, which is the one that governs. Derive N before quoting a clean count, and prefer a structural argument, which needs no N at all. |
| 189 | **A GREEN required check on a PR is a statement about a merge that may no longer exist — and unlike a red one, nobody ever re-derives it** | Trap-numbering, 2026-08-17 (#2211, #2089). A `pull_request` job validates `refs/pull/N/merge`, which GitHub computes **when the event fires** and never recomputes as the base moves; trap 187 records the same staleness for `gh run rerun`. That entry is about a **red** result, and the direction nobody looks is the other one. #2581's `Docs` job was **green** against a base predating #2574, so nothing warned either author that both had written instrument-trap row **182** — the collision was found by simulating the merge by hand (`git merge-file` → rc=1, then running the gate on the merge result), never by CI. The asymmetry is the whole trap: a red check is investigated, so its staleness surfaces within minutes, while a green check is *acted on* — it is the thing that authorises the merge, and an author who has just been told they may proceed has no reason to ask what tree the answer was about. **On any PR touching a shared-key file (a numbered table, a registration list, a manifest), run the gate against the MERGE RESULT against fresh `origin/master` before merging** — `git merge --no-commit` or `git merge-file`, then the check — because the gate at head time cannot see a concurrent append by construction. Cheap tell: compare the check's completion time against `git log -1 --format=%cI origin/master`. |
| 190 | **A NEGATIVE control cannot be validated by "require the arm to show a failure" — so a setup step that dies leaves it printing the exact green it was supposed to print** | #2588 lane, 2026-08-17. Validating the new ASCII-output gate, three by-hand mutation arms were run against a file that **did not exist** (`src/loader/loader.cpp`; the tree has `linker.cpp`). Arms 1 and 2 were positive controls and self-reported: the mutation script raised `FileNotFoundError`, the gate stayed green, and an arm expected to go red printing `RC=0` is unmissable. **Arm 3 was the negative control** — an em dash in a *comment*, which must NOT fire — and it printed `all checks passed`, `RC=0`, which is character-for-character the result a correct run produces. Nothing in it was wrong except that the subject was never mutated, and the `cp: cannot stat` above it scrolled past in a log whose verdict lines were the part being read. Traps 98 and 153 both prescribe *"an arm that cannot show its lever moved is no result"* — that defence is **structurally unavailable here**, because a negative control's whole claim is that it stays green, so there is no failure to demand. **The fix is to make the arm assert its own precondition and print it beside the verdict**: each arm here now emits `MUTATION PRESENT: True` from a re-read of the file on disk before the gate runs, so "green because the rule permits it" and "green because nothing happened" stop looking identical. Generalises past mutation arms to every experiment whose expected outcome is the null: **when the hypothesis predicts silence, the apparatus must say out loud that it was pointed at something.** #2588. |
| 191 | **A fixture smaller than the code's own chunk size cannot exercise the loop it is guarding — and the suite stays green saying so** | AvPlayer guest file-replacement reader (#1955 / #1974 / #2618), 2026-08-17. `avp_add_source` reads the guest's media in **1 MiB** chunks; the regression fixture that shipped with the reader serves **5,000 bytes**. So `test_avplayer`'s read loop had only ever run **one iteration**, and multi-chunk assembly, an honest short read, and the position arithmetic that carries across a chunk boundary were *structurally inexpressible* against it — the same shape as trap 122, with the fixture's SIZE rather than a generator's geometry as the thing that removes the case from the space. Nothing about the passing suite distinguished "the loop is correct" from "the loop ran once". Refilled at 2 MiB + 12,345 bytes behind a reader capped at 700,000 bytes per call, with `read_calls >= 4` asserted so the loop must demonstrate it iterated; a mutation treating a short read as a failure then kills three checks that the old fixture could not have expressed. **When a test guards a loop, check the fixture against the loop's own step size before believing the green** — and assert the iteration count, because that is the one property a too-small fixture cannot fake. |
| 192 | **A test fixture at a fixed path in the shared build directory — the failure text names the SUBSYSTEM, never a filename, and "the box was busy" is the diagnosis that fits every observation and is still wrong** | Test infrastructure, 2026-08-17 (#2582, #2592, #2632). `file_binary_roundtrip` failed 16 sub-checks under concurrent load and passed alone, so #2582 concluded "the only variable left between the passing and failing arms is **concurrent machine load**" — supported by a runtime spanning 0.01 s on CI to 861 s here. **Load is not the cause and cannot be fixed.** The pre-#2592 binary reproduces all 16 on an **idle** box in 29 s against one competing writer: `while :; do rm -f prosper-test-file-binary.tmp; sleep 0.05; done &` next to a run in that directory. Load only widens the overlap between two ctest invocations sharing one build directory. Three arms pin it — fixed-name binary under that deleter **FAIL 16**, scratch-rooted binary under the **same** deleter **PASS**, scratch-rooted binary with the deleter re-aimed at its **actual** scratch fixture **FAIL, the identical 16** — so the pass is the path moving and not the lever going dead, which the second arm alone could not have shown. **What makes this class expensive is that the failure never mentions a file:** the 16 name `sceKernelChmod`, `sceKernelUtimes`, `sceKernelDup` and binary reads, so it reads as an HLE defect; the same day, 6 concurrent `spv_validate` sharing one output directory printed `spirv-val REJECTED it: Invalid SPIR-V magic number` — an accusation against the **recompiler** — and 6 concurrent `test_runtime_prx_load` sharing one root failed 4 with `no module was loaded`, an accusation against the **loader**. Every one is a filename. Both controls used distinct paths at identical concurrency and passed 6 of 6. **Before believing a test failure that appeared under load, re-run it against a single competing writer on an idle box; if it reproduces, the defect is the path, not the load.** **And the one observation the load story actually rested on turns out to have a cause of its own — load and failure here are correlated through a COMMON CAUSE, not causally linked, which is what made the wrong hypothesis rational rather than careless.** The 0.01 s on CI against 861 s here is not the fixture race showing up as elapsed time: `tests/hle/test_file_binary.cpp:173` calls `sceKernelSync`, and `src/hle/fs/hle_file.cpp:1731` implements it as a bare `::sync()` — a whole-filesystem writeback barrier whose cost scales with **machine-wide dirty data**, i.e. with every other lane's build traffic (#2651). Read off the live processes rather than inferred: during a 4-concurrent arm every running process sat in `syscall=162` (`sync`) in state D for minutes, with `/proc/<pid>/wchan` reading `rq_qos_wait`, `wb_wait_for_completion` and `sync_inodes_sb` — measured twice, by the reviewer and by a second lane, different pids, same three wchans. A busy shared box therefore produces **both** observations independently: it makes `sync()` slow **and** it makes a second ctest invocation against the shared `build-linux` likely. So load tracked the failures perfectly while causing none of them, and #2582's arm 5 — arm 3 repeated once the box quietened — coming back fast *and* green is one fact instead of two coincidences. **A runtime that inflates alongside a failure is not evidence that the load caused the failure. Find out what the slowness is made of before promoting the correlation: a third variable driving both is exactly what a perfect correlation looks like.** Fixtures go through `tests/fixtures/test_scratch.h` (per case, per pid, removed at exit) — its header carries this reproduction. |
| 193 | **A diagnostic that perturbs the subject: arming a software WATCHPOINT made a PRODUCTION permission check answer wrong** | The inverse of the usual entry on this list, and the reason it belongs on it. `PROSPER_WATCH_LABEL`/`_ABS`/`_HOT` and `PROSPER_WATCH_COMPANION` work by mprotect-ing a live guest page down to `PROT_READ` and back. `guest_writable`'s range cache (#2389) and `guest_readable`'s memoize POSITIVE permission answers and drop them only when `host::guest_mapping_generation()` moves, and the handler advanced nothing -- so while a watch was armed `guest_writable` answered TRUE, from cache, for a page the instrument itself had just made read-only. **Any observation of write-guard or write-refusal behaviour taken with one of these watches armed, before #2393, is void rather than negative.** Before #2389 the Linux arm re-parsed `/proc/self/maps` and saw `r--p`, so the perturbation is younger than the instrument. The general rule: **an instrument that MUTATES the subject's state owes every cache of that state an invalidation**, and the audit that finds those consumers is cheap next to the class of bug it prevents -- here, six of the eleven further sites in `guest_write_watch.cpp` were inside the fault handler, where the obvious fix (the mutex-taking `notify_guest_mapping_*`) would have traded a stale cache for a deadlock. Fixed: every protection change in both files now routes through one notifying wrapper, and `tests/host/memory/test_watch_mapping_generation.cpp` asserts mechanically that none is added without one. #2393, #2601. |
| 194 | **`git merge-base --is-ancestor` and `git branch --contains` both answer NOT MERGED for squash-merged work, because the reviewed branch tip never becomes an ancestor** | Merge tooling, 2026-08-17 (#2639, #2610, #2614). A squash merge writes a **new** commit; the tip that was reviewed and pushed is not in its history, so every ancestry test aimed at that tip answers "no" about work that is entirely on master. Reproduced on #2639, squash `b56ffda1`: `git merge-base --is-ancestor b56ffda1 origin/master` → **yes**; the same test on the reviewed tip `e08894b9` → **no**; `git branch -r --contains e08894b9 \| grep -c origin/master` → **0**; and `git show origin/master:prosper/tools/screenshot/capture_manifest.cpp \| grep -c early_stop_armed` → **1**. The work was on master throughout. **It fired twice in one day, on two lanes, and both conclusions were confident and acted on.** One lane read a merged dependency (#2610, branch `fix/issue-2089-trap-numbering`) as unmerged and nearly re-briefed four PRs on that basis. Another compared its own pushed branch against #2614, found a byte-identical paragraph of its own prose in #2614's tree, established that its commit was "not an ancestor" of #2614, and concluded a peer lane had **copied file content out of its branch** — raising a "clean merge, broken file" alarm about a divergent snapshot going stale. False: #2614 had rebased onto a master that already carried the paragraph, its merge-base `f46a8e58` containing the squash, so it had inherited the text legitimately and in order. **Test the MERGE COMMIT or test by CONTENT (`git show origin/master:<path>`) — never the branch tip.** `merge_commit_sha` alone is not enough: GitHub populates it on **open** PRs too, as a test-merge object, so read `--jq '[.merged, .merge_commit_sha]'` and use the sha only when `.merged` is true. Measured on the open #2602: `merged=false`, `merge_commit_sha=31dc4719`, and `git merge-base --is-ancestor 31dc4719 origin/master` exits **128** (object not in the clone), not 1 — so a script testing `rc != 0` reads a bad object as "not merged" and is right by accident until it is not. The tell is ancestry saying no while content says yes. **Ancestry is sound about the COMMIT and silent about the CONTENT**, which is the distinction that matters and is stronger than "it never lies optimistically": it never reports a commit that was never merged, but a **revert** makes it report one whose work is gone. #2026's squash `f36fe758` **is** an ancestor of master while #2044 ("Revert #2026") removed its 437 lines — ancestry answers "merged" about content that is not there. The content test is the only one sound in both directions. What makes the trap durable is that the one question a sceptical reader thinks to ask, "could this be too optimistic?", is the single direction the instrument does not fail in. |
| 195 | **`gh api .../files` without `--paginate` returns ONE PAGE, and a truncated list read through `grep` produces a clean, wrong "nothing there"** | #2639 review follow-up, 2026-08-17 (#2614). `gh api repos/OWNER/REPO/pulls/N/files` answers with the API's default page — **30 items** — and nothing in the output says it is a prefix. Ask "does this PR touch any file I changed?", pipe it to `grep`, and a PR larger than one page yields an empty result that reads as **no overlap**. Measured on #2614 the same hour: unpaginated → **30** filenames, of which **0** matched `screenshot\|AGENTS`; `--paginate` → **59** filenames, of which **2** matched, and both were files the asking lane had edited (`prosper/tools/screenshot/screenshot.cpp`, `prosper/tools/AGENTS.md`). The conclusion drawn from the short list — no shared files, so merge order is irrelevant — was the reverse of what the data said, and it was reached in one command with no error and no warning. **Pass `--paginate` on any `gh api` list you will draw a NEGATIVE conclusion from** (`files`, `commits`, `comments`, `reviews`, `issues`), and cross-check against the count the API itself reports: `gh api repos/OWNER/REPO/pulls/N --jq .changed_files` returned **59** here, contradicting the 30 on its own. That makes the check an experiment that detects its own invalidity, rather than one relying on a reader noticing that 30 is a suspiciously round number. Same family as the charter's "absence from grep is not evidence", aimed at a paginated API rather than a file tree: the instrument answered a narrower question than the one asked, and said so nowhere. |
| 196 | **Killing a `distrobox enter` wrapper does NOT kill the build inside the container — the log stops growing, which is indistinguishable from a hung build, and restarting is the response that costs** | Shared-box build tooling, 2026-08-17. A lane's 10-minute Bash timeout killed its `distrobox enter` wrapper while `ninja` kept compiling **inside** the container. **Nothing container-side died** — a probe on this box SIGKILLed the wrapper and found both the `podman exec` client and the container-side `bash` still running, with the marker file appearing on schedule 34 s later. What went away was the thing *reading* the output, so the log simply stopped updating — the exact signature of a stall, and the tempting reading. (Do not go looking for a dead podman client to confirm the trap: you will find it alive and conclude the trap does not apply.) Restarting on top of it produced **two builds in one directory**: three concurrent `ar` processes writing one `libprosper_core.a`, a truncated archive, and about an hour of link failures that read as code defects rather than as apparatus. **After a `distrobox enter` wrapper dies, wait on the CONTAINER-SIDE process instead of restarting on top of it** — `pgrep -x ninja` / `pgrep -x cc1plus` before concluding anything — and never start a second build in a build directory that may still have one running. **A log that stopped updating is evidence about the STREAM, not about the BUILD.** Note the wait loop itself has a trap: `pgrep -f` matches the shell running it (trap 135), so use `pgrep -x` on the program name, or check the process's `cwd` under `/proc`, rather than a `-f` pattern. Closest relative is **trap 135**: a wait state that looks exactly like a slow build. The resemblance is the whole row, not just the `pgrep` clause — in both, a process that is fine is read as a process that is stuck. |
| 197 | **A broad `pkill -f` crosses lane boundaries on this shared box — and a killed `ctest` does not look killed, it looks like a test failure** | Shared-box process hygiene, 2026-08-17, reported independently by two lanes the same day. One ran `pkill -f "ctest --no-tests=error"`, a pattern matching **every** worktree's run rather than its own; another had its `ctest` SIGTERM'd twice mid-run — once at **265/266 with exit 143**, once killed before any exit line was written at all. Both disclosed it, which is the only reason either is recordable. **The cost is not the lost run, it is the hour spent on a defect that does not exist:** a signalled `ctest` yields a partial pass count, or a non-zero exit that reads as a genuine failure. The second form is the nastier one, because there is no exit code to be suspicious of — the run simply ends. **Never `pkill -f` on a pattern that could match another worktree's process: scope to your own PID tree, or match on your worktree path.** **Before believing a red `ctest`, confirm the run was not signalled — exit 143 is SIGTERM, not a test failure, and a run with no exit line at all was killed rather than completed.** Cross-reference `pgrep -x ctest` before concluding the box is idle, remembering that `pgrep -f` over-counts by matching its own shell (trap 135) and that a padded `ps` column can under-count. Same family as **trap 192**: a shared-path failure whose text accuses the wrong subsystem — there an HLE or loader defect, here a test failure — on the same shared-box substrate of one build directory and two `ctest` invocations. Trap 196 is the sibling case where the apparatus reports a stall that is not there. |
| 198 | **A checker's own test fixtures written into `$TMPDIR`, filtered by a rule that reads the ABSOLUTE path — so the CALLER's directory name decides what the checker can see** | `check_diag_gates.py` excluded build trees with `if "build-" in str(path)`, a substring test on the absolute path, while its `--selftest` writes fixtures into `tempfile.TemporaryDirectory()`, i.e. under `$TMPDIR`. This charter tells every agent to put `TMPDIR` next to the build, and the build directory the rest of it names is `prosper/build-linux`. So following that guidance discarded **every fixture**: all twelve positive arms printed `got=[]` under the banner *"the scanner's own signatures are broken"*, while the twelve **must-not-match arms passed vacuously** — loud in the half that cannot mislead, silent in the half that can. Three lanes hit it within five minutes on three unrelated tasks, and one read its `259/260, 1 failed` as a real regression in a diagnostic gate. It reproduces only inside a full `ctest` run, where the harness sets `TMPDIR`, and passes under `ctest -R diag_gate_selftest` by hand — which reads as flakiness rather than as an environment dependence. The trigger was one keystroke wide: the recipe as literally written said `build/tmpdir`, no hyphen, and was safe. **A path filter is a statement about a subtree, so express it as components RELATIVE to the root you own** — an absolute-path substring cannot distinguish "inside the build tree" from "underneath a directory that happens to be named like one", and the second is the caller's business, not the tool's. Two further rules the fix carries. A self-test whose fixtures resolve to **zero collected files** must report APPARATUS rather than report its arms, because "no findings" is also exactly what a broken signature produces. And the regression arm must construct the hostile path **by hand** rather than by setting `TMPDIR`: `tempfile` falls back to `/tmp` without a word when `$TMPDIR` names a directory that does not exist, so an arm built out of the environment goes green on an environment it never had — trap 122's rule, applied to a fixture root. #2658, #2660, #2661. |
| 199 | **A tool's refusal to arm itself, printed once at the top of a long log and read as an armed instrument** | GTA V, 2026-08-18. `PROSPER_CFG_TRIP_BOUND=N` plus `_PROGRAM=` emits **no bound at all** without the third variable `_PHASE`, and says so exactly once via `std::call_once`: *"PROSPER_CFG_TRIP_BOUND_PHASE is REQUIRED and is unset: no bound emitted"*. In a 1,200-line filtered run log that single line is functionally invisible. Three runs were made, analysed and written up as bounded; none of them had a bound — they differed only by an unrelated flag, so an A/B that read as bounded-vs-unbounded was **unbounded on both sides**, and a "the bound changes nothing" conclusion was drawn from two identical arms. The requirement was **also** already stated in `docs/GTA5_STATUS.md`'s own env table, so the failure was an unread warning rather than a missing one — twice over. Distinct from trap 172, which is about that same witness's field *labels*: this one is about whether the witness exists. There is a **fourth** silence behind it: the refusal notice is emitted *after* the `_PROGRAM` filter, so a mistyped address yields neither a bound nor a notice — while the dispatch-map print is gated on the bound variable alone and fires for every program the dispatcher lowers, which is precisely what makes the log read as armed. **Grep for a tool's own refusal line before trusting an arm**, and do not read a neighbouring line's presence as evidence the arm took; a `call_once` notice is silent at log scale. |
| 200 | **A control read before it reached the stage it controls, manufacturing a discriminator out of nothing** | GTA V, 2026-08-18. The guest flip counter read `present/frame` **1.00** for the `SKIP` arm against 0.22–0.32 for the others at t=60 s — apparently clean evidence that the declined compute program caused the stall, and it was written up as such. Run to completion the same arm reads **0.19 with a 300 s contiguous stall**: indistinguishable. The entire difference was that the control had not yet reached the stalling stage. In the pair measured, both arms lost the device — at different times, from different programs — and the stall merely follows the loss. Two other runs declining `0x413dc6700` alone did not lose it. Whether that is nondeterminism or simply a window that ended before the next program's loss is **not established** — neither of those runs finished the route, and this title's own status doc holds that a zero-loss run which is not a route completion does not close the search. **A partial trace is not a weaker version of the full one — it can point the opposite way**, and a control is only a control once it has passed the phenomenon it is controlling for. |
| 201 | **A reporter that speaks on one branch only, whose silence spans several different states** | GTA V, 2026-08-18. `[structured-wave-reject]` fires when `exact_compute_wave_cfg && !structured_compute_wave_cfg`. Its silence for a program therefore means the decision was never reached, **or** was reached with `exact_wave` false, **or** was reached and structured emission *succeeded* — and the third would overturn the mechanism the whole investigation assumed. A device loss was spent on a probe designed around that null, which could not discriminate between the three. The question was answerable without any run: `emitted_loop_trip_bound()` has a single call site, inside `emit_cfg_state_machine`, so an already-recorded witness count in the title's own status doc proved dispatcher emission. **A diagnostic that cannot report its positive outcome cannot be read negatively** — and before designing an experiment around a null, check what a null could mean. Filed as #2702. |
| 202 | **A first-wins classifier over a multi-entry collection, naming the first failing reason rather than the reason belonging to the entry that was actually rejected** | GTA V, 2026-08-19. The sampled-depth bridge's miss classifier set `extent_bad` from **any** mismatching sibling at an address and tested it before every validity reason, so `0x20945c0000` reported `extent (T# wants 1024x1536, retained image is 3840x2160)` **1403 times** while the cache simultaneously held a 1024x1536 `D32_SFLOAT` entry **at that very address**. One address routinely holds several entries — a live surface plus a stale sibling at another size — so the two reasons differ as a matter of course, not rarely. The true reason was `depth-invalid`. **The mislabel cost a whole investigation**: an issue filed, chased, and self-corrected twice before the classifier itself was read. Two things made it durable. It was **specific** — it named both extents and a plausible mismatch — and specificity reads as having been checked, the same authority-transfer `CLAUDE.md` records for a cited claim. And it pointed **away from the record that would have stopped it**: `depth-invalid` is falsified ground in `GTA5_STATUS.md` with a strong arm behind it, while `extent` was unexplored, so the wrong label was also the more interesting one. A second, independent copy of the same defect sat in the detail string, which keyed off `extent_bad` rather than the classified reason — so the printed text could contradict the counter it was filed under. **When a lookup iterates several candidates, the reported reason must belong to the candidate that lost, not to the first one that failed any test** — and a diagnostic whose reason and whose counter are computed from different expressions will drift. Fixed in #2724, whose regression arm reproduces the production string character for character; the classification is now a pure function so it is testable without arming the env var that gates the log (which trips `cached_env_arming_logic`). Nearest relative is **trap 199**: there the instrument never armed, here it armed and answered confidently with the wrong field. |
| 203 | **A live run-log filter whose regex anchors on a tag's closing bracket, dropping the `-suffix` variant of that tag — so input the guest demonstrably received reads as input never delivered** | Asterix & Obelix: Babylon Mission, 2026-08-19. This charter requires long run logs to be filtered as they stream (`PROSPER_FILELOG` alone reaches gigabytes, and `/tmp` is a shared RAM tmpfs), so nearly every lane writes one of these patterns. The filter contained `\[pad\]`, which matches `[pad] …` and **cannot** match `[pad-script] …` — and `[pad-script]` is the tag `PROSPER_PAD_SCRIPT_LOG=1` uses for the one thing being verified, the state transitions the guest actually observed (`src/hle/input/hle_pad.cpp:340`; the plain `[pad]` tag belongs to `padlog()`, gated on the *different* env var `PROSPER_PADLOG`, `hle_pad.cpp:81-82`). The filtered log therefore showed **zero** pad lines for a run in which **278** transitions occurred, 63 of them the button the route turned on. The obvious reading is that the route never loaded — which sends the next hour into a mechanism that is working, and, worse, invites 'fixing' it. **What made the null credible was that the rest of the filter worked**: the same pattern captured the scene-load sequence perfectly, so the log looked healthy and complete, and only the one absent signal was absent. That is trap 122's shape at the level of the log filter — the loud, working part of an instrument launders the silence of the broken part. Three rules. **Write tag patterns open-ended** (`\[pad`, not `\[pad\]`), because a tag family with a hyphenated variant is the norm here, not the exception. **Before believing that a diagnostic produced nothing, grep the source for the string it actually prints** rather than the string you would have named it — `CLAUDE.md`'s standing rule that absence from grep is not evidence applies to your own filter first. And **prefer a filter that can report its own emptiness**: a per-tag count printed at the end of the run turns a silent zero into a visible one, which is the difference between a null you can act on and a null that acts on you. |
| 204 | **A census that reports only at power-of-two totals and never at exit — so a program that starts failing LATE is missing from its own inventory** | UE4 survey, 2026-08-19. `PROSPER_COMPUTE_PROGRAM_CENSUS=1` prints only when the running total is an exact power of two and at least 512 (`gpu_executor.cpp:2114`), with no teardown report, so a run's last block is the last boundary it crossed and everything after is discarded — up to 65,535 decisions between 65,536 and 131,072. *Little Nightmares III*'s volumetric-fog program `0x30114c0000` appears in **73** `[compute-table]` lines and in **zero** census rows of a 316 s arm, because its first skip landed past 65,536; a 501 s arm of the same route on the same binary put it in the census at `executed=0 skipped=172`. The failure is silent and one-directional — it can only under-report, and it under-reports exactly the programs belonging to the phase you routed to. It also makes two arms of one A/B incomparable for reasons unrelated to the change: a 70k-decision and a 130k-decision run both report "at 65,536". Filed as #2746; the adjacent filter gap is #2745. |
| 205 | **A buffer size read as a resource IDENTITY — the froxel factorisation of a full-screen buffer is exact, so the arithmetic "confirms" whichever reading you brought to it** | UE4 survey, 2026-08-19. A UE volumetric-fog froxel volume is `(W/16) x (H/16) x 64 x 8` bytes, which for a 4K view is `16,588,800` — and that is **also exactly** `3840 x 2160 x 2`, a full-screen 2 B/px image. Likewise `7,372,800` is `160x90x64x8` **and** `2560x1440x2`, and `8,294,400` is `240x135x32x8` **and** `3840x2160x1`. The survey's detector flagged *Sonic Racing: CrossWorlds* `0x25c0056e00` as fog on the size alone; it is a **reduction**, cleared by the rest of the evidence — a flat 1-D 69,120-thread dispatch whose two storage outputs are **512 bytes**, where fog writes a volume of its own input size. **Identify a resource from the dispatch shape and the whole binding set (N sampled inputs plus one same-sized storage output), never from one number that factorises the way you hoped.** #2747. |
| 206 | **A shell script edited while it is running — bash reads a script incrementally, so the edit lands as a byte-offset shift and mangles a command line that already ran correctly for minutes** | UE4 survey, 2026-08-19. A 342 s `PPSA05143` census run was in flight when its driver was edited to widen an unrelated title's window. Bash resumed the file at its old offset, parsed the tail of `--allow-guest-fault > "$RUN/run.log" 2>&1` as a fresh command (`line 58: -fault: command not found`), and **the mis-parsed redirect truncated the 64 KB run log the completed run had just written** — the census output was gone while 35 screenshots and a healthy manifest remained, so the artifact set looked fine. Nothing warns. **Freeze a copy of any driver before launching it** (`cp driver.sh .driver-$$.sh && ./.driver-$$.sh`), or do not touch it until the run exits. |
| 207 | **A containerised run that never started, whose empty result reads as a clean negative** | `distrobox enter` (podman) fails with `Error: beginning transaction: database is locked` when another agent on the box is using podman, and then *offers to create the container* — so the wrapper sits on a `[Y/n]` prompt and the command inside it never runs. Measured 2026-08-20 while verifying #2734: a positive control seeded a foreign save payload, ran a 200 s `boot_trace`, and reported the file **unchanged** — which is exactly what the PASSING outcome looks like. The emulator had not executed a single instruction. *Recognition cue:* **the artifact you are about to interpret is the one you wrote yourself, and "nothing happened to it" is the result you were hoping for.** Check the launcher's exit status before the artifact, every time: a real 200 s capped run exits **124** (the `timeout` cap), while this failure exits **1** within seconds. A second tell is an output file that does not exist at all rather than existing and being short. Two mitigations, both cheap: bound any container run with `timeout` and assert the 124, and prefer an arm whose PASS is a positive change ("the file I expected to be written exists") over one whose pass is an absence. On this box the container-built binaries also run directly on the host for CPU-only work such as `boot_trace`, which is the fastest way around a contended podman. #2734, #2759 |
| 208 | **A `CLAUDE.md` claim quoted from the session's own context snapshot, which is loaded once at start-up and never reloads — so the charter an agent "reads" can be a fortnight older than the one on disk** | Four agents, 2026-08-20. All four independently reported that this file's docs-gate recipe spells the removed `--sequential`; `origin/master` line 533 has spelled `--ordered --baseline` since #2089, and its only `--sequential` is the line recording the removal. Two routes produced the same false claim, and **the second survives every obvious fix**. (1) The shared main checkout sat **438 commits / 13 days behind**, and its copy genuinely did say `--sequential`; a worktree taken from the stale local `HEAD` inherits it. Fixed at source by fast-forwarding — but that does not reach agents already running. (2) **The snapshot itself.** One reviewer created every worktree explicitly from `origin/...` refs, and its own working tree already said `--ordered`: it was quoting *context*, not any file. It had even run `git show origin/master:CLAUDE.md` in an earlier round — **the 37 KB output was truncated to a preview**, so it read the preview and fell back on the snapshot. The cost is not the flag. It is that the claim arrives with a reviewer's authority, aimed at an author's evidence: one round asked an author to re-quote a `--ordered` result that was already correct, and the only `--sequential` invocation anywhere near that PR was the reviewer's own, which **exited 2 having checked nothing** — the `ctest`-that-ran-no-tests class, inside the very check meant to catch it. **Verify at the moment of citation, not at session start or worktree creation, and make the check targeted so it cannot be truncated:** `git show origin/master:CLAUDE.md \| grep -n -- '--ordered\|--sequential'`. Never `git show` a large file and read its preview. Nearest relatives: trap 41 (taking a whole file from a stale branch) and this file's own rule that a cited claim reads as a verified one — here the stale source is the context window rather than the disk, which is why re-reading the repository does not clear it. |
| 209 | **A rate-limited log sampling a low-duty-cycle signal reports its ABSENCE with perfect consistency — and "the guest never sees our input" is the most load-bearing negative a route derivation can get wrong** | *Sonic Frontiers* route work, 2026-08-20. `PROSPER_PADLOG` prints pad calls 0-7 and then every 512th (`hle_pad.cpp:83-91`). A route pressing a button for 4 flips out of every 200 has a ~2% duty cycle, so every sampled line read `connected=1 buttons=0x0`, over 3,584 calls, across two arms. That is exactly what a broken input path looks like, and the conclusion on offer — prosper is not delivering scripted input to this title — would have sent the lane into `hle_pad.cpp` instead of into the menu. Input was fine: the samples and the presses simply never coincided. **The control is a hand-built maximum-duty instance of the same class, not more of the same route:** one entry, `f1000-4000:cross`, made every sample from call#1024 to call#3584 read `buttons=0x4000`. Note that no amount of *additional* low-duty pressing would have produced it — a second route arm draws from the same generator and inherits the same duty cycle, which is this file's same-source-positive-control failure in its cheapest form. *Recognition cue:* a periodic instrument, a bursty subject, and a negative you are about to act on. Check the instrument's sampling rule against the subject's duty cycle **before** believing the zero. #1891 |
| 210 | **The `CLAUDE.md` in an agent's own context — it is whatever the shared checkout held when the session started, and the rules most likely to be stale are the ones recently corrected because they were wrong** | Measured 2026-08-21. The session's project instructions carried the pre-#2089 docs-gate recipe `check_numbered_table.py --sequential …`; running it errors with `THE CHECK DID NOT RUN`, so I filed it as a charter defect (#2828). It is not one. Master's `CLAUDE.md` already says `--ordered`, already passes `--baseline`, already states **"Gaps are legal"** and already cites #2089 — and is more thorough than the fix I proposed. The `--sequential` text came from the **shared main checkout at `f7cfaf50`**, ten hours and several hundred commits behind master's `70d879ed`; that checkout is what gets loaded as the project instructions. **The trap is that running the command CONFIRMS the false conclusion.** The flag really is dead and the charter really does say `--ordered`; both are true simultaneously because they are two different versions of one file, so an author who verifies by execution gets a genuine error that appears to corroborate the report. Verification by *doing* is normally the strong move, and here it is precisely what launders the mistake — the check that would have worked is the cheap textual one. This is the charter's own "check a claim against the source, never against anything downstream of the thing being checked" rule, hit while writing a bug report about the charter. *Recognition cue:* **a rule that looks wrong, in a file nobody edited on your branch.** A charter is inherited without verification by design — that is what it is for — so staleness in it is silent and the agent follows it faithfully and is wrong. *Fix:* before acting on a surprising `CLAUDE.md` rule, and always before reporting one as a defect, run `git show origin/master:CLAUDE.md` and diff it against the session's copy; treat the checkout's age (`git -C <shared> log -1 --format=%ci HEAD` against `origin/master`) as the staleness bound for every rule you inherited. #2828 |
| 211 | **A screenshot interval that DIVIDES the title's own cycle period, read as a frozen title** | *Earthion* (`PPSA28061`), 2026-08-21. A 900 s probe sampled every 12 s reported the *identical* narration frame seven consecutive times — same CRC, same content metrics, same bounding box — with every other frame black. The natural reading is that the title is stuck on one screen. It is not: the picture cycles with a **~60 s period**, and 12 s divides 60, so the grid only ever visits five phases and one of them happened to hold that frame. Every sample was correct; the *set* of samples was structurally incapable of showing the other 55 seconds. The tell is the one that generalises: **content recurring at an exact multiple of your sampling interval is a statement about the instrument until you have re-measured off-grid.** Two cheap defences, both applicable to any `tools/screenshot` sweep: choose an interval that is not a divisor of any plausible cycle (a prime number of seconds), and before believing a stable state, re-run at a different interval and check the state survives. Related to the aliasing family only superficially — the failure here is not lost resolution but a *sampled subset that cannot express the phenomenon*, the same shape as trap 122's structurally-inexpressible positive control. Also on this title: the frame CRC distinguishes states that are the same picture, because the renderer-drawn CRT bezel carries a small animated indicator outside the game area — crop to the bezel interior before calling two frames different. #1880 |
| 212 | **A generated document that DOCUMENTS its own format — a selftest asserting on `render()`'s whole output matches the worked example in the HEADER, not the row it is checking** | Writing the `FPS record:` column for `PROGRESS_TRACKER.md`, 2026-08-21. `gen_progress_tracker.py`'s `render()` returns HEADER + table + FOOTER, and the header I had just written explains the new column with a worked example: `**3.4** / 59.8 fps · 4K · gameplay · screenshot · 2026-08-21`. My selftest asserted `"**3.4** / 59.8 fps" in render([record])`. It passed — and it passed **unchanged** under a mutation that made the cell print only the presented rate, which is the single reading that column exists to prevent (#2783). The absence arms were void the same way: the needles `\| - \|` and `\| none \|` are produced by the Guard, Oracle and Open-blockers columns of the very fixture under test. Three arms, all green, all measuring prose. *Recognition cue:* **a substring assertion over the output of a function that also emits documentation, a legend, boilerplate or an example.** The better the documentation, the more reliably the test passes for free — and it generalises past docs to any needle that also appears in a header, footer, log preamble or `--help` string. *Fix:* extract the cell **by position** (split the data row on the column separator, take the ordinal) and compare it whole, then re-run the mutation to prove the arm moved. Note what caught it: not review and not reading — the mutation arm. Read back, all three arms looked entirely reasonable. #2843 |
| 213 | **`pkill -x <name>` — the form recommended as the SAFER alternative to `pkill -f`. It fixes matching accuracy and does nothing about scope, so it still kills every lane's process of that name** | 2026-08-21, disclosed by the lane that did it. A Plucky Squire lane ran `pkill -x screenshot` to clear its own run and killed a **concurrent Earthion lane's** run mid-flight — a coprime-interval sweep at 36 of 64 frames. That sweep was the arm that would have settled whether the title's ~60 s cycle is an attract loop failing to draw or a sequence restarting; without it the Earthion lane recorded that question at `CONFIDENCE: MED` and handed it on. **The lost measurement is the cost, and it landed on a different lane than the one that paid it.** The trap is that `-x` is exactly what trap 197 and the accumulated guidance point you toward: `-f` over-matches on substrings and self-matches its own shell (trap 135), so `-x` reads as the careful choice. It *is* more accurate — and accuracy is a different property from **ownership**. `-x screenshot` means "every process named screenshot on this box", which on a shared box with three lanes is three runs. Neither flag has any notion of whose process it is. *Recognition cue:* **any kill whose predicate mentions only WHAT the process is and never WHOSE it is.** *Fix:* scope to your own PID tree (`pkill -P $$`, a stored PID from `$!`, or a process group you started with `setsid`), or match on your own worktree path (`pkill -f "$PWD"` after confirming `$PWD` is your worktree). Before any kill on this box, run `pgrep -a -x <name>` and read the argv of every hit: if more than one appears and they do not all point into your tree, you are about to take someone else's run. Same family as 197, narrowed to the case where the recommended mitigation for 197 is itself the hazard. #2839 |
| 214 | **`tools/screenshot --timeout` cannot bound a BOOT, so a title that hangs in `boot_program()` must be killed from outside — and an externally killed run looks exactly like a defect** | SGW Contracts 2 lane (`PPSA03130`), 2026-08-21. A first boot attempt produced no `[shot]` line, an empty manifest and a five-line log ending at renderer registration, with the outer shell `timeout` firing before the tool's own `--timeout 300`. That reads as "the guest is stalled" or "the tool is broken"; it is neither. `screenshot.cpp` calls `register_live_renderer()`, then `boot_program(...)`, and only then enters the `while (saved < count)` sampling loop where the deadline is checked — **so the tool's `--timeout` is structurally unreachable until `boot_program()` returns.** And `boot_program()` ends by calling `run_guest_inits()`, which executes the guest's own module initialisers, so a title can sit inside it indefinitely. Two consequences. **Set the outer and inner limits to DIFFERENT values and quote which one fired**: the tool's own `[shot] done:` / `[shot] timeout after Ns` lines are the only proof it reached an exit path, and a run with neither has been killed, so its silence is not evidence (same family as traps 197 and 213). **And the phases are now visible**: `PROSPER_BOOTPHASE=1` prints one stderr line per boot phase, so the last line names the phase a stalled boot is inside — which turned this lane's "no output at all" into "deadlocked in `GUEST_INITS_RUNNING` at +81 ms" in a single run. Note the diagnostics that `printf` to **stdout** (`linked N modules ...`) are block-buffered when redirected and are lost entirely when the run is killed; the phase log goes to stderr and flushes per line for exactly that reason. #2867. |
| 215 | **A symlink farm built to A/B one file silently invalidates prosper's bounded-file reads, inventing two defects that belong to the apparatus** | SGW Contracts 2 lane, 2026-08-21. To test whether one bundled PRX was wedging the boot, the lane built a dump tree of symlinks to the real dump with that single file omitted — zero-copy, and the right shape of experiment. The run then reported `[appparam] no usable sce_sys/param.json (unreadable, oversized or malformed)`, `[addcontent] invalid install manifest`, and `[savedata] this application declares no usable titleId`. **None of those are true of the dump.** `hle_addcontent.cpp:130` walks each path component with `symlink_status` and rejects **any** symlink outright — deliberately, so a dangling or redirected manifest cannot authorise foreign add-content — so a symlinked `sce_sys/` or `dlc_emu.ini` reads as malformed. The trap is that the messages are *specific and plausible*: they name real parse failures, and a lane chasing a rendering defect will happily write "this title's param.json is malformed" into a status doc. *Recognition cue:* **a defect that appears only in the arm you constructed, in a subsystem your variable has nothing to do with.** *Fix:* make every path component of `sce_sys/param.json` and `dlc_emu.ini` a real file (copy them; they are tiny) and symlink only the bulk assets — or better, prove the fix in the loader and re-run on the untouched dump, which is the only arm that is evidence about the title at all. #2867. |
| 216 | **A byte counter reporting "no I/O" during heavy I/O — `/proc/<pid>/io`'s `read_bytes` is BLOCK-DEVICE traffic, so a warm page cache reads it as an idle process** | SGW Contracts 2 lane, 2026-08-21. A three-way sampler (CPU climbing = executing, bytes climbing = starved, neither = stuck) was validated on three hand-made controls and worked. Then it called a healthy run **STUCK**: `cpu+0.14s`, `disk-read+0.00MB`, 18 threads all sleeping, over several 20 s windows. The reading was reinforced by a detail that made it persuasive rather than suspicious — exactly one thread, Wwise's `AK::BankManager`, sat in `k_mutex_lock` while every other thread waited on a condition or a semaphore, which is what a deadlock with a named culprit looks like. **It was neither stuck nor deadlocked.** The run resumed and reached a 4K present loop ~30 s later; the earlier run of the same title had done the same at t≈229 s. `read_bytes` counts bytes fetched **from the block device**, and the second run's assets were already in the page cache from the first, so a process reading hard showed zero. The tell was in the same sampler, in the column nobody was reading: `syscr` — read *syscalls* — climbed 38 → 378 → 680 per 20 s while `read_bytes` stayed flat at 0. *Recognition cue:* **a "no I/O" verdict on a second run of anything you just ran**, or on any box with RAM to spare. *Fix:* treat `read_bytes` as a *lower* bound on I/O and never as the liveness signal; key liveness on `syscr`/`syscw` plus CPU delta, and print all three so the disagreement is visible rather than averaged into one verdict. Generalises the family in traps 122 and 190: this instrument was loud, self-consistent, and validated — its controls all passed, because none of them had a warm cache. A control that cannot reproduce the subject's *state* validates the machinery, not the domain. #2867. |
| 217 | **`gdb`'s backtrace through GUEST frames is fiction — no CFI, so it stack-scans, and the plausible-looking chain it prints can name a function that cannot be on the stack** | Khazan (`PPSA20447`) lane, 2026-08-22. A boot stalled with all 79 threads blocked in a syscall. `gdb -p <pid> -batch -ex 'thread apply all bt'` gave the guest main thread as `k_usleep` <- `eboot+0x7964f09` <- `eboot+0x15657da`, which reads like a call chain and is not one: **`eboot+0x7964ee0`, the function containing `0x7964f09`, has no sleep call anywhere in it** — it calls one function, tests the result against `-1`, and returns. It is the function the loop CALLS at `0x796eb5f`, one instruction before the `sceSaveDataGetEventResult` call -- a callee that had already **returned**, whose frame gdb then scanned and reported as a caller. Stale stack residue reported as a call chain. Host frames in the same backtrace were correct and symbolised, which is what makes it convincing: the top four frames (`__syscall_cancel_arch` -> `nanosleep` -> `prosper::k_usleep`) are real, so the two below them inherit their credibility. *Recognition cue:* **any `??? ()` frame under a prosper symbol** — gdb has run out of unwind information exactly there and everything past it is a guess. *Fix:* do not read guest frames off gdb. Scan the HLE handler's own frame for guest code addresses instead (`__builtin_frame_address(0)` + `prosper::guest_va_in_module_code`, the technique `hle_file.cpp`'s `[apr] guest-ra[N]` diagnostic already uses); it filters by range rather than guessing, is stable across repeats, and here produced a chain whose first frame disassembled straight into the real wait loop. Cheap corroboration that a chain is real: print it on several separate calls and require it to be identical. #2908, #2909. |
| 218 | **`grep -r` without `-a` returns a confident, clean ZERO on a pattern that is present -- binary files are skipped silently, so a whole-corpus census reads as a negative result** | Khazan (`PPSA20447`) lane / PR #2910 review, 2026-08-22. A blast-radius census asked which of 54 game dumps import the NID `j8xKtiFj0SY`. `grep -rl 'j8xKtiFj0SY' PPSA*-app0/` returned **zero hits across every dump**; the NID is in **22** of them. `grep -c` on one eboot prints an empty line where `grep -ac` prints `1`. Nothing warns: there is no "binary file matches" notice with `-l`/`-c`, the exit status is an ordinary "no match", and the result is exactly the shape a real negative has. The same census then failed a **second** way for a reason `-a` does not fix: it globbed `eboot.bin` only, and every Unity title's save code lives in `Media/Plugins/SaveData.prx`, so 16 carriers were structurally invisible -- including four reviewed rung-6 guards (`PPSA24651`, `PPSA13579`, `PPSA02664`, `PPSA25009`). The published figure was 5 of 54. *Recognition cue:* **any grep over a dump, a binary, or a `.prx` that returns a round zero** -- and any census whose file list is a single well-known filename. *Fix:* always `-a` when grepping dumps, enumerate `eboot.bin` **and** every `.prx`, and **validate the instrument on a known-positive before believing a zero** -- one dump you have already proven carries the pattern turns a silent-skip into an immediate, loud failure. This is the trap-122 family with the loudness removed: not a control that passed for the wrong reason, but no control at all behind a number that looked decisive. #2910. |
| 219 | **A reject line that prints the DECODER'S DEFAULTS for the field it is refusing — `[recompile-reject] ... sdwa=6/0/6/6` described an instruction that was not there** | Beast of Reincarnation lane (`PPSA29343`), 2026-08-22. The title's scanout pixel shader was rejected on `words=7e040ef9,00061402 fmt=7 op=0x7 ... modifier=1 dpp=0 sdwa=6/0/6/6`. `6/0/6/6` is `dst_sel=DWORD, dst_unused=PAD, src0_sel=DWORD` — a **semantically inert** SDWA wrapper — so the line reads as "the wrapper does nothing, therefore the opcode itself is unimplemented", and `v_cvt_u32_f32` is not a plausible thing to be missing. Both halves were wrong. `llvm-mc -arch=amdgcn -mcpu=gfx1030 -disassemble` on the two printed dwords gives `v_cvt_u32_f32_sdwa v2, v2 dst_sel:WORD_0 dst_unused:UNUSED_PRESERVE src0_sel:DWORD` — `4/2/6`. The opcode was implemented; the **WORD-destination shape** was not. The mechanism: `decode_operands`' VOP1 SDWA arm assigns `sdwa_dst_sel`/`sdwa_dst_unused`/`sdwa_src0_sel` only inside the per-opcode branches that **accept** a shape, so those fields do double duty as "what the instruction says" and "what the recompiler agreed to model", and a refused instruction keeps the struct defaults — which the reject printf then prints as though they had been decoded. *Recognition cue:* **any diagnostic field that is also a decision, printed on the path where the decision was NO.** The give-away here is that the same line carries `modifier=1` (a modifier word is present) beside selects that say the modifier does nothing — an internal contradiction, if you read both. *Fix:* re-derive the fields from `in.words[1]` at the reject site for display only, and leave the accept-only assignment alone — relaxing the decoder is what keeps `has_modifier` honest. Same family as trap 122: the instrument was loud and specific, and the specificity is what made it credible. #2912, #2917. |
| 220 | **A negative input-gating result, read as a property of the TITLE rather than of the DEPTH it was measured at -- and measured on an ASSET ORACLE, so it could not have seen the thing it was quoted against** | Sonic Origins lane (`PPSA05325`), 2026-08-22. On 2026-08-06 a ~12,000-read input probe was run against this title and recorded, correctly, that its post-logo state *is not input-gated*: the probe arm resolved the same 162 APR paths as an input-free arm and neither ever requested `ui_mainmenu*`, `stage_title*` or any `.rsdk`. That row then sat in `GRIS_SONIC_COBRA_BRINGUP.md` for sixteen days as the reason nobody re-tried input, while three fixes (#2571, #2901, #2910) moved the boot four screens further on. On current master a **default launch reaches the game's own "This title supports auto save." modal at t=200 s and holds it to the end of a 420 s run** -- ten consecutive samples at a per-pixel mean delta of 0.03-0.12/255, `guest=running`, 39 of 42 samples pixel-distinct. Its only control is a `[X] Close` button. **One `cross` reaches the title screen, 2 of 2 runs.** Two compounding faults, and the second is the transferable one. (a) The finding was scoped to a state that no longer existed, and nothing in its wording said so. (b) Its instrument was a **resolved-path set**, not frames -- so it could only ever answer "did input change what the loader asks for", and a modal that is fully loaded and merely waiting changes nothing about the loader. The oracle it was quoted against (`stage_title.pac`, `ui_mainmenu.pac`) is *downstream* of the button, so its absence was the predicted consequence of not pressing it, re-read as proof that pressing it would not help. *Recognition cue:* **a negative result about input whose evidence is anything other than pixels, and any `## Ruled out` row about input that does not name the boot depth it was taken at.** *Fix:* date-and-depth every negative input row, and re-run it after any fix that moves the boot; when frames are still changing, the wall is not a wall. Fourth title here where input mapping impersonated a rendering wall, after `PPSA30490` (skip was Triangle), `PPSA19991` (OPTIONS gates, Yes/No defaulting to No) and Hi-Fi RUSH (confirm cursor starts on the right-hand option). #1871, #2919, #2920. |
| 221 | **A grep for a log tag that the code does not print, whose clean zero was reported as a property of the TITLE** | pad-routes lane (BALAN WONDERWORLD, `PPSA02058`), 2026-08-22. Asked whether BALAN's opening cutscene was engine-rendered or a decoded movie, the lane grepped the run log for `videodec\|videodec2\|sofdec\|criware\|ajm` **as bracketed log tags**, got nothing back for the video ones, and wrote *"the boot logs no video decode of any kind, so these are real GPU draws of a real 3D scene"* -- into a commit message, `COMPATIBILITY.md`, `BLOG.md`, the survey doc, a tracker comment, a route header and a route README before it was checked. **The tag prosper prints is `[vdec2]`.** The same run log holds two `access-unit H.264 decoder opened (VA-API)` lines, a `first picture: 3840x2160 NV12`, and 3072 access units / 3070 pictures, all beginning at t=125.6 s -- and every frame the lane had called an in-engine 3D scene postdates that line. What caught it was re-running the grep **unanchored**, which returned 664 `criware` and 339 `movie` hits the bracketed form had silently dropped; those turned out to be `.ini` paths, but they proved the pattern, not the title, had produced the zero. *Recognition cue:* **a negative claim resting on a grep whose pattern was written from memory of what the tag "should" be rather than copied from the emitting code** -- and the tell that it is dangerous rather than merely wrong is that the conclusion was the flattering one (in-engine 4K draws is a better result than a decoded movie), so nobody wanted to re-check it. *Fix:* before believing a clean zero from a log grep, `grep -rn` the **emitter** in `src/` for the literal it prints, and run the same pattern against a positive control -- here, any title known to decode video. Same family as traps 122 and 218: the instrument could not express the case, and its silence read as the title's. |
| 222 | **`pgrep -x a b c` — the multi-pattern "is the GPU free?" check — is a USAGE ERROR that never answers the question, and the common `\|\| echo "GPU free"` spelling prints the reassuring answer ON the error** | Shared-box run hygiene, 2026-08-23. `pgrep -x` accepts exactly **one** pattern; given three it prints `pgrep: only one pattern can be provided` to stderr and exits **2**. So `pgrep -x prosper-app screenshot boot_trace` has never once reported whether a peer lane was mid-run — and the two idioms built on it both fail *safe-looking*: `pgrep -x a b c 2>/dev/null; echo "(none above = free)"` prints nothing and reads as a clean box, while `pgrep -x a b c \|\| echo "GPU free"` prints **"GPU free"** precisely because the command failed. Note this is the opposite failure direction from the `ps \| grep` pair in the counting rule above: those miscount a check that *ran*, this one never runs at all, so no amount of scrutinising the number helps — there is no number. *Tell:* the check emits **nothing** on a box where you know something is running; run it once without `2>/dev/null` and the usage error is immediate. *Fix:* one ERE argument — `pgrep -x 'prosper-app\|screenshot\|boot_trace\|screenshot_snap'` — or the `for` loop already given under *Count with `pgrep -x`*, which is correct because it passes one name per call. Include `screenshot_snap` either way (see the note above): a `snapshot.py` run is invisible under the other four names. Recorded because it propagated into roughly ten lane briefs before anyone ran it without stderr suppressed, and because a GPU-free check that cannot fail loudly is worse than no check — it authorises the run it was meant to gate. Cousin of traps 88/100/135: those mislead about *which* process or *how many*, this one silently declines to look. #2948 |
| 223 | **A pass/fail rate that drifts machine-wide over minutes, so an A/B measured as "arm A for N runs, then arm B for N runs" is VOID rather than negative** | renderer-determinism lane (#2945, BALAN WONDERWORLD `PPSA02058`), 2026-08-23. One frozen `.prgbundle` replayed offline by `gpu_replay` fails intermittently, and the failure rate of the **identical command with the identical binary** ranged from **0 of 20** in one ten-minute window to **12 of 12** in the next, and back, all session. Interleaved arms flip together, which is the tell. Six separate "levers" were found and then unfound this way: `PROSPER_SERIAL_DRAW_REALIZE` measured 19/20 stable against a 10/20 baseline and is indistinguishable interleaved; `RADV_DEBUG=zerovram`, `RADV_DEBUG=nodcc` and `PROSPER_NO_BACKEND_PERSISTENT_TEXTURES` each measured 4-distinct-of-10 against a 10-of-10 baseline. What exposed those three was not suspicion but arithmetic: **two different levers produced byte-identical 10-run hash sequences**, which no real mechanism explains. *Recognition cue:* **any A/B on this class where the two arms were not run alternately, and any result whose arms were minutes apart** -- and note the second-order hazard, that a lane which knows the rate drifts will still trust a *large* difference measured sequentially. *Fix:* interleave run-by-run inside one loop, quote n and the modal fraction rather than a distinct-hash count, and re-run the baseline arm in the same loop even when you think you already know it. Same family as trap 122 (a control drawn from the source it validates): here the control is drawn from the same *time window* as the null. #2945. |
| 224 | **`cmake --build --target X` rebuilds ONLY X, so an A/B run against a different frontend executes the PRE-FIX binary — and its unchanged failure reads as a clean falsification of the fix** | Metaphor lane (`PPSA20800`), 2026-08-23. With a one-constant allocator fix in the tree, `--target boot_trace` was used to shorten the rebuild. `boot_trace` then showed the title getting six seconds and 63 frames further — and the next two runs, made with `tools/screenshot` to capture those frames, reproduced the ORIGINAL crash byte-for-byte: same faulting rip, same thread name, same non-canonical pointer, same corrupted memory window. That is exactly what a real falsification looks like, and the arm was seconds away from being written up as one — a lever tested in the same run (`PROSPER_NO_BACKEND_PERSISTENT_TEXTURES=1`) would have been recorded as having *restored* the bug. `screenshot` had simply never been relinked. *Recognition cue:* an A/B whose two arms used **different executables**, and whose "before" arm reproduces the old symptom with suspicious exactness — a genuine partial fix usually moves *something*, while a stale binary reproduces the baseline perfectly because it IS the baseline. *Fix:* after any source change, run `cmake --build <dir>` with **no `--target`** before any run you will quote, or check the frontend's mtime against the object you changed. Sharper than the parenthetical in trap 40 (a restored-but-unrebuilt test source), because here the build SUCCEEDED, the changed target really was rebuilt, and the stale one was a peer executable nothing in the build output mentioned. #2934 |
| 225 | **GPU load from another process INDUCES #2945's failing regime on demand — so a quiet-box null is weak evidence, and the prescribed free-GPU check cannot see the load that matters** | #2945 lane, 2026-08-23. Trap 223 recorded that this defect's rate drifts machine-wide over minutes and prescribed interleaved arms; this gives you a lever over the drift instead of waiting for it. Measured with `tools/vkprobe` on Linux/RADV: three heavy `gpu_replay` full-submit replays flipped a passing box into failing **within one round** of a 3-second detector loop, and it recovered ~15 s after they stopped — repeatable, and a second `vkprobe` used purely as load reproduces it more weakly, so the load need not be prosper. **The converse does NOT hold, and this is the half that was wrong when the row was first written:** an independent reviewer measured 13/20 and 8/20 failing on a box with no `prosper-app`, `screenshot`, `boot_trace`, `gpu_replay` or `vkprobe` running, verified through `/proc/*/fd`. So load is **sufficient, not necessary**, and a clean arm on a quiet box is weak rather than a clearance — the honest reading of any 0-of-n here is *undecided*. *Recognition cue:* an arm whose good half ran on a quiet box and whose bad half did not; and any n quoted without its wall-clock span, since 400 runs inside 173 s are one drift period rather than 400 samples of the thing that varies. *Fix:* put deliberate GPU load beside the measurement, quote the span as well as the n, and pair the arms inside one process so both meet the same window. **And do not trust the free-GPU check to tell you the box is quiet:** `pgrep -x 'prosper-app\|screenshot\|boot_trace'` — even spelled correctly per trap 222 — matches none of `gpu_replay`, `vkprobe`, `ctest` or a peer's test binaries. #2945 |
| 226 | **A correction that ADJUSTS a number instead of re-deriving it inherits the error it is correcting — and arrives wearing a reviewer's authority** | Metaphor lane (`PPSA20800`), 2026-08-23, #2951/#2956. A code comment enumerating an ABI said the guest read **four** fields of a struct. Independent review re-disassembled the range, found a field the list had missed, and corrected the count to **five** — by adding one. Re-review found the range holds **four read instructions covering six distinct 32-bit fields**, because two of the reads are qwords: neither the original number nor the correction counted anything that exists. The author verified the *new field* against the disassembly (correctly, per the charter's citation rule) and did not re-verify the *total*, because a total is not what a citation points at. **The repair is structural, not arithmetic: list the instructions and let the reader count.** A stated total is the thing that drifts; an enumeration cannot. Generalise it — whenever a review corrects a count, a size, a rate or a percentage, re-derive the quantity from the source rather than applying the delta, and prefer publishing the raw observations over the aggregate. |
| 227 | **prosper's `0xaf` poison fill reads as a float that PASSES a near-zero assertion, so a test checking `fabs(x) < eps` cannot tell "written 0" from "never written"** | Metaphor lane (`PPSA20800`), 2026-08-23, #2951/#2956. `0xafafafaf` reinterpreted as IEEE-754 float32 is **-3.196e-10**; as float64, `0xafaf…af` is -5.3e-79. Both sail through any "close to zero" tolerance. A test asserting a glyph landed at the surface origin therefore **passed against a buffer nobody had touched**, and only a mutation arm that removed the writer exposed it. This poison is unusual in that respect and that is what makes it dangerous — every other common fill fails such a check loudly (`0xcd` → -4.3e8, `0xcc` → -1.1e8, `0xdd` → -2.0e18, `0xfe` → -1.7e38, `0xbaadf00d` → -1.3e-3). **Assert on the raw bits, not the decoded float, whenever the field's untouched state is the thing you are ruling out**, and never give a byte-fill sentinel and a float-value sentinel the same name. Recorded again in `docs/VERIFICATION.md` where a test author will meet it. *(The -3.196e-10 above was itself published as "-3.2e-13" in the review, the PR and a shipped code comment before anyone recomputed it — trap 226, same day, same PR.)* |
| 228 | **A fixed READBACK WINDOW over a variable-position record — a CORRECT fetch reads back as all-sentinel and is indistinguishable from "the thing under test never ran"** | #2961 lane (RADV STRIX_HALO), 2026-08-23. `vkprobe`'s index-readback VS stores `records[16 + gl_VertexIndex]`, but the readback was taken at a fixed `--readback-dwords 16:3`. For identity indices that window is right; for `[3,4,5]` a **correct** fetch writes dwords 19–21 — so the fixed window read all-sentinel ×300 and the draw was written up as "RADV hands sequential vertex ordinals; the index buffer is ignored" (issue since closed falsified). The identity-list control could not discriminate: both hypotheses agree on `[0,1,2]`, so it proved the harness *drew* while saying nothing about *fetch*. An independently written bare-Vulkan probe (`tools/index_fetch_probe`, zero shared code) fetched correctly on the same box under Mesa 26.1.4 and 26.2.1, and re-running vkprobe itself with the window moved (`--indices 3,4,5 --readback-dwords 19:3`) printed `[4,5,6]` ×300 — the driver answering correctly through its own accuser. *Tell:* when records are keyed by OBSERVED VALUES, the reader must be positioned by those same values; a window pinned by one hypothesis only ever samples inputs where both hypotheses coincide. *Fix:* derive the window from the list (`--readback-dwords 19:3`), or key records by payload like the probe does — and treat "instrument agrees with me on the cases it was built for" as zero information about the others. |
| 229 | `gpu_replay --bundle` (offline `.prgbundle` replay), used to test an INDEX or VERTEX decode hypothesis | **A bundle replays PRE-DECODED draws, so an instrument upstream of the decoded draw never fires and its silence reads as a result about the guest.** The offline bundle is the project's recommended fast loop for a rendering bug, and for *which draw wrote this pixel* it is exactly right — but index and vertex decode happen before the draw is stored in the bundle. Measured on Tomb Raider (`PPSA16901`, #2990): the same `[idxtype]` diagnostic emitted **0** lines under `--bundle` and **508,688** on a live run of the same route. Nothing in the replay output distinguishes the two cases — it reports `operations=75/75` and a complete temporal closure either way — so the tool looks like it is answering. Test decode-path hypotheses **live**; keep the bundle for questions about the draws themselves. |
| 230 | **A screenshot of a game's own PRE-RENDERED LOADING PICTURE, mistaken for the rendered 3D world** | The most convincing false-progression evidence this project has produced, and it survived a PR, a `BLOG.md` entry and a merge. *Tomb Raider I-III* (`PPSA16901`, #2990) blits a full-screen 1920x1080 picture between levels; a capture of that moment shows Croft Manor through a brick arch in photographic detail — brickwork, leaded windows, the dolphin fountain, grass, individual leaves — and was published as *"Tomb Raider's world is textured"*. It is `2/PIX/HD/MANSION.DDS` blitted to the screen: pixel-compared against the checked-in capture it scores **mean abs diff 0.02/255 with 100.00% of pixels within 8/255**. The world was untextured in the pre-fix "before" shot this was published beside — but that shot came from the SAME commit, so it says nothing about the post-fix state, and a later capture shows textures do reach world surfaces. The blit argument stands on its own and needs no claim about what the world looked like afterwards. **The tells are all in the picture and all misleading in the same direction:** it looks *better* than the emulator could plausibly render, which reads as success rather than as the warning it is — soft shadows, ambient occlusion, depth of field, per-leaf foliage. It was even paired with a genuine untextured 'before' shot, so the A/B looked rigorous while comparing a 3D render against a 2D blit. **Check any unusually good frame against the dump's own picture assets before publishing it**: run `python3 tools/evidence/prerender_check.py <shot> <DUMP_ROOT>`, which does exactly this and exits 2 on a match. **A DOMINATED verdict is not automatically a retraction**: the difference is in the CLAIM, not the pixels. "The world is textured" is an assertion about 3D rendering that a blit cannot support at any overlap; "the title screen renders" is *supported* by a correct blit, because that is how a title screen renders. `tomb-raider-title-screen.png` (95.94% of `TITLE_EU.DDS`) and `tomb-raider-title-screen-tr2.png` (94.87% of `TITLE_US.DDS`) are DOMINATED, correctly captioned, and deliberately kept (the real one scores near zero and nothing else comes close — next best here 50.86; the 52.77 first reported came from an earlier luma metric and is corrected here, since the tool now compares per-channel maxima). A loading screen is rung-1 evidence at most, since displaying a 2D image requires no world rendering at all. |
| 231 | **A claim that arrives already labelled as checked — a reviewer's finding, a cited file:line, a "derivable" bound** | The charter's authority-transfer section records that a cited claim reads as a verified one. This adds what that section does not say: **which claims actually get questioned, and how little that depends on care.** Measured over one review (#3060 / #3077): two of a reviewer's phrases reached the author's tree underived — an analytic bound stated as `63.75` (the true bound is `69.75`; it dropped the matching pixels' own contribution, `0.75 x 8 = 6.00`) and the claim that real assets "cannot saturate" it (a caption panel alone reaches **85%** of the bound, and adding an ordinary colour-conversion shift takes it to **94%**). Both were caught by the same question asked twice about successive revisions of one paragraph — *does the replacement overstate in the other direction?*, then *in some third direction?* — and **not by the ordinary review pass over the same text: the reviewer approved the very commit that ADDED both `63.75` lines (`0e5d1ec6`, twice), two rounds after supplying the number.** A review read the diff introducing the wrong figure and passed it. **The uncomfortable part is what that question was aimed at.** Not the reviewer's numbers: both times it was aimed at the AUTHOR'S OWN rewrite. The reviewer's claims were re-read only because they happened to sit inside a paragraph being rewritten for an unrelated reason. **Two accidents were required: a background probe the reviewer had written off as dead completed anyway, and the error it surfaced happened to sit in the SAME PARAGRAPH as the bound.** The second carries the argument — had that count error been in another file, the bound would never have been re-read. Had the count been right, nobody would have reopened that paragraph and `63.75` would still be in the tree wearing the label *"derivable, belongs to nobody's sample"* — the strongest label a number can carry here, and the one that most discourages anyone from deriving it. So: **(1) ask the adversarial question exactly where a claim arrived pre-labelled as checked**, since that is where it feels least necessary; and **(2) compute any load-bearing quantity twice, by independent routes, and compare — this is the only one of the two that fires without anyone suspecting anything.** A third wrong figure from the same review never propagated at all because its author computed it two ways, got 43.6% and 1.27%, and the disagreement exposed an int16 overflow in their own numpy: caught before publication, where the questions caught theirs two rounds after. The first draft of this entry said three phrases propagated. The record said two. |
| 232 | **A batch edit script that fails ATOMICALLY while reporting PER-EDIT — so a commit message describes work the tree does not contain, and every gate stays green** | #3073 lane, 2026-08-27. A script applied two edits to one file: a comment rewrite and a `SDL_Log` fix. The second `assert` missed its anchor, so Python aborted the whole script *before writing anything* — and the traceback names only the failed anchor. Read as "the second edit did not apply", it is really "neither did". I fixed the log line separately by hand, then committed both under the subject *"scope the mechanism to the measured regime, **and** one record per line"*. Only the second half existed. **Every gate was green and none of them could have seen it:** the build passes (comments), 247/247 tests pass (comments), `diff --check` passes, CI passes, and the diff *is* internally consistent — it simply omits a change the message claims. An independent reviewer caught it by grepping the phrases the author's own report quoted (`while it is behind` 0, `REMAINDER` 0, `keeps up` 0). *Why it matters more than a stale comment:* `git log` then records the correction as **done**, so nobody re-derives it — the exact failure mode the correction was fixing, one layer up, and the commit message is the layer no test reads. *Tells:* a script with more than one `assert`-guarded edit and a single write at the end; a traceback you did not read to the last line; a commit message enumerating changes you applied in a different order or session than the commit. *Fix:* write each edit separately and **verify by the artefact, not the intent** — grep the file for a distinctive phrase from every claim the message makes, which is two commands and is what the reviewer did. Never let a multi-edit script's success message print before its write. |
| 233 | **`tools/screenshot`'s `source-distinct=N` footer, read as "N different PICTURES were published"** | #2932 lane (Unbound `PPSA03274`, RADV STRIX_HALO), 2026-08-29. The run footer prints `source-distinct=80 pixel-distinct=3` beside a present-layer line reading `119 distinct of 27227 published`. Read naturally, those say the renderer published many different frames while the capture only ever saw three — a flat contradiction, and the obvious explanation is that the readback path is reading a stale or wrong buffer. **It is not a contradiction, because the two counters do not measure the same thing.** `source_advanced` is `observation.source != previous_.source \|\| observation.source_seq != previous_.source_seq` (`tools/screenshot/capture_manifest.cpp:186`) — **buffer identity and sequence number, never content**. It goes up once per sample whenever the renderer hands over a fresh buffer, so on any live run it equals the sample count and carries no information at all. Only `pixel-distinct` compares bytes. *Why it is worth a row:* the word `distinct` appears in both lines, one of them IS content-based (`frame_content_signature`, which by contract cannot report identical bytes as new — `present_frame_rate.hpp:109`), and they sit two lines apart in the same footer. I had written the mismatch up as evidence for a harness defect before checking the definition. *Fix:* quote `pixel-distinct`, or the per-sample manifest's cumulative `distinct_frames`, for any claim about content; treat `source-distinct` as a liveness signal only. *And the payoff for reading it right:* that per-sample `distinct_frames` is cumulative and content-based, so its increments DATE each change — which measures a duty cycle directly instead of estimating it from how often a sampling grid happens to land on content. On this title the grid said 9%; the counter said **0.3%**. |
| 234 | **A SAVE DIRECTORY left at the shared default location by an earlier run — the title resumes into it and renders nothing, which is indistinguishable from a composite defect** | #2932 lane (Unbound `PPSA03274`, RADV STRIX_HALO), 2026-08-29. Every routed run of this title went black ~12 s after the first press and stayed black for the remaining 385 s. It survived every check I put on it: the guest was alive and flipping at 29/s, the renderer produced 60 frames/s with the GPU ~90% busy, an F9 bundle reproduced the black frame offline byte-for-byte, and **both present paths agreed** (`tools/screenshot`'s forced readback and `prosper-app`'s shipped GPU present), which is exactly the evidence that rules the harness out and points at the guest. It was a `PPSA03274` directory under `~/.local/share/prosper/savedata0/` dated three days earlier. Matched A/B on one commit: **4 of 20 samples carried content with the shared save, 17 of 20 with a fresh `PROSPER_SAVE0` + `PROSPER_SAVEDATA_DIR`** — and the clean arm plays the entire intro cinematic in full colour. *Why it is vicious:* it is **stateful across runs, lanes, titles and weeks**, so it does not reproduce for whoever wrote the route and does reproduce forever for whoever inherits the box; it is invisible to `git status`, to the run log and to every diagnostic; and it makes a WORKING title look like a broken renderer rather than the reverse. It also manufactures a **phantom regression**: the route README recorded a visible cinematic on an older commit, so I built that commit — and it reproduced the black, because it read the same stale save. Hours went into a bisect range that could never have contained a culprit. *Tell:* a title that renders correctly with NO input but collapses shortly after the first press, wherever that press falls (shift the press, the collapse shifts with it). *Fix:* isolate BOTH save variables per lane, as the DQ7 routes do; and when a route's recorded result will not reproduce, check what the previous run left on disk before suspecting the code. |
| 235 | **Reading LOG LINE ORDER as a timeline — stderr from different threads is interleaved, not ordered** | #3142 (Stray `PPSA02101`), 2026-08-30. I published "the pak read completes **1,428 log lines before** the sample, so the memory is still zero after a successful write" and built a whole investigation on it. Line distance is not a happens-before relation: the APR reader and draw realization are different threads writing to one stderr stream, so their interleaving says nothing about which executed first. The claim had to be withdrawn — and the retraction mattered, because the two candidate mechanisms it conflated ("the data is zeroed after the write" vs "the sample preceded the write") are different defects with different fixes. *Why it is vicious:* 1,428 lines of separation FEELS like overwhelming evidence, and the bigger the gap the more convincing the wrong inference gets; a two-line gap would have been questioned. *Fix:* stamp a shared monotonic reading on both diagnostics — `prosper::diagnostics::diag_now_us()` (`src/diagnostics/diag_clock.hpp`) exists for exactly this and needs no anchor, since `steady_clock` is process-wide. With it the same pair measured **+62.351 s** on the run that produced this trap, and **+65.735 s** on a later re-run with the corrected byte-identity verdict — two runs, not one figure quoted twice, which is the distinction a durable row has to make. Either settles the ordering the log never could. |
| 236 | **A verifier that compares POPULATIONS instead of CONTENT — it reports MATCH on entirely different bytes** | #3144 review, 2026-08-30. `PROSPER_APR_VERIFY` read a guest destination back and compared it against its source by counting non-zero dwords per window and testing `src_nz == dst_nz`. That is not the property anyone wanted: a destination holding completely different bytes reports MATCH whenever the totals agree, and a loss in one window cancels against a gain in another. On the strength of it I published "byte-exact when written" on #3142 — a claim the instrument had never tested. Caught in review, not by any run, because **every arm was green**: 21,217/21,217 MATCH, and the positive control fired correctly, since zeroing a window does change its count. *Why it is vicious:* a population check passes every test you would think to write for it, including a mutation arm — the mutation has to be content-preserving to expose it, which is precisely the case nobody constructs. It is also strictly weaker than the obvious alternative while looking more thorough, because it prints numbers. *Fix:* `memcmp` when both buffers are in hand, and keep the counts only as detail. Related: a zero-valued poison in a control is undetectable wherever the source is already zero — poison with a pattern, and assert the poison landed. |
| 237 | **A PROFILING HARNESS whose own readback is the workload you are measuring — `tools/screenshot` inflated tiling work 25x** | #3150 (Stray `PPSA02101`), 2026-08-31. A `PROSPER_TILECENSUS` sweep of the title-screen phase showed 158,381 MiB of tiling with `tile_surface` (writeback) apparently dominating at ~41%, which reads as a second, writeback-bound bottleneck sitting behind the one being fixed. It is the harness. `tools/screenshot` never calls `set_gpu_present_active`, so it copies every scanout frame back to the CPU, and that readback generates the tiling it then measures. The same phase under `prosper-app` (GPU-present, the shipped path) moves **6,254 MiB** — 25x less — and the row under investigation is unchanged between them. **A census taken under `tools/screenshot` is a census of `tools/screenshot`**; re-measure any tiling, copy, or readback figure under `prosper-app` before believing it, and note this is the same harness the charter already warns about for FPS. The finding was one command from being filed as an issue. |
| 238 | **A LINKER KILLED MID-WRITE leaves a truncated binary with a FRESH mtime — the build then reports success about a file it never rebuilt, and the damage surfaces as unrelated TEST failures** | #3150 (Stray `PPSA02101`), 2026-08-31. A background build task was killed by the harness, leaving orphaned `ld.bfd` processes wedged writing to a pipe whose reader was gone. Clearing them by PID left six test executables **88 MB and non-executable** — `ld` had not yet set the exec bit. Their mtimes were newer than their inputs, so the next `cmake --build` printed `[100%] Built target ...` and skipped every one of them. The failure then appeared two steps away from its cause: `ctest` reported **6 failed / 315** with `BAD_COMMAND`, all on *render* tests, which reads as a graphics regression from the change under review rather than as a build artifact. `rm` the six and relink → 315/315, no code change. **After killing a build, verify the artifacts rather than the build's own success line**: `find <build> -type f -perm -u+x -newer <a source>` or simply `ls -l` the targets — a build system cannot detect a file it decided not to rebuild. Related trap: the same kill also produced a `(Failed)` grep that returned 0 on a run with 6 failures, because ctest writes `***Failed` and `The following tests FAILED:`, never parenthesised. |
| 239 | **A MUTATION TEST that never ran, reported as a dead guard — a same-size edit leaves `__pycache__` valid** | #3153 review, 2026-08-31. Mutation testing a Python module by rewriting it and re-running its test file: `return None` -> `return True` is the **same number of bytes**, and CPython validates cached bytecode on `(mtime, size)`. Rewritten inside the same second, both match, so the interpreter re-imports the UNMUTATED bytecode and the mutant never executes. The arm then passes, and the reader concludes the test does not discriminate. **The failure direction is the dangerous one**: it reports a LIVE guard as dead, inviting someone to delete or weaken a test that works. A reviewer hit this while checking another lane's mutation claims and was one step from reporting a true claim as false; clearing `__pycache__` between arms gave the correct answer (1 failure, not 0). **`rm -rf __pycache__` between every mutation arm**, or copy the tree to a fresh path per arm. The same hazard applies to any cache keyed on size and coarse mtime, which is most of them. |
| 240 | **`gh pr view <N> --comments` FAILS on this repo and prints nothing — while `--json comments` on the SAME command works** | #3160 review, 2026-09-01. A reviewer established a negative ("this figure appears nowhere in the PR or its comments") by grepping the output of `gh pr view <N> --comments`. That command **exits 1 with 0 bytes on stdout** here, emitting 206 bytes of `GraphQL: Projects (classic) is being deprecated ...` to stderr instead — so the grep searched an empty string and the negative was guaranteed before it ran. Measured the same minute on the same PR: plain `--comments` rc=1 / 0 bytes; `gh pr view <N> --json comments --jq '.comments\|length'` rc=0 / **2**; `gh api repos/:owner/:repo/issues/<N>/comments --jq length` **2**. **The `--json` variant of the same subcommand succeeds**, which is what makes this insidious — two near-identical invocations, one silently empty, and the failing one is the shorter and more natural to type. Same family as the known-broken `gh pr edit` (also Projects-classic GraphQL), so treat any non-`--json` `gh pr` read here as suspect. **Use `--json`, or `gh api`; and never assert a negative from a command whose exit code you did not check** — the reviewer who hit this had raised a finding of exactly that shape earlier in the same review. |
| 241 | **A REJECT CENSUS that names an INSTRUCTION reads as a missing lowering — the field that says otherwise was only printed on the sibling path** | #3137 (Stray `PPSA02101`), 2026-09-01. `[recompile-reject] mode=unresolved-operand pc=57 words=e00c2000,6a010000 … stage=vertex` names a `buffer_load_format_xyzw … idxen`, and an issue was opened proposing the two things that line can support: a missing MUBUF lowering, or a stage arriving with no resource table. Neither was true — the opcodes are lowered and `[buf-op] … rt=1 reject-unresolved` shows five resources present — and the actual cause is the #305 user-data window, three files upstream. The trap is not that the line is wrong; it is that the FORMAT path printed strictly fewer fields than the RAW path (`[mubuf-raw-unresolved]`) beside it, omitting `rewritten=` and `pc_res=`, which are exactly the two facts that separate "the table has no entry" from "provenance was correctly suppressed because the shader assembles its own descriptor". Settling it needed the game re-run under `PROSPER_DYNTRACE_FAIL=1` plus `PROSPER_UDPROV=1`. **When two sibling diagnostics report the same class of failure, diff their format strings before trusting the poorer one** — an instrument's silence about a discriminator is not evidence that the discriminator does not apply. Both fields are printed now. |
| 242 | **A PR with MERGE CONFLICTS gets no CI runs at all — which looks exactly like Actions being broken** | 2026-09-01. A branch was pushed and **zero** workflow runs were created for the head SHA, while other branches' runs completed normally minutes earlier. Diagnosis went to Actions: permissions (`enabled=true allowed=all`), a `concurrency:` group (none), a `paths` filter (none), the workflow's triggers (`pull_request` with default types — correct), closing and reopening the PR, and finally force-pushing a fresh SHA. **All of it wasted.** The cause was one field: `gh pr view <N> --json mergeable` said **`CONFLICTING`**. Master had moved underneath while other PRs merged. A single rebase produced a run within a minute. Two independent lanes hit this the same afternoon; the second reported it as an "Actions backlog", which is the wrong conclusion from the same evidence. **Check `mergeable` FIRST when CI has not started** — it costs one command, and with several lanes merging, branches go conflicting constantly. The dangerous shape is not the delay: it is that "no checks reported" plus an approving review is indistinguishable from green to anything that counts failures rather than requiring a nonzero PASS count. |
| 243 | **Two builds against one build directory silently corrupt it — and the failure surfaces as phantom TEST failures, not a build error** | 2026-09-01, hit by three independent lanes in one afternoon. Symptoms varied and none named the cause: five Vulkan-execution tests (`indexed_render`, `multidraw_render`, `descriptor_array_render`, `gpu_execute`, `recompiled_shaders_render`) failing on a PR branch **and on `origin/master`**; a truncated `libprosper_core.a`; and a `build_rc=1` on a tree nothing was wrong with. In every case a **fresh `cmake -S prosper -B <new dir>` passed everything.** The master-too symptom is the trap's sharp edge: "it fails on master as well" normally EXONERATES the branch, so the reasoning feels rigorous while pointing at the wrong culprit entirely — one lane was a step from reporting a master regression that did not exist. Two ways in, both easy: running two builds against one build dir, and checking out a different branch in a worktree while a background build is still running there (the build is invisible at the moment you switch — it may not have printed anything yet). **A background build OWNS its worktree until it reports**, and verifying N PRs concurrently needs N worktrees. Before believing any surprising test failure, re-run it from a fresh configure. |
| 244 | **An instrument whose WITNESS is bound in one stage only, armed in another -- and its failure mode is indistinguishable from the answer you wanted** | Astro Bot, 2026-09-02 (#3193). `PROSPER_CFG_TRIP_BOUND` caps the CFG dispatcher's back edge and publishes a device-side hit record into the internal GDS buffer at descriptor set 0, binding 127. That buffer is bound, prepared and read back **only by the compute executor** -- `kComputeInternalGdsBinding` appears nowhere else in `src/` or `frontends/` except the compute test fixture. The selector, however, takes a program ADDRESS and happily accepts a graphics one, and the hang under investigation was in a **fragment** program. Arming it there would have emitted a storage-buffer declaration no graphics pipeline layout contains. **The damage is not the missing record.** A draw whose pipeline cannot be created never runs; the device then survives the run, and survival is precisely the signal the instrument exists to produce -- so the arm would have read as *"the bound fixed it"* while the bound never executed, which is trap 199's announced-but-inert shape with the announcement telling the truth and the CONSEQUENCE lying. Fixed by emitting the witness only in compute and printing, once per program, that a graphics arm is capped but publishes nothing, so a surviving device has to be corroborated by showing the draw still ran (`PROSPER_DRAW_PROGRAM_CENSUS`). *Recognition cue:* **a diagnostic that writes anywhere -- a buffer, an image, a counter -- has a binding contract, and that contract is usually owned by ONE execution path. Before arming it somewhere new, grep for who binds its destination, not for whether the selector accepts your argument.** The general form is broader than this instrument: a selector's acceptance is a statement about parsing, never about plumbing. #3193. |
| 245 | **A one-shot failure injection aimed at a site behind a process-wide CACHE never fires -- and stays armed into the NEXT test's control run, where it reads as that test being broken** | #3210, 2026-09-02. `test_render_object_create_failure` arms one Vulkan create site at a time and renders. The `shader-module` arm printed nothing, and two assertions in the *following* block -- a texture control run that should render red and stay silent -- went red instead. Neither symptom points at the cause. `render_draw_pass_rgba` calls `vkCreateShaderModule` only when the persistent graphics-pipeline cache MISSES, and the arm's own control run (identical shaders, identical state, run first, by design) had just populated that cache -- so the injected run never reached the site, the one-shot arm was never consumed, and it fired inside the next block's control render. **The control run that makes an arm trustworthy is the same run that can make it unreachable**, which is why this is not caught by adding a control. Recognition cue: a one-shot injection that produces NO report is not evidence the site is unreachable -- check whether it is still armed (`consume_...` returning true) before concluding anything, and disable any cache that sits between the render and the site. Generalises past pipelines: the same shape exists for the persistent texture-image, texture-binding, pipeline-layout and descriptor-set-layout caches in `render_runner.h`, each of which can skip the create an arm is aimed at. |
| 246 | **A diagnostic selector keyed on a resource's BYTE SIZE cannot tell a buffer from an image, and 3840x2160x4 collides with a great many things** | #3214, 2026-09-02. Hunting the producer of Astro Bot's 33,177,600-byte world-map light-record pool, `PROSPER_COMPUTELOG_SIZE=33177600` matched three compute programs -- and all three bound a *texture* of that size, because 3840 x 2160 x 4 bytes is the same number. The filter was not broken and its output was not wrong; it simply could not discriminate, and a reader who stopped there would have named three innocent programs. **Prefer a selector keyed on the guest ADDRESS** -- `PROSPER_COMPUTE_BINDS=0xADDR` named the real producer (`0x5006e8500`) in one run, with its outcome attached. Recognition cue: a size filter that returns several unrelated programs on a full-screen-derived size is answering "which resources are 4K-shaped", not "which resources are this one". The same hazard applies to any per-title size a screen dimension can reconstruct -- 8,294,400, 2,073,600, 129,600 and 32,400 all appear on this one route as both pixel counts and unrelated buffer lengths. |
| 247 | **`git stash` inside a dedicated per-agent worktree -- the one piece of state that the "work in your OWN worktree" rule does not isolate** | #3174, 2026-09-01. Two lanes (`fix/issue-3051-spirv-flat`, `fix/issue-3068-sem-init-return`) each did exactly what this project prescribes -- their own `.claude/worktrees/<slug>` tree, their own branch off one master commit -- and collided anyway, because a worktree isolates `HEAD`, the index and the working tree but **not `refs/stash`**, which is a single ref in the shared `.git` directory. Lane A stashed one file so it could rebuild without it and confirm a regression arm failed; lane B stashed in its own tree a moment later; A's `git stash pop` took the LIFO **top**, which was B's, applied an unrelated `hle_kernel.cpp` `sem_init` change into A's tree and **dropped B's entry** doing it. The measurement A was about to take would have run against a tree carrying a second lane's edit, under a banner saying only its own change had been removed. Nothing was lost only because **both** agents happened to recognise foreign content and recover by hand (`git cat-file -t` on the detached commit, then `git stash store`); a dropped stash commit is unreachable and GC-eligible, so the silent version of this loses another lane's work with no trace and mixes two issues into one branch. *Recognition cue:* **a `pop` whose diff touches a file you never edited** -- and, more generally, any shared repository state that the worktree rule is quietly assumed to cover. *Pinned:* the first arm of `tools/test_wt_stash.py` builds two linked worktrees by hand and asserts the collision still reproduces, so every later arm has a positive control. *Fix:* `tools/wt_stash.py` parks under `refs/worktree/prosper-stash/<slot>`, which `git-worktree(1)` documents as not shared, addressed by name rather than by top-of-stack and refusing an occupied slot instead of stacking. An alias cannot shadow `git stash` (measured), so the rule is written, not enforced. #3174 |
| 248 | **A watchdog whose verdict covers ONE 256-byte window, read -- and published -- as a verdict about a multi-megabyte range** | #3142, 2026-09-02. `PROSPER_ZEROWATCH` arms on the first non-zero 256-byte window of an APR payload and polls *that window*. Its line reads `dst=0x… size=13674533 … WENT-ZERO`, naming the whole payload size, and the verdict was carried into an issue and into a `## Ruled out` row as "the range is zeroed" / "the data is gone". Profiling 16 windows at the instant of the flip says otherwise: **11 of 14** destinations still hold most of their payload, and still hold it 45 s later; only 3 are genuinely all-zero, and one window that read zero at the flip read non-zero again at +10 s. What the guest clears is usually the chunk *header* -- window 0 -- which is exactly the window the arming rule always picks, so the instrument was aimed at the one place that always moves. *Why it survived so long:* every surrounding measurement was sound (the write-back verifier, the shared monotonic stamp, the `/proc/self/maps` dump, the `libc.prx` attribution) and each one investigated the flip rather than its extent, so the scope error was never the thing under test. It also passed a positive control: the sampler *does* report content when content is at that window. *Fix:* the verdict line now prints a 16-window profile against the SOURCE's own per-window census, plus re-profiles at +10 s and +45 s, so "the head went" and "the range went" can never again be the same log line. *General form:* when a watchdog samples a subset and names a superset in its output, the output is the bug -- state the sampled extent in the line itself, or somebody will quote the name. Cousin of 236 (same instrument family, and the same failure of a green control to bound what was measured). **And the fix reproduced the trap inside itself, which is the part worth reading:** review of #3243 found that the new re-profile timer fires immediately on the `WENT-UNMAPPED` path, because that branch never stamps `t_reported` and `diag_now_us()` is CLOCK_MONOTONIC since boot -- so a range that vanished entirely printed `+10s` and `+45s` profiles reading `lost=0 kept=0` within 100 ms. A correction that reintroduces the shape it corrects is the normal outcome, not a surprising one; budget a review for it. |
| 249 | **A ctest per-test DURATION on the macOS/Rosetta runner measures process startup, not the test -- so a failing run reads FASTER than a passing one, and reasoning from the duration inverts the sign** | #3067 lane, 2026-09-02. `kernel_sem_timedwait` failed twice at **1.02 s** and **1.22 s**, and an issue comment reasoned from those figures to ARM 4 (a posted acquire granted a one-second timeout) waiting that timeout out -- a plausible chain, offered honestly as a lead. It is arithmetically impossible: **every** test in that job that links `prosper_core` pays ~1.4 s for process start plus `register_builtin_hle()` under binary translation (`kernel_nanosleep` 1.48 s, `hle_functions_registered` 1.48 s, `json2_hle` 1.46 s), against **0.01 s** for the same test on Linux. Five green `main` jobs the same day put the PASSING duration at 1.08/1.20/1.35/1.47/1.52 s -- so 1.22 s sits inside the passing distribution, 1.02 s is below its minimum, and an ARM 4 that really ran to its deadline would have read ~2.4 s. The duration is ~99% constant overhead and carries no signal about the wait at all. **The answer was in the job's own log the whole time** -- `[FAIL] and returned inside one winpthreads tick of the request, not after it` / `(timeout took 13.58 ms via none)` -- which is the general rule: when a test prints per-arm output, read the output, and treat the harness's summary line as a scheduling statistic about the runner. |
| 250 | **A test binary that ASSERTS a capability it does not have -- so the one setting that exercised the mechanism crashed there and nowhere else, and the crash was read as a property of the emulator** | #3156 / #3155, 2026-09-02. `guest_write_watch_set_fault_onstack(true)` is a promise to the watch layer: a SIGSEGV handler is installed, on a sigaltstack, and it will service a write fault on an armed page. `test_game_compute.cpp` made that promise in **seven** places while installing **no handler at all**, and compensated by calling `guest_write_watch_notify_host_write()` at the write sites it knew about -- which works exactly as long as nothing arms a watch the test did not anticipate. At `PROSPER_COMPUTE_WRITE_WATCH_PROMOTE_HITS=1` the compute cache arms on a first acquisition, the test's own `large_result[0] ^= 0xffffffffu;` stored into a page that had just been made read-only, and the process died: `notl 0x0(%r13)` at a page-aligned mmap address with `#0 main ()` as the entire backtrace. **The reachable conclusion from that crash is the wrong one.** It was filed as a user-facing trap in a `PROSPER_*` switch, and the sibling issue then recorded that `hits=1` -- the only value that measurably reduced `memcmp` traffic in production -- was "unusable", so the measurement everybody wanted was blocked by the harness rather than by the emulator. Production installs the handler and ran `hits=1` repeatedly without incident. *Recognition cue:* **a failure that reproduces in exactly one binary, on a code path that binary opts into by asserting a precondition about itself.** Grep for the assertion, not for the crash. *Fix:* install the one production behaviour that matters (`tests/host/test_dmem.cpp` already had the pattern), keep every other fault fatal and loud, and add an arm that takes a raw unhooked store through an armed page -- it reddens the whole suite the moment the handler goes away, at any `hits` value. A second arm forks a child onto a `PROT_NONE` page and requires it to die: a handler that swallows is worse than the crash it replaced, and without that arm nothing would notice. |
| 251 | **A diagnostic that arms itself on a precondition it never checks -- so `PROSPER_GEOM_PROBE` reported "the draw produced no primitives" about draws that produced primitives** | #3248, 2026-09-02. The probe reads a draw's post-transform positions back through `VK_EXT_transform_feedback`, which captures nothing unless the last pre-rasterization stage declares `OpExecutionMode ... Xfb`. The recompiler emits that decoration under its OWN separate gate (`capture_position` / `capture_geometry_position`), and several paths skip it -- a hand-written fixture shader, a draw reaching the generated geometry stage without the flag, any module the probe did not itself cause to be recompiled. The probe checked only that the env var was set and that the device had the extension, so on such a draw it bound transform feedback anyway (`VUID-vkCmdBeginTransformFeedbackEXT-None-04128`), read a zero counter, and printed *"transform feedback wrote 0 vertices (draw produced no primitives)"*. That is a **wrong** answer, not a missing one -- and the expensive kind, because `GEOMETRY-VANISH` is precisely the hypothesis that sends someone to this probe in the first place, so the instrument agreed with the belief that summoned it. *Recognition cue:* **a diagnostic whose null output is also its most-expected finding**; and, generally, an arming condition that names the environment variable rather than the artefact the variable was supposed to produce. Both were checked here and the two diverged. *Why nothing caught it:* the validation layer names it in one line, but `tools/vkval` observes ctest and **no test ever armed the probe**, so the VUID had nowhere to appear -- a second blind spot on top of synchronization validation being off, and one that no configuration of the guard could have fixed. The same blindness had `PROSPER_DRAW_ISO` replaying every draw's pipeline with **none** of the dynamic state the main loop sets (`VUID-vkCmdDraw-None-07831`/`-07832`), i.e. naming a culprit draw from a render that was not the one being isolated. *Fix:* the backend tests the SPIR-V words it is about to hand Vulkan and refuses to arm, loudly, when the capture is absent; one shared recorder puts the dynamic state in both loops; and `render_diagnostic_paths` runs both diagnostics once so the layer sees them from now on. **The isolation half is improved, not fixed, and the residual is exactly the shape of this trap:** the re-render still omits the per-draw `vkCmdClearAttachments` depth/stencil clear, so a submit carrying a guest depth clear is still isolated against different depth contents. Dynamic state is the part the layer can see; this part it cannot, which is why it needs writing down instead of a VUID. **Arm a new env-gated render path there in the change that adds it, or the guard cannot cover it.** |
| 252 | **The ELF program header's `p_offset` is not where a SELF segment's bytes are -- so a disassembly taken at it is off by a constant, lands mid-instruction, and reads as "these addresses are wrong"** | #2928 (review of #2924), 2026-08-22. In the eboot #2924 took its wrapper addresses from, `PT_LOAD[0]` declares `p_offset = 0x4000` while the **SELF segment table** puts that same payload at file offset **`0x8010`**. Reading the file at `p_offset` therefore shifts every address by `0x4010` and disassembles unrelated AVX code from a byte boundary that is not an instruction start -- so it produces *plausible* instructions that are not the ones at the address you asked for. The reviewer nearly filed the addresses in #2924 as wrong before catching it, which is the shape worth recognising: **the instrument does not fail, it answers a different question, and the wrong answer arrives dressed as a finding about someone else's work.** prosper's own loader has always been right here -- `src/self/module.cpp:91` reads the `SelfSegment` table at `0x20` and overwrites each phdr's `file_off` from it (`:111`) -- so the trap is confined to reading a SELF by hand with `objdump`/`readelf`/a hex editor, which know nothing about the segment table. *Cue:* addresses that are all wrong by the same amount, or a disassembly whose instruction stream looks valid but has nothing to do with the function you named. *Avoid it:* take the offset from `tools/self_dump`, or add `SELF segment file_offset - phdr p_offset` to every address before seeking. |
| 253 | **`ulimit -c 0` suppresses the core FILE but not the handler that costs the time, so it reads as a fix while the stall it was meant to cure survives** | #3269/#3306, 2026-09-03. Tests that kill children on purpose (`test_libc_alloc`, `test_raw_syscall`) each trigger `systemd-coredump`; across concurrent lanes the handlers pile up and stall an unrelated third suite. `ulimit -c 0` was the obvious remedy and is in wide use here. Measured, 20 SIGSEGVs per arm: under `ulimit -c 0` the handler is invoked **20/20** and stores **0** files (0.78 s); unlimited stores 20 (8.53 s); `prctl(PR_SET_DUMPABLE, 0)` invokes it **0/20** (0.009 s). A pipe-prefixed `core_pattern` makes the kernel spawn the handler regardless and leaves `RLIMIT_CORE` for it to interpret, so the process spawn and journal commit are paid on every crash -- which is the stall. **The cheap check confirms the wrong thing:** crash under the flag, look in `/var/lib/systemd/coredump`, find nothing, conclude it worked. `coredumpctl list`'s COREFILE column reads `none` for exactly those entries, and an earlier draft of this row's own fix counted journal ENTRIES as dumps and so claimed the flag did nothing at all -- wrong in the other direction, and caught only because a reviewer re-ran it at 20 crashes per arm instead of one. *Recognition cue:* a mitigation whose evidence is the ABSENCE of an artifact, where the cost you care about is the work that produces it. Verify by timing or by handler invocations, never by looking for the file. |
| 254 | **A reject diagnostic that prints every provenance route EXCEPT the one that runs in the failing case -- so four fields that are null by construction were read, three times over two issues, as evidence that the descriptor was absent** | #3126 / #1634, 2026-09-03. `[mimg-unresolved]` names four routes for an image op's descriptor: the `s_load` tag (`srt_tag`), the SRT key (`key_res`), the per-pc fold (`pc_res`) and the #1773 copy alias (`alias_res`). It did **not** print `by_sgpr_base(SRSRC)`. The same line also prints `written`, and `written=0` means the shader never wrote the SRSRC registers -- the descriptor is entry user data by the shader's own construction, so there is no `s_load`, hence **no tag, no SRT key and no copy alias can exist**. For such a site all four printed fields are null *whatever is wrong*, and the quadruple is a restatement of `written=0` rather than a measurement. The one route that can fire is the one that was invisible -- and it was not returning null: `by_sgpr_base` is first-match-wins and class-blind, so it returned a `ConstantBuffer` sitting at exactly the requested SGPR, which the resolver then discarded on class in silence. **Three of the four `written=0` sites on Stray's title screen were like that**, and "nothing is bound at s8" and "a cbuf is bound at s8" printed the identical line. The cost is measured, not feared: #1634 was filed on the null quadruple; #3126's 2026-08-30 thread posted "FOUND IT" on it, retracted it, then wrote "the resolver correctly falls through to `by_sgpr_base(8)` -- and that is what returns null", which was never observable and is false on the vertex site; and a 2026-09-03 lane re-derived the same reading a fourth time. *Recognition cue:* **a diagnostic whose fields enumerate mechanisms, plus a field that says which mechanism applies -- check whether the enumerated ones CAN fire under the stated mode before reading their emptiness as data.** A null that a field's own preconditions force is void, exactly like a positive control drawn from the source it validates (trap 122); it is worse here only because it is printed next to a true fact (`written=0`) that explains it, so it reads as corroboration. *Fix:* the line prints `sgpr_res=<class>` and `need=<sampled\|storage\|either>`, and its `available:` list prints **every** key a resource carries -- it used to print one, `srt=0x%x` whenever `sgpr_base` was unset, so a resource keyed only by `fetch_pc` (how the const-fold publishes a seed- or table-recovered T#) read as `srt=0xffffffff`, i.e. as unkeyed; four of nine resources in the failing pixel stage were that. The lookups are class-filtered too, so a wrong-class hit can no longer shadow a usable one or suppress the fallback beneath it. **The follow-up run vindicates the instrument rather than the reading**: `PROSPER_DYNTRACE_FAIL=1` puts all five sites at `have_t8=0 seed_t8=0` against 29 `have_t8=1` and 21 `seed_t8=1` elsewhere in the same run -- a *measurement with a live discriminator*, which the four null fields never were. *And the trap kept biting after the fix:* that same run was read as "the bytes are absent, #305 window family" and then, on a second look, as "a texture descriptor consumed as a buffer" -- both withdrawn within the hour. `seed_t8=0` is a **shape** verdict, and the bytes turn out to be present, readable, and two well-formed V#s. A field named for the thing you are hunting invites you to read its zero as that thing's absence; decode the bytes it was judging before you do. #3313. |

**Detecting the generated-prose trap (#27), because it generalises past `.pad` files:**

When several artifacts claim *independent* findings, **diff their substance, not their prose**. Identical
bodies under differing explanations means the explanations were written to sound like observations:

```bash
# strip the commentary and compare only the load-bearing content
for d in joe-mac asterix summer-sports; do grep -v '^#' "$d/reach-gameplay.pad" | md5sum; done
```

Seconds of work invalidated three documents. The same check applies to route scripts, per-title status
docs, issue comments and PR bodies — anywhere parallel artifacts assert parallel results.

**It also happens in source comments, which is where it does the most damage.** "It happens in `.pad`
files" understates the trap; a second lane hit it the same day in `hle_service.cpp`. Recording the
first live confirmation of `VdecConfig` (#1687), the comment claimed the guest's values were
"individually meaningful at **every** offset" and that a wrong field order "**could not**" produce
them. Checking the pairs one at a time instead of asserting the general claim: `profile=100` /
`max_level=41` cannot swap (41 is not a `profile_idc`, 100 is not a `level_idc`), `max_dpb` /
`input_depth` cannot (depth would go negative), width/height cannot (the movie would be portrait) —
and `resource` / `codec` are **both `1`** in that title, so that one pair is not discriminated at all
and still rests on the commit that introduced it. The sweeping version was written first and read as
established fact.

A doc carries a `docs/` path and some ambient suspicion; a source comment sitting above a
`static_assert` reads as verified by construction, nothing marks it as inference, and every future
reader inherits it. So when a comment records evidence, **enumerate what the evidence forces and name
what it does not** — "HIGH except this pair, which is MED, and a non-AVC title settles it in one line
of log" is a comment the next reader can act on. A general confidence claim is one they cannot check.

This is **worse than a stale `UNVALIDATED` banner**, and for a specific reason: a banner tells the reader
to check. A confident, specific, plausible header tells the reader **not to bother**, so it survives every
subsequent reading. The remedy is not to flip a banner but to **add accurate provenance where a confident
claim has none** — hold a route header to the `scripts/greak/reach-gameplay.pad` standard, which cites
*observed* phase timings ("the title screen appears at roughly 112 s on a Linux hardware-Vulkan boot")
rather than asserted behaviour. A claim marked ✅ on a run nobody can reproduce is the same defect one
level up.

**Working rules that follow:**

- **A decoded-draw census is meaningless without knowing the render phase, and needs a positive control.**
  Menu, loading, and 2D-UI phases legitimately decode single-digit to low-double-digit draws per frame, so a
  low count is evidence of nothing on its own. Count **per sample interval**, not per run — a whole-run
  average describes neither regime (Blue Prince's is 622.7, which is neither its 10 nor its 2,200) — and
  calibrate against a title **known to render the thing you claim is missing**. Best of all is one control
  exhibiting *both* regimes in a single run, which rules out per-title decode differences as the
  explanation for the gap. Then confirm the phase by **opening the frames**: #1641's Nikoderiko row was
  only interpretable once the images showed samples 10-11 carrying ~160k distinct colours while 3-9 were
  black, which proved the census was tracking real workload rather than drifting.
- **Build a timing measurement that cannot lie quietly.** Everything above is "this instrument lied"; this is
  the counterpart — the shape of a measurement that reports its own invalidity instead of averaging it away.
  Seven lanes share one GPU, so vigilance does not scale: a run *will* eventually overlap someone else's
  capture, and a contended sample looks exactly like a real regression. Three properties, all cheap:
  1. **Self-lock** (`flock` on a lockfile) so a second copy of the driver refuses to start rather than racing
     the first. Two waiters queued behind the same "GPU free" condition both fire when it clears.
  2. **Build strictly before measuring, never concurrently.** A `-j8` compile landing on top of a run costs
     more throughput than whatever is being measured. Put the build inside the driver, ahead of the loop.
  3. **Discard, don't average.** Sample the foreign-process count **before and after** each run; if either is
     nonzero, **rename the log to `DISCARD-…`** instead of dropping it. Contention then becomes *visible in
     the record* rather than invisible in the mean, and the discarded run stays auditable afterwards instead
     of surviving as an unexplained outlier.
  4. **Alternate the arms, then check the residuals for cycle correlation** before attributing any
     difference to the treatment. Running all of arm A then all of arm B lets a *time-varying* confound
     land entirely on one arm and become a phantom effect. Alternating forces it onto both, which makes it
     detectable: in #1609's 12-run experiment the slowest run was **cycle 2 in all four title × arm
     combinations**, so the residual variance tracked *when* a run happened, not *which arm* it was. That is
     not an argument that the gap is noise — it is a demonstration, from the same data that would otherwise
     have produced the phantom. If the extremes line up by cycle rather than by arm, the treatment is not
     the explanation.
  Report **whole-run and steady-state (tail-window) rates separately**: a one-off boot cost and a per-frame
  cost are indistinguishable in a whole-run average, because a run that reaches steady state one second later
  and then performs identically still shows a lower average. Report min/max spread and an explicit
  **overlap verdict** across arms — if the arms overlap, say so plainly and the question is closed.
- **Joining two instruments requires an explicit shared ordinal, never stream adjacency.** If two diagnostics
  are emitted at different pipeline stages, their interleaving in the log is an artifact of *when each stage
  ran*, not of when the events happened. Print a common ordinal (`command_order`) on both and join on it. This
  is why the `order=` field exists on the `[exec] skip draw early` line — **it is not redundant with the
  register values on the same line, and removing it silently re-opens the phantom above.**
- **A guard keyed on a threshold has an expiry date and gives no signal at expiry.** Key it on the thing
  itself. #1675 added a check that CI must register at least 160 tests, so a configure that quietly lost
  Vulkan would fail instead of reporting a smaller green suite. It read as the careful part of the change
  and it was decoration with a delay: CI has 138 non-Vulkan tests, the suite only grows, and once ~22 more
  exist, losing all 27 Vulkan tests still clears 160. The guard would have stopped protecting the thing it
  was added for, at some unremarkable future commit, silently — the same shape as the defect it was
  guarding against. Replaced with a check that three **named** Vulkan-gated tests are registered: they
  cannot exist unless Vulkan was found, and that stays true at any suite size. The general form — **a
  threshold derived from today's numbers decays as the system grows; assert the property, not a count that
  happens to imply it today** — and the corollary: verify a guard fires in **both** directions, because
  one that cannot fail is worse than none.
- **Before recording that something cannot be measured, check the tools you are claiming cannot measure
  it.** This is nastier than a wrong fact and it is self-sealing. A wrong claim about the *subject* gets
  tested by the next measurement; a wrong claim about the *instrument* stops the measurement from
  happening, so nothing ever contradicts it. #1635 shipped "severity is unknowable — `self_dump` has no
  import listing" into a PR body. `self_dump` prints `[IMPORTS BY LIBRARY]`, documented in
  `tools/re/README.md`, and answering the question took ten minutes once someone looked: 21 aliased NIDs,
  **0** imported by anything. An "unanswerable" is a claim about a tool, and claims about tools are the
  cheapest of all to verify — run `--help`, grep the README, read the source. The same session produced
  the mirror image in #1675: CI's green tick was believed to mean "the suite passed" when it meant "the
  138 tests that got registered passed", and the 32 missing ones — ~1,822 assertions — were invisible
  because nothing reports a test that was never registered.
- **A fix that makes an instrument lie more convincingly is worse than the bug.** This one is about
  *repairs*, not measurements, and it is the inverse of everything else on this page. #1659's own fix
  briefly introduced it: the diagnostics were widened to resolve any guest module, but several kept a
  hard-coded `eboot+` label. Before the change an Il2Cpp frame printed an offset *larger than the image* —
  the exact tell the entry below tells you to look for. After it, the same frame printed a small,
  plausible, in-range offset under the **wrong module name**, with no tell at all. The fault was
  unchanged; only the symptom was removed. Any correction that deletes a symptom without deleting the
  fault produces a **quieter** failure, and quiet failures are the expensive ones. When you fix an
  instrument, ask what its wrongness used to look like and confirm you removed the wrongness rather than
  the appearance of it. Corollary for tests: a test that exercises the *helper* you added will not catch
  this — it has to render through the real formatter and assert the text a reader would see.
- **A `<module>+0x<rva>` offset larger than the module's image is a labelling artifact, not a wild pointer.**
  Guest module bases are fixed constants in `src/host/image/boot_program.hpp`, and they *move*: #825 relocated the
  eboot from `0x400000000` to `0x410000000` so Astro Bot's direct-memory mapping would stop aliasing code.
  Diagnostics that had hard-coded the old literal kept printing `eboot+0x…` against it, so every offset was
  the real RVA **plus `0x10000000`** — an offset past the end of a 161 MB image, and one that no longer
  round-tripped through `PROSPER_BP` (which adds the *mapped* base). #1659 converged every printer onto
  `prosper::guest_module_name()` / `guest_module_offset()`; if you are hand-computing a base anywhere, that
  is the bug. Two corollaries worth keeping: an address below the lowest module base is **data, not code**
  (that region is now a DMEM aperture), and a single wide range test labelled every module in it "eboot",
  so a wrong *binary* is as likely as a wrong offset. Static tools are unaffected — `xref.py`, `edis.py` and
  `prx_to_elf.py` compute from ELF `p_vaddr`/file offsets and never add a runtime base, so RVAs recorded
  from disassembly stay trustworthy.
- **Before quoting a diagnostic's line count as a rate, grep for its cap.** These sites use a
  `static std::atomic<int>` counter or a per-key dedupe set; check for one before concluding "this fires N
  times" or "this is rare". When the site prints its own counter — `WaitRegMem #%d` *is* `ln` — the true
  total is free: the highest `#N` in the log bounds it, and a run whose maximum stays under the cap has
  printed *every* occurrence. That read turned #1606's "dozens per second" into 18 and 21 events for two
  entire runs.

- **Before concluding that geometry is missing, establish what content is actually resident.** A draw
  count describes the frame; it says nothing about whether the world has anything in it. For a
  UE4/pak title this is one offline command against a log you already have —
  `tools/re/pak_index.py GAME.pak --log run.log --distinct` resolves the `[apr] read-submit` byte
  offsets to asset names, so "which maps/blueprints/widgets did the guest load, and where did
  loading stop?" is answered with no boot and no GPU. Run it *first*. On #1641 it dissolved a
  session's worth of GPU investigation in one pass.
- **Read an unimplemented call's *arguments* before inferring what it is doing.** This is cheap and
  almost never done. `PROSPER_STUBDUMP=1` prints one line per import — `[stub] #877 off=0x148e0
  libScePosix::T8fER+tIGgk` — and each stub lives at `0x600000000 + off`, so a gdb breakpoint at the
  **stub entry** catches the guest's argument registers fully intact (the stub has not clobbered
  anything yet; note the generic unimplemented path *does* overwrite `rdi`/`rsi`/`rdx` by the time it
  reaches `prosper_on_unimpl`, so break on the stub, not the C++ handler):

  ```bash
  PROSPER_STUBDUMP=1 … boot_trace <dump> 2>&1 | grep '<NID>'      # -> off=0x148e0
  gdb -p $(pgrep -x boot_trace) -batch -ex 'set pagination off' \
      -ex 'break *0x6000148e0' -ex continue -ex 'info registers rdi rsi rdx rcx r8'
  ```

  The stub table is per-run — re-read it, never reuse an address. On PPSA19244 this converted
  "1.3 billion socket polls" into "a 3-second sleep that returns instantly", and stopped a
  substantial piece of shared work from being scoped against a wrong premise.
- **A trap you have already recorded is the one most likely to catch you.** Trap 21 is trap 14, hit
  by the lane that wrote trap 14 down, because the rule had been internalised as "check the render
  *phase*" when the general form is "check the *denominator* — phase, resident content, and engine
  architecture — before treating a low count as a defect."
- Prefer experiments that **detect their own invalidity** — e.g. render two adjacent operations and assert both
  return the same resolution, so a surface switch invalidates the comparison instead of producing a phantom.
- **Open the image.** Believe neither a metric win nor a metric failure without looking. A diagnostic clear can
  out-score real content on colour count; a healthy frame can fail SSIM on animation phase.
- A measurement that is **right for one question can be silently wrong for the next one asked of the same data**.
- Never re-derive a bisect boundary without confirming both sides render the **same target**.
- Check whether a later comment or merged PR already **superseded** an inherited premise before acting on it
  (Bendy's "CPU software-detile dominates" had been overtaken by #1179/#1190/#1269 in its own issue).
- Write `Refs #NN`, not `Fixes #NN`, when a PR only partially addresses an issue — a parenthetical qualifier does
  **not** stop GitHub's auto-close, and #1554 was closed that way despite a comment saying to keep it open.

### State only what is true at the moment and scope you state it

**A statement should only assert what is true at the moment and scope it is made. A line that announces an
*intention* in the grammar of a *fact* is indistinguishable from a result — and both humans and agents act on it
as one.** A confident statement reads identically whether or not it is earned, so nothing announces itself when
it stops being true.

Instances, all the same shape:

- The F9 arming line announced a filename it could not guarantee. A grab that aborted then left a `.bmp` beside a
  same-named `.prgbundle` from a different boot 51 minutes earlier, and a lane was handed the two as a matched
  pair. Fixed by naming the title at arm time and the real path per artifact after each file exists (#1693).
- A PR body asserted a `CONFIDENCE: HIGH` the source had already retracted; merging would have written the
  withdrawn version into the permanent record.
- An issue title asserted a mechanism that had been falsified, and kept sending agents at it for a day.

The practical form:

- If you cannot guarantee it when you write it, **state what you do know** and emit the fact when you have it.
- If a claim rests on three examples, **say "verified on three cases"** — not "for every input".
- A document, title, or comment that outlives its reasoning becomes **an assertion nobody is checking**.
  Retitling a falsified issue and correcting a stale PR body are correctness work, not tidying.

Same family as trap #34 (a check of the *answer* read as a check of the *rule*) and the source-comment case
under the generated-prose trap above: those are claims whose **scope** outran their evidence, this is claims
whose **moment** outran theirs. In both the grammar is what does the damage — nothing in a confident sentence
distinguishes the earned version from the unearned one.

### A test seam that pins a policy makes its env-var A/B silently void

An A/B was run by setting `PROSPER_ULT_RETURN_SUCCESS=1` to force libSceUlt's mutex to a no-op, expecting the
mutual-exclusion test to fail. **It passed**, which read as "the test is too weak to detect a broken lock" — a
believable and entirely wrong conclusion. The test calls `ult_set_return_success_for_test(false)` in its own
`main()`, deliberately, so that the environment cannot decide whether it passes. That seam overrode the variable,
so the "no-op" arm was never a no-op; both arms ran the same real mutex. Re-run with the pin removed, the
original test caught the broken lock immediately (27,984 of 40,000 increments surviving).

**The rule: any A/B driven by an environment variable is void until you prove the variable actually took effect.**
Have the run print the policy it is operating under, or assert the arm's expected *behaviour* before trusting its
result. This is the same shape as trap #10 (a switch that is right for one question and silently wrong for the
next) and trap #7 (a stale label read as state): a control that legitimately exists for one purpose silently
neutralises a measurement made for another.

Two corollaries worth keeping:

- The tell is an A/B where **the arms agree when they must differ**. Treat that as an instrument failure first,
  not a finding. A negative control that cannot fail is not a control.
- A test that pins its own configuration is still the right design — it stops the environment deciding the
  verdict. The defect is using that same variable as the A/B lever. Give the A/B its own lever, or remove the pin
  for the experiment and say so.

### A host function is not a valid stand-in for guest code — and only Windows will tell you

A test for libSceUlt's ulthreads used an ordinary host C++ function as the ulthread entry. It passed on
Linux and macOS and failed on **Windows MinGW only**, for three hours, on the single assertion that read the
entry's argument back out.

The guest is **System V AMD64**; prosper enters guest code through `prosper_call_guest_on_stack`, which
marshals into SysV (first argument in `%rdi`). On Linux and macOS the host ABI *is* System V, so a host
function is an accurate model and the test is sound by accident. On Windows the host is **Microsoft x64**,
where the first argument arrives in `%rcx` — so the untagged host function read a garbage argument and
returned a garbage status **while still executing correctly and touching its stack**, which is what made it
look like it worked. The product was never wrong; the test was. `win_thread_trampoline` already documents
this exact hazard for the production path ("a bare `pthread_create(entry, arg)` mis-passes the arg (MS x64
vs SysV)").

**The rule: any test that supplies a function for prosper to call *as guest code* must tag it with the guest
convention**, `__attribute__((sysv_abi))` on x86-64, not leave it at the host default. Prefer the tag over an
`#ifdef` around the body: "this function uses the guest's calling convention" is true on every platform and
the attribute is simply redundant where the native ABI already matches. Keep such entries plain leaf
functions — no destructors, no exceptions — because the attribute conflicts with MinGW's SEH-based C++
unwinding, which is why `PROSPER_SYSV_ABI` is empty for the HLE handlers (see `dispatch.hpp`).

The generalisable half: **a green Linux run says nothing about ABI-boundary code.** Guest entries, callbacks
the guest invokes, and anything reached through the call-guest shims are the cases where the dev platform
cannot fail and Windows is the only place the bug is visible.

### Registering a NID with an error return can be worse than leaving it unregistered

A trap of the same family as the list above, but in the HLE surface rather than an instrument. It cost a
merged defect (#1618, introduced by #1614), so it belongs beside them.

The dispatcher's default for an **unresolved** NID is `return 0` (`hle/dispatch.cpp` `prosper_on_unimpl`).
That default is safe for a function whose contract returns a **value**, and unsafe for one that returns a
**status** — which is exactly why fail-visible registration is usually an improvement. The trap is that the
inverse is equally true, and easy to miss: **an error sentinel is unsafe for a value-returning contract,
because such a signature has no error channel and the sentinel is read as data.**

`sceUltWaitingQueueResourcePoolGetWorkAreaSize` returns a `size_t` in `rax` which the guest passes straight
to `malloc`. Registering it to return `SCE_KERNEL_ERROR_ENOSYS` therefore asked for a 2.0 GiB allocation
where the unregistered default had produced a harmless `malloc(0)` — the #544/#660 class, reintroduced by
the very change meant to prevent it.

**The rule:** before registering a NID purely to make it visible, establish from the call site whether its
contract returns a status or a value. If it returns a value and the real one is unknown, the dispatcher's
`0` — or better, an honest computed value — is correct, and the visibility belongs in the log line, not the
return. Disassembling one call site is enough: `call …; mov [rbp-N],rax; …; call malloc` settles it in
seconds, and no amount of reasoning about the function's *name* substitutes for it.

### Proving "the guest never touches X" — pick the exhaustive instrument, not the plausible one

Recurring question when deciding whether prosper may write bookkeeping into a guest-owned struct: *does guest
code ever read inside it?* Answering it for libSceUlt (#1603) produced one sound instrument and one tempting
unsound one; the difference is worth reusing.

**Unsound:** identify the functions that operate on the owning C++ class by the displacements they use
(`[reg+0x398]`, `[reg+0x410]`, …), then look for accesses inside the member's range. Displacement sets are not
identities — `memcpy`'s bulk AVX stores and every function's own `[rsp+0x...]` stack frame match them. This
reported **1,361** then **707** hits, all false, and tightening the heuristic eventually matched *nothing*,
which is equally uninformative. A heuristic that can be tuned from "everything" to "nothing" is measuring the
tuning, not the subject.

**Sound:** enumerate the objects' actual addresses, then scan **every** RIP-relative reference in the whole
executable segment and ask which land at a non-zero offset from one. That is exhaustive over the only
addressing mode that can reach a static object, so zero hits is a proof rather than an absence of evidence. It
found 16 static Ult objects and **zero** interior references — every reference a `lea` of the base feeding an
Ult call.

Generalising: prefer the instrument whose **negative result is exhaustive over a closed set** (all
RIP-relative references) to one that pattern-matches an **open set** (all functions that might alias a
pointer). When only the open-set instrument exists, its negative result is a hypothesis, not a finding.

### Cross-title: the colour-state registers are decoded correctly — start elsewhere

Two lanes independently established this, and both results killed a live hypothesis in a *different*
lane. Recorded here rather than in either lane because the next agent to see a zero colour write mask
will reach for the same explanation.

**`MODE` decode is not remapped.** A tempting hypothesis on Astro Bot (#1459) was that prosper mis-maps
the `CB_COLOR_CONTROL` `MODE` enum — its live trace never once observed `MODE=1 (CB_NORMAL)`, only modes
0 and 6, which is not what an ordinary renderer looks like. **Blue Prince falsifies it**: that title
writes `MODE=1 CB_NORMAL` and renders correctly through the same decode. Any remapping hypothesis now
has to explain Blue Prince too, which no simple remap does.

**`CB_COLOR_CONTROL` tracking is not lossy.** An Oregon Trail investigation measured the indirect
register path directly: **~12,000 observed `CB_COLOR_CONTROL` writes**, `MODE=1` dominant at roughly
**7:1**, and — joined on **each draw's own `command_order`** — **zero mismatches across all 1,050
suppressed draws**. The register prosper believes is in force at a draw is the register the guest wrote.

**So "prosper is misreading MODE" starts from a losing position.** When a draw resolves `cwm=0`, the
productive question is **who wrote the zero and when**, not whether the decode is right.

**The join is the trap in that second result, and it is worth its own line.** The same measurement read
the *other* way — joining `PROSPER_REGWATCH` against `PROSPER_EXECLOG` by **stream adjacency** rather
than by `command_order` — reported that 1,049 of 1,050 suppressed draws resolved against a **future**
register write. Clean, compelling, and entirely false: the two instruments timestamp at different
pipeline stages (REGWATCH at command-processor *decode*, EXECLOG at *realization*) and the phases
interleave per submit. See instrument-trap 12; #1633's `order=` is what makes the honest join possible.

### Cross-title: aggregate frame metrics cannot tell an animating menu from gameplay

A bring-up run that reports healthy frame statistics has not thereby reported that it reached
anything. On *Asterix & Obelix: Babylon Mission* (2026-08-19) a run that reached the `World_3_10`
harbour and a run that sat on the `SoloCoopMenu` character-select screen for 495 s were **identical**
on every aggregate the capture tool produces — same route file, same binary, same dump:

```
skip2    60/60  source-distinct=60 pixel-distinct=58 stale=0.0s guest=running status=ok  -> gameplay
repro3   60/60  source-distinct=60 pixel-distinct=58 stale=0.0s guest=running status=ok  -> character select
```

**An animating menu is exactly as pixel-distinct as a level**, and `status=ok` reports the harness's
health rather than the guest's progress. Frame count, pixel-distinctness, staleness and liveness are
each necessary, and not one of them is a discriminator. A route validated on those alone is validated
against nothing.

**So state a content-level success condition BEFORE the run** — a scene token, a level or map name, a
HUD element that exists only in gameplay — and read the aggregates as a check that the run was *valid*,
never as evidence of what it *reached*.

**For Unity titles there is a cheap and self-checking one.** `Media/levelNN` is the Unity
build-settings scene index: on that title, 76 scenes against 76 files, confirmed by a byte-level scan,
and self-checking because each world level appears as a `World_X_Y` + `World_X_Y_Level` pair — so a
mis-derived index surfaces as a broken pairing rather than as a plausible wrong answer. Twelve of the
fourteen titles at gameplay are Unity/IL2CPP, so it generalises. The scene sequence was the **only**
instrument that separated the two runs above; the successful one went Loading -> region map -> level,
the others Loading -> `SoloCoopMenu` -> stuck. Full derivation in
[`ASTERIX_BABYLON_STATUS.md`](ASTERIX_BABYLON_STATUS.md); the route-reliability defect it exposed is #2743.

**A cheaper self-check when the scene names do not pair: the `.resS` partition.** Asterix's check is
the `World_X_Y` + `World_X_Y_Level` pairing, which only exists because that title names its scenes
that way. *Beneath* (`PPSA27640`) does not, and a second check was still available for free from the
dump's own file list: a `levelNN.resS` sidecar holds a scene's streamed resource data, so it attaches
to heavy world scenes and not to loading screens. On that title 46 scene paths match 46 `levelNN`
files, and **every one of the 12 `.resS` files above `level5` lands on a world scene while not one
lands on a loading screen** — in a dump that is more than half loading screens. A mis-derived index
breaks that partition rather than yielding a plausible wrong answer, which is the property that makes
a check worth having. `sharedassetsN` gives a third reading on titles that pair it per scene (see
[`BENDY_DARK_REVIVAL_STATUS.md`](BENDY_DARK_REVIVAL_STATUS.md)). Full derivation:
[`BENEATH_STATUS.md`](BENEATH_STATUS.md).

**Establish that the oracle discriminates before relying on it — the failures are not exotic.** It
has now been used on four titles and been void on two: *Tales of Graces f* ships exactly one level
file, so the index is a constant, and *Bendy and the Ink Machine*'s layout does not transfer to its
own sequel. Counting the title's own `levelNN` files is a one-command precondition, not a formality.

**For Unreal titles the equivalent is the IoStore package stream, and it is now tooled.** A large
share of the titles still short of gameplay are Unreal — read `COMPATIBILITY.md` for the current
split rather than a number restated here — and the Unity oracle covers none of them. The Unreal
analogue is *which package the guest read*, decoded from a `PROSPER_FILELOG=1` stream by
`tools/re/iostore_index.py` (`--maps` for just the `.umap` sequence). It is self-checking in the same
way: Unreal cooked package paths are semantic, so on `PPSA17942` the title screen reads
`Map/Product/Title/Title_PL.umap` — which `DefaultEngine.ini`'s own `GameDefaultMap` names as the
startup map — and a town reads a cluster of
`Map/Product/World/Field/F001/C001/Modern/C001_M_Env_*.umap` sub-levels. (Note the era folder:
the path without `Modern/` matches **nothing** in the container, and a glob that quietly returns
zero is the easiest way to mistake a working oracle for a broken one.) A mis-derived mapping produces nonsense rather than a plausible wrong answer.

Two things make this different from just pointing `pak_index.py` at the container. First, an
IoStore title's `.pak` holds **no packages at all** — only configs, fonts and localization — so
`pak_index.py` correctly reports zero maps and names nothing; the packages are in the `.ucas`.
Second, a chunk's offset in the `.utoc` is in the container's **logical** space while a read syscall
carries a **physical** offset into the packed `.ucas`, and on `PPSA17942` the two differ by 4.1 GB at
the startup map alone — so a one-step lookup misnames most of a stream while still looking plausible.
`iostore_index.py --self-test` pins that mapping and is registered in ctest.

**And test the phase, not the membership — a front-loading title reads its first world's packages
before you ever leave the menus.** Measured on `PPSA17942` (2026-08-20): the town sub-levels
`T001_M_Env_GOut`, `T001_M_Gmk_GOut` and `T001_M_PartyTalk_GIns` are all read at **58 s**, while the
run is still on the save-slot screen and roughly ten minutes before the world is entered; two
further World maps are read at 0.0 s, *before* the title map. A "did it read a World map" predicate
would have reported gameplay on a run that never left a menu — the exact failure the oracle exists
to prevent, arrived at through the oracle. What separates the states is **bytes per minute by
content directory**: 0.25 MB of Environment while the setup menus are up against 285 MB in the
minute the world loads, and a persistent level (`T001_M_PL.umap`) plus a level-sequence package
appearing only in that window. On a title that streams on demand the membership form is enough;
check which kind you have before quoting it.

Validate it the way the `## Ruled out` rule requires: build a positive instance **by hand from the
container index**, outside any run — compute a known package's physical offset and check that
`--resolve` names it back — rather than trusting that the instrument fires because a run produced
some output.

## The orchestration contract

### Orchestrator responsibilities

The orchestrator owns integration and scheduling, not every line of investigation. It should:

1. Read `CLAUDE.md`, this document, the title status document, and the active issue before assigning work.
2. Fetch `origin/master` and give every subagent a private worktree and a fresh branch from the exact remote head.
3. Assign one title or one precisely bounded shared-infrastructure question per subagent.
4. Prevent duplicated hypotheses by requiring agents to read the current issue evidence before running anything.
5. Keep each agent on an evidence ladder: retained offline artifact first, bounded live capture only when the artifact
   cannot answer the question, implementation only after a generic behavioral contract is identified.
6. Decide GPU scheduling. Ordinary correctness replays may overlap; reserve exclusivity only when measurements,
   memory pressure, or observed interference require it.
7. Review every proposed diff itself, including assumptions, scope, tests, `git diff --check`, and exact base/head.
8. Open short-lived PRs for proven progress, wait for every applicable CI job, confirm release publication skipped on
   an ordinary PR, then merge when authorized.
9. Keep issues and PR descriptions self-contained. Post exact commands, hashes, conclusions, and falsified hypotheses.
10. Update compatibility docs and representative screenshots when a title reaches a new visible checkpoint.
11. Rebase or restart long-lived investigation branches from current master before they drift.
12. Stop stale processes and keep evidence off the repository and off RAM-backed `/tmp`.

The current user explicitly authorized the orchestrator/subagent arrangement as pair programming and allowed the
independent-review step to be skipped for PRs produced by this coordinated work. The orchestrator still has to inspect
the complete diff, verify the exact head, wait for all CI, and merge deliberately. Treat this as engagement-specific
authorization: if a future user has not granted it, follow the independent-review default in `CLAUDE.md`.

### Subagent responsibilities

Every game subagent should receive and follow this contract:

- Work only in the assigned private worktree and worktree-local build directory.
- Start from the exact remote master SHA supplied by the orchestrator; report branch, head, merge-base, and status.
- Read the title issue and status docs before running a new experiment.
- State one falsifiable question before each run and the outcome that would distinguish the competing explanations.
- Prefer immutable F9/timeline/capsule evidence and offline `gpu_replay` over repeated game boots.
- Put captures, logs, screenshots, raw shaders, and scratch analysis under `~/`, never in git and never in `/tmp`.
- Never publish private absolute host paths. Public text uses `~/`, `<REPO_ROOT>`, `<WORKTREE>`, and `<DUMP_ROOT>`.
- Do not make title-address special cases. Shared GPU/recompiler behavior needs a generic contract and regression tests.
- Do not silently skip unsupported guest behavior. Retain fail-visible rejection until semantics are proven.
- Do not edit after the orchestrator asks for a freeze or design review.
- Report meaningful progress early: evidence paths/hashes, issue comment links, exact next blocker, and whether a code
  change is ready. Avoid holding a useful generic fix for days while exploring a later title-specific blocker.
- Commit and push a focused branch when the orchestrator approves implementation. The orchestrator owns PR creation,
  CI gating, merge, and cross-agent integration unless explicitly delegated otherwise.
- A visual milestone requires an unmodified frontend screenshot. Diagnostic substitutions may illustrate a hypothesis
  but are labeled diagnostic and never presented as progression evidence.

### Worktree and branch protocol

Never build or edit in the shared checkout. A typical assignment begins with:

```bash
git fetch origin master
git worktree add .claude/worktrees/<agent-title> \
  -b investigate/<title-question> origin/master
cd .claude/worktrees/<agent-title>
git status --short --branch
git rev-parse HEAD
```

Each worktree gets its own build directory. Put compiler temporaries on real disk:

```bash
mkdir -p prosper/build-linux/tmpdir
TMPDIR="$PWD/prosper/build-linux/tmpdir" \
  cmake -S prosper -B prosper/build-linux -DGAME_DUMP=<DUMP_ROOT>/<TITLE>-app0
TMPDIR="$PWD/prosper/build-linux/tmpdir" \
  cmake --build prosper/build-linux -j6
```

Before publishing, fetch master, inspect divergence, synchronize when needed, rerun the relevant checks on the exact
head, and report:

```text
branch / head / merge-base / worktree status
focused build and test commands with pass counts
artifact or live evidence hashes
git diff --check result
known limitations and deliberately unaffected paths
```

Do not keep coding on a branch whose useful part has already merged. Create a fresh branch from the new master for the
next hypothesis.

### GPU scheduling

Exclusive GPU ownership is **not** the default.

Safe to overlap in normal circumstances:

- `shader_inspect`, graph generation, disassembly, capture inspection, and CPU analysis;
- short deterministic correctness replays where timing is irrelevant;
- game-list or no-game frontend tests that only render light ImGui content;
- builds and non-Vulkan tests.

**Omitting `PROSPER_RENDER` does NOT make a run GPU-free.** This has been read as "no GPU" in
briefs and in lane planning, and it is wrong. `tools/boot_trace/boot_trace.cpp:285` gates only the
**live renderer** on `PROSPER_RENDER`; a few lines earlier, `:277` unconditionally calls
`prosper::frontend::register_live_compute()`, which executes the title's compute dispatches through
Vulkan. A "CPU-only" `boot_trace` run of PPSA19244 executed **~135,000 dispatches** over 310 s — a
real, sustained GPU consumer that any peer measuring frame time would have felt.

The genuinely Vulkan-free path is **`PROSPER_NO_COMPUTE=1`**, which selects the progression-only
no-op backend at `boot_trace.cpp:270-276` instead. It retains semantic dispatches and mutates no
guest GPU resources, so it is sound for CPU-side questions — asset/file I/O, pad, HLE call counts,
guest backtraces, boot progression — but **not** for anything whose result depends on compute
output. Validate it per investigation rather than assuming: on PPSA19244 the `NO_COMPUTE` arm was
confirmed to reach the same steady state (same ~23 draws/submit, same asset set, same stall) as the
compute-enabled arm before any conclusion was drawn from it.

**A "sustained all-clear" needs a NUMBER, and 120 s is too short.** Waiting for a peer's GPU work to
finish by checking that all five process names (`prosper-app boot_trace gpu_replay screenshot
screenshot_snap`, plus `/proc/<pid>/cwd` to attribute them) are clear is correct, but a *point-in-time*
check is worthless and a short window is nearly as bad: a lane running back-to-back captures has gaps
between its runs, and a window that fits inside one reports a false all-clear. Measured 2026-08-01: a
waiter requiring **120 s continuously clear** declared SUSTAINED ALL-CLEAR at 12:18:33 and the routes
lane's `screenshot_snap` was back **within seconds** — the window had fitted inside one inter-run gap.

Require **300 s minimum continuously clear** against a lane doing sequential capture runs, reset the
streak on **any** busy sample, and log what reset it so the wait is auditable rather than a guess.

**A before/after peer count is the right pattern and has a blind spot — know which one you are
relying on.** Sampling the foreign-process count immediately before and after a run, and marking the
result `DISCARD` if either is nonzero, makes contention *visible in the record* instead of invisible
in a mean. But a peer that starts **and finishes entirely inside** your run reads zero at both ends,
so "0 before, 0 after" is evidence of a clean run, not proof of one. It is strictly weaker the longer
your run is and the shorter the peer's. The gate width and the before/after check protect different
things: the **gate** decides whether you started into an inter-run gap, the **before/after** decides
whether contention was still present at the boundaries. Neither subsumes the other, so use both, and
for a genuine timing claim add a mid-run sample rather than trusting the endpoints.

**Scope this discipline to the claims it actually protects.** It exists for *timing* results —
FPS, submits/s, throughput A/Bs — where a contended sample is indistinguishable from a real
regression. A **qualitative** result is contention-invariant by construction: a contended run cannot
fabricate a correctly-rendered logo, and it cannot change which field a deterministic CPU-side
validator rejects. When a gating standard tightens mid-session, re-check *timing* claims made under
the older, looser gate; do not reflexively withdraw qualitative ones, and do not spend a GPU slot a
queued lane needs re-confirming a mechanism that already guarantees the answer. Both directions are
errors: applying it indiscriminately withdraws sound results, ignoring it leaves an
`UNCONTENDED: admissible` line standing on a window too short to have earned it.

So the honest classification is:

| run | Vulkan? |
|---|---|
| `PROSPER_RENDER=1` | yes — renderer **and** compute |
| no `PROSPER_RENDER` | **yes — compute still runs** |
| no `PROSPER_RENDER` + `PROSPER_NO_COMPUTE=1` | no |
| `gpu_replay`, `screenshot`, `prosper-app` | yes |
| `gpu_timeline`, `self_dump`, `shader_histo`, `tools/re/*`, `tools/il2cpp/*` | no |

Ask the orchestrator for an exclusive lease when:

- profiling or reporting FPS/frame time;
- comparing small performance changes where contention would swamp the signal;
- a capture/replay has unusually high VRAM or host-memory demand;
- concurrent jobs have already caused timeouts, device loss, or unstable images;
- the user specifically asks to watch one live run without interference.

An exclusive lease has an owner, purpose, start time, and bounded expected duration. The owner announces when it is
released. Correctness replays do not become exclusive merely because they use Vulkan.

Before starting a GPU run, inspect existing processes. A Prosper process older than roughly 30 minutes is presumed
stale because known tests should finish in minutes. Resolve the exact command and owner, then terminate that exact
process safely. Never use a broad pattern that could kill another agent's unrelated work.

**Count with `pgrep -x`. Both `ps | grep` idioms are wrong, in opposite directions.** This is a safety rule, not a
style preference: a wrong count either blocks you from a free GPU or makes you kill a peer's live run.

| Idiom | Failure | Observed |
|---|---|---|
| `ps aux \| grep -cE "prosper-app\|boot_trace"` | **Over-counts** — matches the Bash wrapper shell and any `sleep` watcher whose command line contains the literal | reported **23** when the true count was **0** |
| `ps -eo comm \| grep -c '^prosper-app$'` | **Under-counts** — `ps` pads the `comm` column, so the `$` anchor never matches | reported **0** against a **live** process |

The second is the dangerous one. On 2026-07-31 an agent reported "GPU released" from that idiom repeatedly; a
blind `pkill -f prosper-app` on the strength of one such reading would have killed a **successor agent's run 35
seconds after it started**. It was caught only because the release check was re-run with `pgrep -x`.

Use:

```bash
pgrep -c -x prosper-app        # count, exact name
pgrep -ax prosper-app          # list with full command lines
readlink /proc/<pid>/cwd       # whose worktree is this?
tr '\0' '\n' < /proc/<pid>/environ | grep PROSPER_   # and whose run?
```

**`pgrep -x` takes exactly ONE pattern.** `pgrep -x prosper-app screenshot boot_trace` is a usage error: it exits 2
having looked at nothing, and the two ways it is usually written both read as a clean box — with stderr suppressed it
prints nothing, and `... || echo "GPU free"` prints *"GPU free"* **because** the command failed. Use one ERE argument
(`pgrep -x 'prosper-app|screenshot|boot_trace|screenshot_snap'`), the per-name loop below, or
**`tools/gpu_busy.sh`, which runs the full five-name census below as one tested command with an
exit code that means what it says** (0 consumers running / 1 free / 2 tool error — a broken tool
never answers "free"; `while gpu_busy.sh -q; do sleep 30; done` waits for a free box). Trap 222.

Before terminating anything, confirm the exact PID **and** that its `cwd`/`environ` identify it as yours. Never
kill from a pattern count. Note `comm` is truncated to 15 characters by the kernel, so a longer binary name needs
`pgrep -f` with a **bracketed** pattern (`pgrep -f "prosper[-]app"`) so it cannot match your own shell.

**`pgrep -x screenshot` does not see a snapshot run.** `capture_content` copies the frontend to
`$TMPDIR/snap_<rand>/screenshot_snap` and executes *that*, so every `snapshot.py` profile, `verify`, and
`check` runs under the name **`screenshot_snap`**. Checking only the four obvious consumers reports a free
GPU while a peer is two full boots into a `verify` — the longest-running GPU job in this repo. Include it:

```bash
for n in prosper-app boot_trace gpu_replay screenshot screenshot_snap; do
  printf '%s: %s\n' "$n" "$(pgrep -x "$n" | wc -l)"
done
```

Observed 2026-08-01: a lane's own live profile showed `screenshot: 0` and `screenshot_snap: 1`. The same
applies to any harness that runs a copied or renamed binary — resolve the *executed* name, not the built one.

**A clean count is a snapshot, not a lease.** On the same day, a sustained-idle watch confirmed 120 s with
zero consumers, and a peer lane started a `boot_trace` 60 s later, concurrent with the run that idle check
had authorized. Sustained idle lowers the odds of landing inside a peer's inter-run gap; it cannot reserve
anything. If overlap would invalidate your measurement, say so and re-run — do not kill the peer.

### Fast evidence loop

For rendering correctness, use this order:

1. Reuse an existing `.prgcap`, `.prgbundle`, timeline, raw shader, or issue attachment.
2. Run `gpu_replay --inspect-only`, graph/resource dumps, shader coverage, and source inspection.
3. Replay a bounded operation prefix or one draw/compute stage.
4. Use a narrowly scoped diagnostic substitution with a selector-miss control.
5. Add a reusable diagnostic seam only if current tools cannot separate the remaining stages.
6. Run the title live only to capture missing state or validate the integrated fix.
7. Measure performance only after correctness and with an exclusive GPU window when precision matters.

Every A/B must hold incidental behavior constant. For example, `PROSPER_TESTTEX` enables a generic CPU diagnostic
copy path, so a binding-miss run with the same environment is the control for a binding-hit run.

### Testing, PRs, releases, and screenshots

- Focused tests are part of iteration. The full snapshot matrix is optional for ordinary development and PRs; the PR
  author decides whether it is useful.
- **The default cadence is batched, not per-PR: run the guards once per ~10 merged fixes, then bisect a failure.**
  Many PRs therefore merge with no guard validation at all, which is correct and intended — the guards exist to
  replace manual play-testing of 39 titles before a release, not to certify every commit on master. A per-fix
  cadence costs `N` guard runs; batching at `B=10` costs `N/10 + F·log₂10`, which at a 1-in-5 batch failure rate
  is about **0.17·N** — a ~6x reduction, and still under `0.43·N` even if every batch regresses something. Both
  cadences are linear in `N`, so this is a constant-factor win rather than a complexity-class one; it is a large
  constant and it holds at any realistic failure rate, so do not re-derive this tradeoff per PR.
  - **Re-run a failed guard on the same commit before spending the bisect.** A flaky guard makes `git bisect`
    converge confidently on an innocent commit — an answer-shaped non-answer, worse than no signal. One extra
    run protects the ~4 the bisect will spend. Guards that sample a route window are phase-sensitive, so this is
    not a hypothetical (see the `blue-prince-hall` / `terminator-boot` note above).
  - **One re-run is NOT sufficient, and the first batch run proved it.** `messenger-scene` failed **twice
    consecutively with a byte-identical signature** (`pixel_crc32=d21ae76a`, all 29 window frames identical),
    which passes the re-run safeguard above and looks exactly like a real, deterministic regression. The bisect
    then named #2003 — with an *attractive* mechanism, since Messenger's `param.json` declares
    `userDefinedParam4 = 1` and #2003 changed that answer from 0 to 1. It was wrong: #2003 passes, and it
    differs from the failing head only by a comment block, a docs file and a PNG. Re-running the named head
    settled it — **FAIL, FAIL, PASS, PASS, PASS**. A flake can repeat, and a repeated flake plus a plausible
    mechanism is the most convincing wrong answer this process can produce.
    **So bracket the bisect at both ends:** (1) establish the **good endpoint** first — run the guard on the
    pre-batch base and confirm it passes, or the whole bisect is measuring noise against noise; and (2)
    **re-test the commit the bisect names**, several times, before believing it. Both cost one run each and
    they are what separates a bisect result from a coincidence. Treat "the mechanism is plausible" as a reason
    for *more* scepticism, not less — it is what stops you re-testing.
  - **A bisect that skips environmentally loses resolution silently.** Two steps of the first batch recorded
    `build FAILED -> skip` purely because podman's DB lock fails spuriously under many concurrent lanes, not
    because the commit was unbuildable. `git bisect skip` widens the surviving range without saying that the
    evidence is missing rather than negative. Wrap the build in a retry, and if a step still skips, say so in
    the report rather than letting the narrowed range imply coverage it does not have.
  - Automate with `git bisect run` over the guard's exit code; every candidate builds standalone because each fix
    is its own merged commit. A batch can hold two independent regressions — bisect finds the earliest, so re-run
    after fixing. Expect to attribute a regression to a lane that has already finished: the orchestrator owns the
    fix, not the original author.
- The snapshot matrix is mandatory before every release. A release delay is acceptable; day-to-day development should
  not be delayed solely to keep master regression-free.
- Master may regress during heavy development. A correct fix may intentionally expose a different title regression.
  Record the tradeoff rather than treating a permanently green master as an end in itself.
- Artifacts are uploaded only for a release. A release is created only for a Git release tag. Ordinary PR CI must show
  the release-publication job skipped.
- Keep PRs short-lived so other agents inherit generic improvements quickly.
- **Three mechanical holes in the merge gate, each of which lets a PR read as satisfied when it is not.**
  - **A registered review is not the same as a comment.** `gh pr comment` creates an *issue* comment;
    only `gh pr review --comment` appears in `gh pr view --json reviews` / `pulls/N/reviews`. A review
    posted the first way is fully visible on the PR page and invisible to the gate, so the findings
    are right there while the PR reads as unreviewed.
  - **Never gate on `reviewDecision`; it is empty here for TWO independent reasons.** (1) A
    `COMMENTED` review does not populate it **regardless of who posts** — true even for a third-party
    reviewer, and the reason that survives any change to how reviews are authored. (2) Separately,
    `gh` is normally authenticated **as the PR author**, so `--approve` is not available. Both matter:
    someone "fixing" the gate by provisioning a second `gh` identity would find `reviewDecision`
    **still empty** and wrongly conclude this note was wrong.
    *Provenance, for both halves, because stating it for only one is the asymmetry this page exists to
    prevent:* **neither** was measured here. What WAS observed is that `reviewDecision` is empty with
    `COMMENTED` reviews present, and that `gh api user` matches the PR author — consistent with both
    claims but isolating neither, since every review observed came from the author account. That
    `COMMENTED` never populates `reviewDecision`, and that GitHub rejects self-approval, are both
    **documented platform behaviour**. `--approve` was deliberately **not executed**: running it would
    either fail or register an approval nobody intended.
  - **A rebase detaches every review from head, and the gate cannot see it.** `reviews` stays
    non-empty and still reports `state=COMMENTED`/`APPROVED`, but its `commit_id` points at a SHA no
    longer on the branch. State the consequence precisely: a stale `commit_id` proves the review **no
    longer binds to head** — it does *not* prove the content changed, and for a clean rebase the tree
    is usually byte-identical. The remedy is the same either way: compare each review's `commit_id`
    against the current head, and if it is stale, establish by **content** whether the approval still
    applies (`git diff <reviewed-sha> <head>`) rather than assuming in either direction. This is
    **trap 11 in the other direction** — there a squash made merged content look unmerged by SHA;
    here a rebase makes reviewed content look unreviewed by SHA. Both resolve the same way: verify by
    content, not by SHA. The same applies to CI — re-check bound to the exact current SHA, never to
    the PR number.
  - **Reviewers: bind your post to head at the write site, rather than relying on the merger to catch
    a stale one afterwards.** Read `headRefOid` immediately before posting and refuse if it moved:
    prevention costs one API call, detection costs a round trip and depends on the merger remembering.
    This is not hypothetical — on #1687 the head moved **twice after the branch was called frozen**,
    and a reviewer guarding on `headRefOid` correctly refused both times. Posting unconditionally
    would have left `reviews[]` showing a perfectly healthy row bound to code that had moved on.
  - **A running check reports `conclusion=""` — an empty string, not `null` — so a pending-count gate
    written as `select(.conclusion==null)` returns 0 while jobs are still executing.** Observed
    directly on #1991: `macOS (x86_64 / Rosetta 2)  conclusion=  state=null  status=IN_PROGRESS`,
    while the same query reported zero pending. This is how #1952 merged with a check unfinished; that
    was originally written off as a command-chaining slip, and the gate itself was the cause.
    **Gate on a terminal-success allowlist, never on a pending denylist:**
    ```bash
    gh pr view <N> --json statusCheckRollup --jq '
      [.statusCheckRollup[]?] as $c
      | { total:   ($c|length),
          notdone: ([$c[]|select((.conclusion//"")|IN("SUCCESS","SKIPPED")|not)]|length),
          failed:  ([$c[]|select((.conclusion//"")|IN("FAILURE","CANCELLED","TIMED_OUT","ACTION_REQUIRED"))]|length) }'
    ```
    Merge only on `notdone == 0 && failed == 0 && total > 0 && mergeStateStatus == "CLEAN"`. The `//""`
    coalesce is load-bearing. **The general rule is worth more than the `gh` detail: a filter that
    enumerates the states you consider bad silently admits every state you did not think of** — so
    enumerate what you accept and reject the complement. Note also how this hid: on the same day two
    merges passed the broken gate *correctly*, because their success count already equalled the full
    job count, and a gate that happens to agree with the right answer looks like a working gate.
- The orchestrator opens the PR with a self-contained behavioral contract, issue links, evidence, exact tests, risks,
  and known limitations. Wait for all Linux, Windows, and macOS checks before merging.
- When a game changes from black to visible content, reaches title, reaches gameplay, or materially improves visuals,
  update `README.md`/compatibility docs as appropriate and attach direct unmodified frontend captures. A black frame
  needs no screenshot. A checker/forced-state/debug draw may be attached only as clearly labeled diagnostic evidence.

### The environment is part of the system under test

A sound, log line, or visual artifact that appears to come from the program may be **the desktop responding
to the program's output**, not the program. Rule the environment out before building a theory on top of it.

Worked example (#1630). A short chime was heard repeatedly during library-view test runs and was taken as
evidence of an audio defect — a wrong sample rate, a truncated track, a fade discontinuity. It was none of
those. It was **KDE's terminal bell** (`ocean/stereo/bell-window-system.oga`): Konsole's default bell mode
is "System Notification", so any `0x07` reaching the terminal plays a real sound. Synthetic arrow keys from
an `xdotool`-driven route that miss the app's window land on the shell instead, where readline rings the
bell at the start of a line.

What made it convincing is exactly what made it misleading: the user described it as sounding like "a real
sound effect, identical every time", which reads as strong evidence for real decoded audio being replayed.
It sounded like a real effect because it *is* one — just not ours. **A property that seems to discriminate
between your hypotheses can be equally consistent with one you have not considered.**

Practical consequences for any routed run on a developer's desktop:

- Send every byte of a run's output to a file, never to a terminal, or strip `\007` from it. Free, and it
  removes the confound permanently.
- Guard synthetic input on the target window still existing (`xdotool getwindowname "$WID"`), so keys
  cannot leak to whatever has focus after the app exits.
- Prefer an instrument over an ear or an eye. The default sink can be recorded
  (`parecord --device=<sink>.monitor`) and checked for non-silence, which turns "did it make a sound?" into
  a measurement. Validate the instrument on a silent baseline first.
- When output and observation disagree, suspect the harness before the subject — a capture synchronized to
  a log line can easily record a *different* moment than the line it is labelled with.

## Recently merged foundation

These changes are already on current master and should not be reimplemented:

| PR | Merge commit | Result |
|---|---|---|
| [#1561](https://github.com/mattias800/prosper/pull/1561) | `55437d9235c7fcd70f03b976b7f93418f0a4b796` | Materializes exact scalar 16-bit buffer tails. |
| [#1562](https://github.com/mattias800/prosper/pull/1562) | `4a7245abfd999377a26ebe0a3cc40a74d4e9fb42` | Records Dragon Quest VII name confirmation. |
| [#1563](https://github.com/mattias800/prosper/pull/1563) | `2440f793fc85e9a0059469d7b62684465bb06e62` | Records Dragon Quest VII first-run onboarding. |
| [#1564](https://github.com/mattias800/prosper/pull/1564) | `8f317eadb05ba3ee602bb72b0a5ae8933d78e25e` | Structures scalar multi-loops; live Plucky stage `0x3017450000` executes. |
| [#1565](https://github.com/mattias800/prosper/pull/1565) | `fb086076c73e49e85d5962c12db098b28849fb4f` | Captures frames at exact guest-log phases; enabled deterministic Astro world-map capture. |
| [#1566](https://github.com/mattias800/prosper/pull/1566) | `ca0b02588c5a7c463a725878cd01330029b78092` | Composes multiple structured preludes with counted-loop lowering. |
| [#1572](https://github.com/mattias800/prosper/pull/1572) | `e3781cff` | Admits barrier-spanning VCC branches **proved** workgroup-uniform. Plucky stage `0x3017460000` reaches `unsupported=0`. |
| [#1574](https://github.com/mattias800/prosper/pull/1574) | `55544c03` | `gpu_replay --inspect-only` reports resolved `SPI_PS_INPUT_CNTL` linkage, so a synthesized-constant varying is visible offline. |
| [#1575](https://github.com/mattias800/prosper/pull/1575) | `329153bf` | `shader_inspect` reports a missing resource table as `undetermined` (exit 3), not as a shader rejection. |

Current master is **162/162 ctest** when configured against `PPSA24651`, with all of the above composed.

PR #1566's focused translator/render set was:

```text
recompile_coverage
rdna2_spirv_struct
game_compute_exec
game_compute_exec_no_adaptive_storage_result
rdna2_to_spirv_exec
recompiled_fragment_render
recompiled_shaders_render
```

All seven passed locally, all six platform CI jobs passed, and release publication skipped. The exact retained Plucky
shader moved from four unsupported control-flow instructions to one deliberately rejected barrier-crossing branch.

## Lane A: Astro Bot world map

### Public state

- Issue: [#1459 — world map renders as a dark grayscale pattern](https://github.com/mattias800/prosper/issues/1459).
- Existing status background: `docs/ASTROBOT_LINUX_HANDOFF_2026_07_19.md`.
- Frozen outgoing branch: `investigate/astro-worldmap-closure`, clean at `fb08607`; it contains no source changes and
  is behind current master. Start a fresh branch from `ca0b0258` rather than extending it blindly.

Durable evidence comments:

- [submit 6279 bounded failure classification](https://github.com/mattias800/prosper/issues/1459#issuecomment-5141854392)
- [submit 6284 dependency map](https://github.com/mattias800/prosper/issues/1459#issuecomment-5141880607)
- [compute-writer closure source audit](https://github.com/mattias800/prosper/issues/1459#issuecomment-5141933098)
- [seven-submit relevance result and pivot](https://github.com/mattias800/prosper/issues/1459#issuecomment-5142003672)

### Retained evidence

Evidence root:

```text
~/.local/state/prosper/evidence/astro-worldmap-c5698376-froute.KyRLdP/
```

Important files:

```text
worldmap.prgbundle
closure-analysis-fb086076/submit-{6278..6284}.prgcap
closure-analysis-fb086076/submit-{6278..6284}.inspect-all.log
closure-analysis-fb086076/submit-{6278..6284}.graph.json
closure-analysis-fb086076/current-master-replay.log
closure-analysis-fb086076/current-master.bmp
closure-analysis-fb086076/submit-6284-op19-b5-captured.bin
closure-analysis-fb086076/submit-6284-op19-b6-captured.bin
closure-analysis-fb086076/submit-6284-compute8-stored.spv
closure-analysis-fb086076/submit-6284-compute15-stored.spv
closure-analysis-fb086076/submit-6284-compute15-current.spv
closure-analysis-fb086076/submit-6284-compute15-stored-v-current.diff
```

The b6 seed SHA-256 is `571bd231a552e2613817c87ca2d7fe911cbda679dd41fbf77c4f80fe0c35ab8b`.
The current full replay hash recorded by the outgoing agent is `28b5ea9a3fbd2ca9`.

The route used PR #1565's exact marker gate:

```text
LevelDocument Loaded: worldmap [worldmap]
```

It captured seven submits after one present, ending at submit 6284. The selected world-map output is still dark/near
black, so no progression screenshot was published.

### Proven facts

1. Submit 6279's apparent Vulkan failure is benign. Its only unrealized operation is an intentional no-effect draw:
   color write mask zero and no depth/stencil write. Both shaders have zero unsupported instructions.
2. Submit 6284's 171 unrealized operations are also benign: 170 no-effect operations and one zero-vertex draw.
   There are no ShaderRecompile, DescriptorContract, or Vulkan rejects.
3. Only one unresolved temporal leaf reaches final scanout:
   - op19 compute program `0x500758300`, raw hash `b88321fae48353e1`, reads b6 and writes b7 at
     `0x556760000`, a 1x1 Float32x4 tile-27 image backed by a 64 KiB footprint;
   - op221 compute program `0x500656100`, hash `5ad826b552f23191`, later reads the same allocation at b6;
   - final op236/draw206 fragment hash `5cb70f55e3902078` reads it at b49.
4. There is no earlier access or overlapping color target in submits 6278 through 6284. The graph leaf means “no
   producer in this bundle,” not “no captured backing.”
5. The full 65,536-byte b6 allocation is captured, materialized, detiled, and seeded. Op19's b7 writeback mutates the
   shared capture-owned resource instance, so later consumers observe it.
6. Extending runtime producer history to reflected compute writers would improve future closure diagnostics but
   cannot change this retained bundle's pixels. Do not implement that abandoned proposal as an Astro visual fix.
7. Op19's inputs at texel zero are exact Float32 values `(1, -1, 0.5, 0.180000007)`, with b2=1.0 and b3=0.1.
   Stored shader math yields b7 `(0.8, -1, 0.5, 0.180000007)`.
8. Current raw op19 recompilation is byte-identical to stored: 1,053 words, hash `b88321fae48353e1`.
9. Op221 only reads this 1x1 allocation and writes b15/b16 at `0x5460b000`, a 1920x1080 R11G11B10 image. It does
   not overwrite the 1x1 allocation. Both op221 and final op236 use only the control value's x component, `0.8`.
10. The 1x1 value is sane and finite. Black does not begin at op19. Current evidence has not yet distinguished
    op221's 1920x1080 result from later/final composition.

### Falsified: the R11G11B10 storage fork

Op221's stored (14,543 words, `5ad826b552f23191`) and current-raw (12,019 words, `4d55000d1c32f046`) SPIR-V are
not binary-identical, and the difference does concentrate in R11G11B10 storage lowering — stored packs float RGB
into an R32ui image, current writes a float vector with `StorageImageWriteWithoutFormat`.

**This is not a defect.** Forcing each module explicitly with `--override-compute-spv 15 PATH` proved the two arms
take genuinely different backend paths (`native-storage=0` "exact packed R11G11B10 storage via R32_UINT" versus
`native-storage=1` typed float) and still produce **byte-identical** output `28b5ea9a3fbd2ca9`. The divergence is
by design: `gpu_executor.cpp` forces `native_storage_format_support=0` whenever a capture is bound, keeping stored
modules device-independent. Do not spend a PR here.

Also falsified: seed-skip/poison corruption (`PROSPER_NO_SKIP_SEED=1` gives an identical hash); op221 itself
(it writes zeros, but from a genuinely empty upstream); and the b49 1x1 control (draw 206's only real image input
is `0x5420f0000`).

### SUPERSEDED by #1724: the zero colour write mask was `CB_COLOR_CONTROL.MODE=DISABLE`, and prosper was NOT faithful

> **Correction, 2026-08-02 (#1724).** The measurement below is sound and its conclusion is wrong. It
> established that the guest writes `MODE=DISABLE` and that prosper's decode of that field is exact,
> then inferred that prosper's *response* to it was faithful. It never asked the discriminating
> question: **does anything else in the same state already answer what MODE is supposed to answer?**
> It does. Every one of Astro's 30 affected draws in submit 6179 also carries an explicit non-zero
> `CB_TARGET_MASK & CB_SHADER_MASK` — including a 140,825-dword fragment shader across three MRTs at
> 4K. prosper honoured `MODE` and discarded the mask, dropping most of the frame. #1724 removes that
> override; the write mask now comes from the masks alone.
>
> The instruction below not to re-investigate "the masks, the MODE decode, or the register fold" was
> correct about the **decode** and wrong to extend that to the **response**. Decode correctness and
> response correctness are separate claims, and the cross-title controls cited (Blue Prince, Oregon
> Trail) only ever established the first. Do not read a settled decode as a settled behaviour again.

Measured 2026-08-01 on exact master `687c3862` with a fresh **v43** world-map capture (the v42 bundle
reported `color-state unavailable`, which is why this could not be settled before) plus a
`PROSPER_REGWATCH` write-event count. Both halves agree and the chain is now complete.

**The masks are exonerated.** Across all 207 colour-state records in the captured frame, **no** register
is absent (`present=0`: zero records) and only 5 have a zero mask. Typical records are
`target-mask=1:00000737 shader-mask=1:0000000f effective=07` — healthy, non-zero.

**The disable is the MODE field.** 201 of 207 records hold `CB_COLOR_CONTROL = 0x00cc0000`
(**MODE=0, CB_DISABLE**), 6 hold `0x00cc0060` (MODE=6, DCC_DECOMPRESS), and **none** holds MODE=1
(CB_NORMAL). On the G-buffer `0x520440000` specifically: **136 draws at `cwm=0`, every one `mode=0`**
(the earlier "28 draws" figure understated it ~5x). `0x00cc0000` is the standard GFX10 hardware reset
for this register — ROP3 `0xCC` SRCCOPY, MODE=0 — and is not a literal anywhere in `src/`.

**The guest never enables normal colour writes.** `PROSPER_REGWATCH=Cx:0x202` over the routed run
observed **17,533 `CB_COLOR_CONTROL` writes, all on the indirect path**, with exactly three distinct
values differing *only* in the MODE field:

| value | MODE | count |
|---|---|---|
| `0x00cc0000` | 0 — CB_DISABLE | 7,859 |
| `0x00cc0020` | 2 — CB_ELIMINATE_FAST_CLEAR | 6,109 |
| `0x00cc0060` | 6 — CB_DCC_DECOMPRESS | 3,565 |
| — | **1 — CB_NORMAL** | **0** |

`CB_TARGET_MASK` (32,134 writes) and `CB_SHADER_MASK` (67,018) are written heavily, so the register
stream is alive and being decoded; the three coherent MODE values argue the same. This is DISABLE plus
two *decompression/maintenance* modes — a frame doing colour-buffer maintenance and never ordinary
shading.

**prosper's handling is correct and deliberate.** `render_state.cpp` computes
`color_write_mask = CB_TARGET_MASK & CB_SHADER_MASK`, then zeroes it when `MODE == DISABLE`
(`ps.color_write_mask = 0; ... for (auto& target : ps.color_targets) target.write_mask = 0;`), with a
comment stating that DISABLE suppresses colour writes but not the draw. Guest writes MODE=0 -> prosper
zeroes the mask -> `cwm=0` -> black. Every step is faithful.

**So the frontier moves upstream, off the colour state entirely.** The question is no longer "who wrote
the zero" — the guest did, 7,859 times — but **why Astro's world map never issues a CB_NORMAL pass**.
The MODE **decode** is settled cross-title (Blue Prince renders through this path at MODE=1; Oregon
Trail measured ~12,000 writes with zero mismatches across 1,050 suppressed draws) — but per the #1724
correction above, that never established that prosper's **response** to the decoded value was right,
and it was not.

**The bound was checked and closed.** `PROSPER_REGWATCH` observes writes only in streams prosper
actually *folds*, so a never-folded submit would be invisible to it **and** to the renderer — the #305
class one lane over. Both halves come back clean:

* **Astro never uses the variant submit path.** Scanning every module in the dump, only `eboot.bin`
  touches the submit imports at all, and it imports `UglJIZjGssM` (ordinary `sceAgcDriverSubmitDcb`)
  and **zero** `w1KFAHVqpaU` (`SubmitDcbFinal`). The `UglJIZjGssM` hit is the scan's own control. So
  the cross-thread mechanism cannot apply here.
* **Every submit folds completely.** Across two full routed runs: **zero** `SHORT FOLD`, **zero**
  `SUBSTITUTED STREAM ADDRESS`, **zero** `invalid arg9` / `no PM4 stream` (the #1665 instruments are
  unconditional, so the existing logs answered this without a new run).

So `CONFIDENCE: HIGH` on both halves: prosper faithfully reproduces what it observes, and what it
observes is everything the guest submits. **The cause is upstream of GPU state entirely** — the world
map's render pass is never set up to shade. That is a scene/pipeline question, not a register one.

### Superseded: "a frame-wide zero colour write mask" (the framing above answers it)

The world map is **not** a "dark grayscale pattern" — contrast-stretching the output shows a uniform diagonal
sawtooth with **zero scene content**. The prior "min 0 / max 13 / mean 5.97" reading is a content-free ramp.

**No geometry draw writes colour anywhere in the captured frame.** Across 7 submits / 43 realized draws, 28 draws
target the G-buffer `0x520440000` with `cwm=0` *and* `cwm1=0` while carrying real indexed geometry
(2,776–5,736 verts) and a **140,825-word** material fragment shader; ~170 further draws are suppressed as
no-effect for the same reason. Only 3 draws write full RGBA. Meanwhile the *captured* G-buffer holds 12,267,587
non-zero bytes of real content.

`render_state.cpp` resolves an **absent** `CB_TARGET_MASK`/`CB_SHADER_MASK` to **write-all**, so `cwm=0` means one
of those registers is **present with value zero**, or `MODE` is `DISABLE`. The live trace never once observed
`MODE=1 (CB_NORMAL)` — only mode 0 and mode 6 — which is not what an ordinary renderer looks like.

A tempting prior was the Gen5 stale-register-fold class. It was **investigated and not confirmed**, and the prior
itself turned out to rest on a superseded diagnosis:

- `SetRegsIndirect` reads a flat `{offset,value}` array and drops only `offset >= kRegOffsetLimit`, so the
  present-and-zero *capability* is real and generic.
- But #1364's "second record format / bit31-tag misparse" family diagnosis was **overturned**. The real mechanism
  was an HLE **success stub**: the SDK interpolant helper `dbOlWdppb4o` advertised 32 output records it never
  wrote, leaving **host stack residue** whose dword halves fold as present-and-zero registers. Fixed by #1368
  and #1411; current master emits zero `[dbbase-clobber]` events on 380 s+ routes.
- PRs #1344 and #1363 are downstream *symptom recoveries*, not fold-level invariants.
- Residue historically lands in the **low** offset cluster (DB `0x00-0x1F`, scissors `0x0C-0x0D`), whereas
  `CB_TARGET_MASK` is `0x8E`, `CB_SHADER_MASK` `0x8F`, and `CB_COLOR_CONTROL` `0x202`. That is counter-evidence.
- `PROSPER_DBBASETRACE` **cannot decide this**: it is hardcoded to DB offsets and only fires on a
  nonzero-then-zero-within-one-array signature, so it never fires for a register the guest never writes.

**Next Astro step.** Capture format **v43+** (PR #1576) retains the raw colour-state triple per draw with presence flags
and surfaces it in `--inspect`, which is what makes `cwm=0` attributable offline. The retained bundle is v42, so
it reports `color-state unavailable` — a **fresh current-format capture** is required. Take one, then determine whether the
zero masks are genuine guest intent or a prosper defect. If they are genuine intent, "real content exists but
nothing reaches the screen" via small mipped 4 KiB surfaces becomes the natural next suspect.

Do not recapture the world map for any other reason, revisit submit 6279, or implement compute-writer closure:
b6 is already captured, materialized, detiled and seeded, and no earlier producer exists in the bundle.

## Lane B: Dragon Quest VII overexposed composite

### Public state

- Issue: [#1486 — 4K composite is overexposed](https://github.com/mattias800/prosper/issues/1486).
- Status document: `docs/DRAGON_QUEST_STATUS.md`.
- The outgoing worktree was clean with no new repository changes. Its built replay tool and retained capsule were
  based on `fb08607`; compare relevant tile/sampler source with current master before treating byte parity as final.

Durable comments:

- [corrected op89/op90 diagnosis](https://github.com/mattias800/prosper/issues/1486#issuecomment-5141895497)
- [exact op116 to op117 corruption boundary](https://github.com/mattias800/prosper/issues/1486#issuecomment-5141942105)
- [raw recompile result and b36 metadata](https://github.com/mattias800/prosper/issues/1486#issuecomment-5141986842)
- [controlled LUT substitution A/B](https://github.com/mattias800/prosper/issues/1486#issuecomment-5142022769)
- [full-volume offline decode and sampling pivot](https://github.com/mattias800/prosper/issues/1486#issuecomment-5142103444)

### Retained evidence

```text
~/agent-tmp/dq-cap3/black.prgcap
  SHA-256 c7a863d69823964a2f18ced544385caa9986c04c419da2f449524b330cf237cf
  capture v38, submit 1060, 124 operations

~/dq-submit1060-bisect.goFHHE/
~/dq-op117-lut-ab.xEjqqT/
~/dq-op117-lut-decode.WjwK2H/
```

The A/B manifest SHA-256 is `f2f44602c178226539ba456d9a9879c1ab5b04634f3cb590010434577771290d`.

### The "exact corruption boundary" is a BISECT ARTIFACT — retained as a warning

> **FALSIFIED 2026-07-31.** The op116 -> op117 "corruption boundary" below, on which this entire lane was
> founded, **does not exist**. `--through-operation N` renders **the last executed draw target**, so
> consecutive cutoffs display *different surfaces*. The retained logs show it plainly — every cutoff is a
> different resolution: op104 30x34, op111 480x270, op114 960x1080, **op116 1920x1080, op117 3840x2160**.
>
> **op116 and op117 were never the same buffer, so the transition between them cannot be a corruption event.**
>
> op116's identity is pinned independently: it is a *dispatch*, so its output selects op115's target
> `0x3083db0000` = draw90's `b35`, and its content (R .0124 / G .0443 / B .1865) matches `tap-142` —
> draw90's own sample of b35 — to three decimals. **op116 is a post-process input that draw90 consumes.
> It is dark because such buffers are dark**, not because it is the last good frame before corruption.
>
> Do not re-derive a boundary from prefix replay without first confirming both prefixes render the **same
> target at the same resolution**. This is the single most expensive mistake recorded in this document: it
> shaped every hypothesis in the lane for two sessions.

The original (now-falsified) table, retained only so the claim is recognisable if it resurfaces:

| Prefix | Output | Hash / metrics |
|---|---|---|
| op116 | coherent dark ocean, 1920x1080 | replay `bee945066f3fc113`; BMP SHA `1273a16acea96c465db75021903cd0dadf2f16b1b3fa37e84296a0668866806e` |
| op117 | first overexposed/cyan frame, 3840x2160 | replay `31d794e48c2860dc`; BMP SHA `4dcf4f64ea9ed8e6901a92735b76cfa92bf41845c1a62d0f6818175a77728198` |
| op118 | orange overexposure | luma about 0.819; about 80.3% bright pixels |
| op122 | final opaque black overlay | not the first corruption |

Op117 is source draw90/order1436911. It is a realized fullscreen indexed triangle targeting `0x30867e0000`,
3840x2160 format44, write mask `f`, with blend/depth/stencil disabled.

Inputs:

- b34/b37/b38 from op88 at `0x308cfc0000`, 4K;
- b35 from op115 at `0x3083db0000`, 1920x1080;
- b36 grading LUT at `0x3021fe0000`;
- VS b7 from op103.

Stored VS/FS hashes are `6cde109bb55150ba` and `2810ab556bbfbc4d`.

`--recompile-raw --through-operation 117` is pixel-identical to stored replay. A no-render inspection changed the
installed VS hash to `ed25159f6e9d0b87`, and the mass path swaps VS and FS atomically, proving the draw used regenerated
stages. Stale stored shader translation is not the cause.

### Binding 36 identity

| Property | Value |
|---|---|
| Address / extent | `0x3021fe0000`, 32x32x32 |
| Format | enum21 `Unorm2_10_10_10`, four components |
| Declared / captured | 131,072 bytes / 2,097,152-byte footprint |
| Captured content | 107,001 nonzero bytes; hash `b53c7e7c2e9910e3` |
| Tile mode | 27, `SW_64KB_R_X` |
| Address modes / swizzle | 2/2/2; 4/5/6/7 |
| Filters | 1/1/1 |
| DCC metadata | enabled; address `0x306e9b0000`; 131,072 bytes of `0xffffffff`; hash `3735b73346670383` |

All-`0xff` metadata selects the renderer's ordinary uncompressed-base path. Current evidence does not indicate that
compressed blocks need decoding.

### Controlled LUT A/B

All variants reached op117 and used the same global diagnostic-copy path. Binding999 was the selector-miss control.

| Case | Output | Meaning |
|---|---|---|
| Baseline | hash `31d794e48c2860dc`; RGB mean 0.746193; luma 0.742344; 51.96% pixels at luma >=0.75 | Overexposed reference. |
| Selector miss, draw90/b999 | byte-identical to baseline | Diagnostic-copy side effects are excluded. |
| Checker, draw90/b36 | hash `7e9bec1982cda52e`; RGB mean 0.436836; luma 0.498721; no pixels >=0.75 | Every pixel changes; scene structure remains. |
| Zero, draw90/b36 | hash `ccc433ff6d980383`; one-color all-black frame | b36 materially drives the complete composite. |

The binding is active, its 3D view reaches the shader, and the selector is correct. Do not repeat unused-binding,
wrong-draw, or generic diagnostic-copy hypotheses.

### Full-volume decode result

The existing pre-detile resource dump exported the 2 MiB tiled allocation without a GPU replay. A temporary offline
analyzer called the project's own `detile_volume(..., 32, 32, 32, tile27, 4)` and the renderer's packed conversion.

Hashes:

```text
tiled allocation  60c100ff926e6ae226802e12b1510c54047b1472bbca120f6a47618fe9a0cf1b
linear packed     e9de4555ad4b65ea21de498c6d00443741773efa19269bfd98406ef64e61bfe1
linear RGBA8      eb27c3589d67553f86bf6f833894cee5f4ab3970c3b0bf2e0a51873ed0c89dca
metrics           b606c230b3ce953c9158015eacd5fe8eaf15eb9e7dbd223358bceb6d99abc257
z-slice sheet     e928a699a160757b6605c11a289633851440cb84279c989feeb2fd91371e31cb
```

Decoded channel metrics:

| Channel | Min | Max | Mean | Unique values |
|---|---:|---:|---:|---:|
| R | 0 | 255 | 176.9178 | 255 |
| G | 0 | 255 | 178.4504 | 254 |
| B | 0 | 255 | 153.9832 | 250 |
| A | 0 | 0 | 0 | 1 |

Mean adjacent differences are small and smooth:

| Axis | R | G | B |
|---|---:|---:|---:|
| X | 5.0448 | 2.8005 | 3.1537 |
| Y | 3.9890 | 5.1508 | 3.9264 |
| Z | 3.4532 | 3.5657 | 6.3019 |

The 32-slice contact sheet is visually coherent, with smooth RGB transfer surfaces and no checkerboard, tile-sized
scrambling, alternating bad slices, or discontinuity at 8/16-voxel boundaries. Tile27 detiling and packed
10:10:10:2 unpacking are now unlikely primary causes. Before citing exact runtime parity, confirm that current
master's tile conversion source matches the tested `fb08607` version.

### Binding 36 is exonerated — do not re-open it

A point-versus-linear A/B produced `linear` byte-identical to baseline **because the T# already reports
`filt=1/1/1`**, with `point` (`b005d9f5045a2f6d`) differing on 8,243,869 of 8,294,400 pixels. Both arms provably
took distinct paths, so this is a real negative, not a broken experiment.

`PROSPER_FS_TAP` measured draw90's actual sample coordinates as **(0.385, 0.536, 0.688)** — none saturated — and
the LUT correctly returns (0.424, 0.768, 0.975) there. draw90's FS is a faithful UE4 tonemapper with
`unsupported=0`. Ruled out and not to be re-run: LUT contents, tile27 volume detile, DCC, 10:10:10:2 unpack,
binding/view/3D-coordinate wiring, dimensional opcode, dmask, swizzle, address modes, sampler filtering, and
draw90's shader translation.

The LUT's corners look absurd as an RGB cube (pure R gives 255,224,190) but are exactly right for UE4's
**log-encoded `CombineLUTs`** volume.

**Caution:** the older "`--recompile-raw` at op117 is byte-identical" result **predates a `rdna2_to_spirv.cpp`
change** and must be re-measured before being cited again. Verify source parity by hashing the specific
translation units rather than assuming.

### Also cleared: the auto-exposure producer and delivery

- **Producer format** — `b15` is `fmt=4 nc=4` Float16x4, `declared=16320 = 60x34x8`, seed tagged `rgba16f`. Exact
  agreement, so this is **not** a #773-class native-format loss.
- **Producer content** — draw77 writes R .0973 / G .3472 / B .9731, luma `.33929`, with `rgb_nonblack=2040`
  (all texels) and `HIT` on every sample.
- **Stale seed** — falsified; the near-black buffer is draw79 (operation 105, *after* op103), which is capture
  ordering rather than a bug.
- **Delivery** — `1=param1`, a real `PARAM1` export consumed at `pc=0177`. No draw90 input is a
  `ConstantDefault` (`valid=ffffffff`, only `param0..3`), and every PS attribute read falls inside its VS
  export's EN mask. Confirmed with the #1574 seam.

The eye-adaptation buffer reporting average luminance **629** is **not** the cause, and the arithmetic shows why:
too-high average luminance implies too-*small* exposure implies a too-**dark** frame, but the frame is too
**bright**. Against the oracle the error is ~1–2 stops (a factor of **2–4**), nowhere near the ~8192x a broken
exposure implies.

Two retractions worth preserving so they are not re-derived: a "multiplier is exactly 1.0" claim derived by
inverting *means* through a `log` is invalid under Jensen's inequality — it was re-established by direct
measurement instead (`tap-177` uniform at `0.984314`, one distinct value across all 8.29M pixels). And a
"~14x amplification" in the post chain was an artifact of comparing **different registers** via `PROSPER_FS_TAP`'s
`(dst..dst+3)` export window.

### Not bloom: film grain and chromatic aberration

pc=214–256 decodes to literals summing to `0.99999999` (Rec.601 luma), then `1/(2π)` → `v_sin_f32` (which prosper
lowers as `sin(x·2π)`, cancelling it) → `493013.0` → `v_fract_f32`: `fract(sin(x)·493013)` hash noise applied as
`c·(1 + noise·s)`. Zero-centred and multiplicative, so it averages to ~1.0 and **cannot** produce systematic gain.
pc=154/157/164 use the *same* T# and sampler with different coordinate VGPRs and dmasks 1/2/4 — one texture
sampled three times at offset UVs, one channel each: **chromatic aberration**. b34/b37/b38 aliasing one surface is
three descriptor slots on one texture, not three reads.

### Next Dragon Quest assignment

The lane reduces to one number: the frame is bright because b34 is bright and the effective exposure is ≈1.0,
since `s97 x ExposureScale` is a measured uniform `0.984314`. `tap-171` renders exactly 0 across all 8.29M pixels,
bounding delivered `ExposureScale < 1/510`, so **`s97 > 502` is already established** and `s97 = 1.0` is excluded.

Measure `s97` at `b33+0x878` and `s106` at `b32+0x50` directly with two bounded `--dump-resource 90:ps:32` and
`90:ps:33` runs rather than inferring them. Two outcomes:

- **`s97 ≈ 8192`** — a UE4 **PreExposure** reciprocal, meaning the ≈1.0 cancellation is by design and the defect
  is upstream: prosper's base pass never applied the matching PreExposure factor. That would be a shared UE4-path
  change touching Plucky, ArcRunner and The Pathless, so it needs a generic contract and regression first.
- **`s97` is some other value > 502** — op103 wrote a different value in replay than the capture recorded,
  relocating the defect to op103's execution with no cross-title blast radius.

Note the standing counter-evidence against the PreExposure reading: if the base pass had applied
PreExposure = 8192, b34 would hold values in the thousands, but b34 is a smooth gradient at (0.098, 0.348, 0.971).
And the ~2–4x magnitude needed to match the oracle is far too small for a PreExposure-scale defect.

## Lane C: The Plucky Squire skipped compute

### Public state

- Issue: [#1554 — three compute programs skipped after chapter-one setup](https://github.com/mattias800/prosper/issues/1554).
- Current clean branch/worktree may be transferred: `investigate/plucky-pc915-proof` at exact current master
  `ca0b0258`. It has no edits or commits.
- Live proof: [issue comment](https://github.com/mattias800/prosper/issues/1554#issuecomment-5141779159).

Retained evidence:

```text
~/plucky-work/post1564-live-retry.fwpVFI/
  guest.log
    SHA-256 b6c9ba2bf32762796cdf342d900c109105f3f80f31934207728e3e7fc7f71c0d
  shader-dumps/exec_cs_3017460000.bin
    SHA-256 993af8855a62ac24203b12bcf2a968efb1ba163b64937f325353217d411d7342
```

### Proven progression

PR #1564 made the preceding target `0x3017450000` execute successfully 48 times with zero skips. The immediate next
unsupported stage is `0x3017460000`.

PR #1566 then composed its two disjoint pre-loop choices with the canonical counted-loop lowering. The exact retained
stage is:

- 8,448 bytes / 2,112 dwords;
- 1,964 decoded dwords / 1,327 instructions / valid END_PGM;
- coverage `total=1326 alu=1260 exp=0 table=65 unsupported=1`;
- 65 table-dependent MUBUF operations: 33 loads and 32 stores;
- one remaining structural rejection: `s_cbranch_vccz` pc915 to pc1963/END_PGM, whose fallthrough arm contains
  `S_BARRIER` pc1118.

Raw stage recompilation without the live resource table first stops at table-dependent MUBUF pc31. That is separate
from the control-flow coverage result.

### Pc915 predicate proof so far

The scalar chain is known exactly:

```text
pc900: load four scalar dwords into s8:s11 from s12:s13 + 0x20
pc903: load s16:s17 from descriptor s8:s11 + 0x0c
pc906: vcc_lo = s17 - 1                         # VCC_LO used as scalar data here
pc907: SCC = unsigned(s16 > 0)
pc908: VOPC writes its wave mask to s8:s9       # does not feed pc914 VCC
pc910: s10:s11 = SCC ? EXEC : 0
pc911: SCC = unsigned(s14 == vcc_lo)
pc913: VCC = SCC ? EXEC : 0
pc914: VCC &= s10:s11
pc915: branch if VCC is zero
```

Therefore, immediately before pc915:

```text
VCC = EXEC iff (s16 > 0) && (s14 == s17 - 1); otherwise VCC = 0
```

This proves uniformity within one guest wave. It does **not** prove that every wave in the workgroup makes the same
decision. `s14` has no decoded writer before pc915 and is an entry/system/user SGPR. `s12:s13` are also entry values
and source the descriptor/data chain. The retained raw file and log do not include enough launch/register metadata to
map those values.

Do not infer safety merely from the fact that the guest shader exists. If these scalar entry values differ by wave,
placing a Vulkan workgroup barrier inside an ordinary structured arm can violate uniform barrier participation.

### Resolved and merged (#1572)

The pc915 branch is **proven workgroup-uniform** and admitted by a generic rule. Two obligations are proved
independently and mechanically:

1. **EXEC is full at the branch on every path**, by forward must-dataflow (greatest fixpoint, sound across
   back-edges). This leg was missing from the original framing and is load-bearing: `VCCZ` is *also* true for a
   wave whose EXEC is empty, a genuinely per-wave property no amount of scalar uniformity recovers.
2. **VCC is `SCC ? EXEC : 0`**, optionally combined by `s_and_b64`/`s_or_b64`, with every contributing SCC from a
   scalar compare whose every scalar input traces to launch data.

Unproved provenance stays rejected: EXEC/SCC as data, `V_READFIRSTLANE`/`V_READLANE`, VOPC masks, SGPRs above the
launch range, and definitions entering from outside the straight-line region.

The uniformity guarantee is a property of **prosper's own compute entry seeding**, not an assumption about the
guest: user SGPRs come from `load_push_constant` (dispatch-uniform), system SGPRs from `b.groupid[]`
(`gl_WorkGroupID`, workgroup-uniform), TG_SIZE is a constant, and local invocation IDs are seeded into **VGPRs**,
which the provenance walk rejects outright. Nothing at compute entry places a wave-varying value into an SGPR.

Supporting CFG facts for the retained stage: in `[916,1963)` there are 12 barriers and 35 branches, all forward
`s_cbranch_execz` with no back-edges, none spanning a barrier; across the whole program only three edges cross any
barrier (`pc83→899`, `pc898→82`, `pc915→1963`), making pc915 the sole barrier-crossing edge. Coverage moved from
`unsupported=1` to `total=1326 alu=1261 table=65 unsupported=0`.

### Live confirmation, and a retraction that matters

**`0x3017460000` executes live**: 196 dispatches, every one `result=ok`, zero skips, deterministic at a single
SPIR-V hash, zero Vulkan errors across 51,808 log lines.

The proof was **genuinely exercised, not trivially satisfied**: `local=128x1x1` with `subgroup=0` (the portable
path) means **more than one guest wave per workgroup**. In a single-wave workgroup, wave-uniform would imply
workgroup-uniform for free and the barrier promotion would never have been under load. Twelve barriers inside a
VCC-entered region, multiple waves that must agree, 196 times.

All three programs named in #1554 now execute: `0x3017d90000` (#1561), `0x3017450000` (#1564),
`0x3017460000` (#1572).

**RETRACTED — the "three remaining blocked programs" finding was wrong.** A follow-up investigation reported that
`0x30133e0000`, `0x3013430000` and `0x30194c0000` were blocked by descriptor-resolution failures. Unfiltered
logging showed they execute **3,501 times** with a **single** transient failure (1 in 763, self-recovering, 108
successes before and 655 after). See instruments #9 and #10 in the list above: deduped skip lines plus a targeted
`PROSPER_COMPUTELOG_CODE` filter hid thousands of successes. Tracked as **#1581**, retitled to match.

What survives unchanged: `image_gather4_lz` (0x47), `image_store` (0x08) and MUBUF `<=0x07` are **all already
implemented**, and offline coverage is `unsupported=0` for all three. This was never an instruction-coverage gap.

Also: **#282's own comment retracts its premise** and its design doc is gone from master, so neither #282 nor
#485 covers this area — do not inherit them as background.

### Next Plucky assignment

**Rung 4 needs a human.** The one thing no agent here can produce is a visual milestone: a screenshot attempt
returned the KDE lock screen, and was correctly quarantined as non-evidence rather than published. An unlocked,
human-present session is required.

The remaining lead is narrow and **not diagnosed**: for the single failing dispatch the realized resource table
is **byte-identical** to succeeding ones, yet `[compute-cfg]` reports `branches=2` on failure and `branches=1` on
success with the same `hash4k`. Recorded on #1581 as a lead. Severity is low — one dropped dispatch in 763,
self-recovering, visible impact unmeasured — so **do not spend a lane on it** until something visible is
attributed to it.

## Lane D: Alex Kidd in Miracle World DX (PPSA02664)

Added 2026-07-31 because the inherited allocation was skewed to the hardest titles. **It reached the gameplay
rung in one session**, and the root cause was a **generic tiling defect**, not a title quirk.

`tiled_mip_level_layout` converted `mip_x`/`mip_y` — which count whole **256-byte** blocks — to elements using a
multiplier derived from the *macroblock* (`block_width >> 4`). That is correct only when the macroblock is 16x the
256-byte block (the 64 KiB modes) and **four times too small for every 4 KiB mode**. The struct contradicted
itself, reporting `byte_offset=0x800` alongside `tail_x=4` (byte `0x80`) for the same level, and the consumer used
`tail_x`. **53 of 130 packed-tail levels violated the origin-agreement invariant; zero after the fix.**

The correct multiplier is the 256-byte block's element extent, which depends only on element size (AddrLib
Block256_2d): 1B=16x16, 2B=16x8, 4B=8x8, 8B=8x4, 16B=4x4 — every entry multiplying to exactly 256 bytes.

The visible consequence shows how far a texture-origin bug can travel: mipped 4x4 SpriteMask sprites decoded as
foreign, fully transparent texels, so every mask fragment failed its alpha test and a **legitimate** "visible
outside mask" fill covered the entire frame. A black screen, several layers from its cause.

Falsified along the way: both prior size hypotheses (the guest's T# really is 4x4 with `base_level=0`), descriptor
selection, Gen5 size decode, the 4x4 fallback, blend/stencil semantics, guest-state divergence, and stale texture
content. The much older "`fs2949` premultiplies by vertex-colour alpha 0" diagnosis remains falsified — that
capsule was the intro narration crawl, where alpha-0 is correct.

**Two things to carry forward.** Existing `.prgcap` files **bake the resolved tail coordinates**, so pre-fix
capsules replay unchanged even on a fixed binary; re-verifying anything tiling-related needs a **fresh** capture.
And scene identity must be established from rendered *semantic content*, never from submit ordinals, guest VAs,
draw counts, or shader-hash signatures — that invariant was violated once here already.

**The title now reaches rung 6.** The `alexkidd-gameplay` guard is reviewed and landed, so PPSA02664 is
"done" by the ladder's own definition and the lane can close.

The adopted contract keeps the proposed `min_colors=20000` but moves the window to **95–145 s**. Measuring the
route first (`tools/snapshot/profile_route.py`, added with the guard) showed the level loads at 75 s at scale 4
and 72 s at native, so the proposed 70–120 s window would have straddled the load transition: five dark frames
sat inside it, and a run loading only ten seconds later would have pushed the guard below its content-match
ratio and failed on healthy output. The window now opens twenty seconds after the observed load **and after the
route's last pad press at 89 s**, so it samples a settled scene whose only motion is cloud drift and enemy
animation. That is what buys the margin: the worst of 100 reviewed frames scores SSIM **0.93** against a
0.85 floor, where the phase-sensitive `blue-prince-hall`/`terminator-boot` guards fail on healthy frames.
Non-black is `min_nonblack_ratio=0.5` (the tool's conservative half of the reviewed 1.0), not the proposed
0.95 — the approval flow derives that floor and it still sits 3.6x above the pre-fix 0.137. Colour count is one
of five conjoined conditions and must never become the contract alone.

Three independent runs: verify CONTENT-STABLE (richest 35,308/34,984, 50/50 qualifying in both), then
`check` OK (50/50 qualifying, 50/50 structural, 50/50 non-black). Pre-fix was 3,010 colours at 13.7%.

**#710 (title/menu renders incorrectly) is substantially resolved but not closed.** Against the hardware oracle
the panel base is now (253,183,0) vs (250,184,1) — it was (91,66,14) — and the logo, previously "faded /
semi-transparent", matches the oracle's regional mean within 3/255. What remains is narrow and measured: the
panel's watermark overlay lifts the base by only about **half** the hardware amount (G +14 vs +26, B +53 vs
+104). That ratio is a consistent 0.52 in display space but 0.51/0.26 in linear, which argues **against** a
linear-vs-sRGB blend-space explanation and leaves the earlier "semi-transparent tiles composite at too-low
alpha" factor as the live hypothesis. Re-verify any tiling-related claim with a **fresh** capture: pre-fix
`.prgcap` files bake the resolved tail coordinates.

## Lane E: The Pathless (PPSA01826)

> **Superseded 2026-08-02 by a boot sweep on `c79f742e`: this title is at rung 2, not rung 0.** A plain
> no-input boot renders the **title screen** (`NEW GAME` / `OPTIONS`) at 2560x1440 and holds it for a full
> 200 s run with `max_stale_seconds=0.0`, so neither "every presented frame is a flat colour" nor "progression
> freezes at ~18 s" reproduces. Evidence and manifest numbers are in #1570; the screenshot is
> `assets/screenshots/pathless-title.webp`. **Do not start this lane from the packed-FP16 MRT0 export lead
> below** — it was derived from a capture taken in the flat-colour state that no longer occurs, so re-take a
> capture before trusting any address, submit ordinal or hash in the rest of this section. The remaining real
> gap is that no input route has been tried and gameplay has not been reached. The section is retained
> unedited below as the 2026-07-31 record.

First triage 2026-07-31. **Not greenfield** — #1213 exists with prior work, was closed apparently by accident,
and has been reopened. Current state is tracked in **#1570**.

UE4 4.27-class with Wwise. Boots remarkably far — Wwise up, full UE4 thread topology including a Navmesh Tile
Builder (so a real level is loaded), 6,150 submits / 28,859 draws / 27,350 dispatches in 75 s — but **every
presented frame is a flat colour** (blue then white) and progression freezes at ~18 s. That is **rung 0**.

The #1213 worker SIGSEGV at `eboot+0x1b70cd0` **did not reproduce** across four runs. An offline capture (485
draws, 1 dispatch, **0 failures**, replaying hash-exact at `a5e7b61cbf984383`) shows 482 draws plus 1 compute
writing a 768x768 Slate/UMG surface, and only **3** draws touching the 3840x2160 front buffer — all three binding
a 1x1 white texture. prosper faithfully realizes what the guest submits; the defect is composition/progression.

Later stepping showed the front buffer goes black → white, and the three front-buffer draws are the ones painting
white. Their pixel shader's entire constant buffer is a single RGBA of `(1.0, 0.4545, 0.0, 1.0)` — **orange** — and
the export is a COMPR packed-FP16 MRT0 export fed by `V_PACK_B32_F16`, consistent with `export=4` (FP16_ABGR). So
the shape is "a tint constant that should produce orange yields white", pointing at the **packed-FP16 export
path** rather than texel decode. The 1x1 white texture looks like a deliberate Slate white-brush dummy.

Open question the capture cannot yet answer: whether the composite lives in a different submit or the game is
genuinely blank — the capture is 1 submit of ~6,150. A `.prgtl` timeline survey of all submits is the next step
and is CPU-only.

Generic gaps found, all `area:infra` and all benefiting other UE titles: DCC `tile=27` sampled images and metadata
extents unsupported; a 400x400x212 image exceeding the 512 MiB backend bound; and 40+
`[agc] WaitRegMem q=F … dependency violated` — the same per-queue barrier gap as ArcRunner #1226. Estimated cost:
**rung 1 ≈ one medium focused investigation** (answerable offline from the existing capture), **rung 3 ≈ several
sessions**.

## Lane F: Nikoderiko (PPSA23760) — the #305 user-data condition

Triaged 2026-08-01. Tracked in **#305**, which was **retitled that day**: its founding premise —
*"first draws run with the PREVIOUS pipeline's user data (missing bind in decoded stream)"* — is
**measured and falsified**. Anyone starting from "stale bind" is starting from a dead tree.

The title renders its logos, title screen and EULA correctly at 3840x2160 and reaches **rung 2**. The
whole 3D world is missing: 25 distinct `(es, ps)` pipelines dropped, the world behind the title black.

**The condition that actually holds.** A stage resolves garbage descriptors exactly when the
user-data block the guest most recently programmed is **larger** than the bound pipeline's user-SGPR
window (`SPI_SHADER_PGM_RSRC2_{GS,PS}.USER_SGPR`, which equals the shader's own
`user_data_range_end`). The shader then dereferences a V# `num_records`/`dword3` tail as a pointer —
the `0x0004dfac…` constant family — the const-fold correctly refuses to invent a descriptor, and the
draw is skipped fail-visibly. 12→12 resolves; 8→12, 12→28, 12→30, 20→30, 24→28, 24→30 all fail. The
original DOLL (`PPSA17942`) evidence in the issue records the **same** numeric relationship (a
12-dword block against an 8-user-SGPR pipeline), so this is one condition across two UE4 titles.

**Falsified, with the measurement — do not re-derive:** the block is not stale (write provenance puts
it a handful of packets before the draw, from the immediately preceding bind); the shader-registry
lookup is not stale (`registrations=1` across a 2,725-entry registry); no bind is missing or
mis-ordered (193,397 bind packets, **0 of 141** distinct register arrays ever applying more than one
`(es_lo, rsrc2)`, and **300,404 of 300,404** draws folding with the immediately preceding bind — see
hazard 16 for the earlier, contaminated version of that number);
the stage's data is **not** the tail of the programmed block (see hazard 12 above); and **#140
(TYPE-0 AGC data packets) is the wrong tree** — every register write that matters arrives as an
ordinary decoded packet whose provenance is now observable, and none is missing.

The same measurement **confirms** two assumptions previously taken on faith: the seeding base
`USER_DATA_<stage>_0 + user_data_range_start`, and the merged-stage convention that user data begins
at shader SGPR `s8`.

**Current frontier:** submit identity is already measured, not missing. For every sampled misfit the
program bind came from a `q1` (Dcb) fold and the larger user-data write from the following `q3`
(DcbFinal) fold; the diagnostic retains order, path, origin, fold and Jump depth in each draw
snapshot. A separate pre-mutex instrument also records call order and thread identity. This localizes
the mismatch but does not prove a cross-queue root cause: the default-off A/B that gave DcbFinal its
own register file left reject counts within variance and the 3D world identically black.

The earlier GS-versus-PS acceptance test is retired. The signature follows window size across the
measured failure population: vertex windows 8/12/28 lose, vertex windows 30/32 do not, and every
pixel stage with a declared pointer also has a 30-dword window. The open question is the hardware
ordering/ring contract that prevents a larger required block from being paired with a smaller bound
pipeline window; neither provenance nor stage identity answers it.

**Instruments** (`PROSPER_UDPROV`, `PROSPER_BINDTRACE`, `[udcand]`, `PROSPER_SHADER_HEADER_NEWEST`,
and the deliberately-off `PROSPER_UD_TAIL_ALIGN`) are on PR #1639, with queue/fold/jump-depth write
provenance following; `PROSPER_SUBMITORDER` adds pre-mutex call/thread identity. The synthetic
`test_command_provenance` contract pins q1/q2/q3, fold, Jump depth, path and order propagation. Use
them rather than rebuilding the measurement.

**Instrument warning:** the diagnostics serialize draw realization, so the title screen arrives at
~112 s rather than ~90 s. A 200 s window times out in the pre-title load with **zero rejects** and
looks deceptively like the issue is fixed. Budget ~440 s per run.

Estimated cost is **not currently bounded**: the condition and its provenance are cheap to detect,
but the hardware ordering/ring contract is still unknown. Do not turn the negative DcbFinal split A/B
into a default fix or claim rung 3 from this diagnostic hardening.

## Suggested allocation for a new orchestrator

**Superseded by the 2026-08-04 wave structure below.** The table that used to sit here allocated
agents to Astro Bot, Dragon Quest VII, The Plucky Squire, Alex Kidd and The Pathless; three of those
titles have since advanced and the allocation is kept only in git history.

### Wave 1 (dispatched 2026-08-04, base `6a40d191`) — every rung-0 title

One title per agent, each mandated to reach **rung 1 (any real graphics from the live renderer; a
logo counts, black or diagnostic-only output does not)** *or* return a precise evidence-backed
diagnosis of the next blocker. Explicitly bounded: a sharp diagnosis handed back beats a sixth
session of depth.

| Agent | Title | Frontier at dispatch | Tracker / blocker |
|---|---|---|---|
| oregon-r1 | The Oregon Trail (PPSA19244, UE4) | guest issues no base pass; prosper decodes 100% of what it submits | #1886 / #1606 |
| sonicorigins-r1 | Sonic Origins (PPSA05325) | black startup loop after real UI/font asset loads | #1871 / #1905 |
| rtype-r1 | R-Type Delta (PPSA26414) | startup race dereferences an empty logged-in-user vector; movie-texture shader emits flat gradients | #1810 / #1746, #1753 |
| babylon-r1 | Asterix & Obelix Babylon (PPSA30490, Unity 6) | sampled 2D-MSAA resolve executes, output still black | #1884 / #1599 |
| arcrunner-r1 | ArcRunner (PPSA21406, UE4 4.27) | real GPU submissions, then the render thread faults | #1817 / #1226 |

#### Wave 1 outcome — all five merged, one rung gained, four blockers relocated

| Title | Result | Landed |
|---|---|---|
| **The Oregon Trail** | **rung 0 → 1.** Renders `WBP_System_Disclaimer_Popup` on a *default* launch (4,009,938 non-black px). Screenshot committed. Enabled by #1933's ErrorDialog lifecycle, established by a single-variable A/B. | #1950 |
| Asterix Babylon | Blocker moved **GPU → CPU**: the guest deadlocks in its video splash because `sceAvPlayerJumpToTime` is unimplemented and the dispatcher's default `0` reads as a successful seek. Kills the whole `DMEM_WRITE_TRACE` writer-hunt premise. | #1953 |
| ArcRunner | The `addr=(nil)` sibling fault is **prosper's own lazy-commit zero**, not a guest lifetime bug — roughly five sessions had been classifying a value prosper wrote. The fault was never the rung-1 blocker either. | #1948 |
| R-Type Delta | The issue's **standing recommendation** ("faithful, slower I/O timing wins the race") falsified quantitatively — it points the wrong way. Display-rate pacing also dead, on a lever-verified A/B. | #1947 |
| Sonic Origins | Five hypotheses dead. The guest is alive, unblocked, and builds **exactly one frame forever** (22 submits / 22 draws / 14 dispatches per flip, constant over 11,274 flips). | #1952 |

**Four new shared-substrate issues, three of which help titles beyond their lane:** #1944
(lazy-commit hands the guest a zero), #1945 (guest UE4 pooled-allocator free-list corruption),
#1949 (AvPlayer seek is missing from `VideoBackend` entirely), #1951 (APR resolve leaves later
entries uninitialized after the first miss).

**The cross-title link worth acting on.** ArcRunner (`eboot+0x127e751`) and Oregon Trail
(`eboot+0x14600df`) both fault on `mov rax,[rcx]` with `rcx = 0x30016000`, popping a pooled-allocator
free-list head — in **different guest binaries**. Either one shared prosper defect or one shared UE4
guest pattern; nobody has separated those yet, and the answer decides where the fix goes. **Oregon
Trail is now the fastest repro in the library**: 3 of 4 *default* launches die within 6 s, exit 90,
no route or diagnostics. Work #1226 there, not on an ArcRunner boot.

**Two lessons about the apparatus, both now traps.** Trap 85: an asset-load trace read as proof the
guest executed the code using the asset — UE4 pulls a level's referenced classes through the map
import table before any Blueprint runs, so the pre-fix arm loaded the same assets at identical
ordinals over a 12× longer run. Trap 86: a generic-named shell redirect into the shared `~/` scratch,
read as writing a private file. Also, recorded in the Sonic notes and worth propagating: **`.pad`
route positions are pad reads, not seconds** — a committed 404-read route ends at t≈6.6 s in a
~61 reads/s arm, so runs that looked input-driven were input-free through their whole steady state.

### Wave 2 (queued) — every rung-1 title, target rung 2 (title screen)

The Forgotten City (PPSA03026, #1890), Sonic Racing: CrossWorlds (PPSA08804, #1895), Sonic Frontiers
(PPSA03831, #1891), Little Nightmares III (PPSA05143, #1893), Crisis Core Reunion (PPSA07809, #1894),
Bendy and the Dark Revival (PPSA27624, #1897).

### Cross-lane serialization, which is the orchestrator's real job here

Wave 1 runs five concurrent lanes and **three of the five titles are UE4** (Oregon Trail, ArcRunner,
R-Type Delta). Title-specific route and doc work proceeds freely, but every lane is required to
report a change touching **shared code — loader, HLE, recompiler, generic GPU/AGC, host mapping —
to the orchestrator BEFORE pushing**, naming the files. The orchestrator reviews the diff, sequences
the merges, and rebases the other lanes. Expect the durable wins to come from exactly those shared
changes: they help titles beyond the lane that found them.

One cheap win deliberately deferred: **Earthion (PPSA28061) is a 79 MB dump already at rung 2** and
its only stated blocker is that the menu needs directional navigation to route into gameplay. It is
the best value-per-token item on the board once the rung-0 and rung-1 queues are served.

**That "cheap win" reading is falsified (2026-08-21) — leave the paragraph above as the record of
what was believed, and do not act on it.** The menu in question is the *wrapper's* and has no start
item; the title's own HOW TO PLAY page binds **Start to the TOUCHPAD**, and a route that delivers it
(13 presses read by the guest) still finds the game picture black. The frontier is that the picture
never leaves a ~60 s narration cycle, which is neither an input problem nor a recompiler one.
`docs/EARTHION_STATUS.md` § Ruled out.

**Balance breadth against depth.** Astro Bot is a AAA showcase title and has produced no shippable title fix
across two sessions; Dragon Quest and Plucky are deep but tractable. Alex Kidd went from stuck to gameplay in one
session, and The Pathless was never attempted. When a hard lane stalls, reassign rather than persist — overall
library progress matters more than any single title, and the user has said so explicitly.

**Expect the generic wins to come from tooling and shared subsystems.** This session's merges were a recompiler
uniformity rule, two diagnostic seams, a tooling-honesty fix, and one shared tiling fix. Every one of them helps
titles beyond the lane that found it.

## Ready-to-send subagent prompts

Give every agent: its worktree path, its branch, the exact `origin/master` SHA, the dump root, the **distrobox**
build command, and the instruction to keep evidence under `~/` and out of `/tmp`, the repo, and public text.

### Astro Bot

```text
Read CLAUDE.md, docs/GAME_COMPAT_ORCHESTRATION.md (Lane A), and issue #1459.
Do NOT re-run the R11G11B10 storage fork (falsified: byte-identical output across different
backend paths), compute-writer closure, submit 6279, or the b49 1x1 control.
The frontier is that no geometry draw writes colour anywhere in the frame: 28 realized draws
resolve cwm=0 and cwm1=0. An ABSENT target/shader mask resolves to write-all, so cwm=0 means
present-and-zero, or MODE=DISABLE. Capture format v43+ retains the raw triple; the retained bundle is
v42 and reports "color-state unavailable", so take a FRESH current-format capture first. Then determine
whether the zero masks are guest intent or a prosper defect. The Gen5 stale-fold prior was
investigated and NOT confirmed; read the lane notes before reviving it.
```

### Dragon Quest VII

```text
Read CLAUDE.md, docs/GAME_COMPAT_ORCHESTRATION.md (Lane B), docs/DRAGON_QUEST_STATUS.md, and #1486.
Binding 36 is EXONERATED and the op103 auto-exposure producer and interpolant delivery are CLEARED.
Film grain and chromatic aberration are both excluded. Do not re-open any of them.
Measure s97 at b33+0x878 and s106 at b32+0x50 with two bounded --dump-resource 90:ps:32 and
90:ps:33 runs. s97 > 502 is already established. If s97 is approximately 8192 it is a UE4
PreExposure reciprocal and the defect is upstream in the base pass (shared UE4 blast radius —
name a generic contract first); otherwise op103 wrote a different value in replay than captured.
Note the counter-evidence: b34 is a smooth gradient, not thousands-valued, and the oracle gap is
only 2-4x. Re-state tile.cpp parity against current master rather than citing an old blob hash.
```

### The Plucky Squire

```text
Read CLAUDE.md, docs/GAME_COMPAT_ORCHESTRATION.md (Lane C), #1554, and PRs #1564/#1566/#1572.
The pc915 uniformity work is MERGED; do not redo it. It is not yet a live-execution claim.
Run the ~10-minute native-cadence route and confirm 0x3017460000 executes, then identify the
next skipped stage. Absence of "[compute] skip unsupported program" proves zero skips. Use the
targeted PROSPER_COMPUTELOG_CODE=<addr>, not blanket PROSPER_COMPUTELOG. Prefer the live path's
own rejection reasons over any offline verdict. Keep PROSPER_RENDER_SCALE=1 and RENDER_EVERY=1.
```

### Alex Kidd

```text
Read CLAUDE.md, docs/GAME_COMPAT_ORCHESTRATION.md (Lane D), and #320.
The generic packed-mip-tail tiling defect is FIXED; the title reaches gameplay. Remaining work is
the snapshot guard: route scripts/ppsa02664/reach-first-gameplay.pad, sample 70-120 s, require the
richest frame to have non-black >= 95% AND >= 20,000 distinct colours, paired with the existing
SSIM comparison. Never use colour count alone — a seed-miss gradient outscores real content.
Run snapshot.py verify and inspect every retained image before adopting a baseline. Then re-check
#710, plausibly the same root cause. Pre-fix .prgcap files bake resolved tail coordinates, so any
tiling re-verification needs a fresh capture.
```

### The Pathless

```text
Read CLAUDE.md, docs/GAME_COMPAT_ORCHESTRATION.md (Lane E), #1570, and #1213 (reopened).
The title is at rung 2 as of 2026-08-02: a plain no-input boot renders the title screen
(NEW GAME / OPTIONS) at 2560x1440 and holds it for 200 s. The earlier "flat colour, freezes at
~18 s" state does NOT reproduce, so the packed-FP16 MRT0 export lead and every address, submit
ordinal and hash in Lane E come from a state the title no longer enters — re-capture before
using any of them. The #1213 SIGSEGV also does not reproduce. The open work is the next rung:
drive an input route past NEW GAME and find where gameplay stops. Generic gaps still worth
fixing for all UE titles: DCC tile=27 sampled images, the per-queue WaitRegMem barrier model
(shared with ArcRunner #1226), and the 512 MiB backend image bound.
```

## Orchestrator cadence and reporting

A useful cadence is:

1. Ask each agent for a compact status after one bounded experiment or one source-design pass.
2. Immediately redirect agents that falsify their premise; do not let them implement a now-diagnostic-only idea.
3. Keep one integration step active at a time: inspect diff, verify exact head, publish PR, watch CI, merge.
4. Let the other agents continue evidence gathering while CI runs, provided they are not modifying the same files or
   depending on the unmerged result.
5. After every merge, fetch master and restart dependent branches from the new exact head.
6. Report to the user in plain language: what became visible/correct, what was ruled out, what is running, and whether
   the GPU is shared or exclusive. Performance numbers are ballpark unless the GPU was isolated.

When an agent appears frozen, distinguish a real long computation from a helper blocked on stdin. Avoid `gh ...
--body-file -` in unattended commands; use a real bounded argument/file so the process cannot wait forever for input.

## Updating this document

After a material event:

- replace the exact master SHA;
- move merged work into the foundation table;
- update the lane's “proven,” “falsified,” and “next” sections;
- link the new issue comment/PR;
- record retained artifact hashes, but sanitize paths;
- remove obsolete experiments so a new agent does not repeat them;
- keep unresolved risks explicit;
- preserve enough history to explain why an attractive old hypothesis was rejected.

This document is successful when a new orchestrator can allocate agents, reuse the retained evidence, and run the next
three discriminating experiments without asking the previous session what it meant.
